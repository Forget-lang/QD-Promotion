# ============ 密钥 / 本地环境变量（禁止提交） ============
.env
.env.local
.env.*.local

# ============ 独立 git 仓库（applet 有专属 git，不套进本仓库） ============
applet/

# ============ 依赖与构建产物 ============
node_modules/
video/node_modules/
__pycache__/
*.pyc

# macOS 系统垃圾
.DS_Store
Thumbs.db

# IDE 配置（本机特有，不入库）
.idea/
.vscode/

# ============ Remotion 缓存 / 渲染产物（源码可复现，不入库） ============
video/.cache/
video/out/
*.mp4
*.mp3
*.wav
**/_preview/

# ============ TTS 语音生成文件（可由脚本复现，不入库） ============
video/public/audio/

# ============ 行业单元交付物（不入库，仅本地留存） ============
outputs/g*/

# ============ 过程产物/临时 ============
.doc-audit-cache/
.tmp/

# ============ 素材库（大二进制/字体/截图，不入库，仅本地保留） ============
商用字体/
图标素材/
插图库/
背景素材/
截图素材/

# ============ 商用字体 bundle（版权禁止再分发，不入库） ============
video/public/fonts/
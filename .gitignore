# ============ 密钥 / 本地环境变量（禁止提交） ============
.env
.env.local
.env.*.local

# ============ 独立 git 仓库（applet 有专属 git，不套进本仓库） ============
applet/

# ============ 依赖与构建产物 ============
node_modules/
video/node_modules/
__pycache__/
*.pyc

# macOS 系统垃圾
.DS_Store
Thumbs.db

# IDE 配置（本机特有，不入库）
.idea/
.vscode/

# ============ Remotion 缓存 / 渲染产物（源码可复现，不入库） ============
video/.cache/
video/out/
*.mp4
*.mp3
*.wav
**/_preview/

# ============ TTS 语音生成文件（可由脚本复现，不入库） ============
video/public/audio/

# ============ 行业单元交付物：文档入库同步，渲染产物不入库 ============
# （原 outputs/g*/ 规则已移除：g* 目录下 md/txt 等文档随仓库同步，
#   mp4 等渲染产物由上方 *.mp4/*.mp3/*.wav 规则忽略）

# ============ 过程产物/临时 ============
.doc-audit-cache/
.tmp/

# ============ 素材库（仅本地保留，不入库） ============
截图素材/
图标素材/
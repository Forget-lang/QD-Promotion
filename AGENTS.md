# 券到卡包 · 内容运营项目（AI 入口）

> 最后校验：2026-08-26（文档体系重构 v2；08-24 审计修复后更新；08-25 新增协作规则：验证与正式产出同标准、问题当场修复；08-26 §六 新增闸门：改规则前先 grep 全库旧关键词；08-26 新增文档冻结决定；08-26 §二 升级为「开工三动作」：跑盘点脚本 + 读 changelog 近况，用户批准的冻结期改进）
> 任何 AI 助手在本仓库开工前，先读本文件（约 3 分钟）。读完按「§二 开工三动作」执行：跑盘点脚本 → 读 changelog 近况 → 按「任务 → 手册映射」读对应手册，**不需要**读全部文档。
> 本项目面向实体店电子券工具（做券→发券→领券→扫码核销→统计），当前做宣传推广线冷启动：抖音短视频 + 小红书图文 + 搜狐长文。
> 产品源码位于 `../applet/`（本文件所在 `promotion/` 的上级目录），功能有疑问时直接查阅代码验证。

---

## 一、项目是什么

- 产品名：券到卡包 ｜ 运营主体：郑州百桨数字科技 ｜ 工作区：`promotion/`
- 阶段：宣传线冷启动，**无真实案例**；正文一律用行业泛称（咖啡店/火锅店/美容店…），不编具体商户名、不编数据
- 三大产品：优惠券（6 型）/ 券包 / 次卡；支持加好友/进群自动发券

## 二、开工三动作（每次新会话必执行）

**动作 1 跑盘点**——`node scripts/list-assets.mjs`（项目根目录）：组件 / 模板 / 样本 / 素材的实时清单。状态类问题（有哪些组件/模板/背景图、什么状态）一律以脚本输出为准，不凭对文档静态表的记忆作答。

**动作 2 读近况**——`outputs/archive/changelog.md` 顶部最近 ~10 条：近期决策、纠错、踩坑。避免重复踩同一个坑，或推翻刚拍板的口径。

**动作 3 按任务读手册**——按下方映射表读对应手册（按需读相关章节，不必整本通读）。

### 任务 → 手册映射（开工先看这张表）

| 你要做的事 | 必读手册 | 按需查参考 |
|---|---|---|
| 内容策划：行业调研 → 选玩法 → 写文案/口播 → 深度自检 | `docs/internal/M1-内容策划手册.md` | R1 事实、R2 流程、R4 句式 |
| 视频制作：设计稿 → 代码 → 无声版 → TTS → 成片 | `docs/internal/M2-视频制作手册.md` | R3 技术原理、R4 审美 |
| 平台发布：封面 → CTA → 抖音/小红书/搜狐规则 → 自查 | `docs/internal/M3-平台发布手册.md` | R5 红线 |
| 写教程类长文，需要产品操作步骤 | `docs/internal/R2-业务流程.md` | R1 事实 |
| 内容涉及产品功能怎么运作（字段/规则/统计维度/视频干货点） | `docs/internal/R0-业务深度图谱.md` | R1 事实 |
| 合规争议：某句话能不能说 | `docs/internal/R5-内容红线.md` | — |
| 需要高转化句式/排版参照/踩坑经验 | `docs/internal/R4-最佳实践库.md` | — |

> 三本手册（M1/M2/M3）是"怎么做"，五份参考（R1-R5）是"查什么"。**每类知识只有一个真源，其他文档只引用不复述。**

### 新增/修改内容归属判定（拿不准往哪放时查此表）

| 内容类型 | 放哪 | 示例 |
|---|---|---|
| 产品业务运作细节（字段/规则/状态机/统计维度/视频干货点） | `docs/internal/R0-业务深度图谱.md` | "核销间隔 0~30 天=锁客节奏核心" |
| 产品事实 / 数字 / 术语 / 能力边界 | `docs/internal/R1-产品事实表.md` | "随机金额券 1-99 整数" |
| 操作步骤 / 状态流转 / 渠道枚举 | `docs/internal/R2-业务流程.md` | "私密发放 5 分钟~永久五档" |
| 策划方法 / 玩法 / 文案口播标准 / 深度闸门 | `docs/internal/M1-内容策划手册.md` | "P-05 老带新转赠奖励" |
| 视频设计规则 / 参数 / 设计稿模板 | `docs/internal/M2-视频制作手册.md` | "字号阶梯、布局模式选择" |
| 平台规则 / 封面 / CTA / 发布 | `docs/internal/M3-平台发布手册.md` | "小红书 3:4 裁切中间区" |
| 技术原理 / 组件 API / 扩展流程 | `docs/internal/R3-Remotion技术参考.md` | "J-cut 为什么能帧级同步" |
| 经验 / 句式 / 踩坑 / 假设 / 审美标准 | `docs/internal/R4-最佳实践库.md` | "高转化句式 H-003、布局模式 L-05" |
| 红线 / 合规口径 | `docs/internal/R5-内容红线.md` | "封面不刻意提免费" |
| 协作规则 / 项目规范 / 闸门 | `AGENTS.md` | "两步确认法、文档写作六条" |
| 变更记录 | `outputs/archive/changelog.md` | 每次改文档追加一条 |

**判定逻辑**：先问"这是**怎么做**（规则/方法 → 手册 M1/M2/M3）还是**查什么**（知识/参考 → R1-R5）"。拿不准时**默认放 R4**（经验沉淀层），验证后归位；改文档后按 `AGENTS.md` §六 闸门跑检查并记 changelog。

## 二·5、近期关键规则速览（2026-08-25 工作流验证后，新会话开工先扫一眼）

> 详情在各手册对应章节；这里只列"开工必须知道的新规矩"，防止新会话按旧习惯产出：

- **验证/生产与正式产出同标准**；发现问题当场修复、解决后再进下一环节（§四 协作规则）
- **文案**：顾客反应不得绝对化（M1 步骤 3）；字数/时长=内容深度的结果不硬凑（M1 步骤 3）；开头句式禁止与上一条同构（M1 步骤 2 / R4 E-020）
- **画面**：呈现手法轮换（7 手法，R3 §5.6），禁"卡片流"；左右安全区 ≥80px（M2 §3.3）；视觉观感比对防换皮同款（M2 §2.3）；视觉预演闸门+可截图抄作业（M2 §2.14）
- **数据屏**：先核对 R0 业务深度图谱/R1 §4.3/4.4 统计维度，禁编造指标（R3 §5.5）
- **产出前强制自检**：踩坑预检（R4 E-001~E-020）+ 用户视角三问 + 产品事实核对（M1 §6.6 / M2 §7.1）
- **TTS 语速**：seed-tts-2.0 的 speed_ratio 实测无效 → 用 ffmpeg atempo 1.2（M2 §五 阶段二）
- **发布稿**：md 不用 txt（M3 §1.5）；文章字数=深度结果不硬凑
- **画面确认**：逐屏关键帧单张，不渲染无声版、不拼九宫格（M2 §五 阶段一）

## 三、三大铁律（不可破）

1. **不虚假宣传**：只宣传代码里真实存在的功能（查 `R1-产品事实表.md`），不编店名/案例/数据；数字从 R1 关键数值取。
2. **不触平台红线**：三平台内容绝口不提微信/小程序/搜索，导流仅限账号资料位，不出现小程序码；完整红线见 `R5-内容红线.md`。
3. **统一口径**：用户拍板的决策/口径/红线必须回写权威文档（docs/internal/ 或 skill），不能只留在聊天里——今天的共识要让明天的 AI 也能读到。

## 四、协作规则

**AI 角色**：策划者 + 设计者，不是执行者。用户给方向，AI 拿方案；用户说感觉，AI 拆成落地动作。

**两步确认法**（所有实质性改动的标准流程）：
```
用户表达需求/想法/感觉
  ↓
第一步 理解确认：AI 用自己的话总结用户的意思（3-5 句）→ 问"我理解的对吗？"
  ↓ 确认后
第二步 规划确认：AI 列出做什么/怎么做/影响范围/先后顺序 → 问"按这个来，可以吗？"
  ↓ 确认后
第三步 执行：动手 → 完成后汇报
```
- **必须走两步确认**：改 2 份以上文档、新增/删除功能或组件、改变工作流程、用户表达的是"感觉/方向"而非明确指令
- **可直接做**：单文件小修改（错别字/格式）、用户明确指令"把 X 改成 Y"、生产回顾等日常事务

**意图落盘三原则**：① 用户原话优先 ② 落盘前回读确认 ③ 推断标注「（AI 理解，待确认）」

**其他规则**：
- 用户建议 = 信号/线索，不是钉死规则；收到建议先分析是否成立，必要时反对并给更好方案
- 严禁把 `outputs/` 实际产出写成「范例/模板/种子」硬塞进文档
- 删除文件 / 改环境配置前，先列清单向用户确认
- 发现文档问题 → 先报告 + 提方案，用户确认后再动手
- 工作流验证与正式产出**同标准**，不用"简化稿"验证（2026-08-25 用户拍板）
- 验证/生产中发现的问题 → **当场定位 + 提方案，用户确认后立即修复，问题完整解决后再进入下一环节**，不积压到最后（2026-08-25 用户拍板）
- **文档冻结**（2026-08-26 用户拍板）：文档体系下限防线已足够（当日架构审计验证），**非红线/事实变更不动文档**；停止大规模整理/重构；审计从每日改为低频定期。依据：outputs/archive/ 内 20260826 G04 审核复盘（已登记 README；结论：文档是记忆装置不是能力装置，质量投入转向模板体系）

## 五、文档写作六条（改任何权威文档必守）

> 文档不是写给人看的，是写给 AI 执行的。越像"代码"越好——明确、无歧义、有输入有输出。

1. **每条规则配"对的例子"和"错的例子"**——例：错「月增营收 4500」/ 对「发了多少张、核销了多少张，后台都看得见」
2. **能用表格就不用段落**——DO/DON'T 对比表 > 一段描述
3. **不用成语、比喻、抽象词**——不说"软种草"，说"像同行分享经验，不像广告"
4. **一个句子只说一件事**——不要长句套从句
5. **检查清单 > 原则说明**——能做成 checklist 的就做成 checklist
6. **"拿不准怎么办"必须写清楚**——例：「拿不准算不算编数据 → 不说具体数字」

更新规则：改内容时一起更新例子；新增规则按六条来写；**改任何权威文档后，同步更新该文档头部的「最后校验」日期**；**不追求"简洁"，追求"不会理解错"**。

## 六、闸门（改前同步 + 改完必跑）

> **改规则前（防漏改，2026-08-26 定）**：改任何规则/口径/参数前，先 `grep -rn "旧关键词" AGENTS.md docs/internal/` 列出全库命中清单，逐处同步后再动手——三闸门只防"引用断链"，防不住"语义矛盾"（教训：08-25 atempo 规则只改 M2/R3，漏了 R4 E-003 和 M1；券包修正只改 R1/R0，漏了 R2，08-26 审计才暴露）。
>
> 改任何权威文档后，三条闸门全跑一遍，全过才算对齐完成：
> `node scripts/check-redlines.mjs` && `node scripts/check-doc-references.mjs` && `node scripts/check-facts.mjs`

- **红线闸门** `check-redlines.mjs`：①画面层/②口播文案层=硬禁（`video/src` 零命中）；④文档层命中=描述红线规则属正常提示
- **文档引用检查** `check-doc-references.mjs`：扫描全部文档的「文件名/章节名」引用，失效即报错（防引用断链/残留旧编号）
- **事实检查** `check-facts.mjs`：验证文档依赖的关键资产路径在磁盘存在（注册表 `scripts/ref-registry.json` 维护）
- **变更记录**：每次改动权威文档，在 `outputs/archive/changelog.md` 顶部追加一条（最新在前）

## 七、目录规范

```
promotion/
├── docs/
│   ├── 战略简报.md          用户简报（人话版，AI 也应了解）
│   └── internal/            AI 权威文档（只此一份，outputs 不放副本）
│       ├── R0-业务深度图谱.md     产品业务深度（字段/规则/统计/干货速查）
│       ├── M1-内容策划手册.md
│       ├── M2-视频制作手册.md
│       ├── M3-平台发布手册.md
│       ├── R1-产品事实表.md
│       ├── R2-业务流程.md
│       ├── R3-Remotion技术参考.md
│       ├── R4-最佳实践库.md
│       ├── R5-内容红线.md
│       └── 素材索引表.md      素材登记（入库=登记，AI 选素材查这张表）
├── outputs/                 交付物
│   ├── gXX-行业/             行业单元最终交付（四件套/攻略/设计稿/视频/封面）
│   └── archive/             README.md（阅读纪律）+ changelog.md + 过程文件/审计/调研（日期前缀）
├── scripts/                 闸门与工具（check-redlines 等）
├── video/                   Remotion 工程（内容在 src/data/，组件在 src/components/ + src/scenes/）
└── .workbuddy/              AI 工作区（MEMORY.md / skills）
```

- `outputs/` 根目录不放散文件；非交付物一律进 `archive/`
- 权威文档需要复制时从 `docs/internal/` 取，不在 `outputs/` 建副本（避免不同步）

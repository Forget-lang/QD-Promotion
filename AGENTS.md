# 券到卡包 · 内容运营项目（AI 入口）

> 最后校验：2026-08-31（知识生命周期重构）。本文是 AI 的短入口，不是项目百科。
> 新会话先恢复当前认知和知识状态，再进入具体生产任务；不要通过历史日志自行猜当前规则。

## 一、项目是什么

- 产品：券到卡包——面向实体店商家的电子券工具（做券→发券→领券→扫码核销→统计）。
- 最终目标：AI 能自主生产准确、有吸引力、高质量的视频与文章，并最终一次生成可发布的完整视频。
- 当前阶段：能力研发 / 方法收敛期。用户仍通过真实产物进行最终判断，方法会继续演进。
- 产品源码：`../applet/`。涉及产品能力、字段、UI 行为的问题，优先回源码取证。

## 二、AI 新会话恢复顺序

1. `docs/PROJECT-UNDERSTANDING.md` —— 当前项目认知、人机分工、演进背景。
2. `docs/KNOWLEDGE-STATUS.md` —— 当前哪些能力稳定、哪些实验中、哪些已废弃。
3. `docs/KNOWLEDGE-LIFECYCLE.md` + `docs/EXPERIMENT-PROTOCOL.md` —— 反馈如何分流、方法如何晋级。
4. `SKILL.md` —— 当前实际生产方法。
5. `spec/` —— 需要产品事实时读取。
6. `docs/internal/`、样本、代码 —— 按任务需要深入。
7. `changelog.md` / Git 历史 —— 只有需要恢复“为什么过去这么改”时读取；不能作为当前规则入口。

## 三、开工动作

```bash
node scripts/gate-all.mjs
node scripts/list-assets.mjs
```

先看真实退出码，再根据任务进入生产；红灯不能交付。状态类问题一律以脚本输出/真实源码为准，不凭文档记忆。

## 四、反馈分流：默认最小变更

用户只需要描述目标、问题、想法或感受，不需要管理知识库。例如：

- “这屏太粗糙。”
- “内容像流水账。”
- “这个功能说得不对。”
- “能不能像这张参考图？”

AI 必须自行判断：

`INSTANCE / PROJECT / SERIES / GLOBAL / PRODUCT / EXPERIMENT`

处理原则：

- 当前镜头/当前视频问题 → 只修当前项目。
- 当前行业/系列问题 → 先在行业项目中验证；不要自动改全局。
- 新方法/新技术/新视觉路径 → 进入实验，验证后再考虑升级。
- 跨项目、已验证、能减少未来决策成本的方法 → 才进入稳定能力/生产规范。
- 产品事实/字段/UI 行为 → 更新对应真源 `spec/` / 产品源码。

**“用户反馈 = 修改 SKILL”不再是默认行为。**

## 五、用户与 AI 的职责

用户负责：目标、真实产物反馈、审美/商业判断、方向性想法、最终拍板。

AI 负责：研究、策划、技术/内容方案、实验、执行、验证、影响范围判断、必要知识沉淀。

用户不需要学习文件之间的同步关系，也不需要告诉 AI 应该修改哪份文档。

## 六、什么时候必须先让用户拍板

以下情况先给“理解 + 方案 + 影响范围”，得到确认后再执行：

- 修改全局生产方法；
- 让实验方案晋级为稳定能力；
- 修改产品事实；
- 改跨项目共享组件/规则；
- 改生产工作流；
- 引入新的自动检查；
- 大规模迁移、删除或重命名文档/代码。

当前项目局部的文案、视觉、分镜等修改，通常可直接执行，然后用真实产物验证。

## 七、质量不是“文档全绿”

硬事实、红线、工程检查是底线；真实画面与用户判断是最终质量反馈。量化指标用于诊断和防止明显退化，不能替代审美和商业效果判断。

## 八、文档纪律

- 不复制会变化的信息；优先引用唯一来源。
- 不把历史说明、实验过程重复塞进稳定生产规范。
- 不因为“让 AI 记住”就把局部意见写成全局规则。
- 新方法先实验，稳定后再进入生产层。
- 已废弃的方法可以保留历史，但必须与当前默认方法明确区分。

## 九、目录与核心角色

```text
promotion/
├── AGENTS.md                         # 新会话入口
├── SKILL.md                          # 当前稳定生产方法/流程
├── spec/                             # 产品事实与机器约束
├── docs/
│   ├── PROJECT-UNDERSTANDING.md     # 当前认知基线
│   ├── KNOWLEDGE-STATUS.md          # 稳定/实验/废弃状态
│   ├── KNOWLEDGE-LIFECYCLE.md       # 反馈分流与知识生命周期
│   ├── EXPERIMENT-PROTOCOL.md       # 实验与晋级规则
│   └── internal/                     # 业务/技术/UI 参考
├── scripts/                          # 闸门、检查、盘点、TTS
├── video/                            # Remotion 生产引擎
└── outputs/                          # 当前交付、视觉样本、历史
```

# 券到卡包 · 内容运营项目（AI 入口）

> 最后校验：2026-09-01（变更史只记 `outputs/archive/changelog.md`，本文不复述；本文自身如何归位，见 §二「文档宪法」）。
> 任何 AI 助手在本仓库开工前，先读本文件（30 秒），然后按「§三 开工三动作」执行。

## 一、项目是什么

- 产品名：券到卡包（面向实体店的电子券工具：做券→发券→领券→扫码核销→统计）｜ 运营主体：郑州百桨数字科技 ｜ 工作区：`promotion/`
- 阶段：宣传线冷启动（抖音短视频 + 小红书图文 + 搜狐长文），**无真实案例**，正文一律用行业泛称
- 产品源码位于 `../applet/`，功能有疑问直接查代码验证

## 二、文档在哪（每类知识只有一个真源）

| 你要什么 | 读 |
|---|---|
| 怎么做一条宣传视频（**唯一作业文档**，七步法） | `SKILL.md` |
| 产品事实 / 平台红线 / 素材登记 | `spec/facts.json` / `spec/redlines.json` / `spec/assets.json` |
| **制券页字段、分组归属、上限与联动**（动笔写"怎么设"前先查） | `spec/coupon-fields.json`——表里没有的字段/组名回 `../applet/` 源码取证并回写该行（带 `src` 行号），不得凭印象编 |
| 业务流程操作 / Remotion 技术原理 / applet UI 真值 | `docs/internal/` R2 / R3 / R6（按需，不是开工必读） |
| 下一片的候选场景怎么挖（官网素材：固定清单、三层采信、避雷台账） | `outputs/archive/官网资源库落地规划.md`（机制未转正，攒三片回看评估） |
| 项目总览 / 人机协作方法（面向用户；同受文档引用闸门扫描，改后照跑 `gate-all`） | `docs/战略简报.md` / `docs/AI使用手册.md` |
| 近期决策与踩坑 | `outputs/archive/changelog.md` 顶部 10 条 |
| 组件 / 素材 / 已产出片的实时清单 | `node scripts/list-assets.mjs` |
| G02~G05 历史测试产物（成片/设计稿/专属屏代码/旧文档） | **已删除**（2026-08-29 清零重启），git 历史可查但不得回流为参照；教训沉淀在 `SKILL.md` §四 |
| 爆款叙事方法 + 985 篇同赛道语料 + 道理弹药库 | `knowledge/爆款整片解剖.md`（18 条通则 + 使用索引，选题/结构/口播全链用）+ `knowledge/洞察库.md`（定律库，闭环故事「讲道理」层的燃料）+ `knowledge/爆款文案技能包/`（已废止的拆零件方法 + 语料库，仅作查证源）；规则见 `SKILL.md` 第 1/3 步 |

### 文档宪法（2026-09-01 用户拍板，对一切规则文档生效：本文件 / SKILL / spec / docs/internal / 启动提示词）

- **铁律一 · 历史只有一个家 = changelog，且台账只增不删。** 规则文档正文禁止写"第 N 轮修了什么 / 此前如何写 / 曾以什么理由照留"这类改动史；防回流只需一行「旧法已废止，详见 changelog」。台账超限按月分卷归档、绝不删条；挂账属现状，住待办文件，台账里只留指针。
- **铁律二 · 校验戳只写日期与指针。** `最后校验：YYYY-MM-DD（变更史见 changelog）` 是合法上限，戳内复述修复内容 = 违宪。
- **铁律三 · 状态快照不进规则文档。** 谁在用 / 多少条 / 行号 / 当前进度这类代码与流程状态，以 `list-assets` / `gate-all` / grep 实时输出为准，正文只写规则与指针——状态写进静态文档必然漂移。
- **铁律四 · 条款只写"做/不做"+ 一句判据。** 理由、案例回放、废止过程一律进 changelog；条款越短，被推翻时的改动面越小。
- 机检：check-doc-references 的宪法层执行铁律一/二（戳超长、"第 N 轮 / R1x"叙事即硬失败；含"见 changelog"的指针行豁免）。

## 三、开工三动作（每次新会话必执行）

1. `node scripts/gate-all.mjs` — 一条命令跑完全部闸门（**闸门清单与条数以脚本输出为准，任何文档内不得复述条数**，这是防口径漂移的硬规矩）。**红灯不产出、不交付**；带着红灯继续 = 视同没读规则。
2. `node scripts/list-assets.mjs` — 状态类问题（有什么组件/素材/成片）一律以脚本输出为准，不凭记忆。
3. 读 `outputs/archive/changelog.md` 顶部约 10 条，避免重复踩坑或推翻刚拍板的口径。

## 四、三大铁律（不可破）

1. **不虚假宣传**：功能、数字、术语必须能在 `spec/facts.json` **或对应产品线的真值表**（制券类内容 = `spec/coupon-fields.json`）找到依据；不编店名/案例/数据。两处说法冲突时以带源码行号的那条为准，并当场把另一处改齐——口径不一致本身就是缺陷。
2. **不触平台红线**：三平台正文、画面、账号资料位（昵称/简介/头像/置顶/私信）一律不写导流——不提微信/小程序/搜索，不写"搜/看简介/加V"等变体暗号；产品名只在内容内客观提及一次，转化靠用户自发搜索品牌词；以 check-redlines 输出为准。
3. **效果优先于合规表**：成片成败只由 `check-motion` 的数字 + 用户看真图的判断决定。"符合规范但没人看"= 方法错，改方法不改说辞。

## 五、协作规则（要点）

- AI = 策划者 + 设计者；实质性改动（改 2 份以上文档 / 动共享组件 / 改流程）先复述理解再提方案，用户拍板后执行。
- **单片反馈不动全局层**（2026-08-31 拍板）：AI 负责判断一条反馈该落哪层（项目局部 / 全局方法 / 产品事实 / 实验）并说明理由，但把结论写进 `SKILL.md` / `spec/` / `docs/internal/`（R3、R6 等）属升级全局，**须用户当轮明确确认，不得从"以后 / 都 / 别再"这类措辞推断**。被否的做法标「已废止、不得回用作参照」，不靠物理删除防回流。
- **落点路由协议**（2026-09-01 拍板，细则见 `docs/AI使用手册.md` §8.1，八句标准口令以该表为准）：用户说「全局标记 / 以后都这样」＝约束范围声明、≠指定写 SKILL；AI 动笔前必须先回报据（属哪类→落哪个文件哪个位置→理由），用户回「落」才写入。判据一句话：**机器能判的进 spec/ 真源表（无脚本消费不许建表）、人判类的进 SKILL 对应步骤旁、原理进 R 层、历史进 changelog**——同一内容只许存在一处，其余位置只留指针。
- **用户拍板的口径必须回写** SKILL 或 `changelog.md`，不能只留在聊天里。
- **审批画面必须给真图**（逐屏关键帧 / 短片）；给文字规格让人想象 = 没交付。
- 改任何权威文档后：跑 `node scripts/gate-all.mjs` + 在 changelog 顶部追加一条。
- **新片不得复用上一条行业的画面布局（2026-09-07 用户拍板，反复踩的头号坑）**：由 `check-layout-diversity` 闸门在 gate-all 里强制——新片每屏带 `layoutKind`，与上一条同 type 屏同布局即红灯。开工先读 SKILL 第 2 步 C-15（禁开旧片代码当模板）/ C-16（布局指纹）；撞红灯时**真换呈现架构**，不许删豁免或改标签蒙混，确需沿用须在 `scripts/ref-registry.json` 的 `layoutWaivers` 写非空 structuralDiff 证据。

## 六、目录

```
promotion/
├── AGENTS.md                      入口（本文件）
├── SKILL.md                      唯一作业文档（v7 清零重启版）
├── spec/                          机器可读真源（事实 facts / 红线 redlines / 素材登记 assets / 制券字段 coupon-fields）
├── docs/internal/                 参考层：R2 业务流程 / R3 Remotion 技术 / R6 applet UI 储备
├── scripts/                       闸门（check-* / gate-all）+ 盘点（list-assets）+ TTS 工具
├── video/                         Remotion 工程：数据在 src/data/，每条视频专属屏在 src/videos/gXX/
├── 商用字体/ 图标素材/ 截图素材/ 插图库/ 背景素材   素材库
└── outputs/                       交付物（gXX-行业/）+ 样本库（排版参照图）+ archive/（历史）
```

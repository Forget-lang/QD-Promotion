# 券到卡包 · 内容运营项目（Agent 入口）
> 最后校验：2026-08-20

> 任何 AI 助手（WorkBuddy / Claude Code / Cursor 等）在本仓库开工前，先读本文件。
> 本文件是「入口卡片」：告诉 AI 去哪读权威信息、守什么硬约束。**权威状态永远在 `docs/` + skill，不在此文件重述。**

## 项目一句话
面向实体店的电子券工具（做券→发券→领券→扫码核销→统计）的内容运营项目，当前做宣传推广线冷启动（抖音短视频 + 小红书图文 + 搜狐长文）。
产品源码位于 `../applet/`（相对于本文件所在的 `promotion/` 目录），功能有疑问时可直接查阅代码验证。

## AI 角色与协作范式（开工前必读）

> **AI 不是执行者，是策划者 + 设计者。** 用户给方向，AI 拿方案；用户说感觉，AI 拆成落地动作。

**三大行为准则**：

1. **主动全局思维**——接到需求后，先想"这件事在整个体系里的位置是什么？会影响哪些环节？哪些文档需要同步改？"，而不是只盯着眼前那一件事。改一处，全量对齐。

2. **两步确认法**——任何实质性改动，必须先出方案再动手：
   - **第一步：理解确认**——先用自己的话总结用户的意思，让用户确认"你理解对了吗"
   - **第二步：规划确认**——理解对齐后，列出具体规划（做什么、怎么做、影响范围、先后顺序），让用户确认"就这么干"
   - 两步都过了，才开始执行。禁止"用户说一句我做一句"的零散式响应。

3. **统一口径不零散**——所有决策、口径、规则必须回写到权威文档（`docs/` 或 skill），不能只停留在聊天里。今天的共识要让明天的 AI 也能读到。

**反面例子（禁止这样做）**：
- 用户说"背景图不明显"，就只调背景图透明度，不管文字对比度、整体风格是否还协调
- 用户说"加字幕"，就只加字幕组件，不管安全区、内容位置是否需要上移
- 改了一份文档，不查其他引用了同一事实的文档
- 遇到新问题不总结，下次还犯同样的错

## 开工必读（按序）
1. `docs/工作流指引.md` —— **AI 接手手册**：新会话读它即明确如何开工（行业单元端到端范式）
2. `.workbuddy/skills/quandao-content/SKILL.md` —— **开工导航页**（红线唯一真源=14 号文档，核心模型与 SOP 见 `docs/internal/`）
3. `.workbuddy/memory/MEMORY.md` —— **项目长期记忆**（决策锚 / 进度 / 协作准则）
4. `docs/战略简报.md` —— 用户唯一需读的人话简报（AI 也应了解）

## 权威分层（改一处 · 全量对齐）
- **事实唯一源**：`docs/internal/00-产品事实表.md`（数字/术语/能力边界，写文案必查）
- **流程唯一源**：`docs/internal/02-业务流程.md`（操作步骤/状态流转，写教程必查）
- **营销视角索引**：`docs/internal/05-内容弹药库.md`（卖点/切入点/行业推荐，策划必查）
- **经验沉淀层**：`docs/internal/09-最佳实践库.md`（高转化句式/踩坑记录/风格偏好，越做越厚）
- **内容质量唯一源**：`docs/internal/08-内容质量三层标准.md`（三层结构：硬边界/方向锚/创作自由）
- **SOP 唯一源**：`docs/internal/12-宣传产品工作完整操作流程.md`
- **视频技术唯一源**：`docs/internal/13-Remotion技术规范与设计稿工作流.md`（动画硬规则/设计稿模板/图片素材规格表/风格五维落地状态）
- **红线唯一源**：`docs/internal/14-内容红线.md`（红线唯一真源，`scripts/redlines.json` 为违禁词机器守卫数据源）
- **记忆唯一源**：`.workbuddy/memory/MEMORY.md`（决策锚/进度/经验沉淀/决策日志，所有更新直接写此文件）；`YYYY-MM-DD.md` 仅为可选过程流水，非权威。

> 产品文档三兄弟：策划选题翻 05（找卖点）→ 写文案查 00（对事实）→ 写教程查 02（对流程）。任何对外描述以 00 号为最终裁决。
> 自进化：每次生产做完，步骤 12 强制生产回顾，沉淀写入 09 最佳实践库。做得越多，系统越强。

## 硬约束（必守）
- **决策回写权威层**：用户拍板的决策/口径/红线，必须同步写进 `docs/` 或 SKILL，不能只留在 `.workbuddy/memory/`（否则其他 agent 读不到，造成记忆与文档不同步）。
- **改红线/CTA/事实口径后必跑闸门**：`node scripts/check-redlines.mjs`，`video/src` 层零命中才算对齐完成。
- **视频 = 代码 + 数据渲染**（Remotion）：内容在 `video/src/data/`、组件库在 `video/src/components/`+`scenes/`；组件库是原子积木非成品模板，每条视频独立设计、风格轮换（相似度检查 ≥3 维不同）；**设计稿必须按 13 §5 模板写到帧级，图片素材由 AI 出规格表、人从设计平台找**（13 §4）。
- **三平台文章/视频绝口不提微信/小程序/搜索**，导流仅限账号资料位。
- **AI 可读性铁律**：所有权威文档的写作必须遵守 `08-内容质量三层标准.md`「文档写作规范」六条——写给 AI 执行的文档，越像"代码"越好：明确、无歧义、有对有错的例子、拿不准怎么办写清楚。
- **删除文件/改环境配置前先列清单向用户确认**。

## 目录规范
```
promotion/
├── docs/internal/   权威文档（00-14），只此一份，outputs 不放副本
├── outputs/
│   ├── gXX-行业/     行业单元最终交付物（四件套/攻略/设计稿/视频）
│   └── archive/     过程文件/审计报告/调研资料（日期前缀）+ changelog.md（文档变更日志）
├── scripts/         红线闸门等工具
├── video/           Remotion 工程
└── .workbuddy/      AI 工作区（memory=MEMORY.md / skills）
```
- `outputs/` 根目录不放散文件；非交付物一律进 `archive/`。
- 权威文档需要复制时从 `docs/internal/` 取，不在 `outputs/` 建副本（避免不同步）。

## 交叉引用表（改一处 · 全量对齐核查清单）

> **用途**：每次修改权威文档中的某条规则时，按此表逐行检查，确保所有复述位置同步更新。
> **维护规则**：新增复述时在此表追加一行；删除复述时删除对应行。

| # | 信息类别 | 唯一真源 | 复述位置（需同步更新） |
|---|---|---|---|
| 1 | **红线规则**（不虚构/不编数据/禁极限词/不提微信/企微泛称/核销活码/免费边界） | `14-内容红线.md` | `AGENTS.md` 硬约束 §、`SKILL.md` §三（**仅此 2 处保留速记**，其余 6 处均已改为引用） |
| 2 | **视频标准**（120-150s / 8-10 屏 / ≥6 信息单元 / ≤200s 硬上限 / ≥2 层递进） | `08-内容质量三层标准.md` §2.5 | `战略简报.md` §五、`工作流指引.md` §三、`12-SOP` 步骤 1.5、`MEMORY.md` 内容模式速记、`douyin-short-video/SKILL.md` |
| 3 | **两步确认法**（理解确认→规划确认→执行） | `工作流指引.md` §〇.1 | `AGENTS.md` AI 角色 §、`12-SOP` §二.5、`SKILL.md` §五、`MEMORY.md` 协作准则 |
| 4 | **AI 角色定义**（策划者+设计者，不是执行者） | `工作流指引.md` §〇 | `AGENTS.md` AI 角色 §、`12-SOP` §二.5、`SKILL.md` §五、`MEMORY.md` 协作准则 |
| 5 | **目录结构**（promotion/ 下的目录树） | `AGENTS.md` 目录规范 § | `工作流指引.md` §五 |
| 5b | **素材库结构与数量** | `11-素材库与图片生产规范.md` §1 | `工作流指引.md` §六（已改为引用，不再复述数量） |
| 6 | **生产回顾**（强制动作 + 沉淀进 09 号） | `12-SOP` 步骤 12 | `工作流指引.md` §五、`AGENTS.md` 权威分层注、`SKILL.md` §五、`MEMORY.md` 协作准则 |
| 7 | **闸门脚本**（`check-redlines.mjs` + `redlines.json`） | `14-内容红线.md` §7 | `AGENTS.md` 硬约束、`工作流指引.md` §四、`08号` §1.1、`12-SOP` §二.5、`README.md` §四、`SKILL.md` §五 |
| 8 | **封面与首帧关系**（封面≠首帧，封面独立构图，首帧复用 hero 视觉） | `13-Remotion技术规范.md` §5.2 | `12-SOP` 步骤 7、`07-渠道与执行.md` §3、`douyin-short-video/SKILL.md`、`MEMORY.md` |
| 9 | **风格轮换**（五维 ≥3 维不同 + 场景序列 ≥3 位置不同） | `13-Remotion技术规范.md` §5 | `08-内容质量三层标准.md` §3.1、`12-SOP` 步骤 4、`工作流指引.md` §三、`AGENTS.md` 硬约束、`MEMORY.md` 内容模式速记 |
| 10 | **免费卖点边界**（正文可自然提及，封面/开头/结尾不刻意提） | `14-内容红线.md` §4.3 | `08-内容质量三层标准.md` §1.5、`12-SOP` §四、`07-渠道与执行.md` |
| 11 | **源码路径**（applet/ 位于 promotion/ 上级目录） | `AGENTS.md` 项目一句话 § | `README.md` 头部、`02-业务流程.md` 头部、`04-宣传策略.md` §8、`战略简报.md` §三 |
| 12 | **开工阅读顺序**（工作流指引→SKILL→MEMORY→战略简报） | `AGENTS.md` 开工必读 § | `SKILL.md` §二（引用）、`工作流指引.md` §一（落地版）、`MEMORY.md` 开工索引（引用） |
| 13 | **核心内容集中原则**（y:200-1100 / 卡片 padding ≥43px / 间距 ≥20%） | `13-Remotion技术规范.md` §2.3 | `13号` §5.4 分镜表模板、`12-SOP` 步骤 4 |
| 14 | **视觉资源索引与检索流程**（先查索引→有就用→没有输出找图需求单） | `11-素材库与图片生产规范.md` §9 | `13号` §5.4 分镜表模板、`12-SOP` 步骤 4 |
| 15 | **排版参照库**（8 个参照条目，设计稿选参照标注借鉴手法） | `09-最佳实践库.md` §八 | `13号` §5.4 分镜表模板、`12-SOP` 步骤 4 |
| 16 | **字幕同步规范**（字幕=口播逐字稿 / 语义断句 / TTS 实测时长） | `13-Remotion技术规范.md` §2.7 | `12-SOP` 步骤 8 |

## 可用技能（内置，无需安装）

| 技能 | 用途 | 工作流中的使用环节 |
|---|---|---|
| **ui-ux-pro-max** | UI/UX 设计智能数据库（67 种风格/96 种配色/99 条 UX 指南），查排版原则、色彩理论、视觉层次 | **设计稿阶段（12-SOP 步骤 4）必用**：AI 写画面布局时直接调用，获取配色方案/排版原则/视觉层次规范，指导设计稿产出 |

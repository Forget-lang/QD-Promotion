# 券到卡包 · 内容运营项目（AI 入口）

> 最后校验：2026-08-27（文档架构重构阶段 3：旧文档存档 docs/archive-legacy/，入口切换到 spec/ 三真源 + workflow/ 四散文 + R2/R3；08-26 文档体系重构 v2）
> 任何 AI 助手在本仓库开工前，先读本文件（约 3 分钟）。读完按「§二 开工三动作」执行：跑盘点脚本 → 读 changelog 近况 → 按「任务 → 手册映射」读对应手册，**不需要**读全部文档。
> 本项目面向实体店电子券工具（做券→发券→领券→扫码核销→统计），当前做宣传推广线冷启动：抖音短视频 + 小红书图文 + 搜狐长文。
> 产品源码位于 `../applet/`（本文件所在 `promotion/` 的上级目录），功能有疑问时直接查阅代码验证。

---

## 一、项目是什么

- 产品名：券到卡包 ｜ 运营主体：郑州百桨数字科技 ｜ 工作区：`promotion/`
- 阶段：宣传线冷启动，**无真实案例**；正文一律用行业泛称（咖啡店/火锅店/美容店…），不编具体商户名、不编数据
- 三大产品：优惠券（6 型）/ 券包 / 次卡；支持加好友/进群自动发券

## 二、开工三动作（每次新会话必执行）

**动作 1 跑盘点**——`node scripts/list-assets.mjs`（项目根目录）：组件 / 模板 / 样本 / 素材的实时清单。状态类问题（有哪些组件/模板/背景图、什么状态）一律以脚本输出为准，不凭对文档静态表的记忆作答。

**动作 2 读近况**——`outputs/archive/changelog.md` 顶部最近 ~10 条：近期决策、纠错、踩坑。避免重复踩同一个坑，或推翻刚拍板的口径。

**动作 3 按任务读手册**——按下方映射表读对应手册（按需读相关章节，不必整本通读）。

### 任务 → 手册映射（开工先看这张表）

| 你要做的事 | 必读 | 按需查 |
|---|---|---|
| 内容策划：行业调研 → 选玩法 → 写文案/口播 → 深度自检 | `workflow/pipeline.md` §1 + `workflow/策划弹药库.md` | `spec/facts.json`、R2 流程 |
| 视频制作：设计稿 → 代码 → 画面验证 → TTS → 成片 | `workflow/pipeline.md` §2 + `workflow/craft.md` | R3 技术参考 |
| 平台发布：封面 → CTA → 抖音/小红书/搜狐规则 → 自查 | `workflow/pipeline.md` §3 + `workflow/发布弹药库.md` | `spec/redlines.json` |
| 写教程类长文，需要产品操作步骤 | `docs/internal/R2-业务流程.md` | `spec/facts.json` |
| 内容涉及产品功能怎么运作（字段/规则/统计维度/干货点） | `spec/facts.json`（含 deep 深度层） | R2 流程 |
| 合规争议：某句话能不能说 | `spec/redlines.json` | — |
| 高转化句式 / 排版参照 / 审美标准 | `workflow/craft.md` + `workflow/策划弹药库.md` §句式 | — |
| 资产状态：组件/模板/样本/素材有什么 | `node scripts/list-assets.mjs` | `spec/assets.json` |

> **spec/ 三份数据文件是三类真源**：事实（facts.json）/ 红线（redlines.json）/ 素材登记（assets.json）；**workflow/ 四份散文是做法与弹药**：流程闸门（pipeline）/ 审美（craft）/ 策划弹药 / 发布弹药。**每类知识只有一个真源，其他文档只引用不复述。**

### 新增/修改内容归属判定（拿不准往哪放时查此表）

| 内容类型 | 放哪 | 示例 |
|---|---|---|
| 产品事实 / 数字 / 术语 / 能力边界 / 业务深度 | `spec/facts.json` | "随机金额券 1-99 整数" |
| 红线 / 合规口径 / 机检模式 | `spec/redlines.json` | "封面不刻意提免费" |
| 素材登记（入库=登记） | `spec/assets.json` | "BG-ABS-001 抽象模板" |
| 流程环节 / 步骤 / 闸门清单 | `workflow/pipeline.md` | "两阶段生产流程、发布闸门" |
| 审美 / 视觉原则 / 排版参照 / 布局模式 | `workflow/craft.md` | "字号阶梯、布局模式 L-05" |
| 玩法 / 卖点 / 客群 / 句式金句 / 视频干货 | `workflow/策划弹药库.md` | "P-05 老带新转赠奖励" |
| 封面 / CTA / 平台细则 / 标题话题 | `workflow/发布弹药库.md` | "小红书 3:4 裁切中间区" |
| 操作步骤 / 状态流转 / 渠道枚举 | `docs/internal/R2-业务流程.md` | "私密发放 5 分钟~永久五档" |
| 技术原理 / 组件 API / 制作硬规则 | `docs/internal/R3-Remotion技术参考.md` | "J-cut 为什么能帧级同步" |
| 协作规则 / 项目规范 / 闸门 | `AGENTS.md` | "两步确认法、文档写作六条" |
| 变更记录 | `outputs/archive/changelog.md` | 每次改文档追加一条 |

**判定逻辑**：先问"这是**事实/红线/登记**（数据 → spec/）、**怎么做**（流程/审美 → workflow/）还是**查什么**（链路/技术 → docs/internal/）"。拿不准时按上表就近归类，跨类经验先记 changelog、验证后沉淀进对应文档；改文档后按 §六 闸门跑检查并记 changelog。

## 二·5、近期关键规则速览（新会话开工先扫一眼）

> 详情在各文档对应章节；这里只列"开工必须知道的新规矩"，防止新会话按旧习惯产出：

- **验证/生产与正式产出同标准**；发现问题当场修复、解决后再进下一环节（§四 协作规则）
- **文案**：顾客反应不得绝对化；字数/时长=内容深度的结果不硬凑；开头句式禁止与上一条同构（pipeline §1 步骤 2/3）
- **画面**：呈现手法轮换（7 手法，R3 §5.6），禁"卡片流"；左右安全区 ≥80px（R3 §7.3）；视觉观感比对防换皮同款（pipeline §2.2）；视觉预演闸门+可截图抄作业（pipeline §2.4）
- **数据屏**：先核对 `spec/facts.json` deep 层统计维度，禁编造指标（R3 §5.5）
- **产出前强制自检**：踩坑预检 + 用户视角三问 + 产品事实核对（pipeline §1.6 / §2.7）
- **TTS 语速**：seed-tts-2.0 的 speed_ratio 实测无效 → 用 ffmpeg atempo 1.2（pipeline §2.5 阶段二）
- **发布稿**：md 不用 txt（pipeline §3.4）；文章字数=深度结果不硬凑
- **画面确认**：逐屏关键帧单张，不渲染无声版、不拼九宫格（pipeline §2.5 阶段一）

## 三、三大铁律（不可破）

1. **不虚假宣传**：只宣传代码里真实存在的功能（查 `spec/facts.json`），不编店名/案例/数据；数字从 facts 关键数值取。
2. **不触平台红线**：三平台内容绝口不提微信/小程序/搜索，导流仅限账号资料位，不出现小程序码；完整红线见 `spec/redlines.json`。
3. **统一口径**：用户拍板的决策/口径/红线必须回写权威文档（spec/ 或 workflow/），不能只留在聊天里——今天的共识要让明天的 AI 也能读到。

## 四、协作规则

**AI 角色**：策划者 + 设计者，不是执行者。用户给方向，AI 拿方案；用户说感觉，AI 拆成落地动作。

**两步确认法**（所有实质性改动的标准流程）：
```
用户表达需求/想法/感觉
  ↓
第一步 理解确认：AI 用自己的话总结用户的意思（3-5 句）→ 问"我理解的对吗？"
  ↓ 确认后
第二步 规划确认：AI 列出做什么/怎么做/影响范围/先后顺序 → 问"按这个来，可以吗？"
  ↓ 确认后
第三步 执行：动手 → 完成后汇报
```
- **必须走两步确认**：改 2 份以上文档、新增/删除功能或组件、改变工作流程、用户表达的是"感觉/方向"而非明确指令
- **可直接做**：单文件小修改（错别字/格式）、用户明确指令"把 X 改成 Y"、生产回顾等日常事务

**意图落盘三原则**：① 用户原话优先 ② 落盘前回读确认 ③ 推断标注「（AI 理解，待确认）」

**其他规则**：
- 用户建议 = 信号/线索，不是钉死规则；收到建议先分析是否成立，必要时反对并给更好方案
- 严禁把 `outputs/` 实际产出写成「范例/模板/种子」硬塞进文档
- 删除文件 / 改环境配置前，先列清单向用户确认
- 发现文档问题 → 先报告 + 提方案，用户确认后再动手
- 工作流验证与正式产出**同标准**，不用"简化稿"验证（2026-08-25 用户拍板）
- 验证/生产中发现的问题 → **当场定位 + 提方案，用户确认后立即修复，问题完整解决后再进入下一环节**，不积压到最后（2026-08-25 用户拍板）
- **文档冻结**（2026-08-26 用户拍板，2026-08-27 重构后延续）：架构重构已完成（10 份散文 → spec/ 三真源 + workflow/ 四散文 + R2/R3），**非红线/事实变更不动文档**；停止大规模整理；审计低频定期。依据：outputs/archive/ 内 20260826 G04 审核复盘（结论：文档是记忆装置不是能力装置，质量投入转向模板体系）

## 五、文档写作六条（改任何权威文档必守）

> 文档不是写给人看的，是写给 AI 执行的。越像"代码"越好——明确、无歧义、有输入有输出。

1. **每条规则配"对的例子"和"错的例子"**——例：错「月增营收 4500」/ 对「发了多少张、核销了多少张，后台都看得见」
2. **能用表格就不用段落**——DO/DON'T 对比表 > 一段描述
3. **不用成语、比喻、抽象词**——不说"软种草"，说"像同行分享经验，不像广告"
4. **一个句子只说一件事**——不要长句套从句
5. **检查清单 > 原则说明**——能做成 checklist 的就做成 checklist
6. **"拿不准怎么办"必须写清楚**——例：「拿不准算不算编数据 → 不说具体数字」

更新规则：改内容时一起更新例子；新增规则按六条来写；**改任何权威文档后，同步更新该文档头部的「最后校验」日期**；**不追求"简洁"，追求"不会理解错"**。

## 六、闸门（改前同步 + 改完必跑）

> **改规则前（防漏改，2026-08-26 定）**：改任何规则/口径/参数前，先 `grep -rn "旧关键词" AGENTS.md spec/ workflow/ docs/internal/` 列出全库命中清单，逐处同步后再动手——三闸门只防"引用断链"，防不住"语义矛盾"。
>
> 改任何权威文档后，三条闸门全跑一遍，全过才算对齐完成：
> `node scripts/check-redlines.mjs` && `node scripts/check-doc-references.mjs` && `node scripts/check-facts.mjs`

- **红线闸门** `check-redlines.mjs`（读 `spec/redlines.json`）：①画面层/②口播文案层=硬禁（`video/src` 零命中）；④文档层命中=描述红线规则属正常提示
- **文档引用检查** `check-doc-references.mjs`：扫描 AGENTS + workflow/ + docs/internal/ 的「文件名/章节名」引用，失效即报错（防引用断链/残留旧编号）
- **事实检查** `check-facts.mjs`：验证文档依赖的关键资产路径在磁盘存在（注册表 `scripts/ref-registry.json` 维护）
- **变更记录**：每次改动权威文档，在 `outputs/archive/changelog.md` 顶部追加一条（最新在前）

## 七、目录规范

```
promotion/
├── AGENTS.md                入口（本文件）
├── spec/                    机器可读真源（事实/红线/素材登记，唯一数据层）
│   ├── facts.json            产品事实+业务深度（唯一事实源）
│   ├── redlines.json         内容红线+机检模式（唯一红线源）
│   └── assets.json           素材登记（入库=登记）
├── workflow/                流程与弹药散文（怎么做 + 取用素材）
│   ├── pipeline.md           流程唯一主线：策划→视频→发布 + 全部闸门
│   ├── craft.md              审美与视觉原则（层级参数/排版参照/布局模式）
│   ├── 策划弹药库.md          玩法/卖点/客群/句式金句/视频干货
│   └── 发布弹药库.md          封面/CTA/平台细则/标题话题
├── docs/
│   ├── 战略简报.md            用户简报（人话版，AI 也应了解）
│   ├── internal/             保留参考（低频查阅）
│   │   ├── R2-业务流程.md     全链路操作步骤/状态流转/渠道枚举
│   │   └── R3-Remotion技术参考.md  技术原理/组件 API/制作硬规则
│   └── archive-legacy/       旧文档存档（只读，不引用；2026-08-27 归档 M1/M2/M3/R0/R1/R4/R5/素材索引表）
├── outputs/                 交付物
│   ├── gXX-行业/             行业单元最终交付（四件套/攻略/设计稿/视频/封面）
│   ├── 模板库/ 样本库/        设计模板与参照样本
│   └── archive/             README.md（阅读纪律）+ changelog.md + 过程文件（日期前缀）
├── scripts/                 闸门与工具（check-* 三闸门 + list-assets 盘点）
├── video/                   Remotion 工程（内容在 src/data/，组件在 src/components/ + src/scenes/）
└── .workbuddy/              AI 工作区（MEMORY.md / skills）
```

- `outputs/` 根目录不放散文件；非交付物一律进 `archive/`
- `docs/archive-legacy/` 只读存档：历史查证可看，**新内容一律不引用不追加**
- 权威文档需要复制时从 `spec/`、`workflow/`、`docs/internal/` 取，不在 `outputs/` 建副本（避免不同步）

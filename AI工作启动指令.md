# 券到卡包 · AI 工作启动指令

> **这是用户侧工作控制台，不是项目规则 Owner，也不是 AI 自动入口。**
> 项目自动入口是 `AGENTS.md`；视频生产唯一作业流程是 `SKILL.md`；其他领域由对应 Owner 负责。
> 本文件只规定：**AI 如何找到当前 Owner、读取当前版本、解析工作流、逐节点执行、证明完成。**
> **本文件不得复制任何具体业务工作流。**

## 一、所有 AI 必须遵守的固定执行协议

无论当前任务是什么，只要需要实际执行项目工作，必须先遵守以下协议：

```text
1. 找到当前任务对应的唯一 Owner。
2. 读取该 Owner 的当前版本全文；不得用历史版本、聊天记忆或常识替代。
3. 把当前 Owner 的工作流解析成临时执行表：
   Step → 输入 → 必须执行 → 必须产出 → 完成条件 → 下一步门槛。
4. 读取当前已有产物，确定已经完成到哪个节点。
5. 从第一个“当前节点未满足完成条件”的地方继续。
6. 前一节点未完成，不得进入下一节点。
7. 不得自行压缩、跳过、重排、合并或替代 Owner 定义的节点。
8. 每完成一个节点，必须用真实文件、命令、产物、截图/关键帧、CI 或其他可检查证据证明完成。
9. 如果 Owner 没有定义某个必要步骤，明确报告：`当前 Owner 未定义`；不得自行发明长期规则。
10. 如果 Owner 内部出现冲突、第二 Owner、第二入口或当前/历史混淆，立即 STOP，回到 Owner/入口审计。
11. 如果任务需要用户方向性决定、最终审美或发布决定，停止修改，只报告需要用户决定的事项。
12. 没有真实证据，不得使用“完成”“已落实”“已闭环”“CLOSED”。
```

### “完成”统一定义

> **完成 = 当前 Owner 的该节点完成条件全部满足 + 有真实证据。**

以下都不算完成：

- 只读过文档；
- 只写了计划；
- 只生成草稿；
- 只给用户总结；
- AI 自己说“差不多”；
- 只通过关键词检查；
- 历史产物看起来已经做过；
- 后续节点已经做了，但前置节点未满足完成条件。

## 二、当前与历史

- 当前规则：只认当前 Owner。
- 当前状态：优先认脚本/当前产物的实时证据。
- 历史：只用于知道过去做过什么、为什么改过、检查重复；不得反向决定当前方案。
- `outputs/archive/` 与 `changelog` 不是当前规则入口。

## 三、Owner 路由

```text
用户方向 / 拍板
        ↓
AGENTS.md
        ↓
当前任务唯一 Owner
        ↓
当前 Owner 的输出
        ↓
Gate / 真实产物 / 用户最终验收
```

视频生产：`SKILL.md`
产品事实：`APPLET / spec / R10`
业务流程解释：`R2`
Remotion 技术：`R3`
UI 视觉证据：`R6`
工程组件：`R7`
全局变更治理：`R8`
视觉导演：`R9`
资源职责边界：`docs/internal/内容资源调用协议.md`
整改前置侦察：`docs/internal/整改作战总纲.md`
历史证据：`changelog / archive`

任何一个概念发现两个当前 Owner 时，立即 STOP，不继续局部执行。

## 四、场景导航

| 场景 | 作用 |
|---|---|
| A | 新会话/换 AI/接班项目 |
| B | 当前时间点选行业/选题 |
| C | 复杂任务拆解与多模型协作 |
| D | 只调查问题，不修改 |
| E | 正式全局整改 |
| F | 整改结果复核 |
| G | 正式生产视频 |
| H | 局部修复 |
| I | Remotion 工程实现 |
| J | 完成证据复核 |
| K | 想法评估 |
| L | 全局审计 |

## 五、场景 A｜新会话正式开工

### 📋 发给 AI

```text
现在正式接手「券到卡包」项目。

不要让我重新介绍项目。
不要凭聊天记忆猜规则。
不要把历史产物直接当当前方案。

严格执行以下协议：
1. 读取 AGENTS.md。
2. 找到当前任务对应的唯一 Owner。
3. 读取该 Owner 当前版本全文。
4. 把 Owner 工作流解析成：Step → 输入 → 必须执行 → 必须产出 → 完成条件 → 下一步门槛。
5. 检查当前已有产物，找到第一个未满足完成条件的节点。
6. 直接完成这个节点；前一节点未完成，不进入下一节点。
7. 每完成一个节点，都用真实证据证明。
8. 如果必须我决定方向、审美或发布，立即停下来报告；否则直接继续。

接班报告只写：
当前任务：
当前 Owner：
当前节点：
已完成及证据：
下一步：
需要我决定：无/具体事项

然后立即继续执行，不要只做总结。
```

新片还必须遵守：历史片只能用于查重和确认过去做过什么；不得把旧片构图、镜头关系、视觉母题、字幕布局、动效编排或整套样式当新片模板。允许复用的只有项目级公共能力、真实产品 UI、品牌资产、R7 已登记通用组件和明确批准的长期公共资产。

## 六、场景 B｜当前时间点选题

### 📋 发给 AI

```text
现在只做选题，不生产视频。
先读取 AGENTS.md、当前生产 Owner、Product Truth、当前已产出状态。
再核实当前时间、节日、消费节点、行业经营周期和近期有效信息。

每个候选方向必须写：
行业 / 经营时刻 / 老板痛点 / 当前做法 / 产品真实能力 / 可研究玩法 / 视频切口 / 已做内容冲突。

至少给 5 个候选。
不要伪造数据，不要用历史信息冒充当前信息，不替我最终拍板。
完成后停在选择节点。
```

## 七、场景 C｜复杂任务拆解

### 📋 发给 AI

```text
先不要大规模执行。
读取当前任务对应 Owner 后，把任务拆成：节点 / 输入 / 输出 / 依赖 / 可否并行 / 验证 / 用户介入点。
只有需要不同能力时才拆多模型；不要为了“多模型”强行拆分。
先形成可执行节点表，再执行。
```

## 八、场景 D｜问题侦察，不修改

### 📋 发给 AI

```text
现在只调查，不修改。
找出问题涉及的 Owner、入口、当前规则、历史规则、重复、冲突、断档、影响面。
不要凭猜测，不要顺手修复。
完成后只报告：问题、证据、Owner、影响面、是否需要整改、需要我决定什么。
```

## 九、场景 E｜全局整改

### 📋 发给 AI

```text
现在进入正式全局整改。
先读取 docs/internal/整改作战总纲.md。
然后按 docs/internal/R8-变更收敛协议.md 执行：
侦察 → Owner → 入口 → 当前/历史边界 → 冲突/重复/断档 → Impact Map → Change Contract → Migration → Verification → Closure。

未完成全局侦察，不得进入 Migration。
不得通过新增第二个 Owner、第二个入口或新增平行流程解决问题。
替代型变更必须清旧并做负向/语义反例测试。
没有最终证据，不得写 CLOSED。
```

## 十、场景 F｜整改完成复核

### 📋 发给 AI

```text
不要相信之前的“已完成”结论。
从当前仓库状态重新核对 Change Contract：
Owner / 入口 / 影响面 / 旧口径 / 当前产物 / 机械检查 / 负向测试 / 语义反例 / 真产物。
任何一项没有最终证据，都不能判 CLOSED。
```

## 十一、场景 G｜正式生产视频

### 📋 发给 AI

```text
现在正式生产视频。
只认当前 SKILL.md 的完整工作流，不得自行压缩或改写。
先解析当前 SKILL 的节点、输入、输出和完成条件。
从当前未完成节点继续。

内容资源调用必须遵守 docs/internal/内容资源调用协议.md：
产品真实能力回 Product Truth；官网用于场景/玩法理解；玩法版图用于组合视角；爆款资源用于传播研究。
先确定“这一条到底教什么”，再研究“怎么讲”。

前一节点未完成，不得进入下一节点。
```

## 十二、场景 H｜局部修复

### 📋 发给 AI

```text
只修当前明确问题。
先定位唯一 Owner 和当前产物。
不修改其他全局规则，不顺手整理其他文档。
如果发现这个问题实际属于流程/共享组件/跨行业规则，停止局部修复，报告它已升级为全局变更。
```

## 十三、场景 I｜Remotion 工程实现

### 📋 发给 AI

```text
只进入当前片的工程节点。
读取当前 SKILL、R3、R7、R9 以及当前分镜/R9 产物。
不得重新决定已经批准的内容方向。
使用已登记公共组件和真实产品 UI；先确认当前片的工程依赖，再实现、渲染、验证。
```

## 十四、场景 J｜完成证据复核

### 📋 发给 AI

```text
现在只做证据复核。
不要把之前的总结当证据。
逐节点检查：实际文件 / 实际命令 / 实际产物 / 当前 CI 或 Gate / 人工验收证据。
发现一项缺证据，就标记未完成；不得补写证据、不得借历史 PASS。
```

## 十五、场景 K｜想法评估

### 📋 发给 AI

```text
现在只评估这个想法，不执行。
先判断它属于局部任务还是全局变更。
检查是否违反现有 Owner、入口、Product Truth、平台红线或当前生产流程。
给出：问题、价值、风险、影响面、应落哪个 Owner、建议下一步。
没有得到执行决定，不修改仓库。
```

## 十六、场景 L｜全局审计

### 📋 发给 AI

```text
现在只做全局审计，不顺手整改。
按 docs/internal/整改作战总纲.md 的资源地图、Owner 地图、能力强弱矩阵、依赖/冲突地图执行。
重点找：第二 Owner、第二入口、新旧口径并存、历史越权、重复流程、无 Owner 规则、检查器与真实语义冲突。
输出问题清单和 P0/P1/P2，不修改文件。
```

## 十七、最终纪律

> **启动指令永远不要复制业务工作流。业务工作流变化只改 Owner；启动协议只负责解释“如何执行当前 Owner”。**
>
> **任何节点都只能由当前 Owner 判定完成；任何证据都必须来自当前状态；任何历史都不得重新进入当前指挥链。**

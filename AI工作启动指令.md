# 券到卡包 · AI 工作启动指令

> **这是用户侧工作控制台，不是项目规则 Owner，也不是 AI 自动入口。**
> 项目自动入口是 `AGENTS.md`；视频生产唯一作业流程是 `SKILL.md`；其他领域由对应 Owner 负责。
> 本文件只规定：**用户如何让 AI 进入正确工作模式，以及 AI 如何开始、执行、停线、证明完成。**
> **本文件不得复制具体业务规则，不得复制 Owner 的具体工作流。**

## 〇、使用方式

本文件分三层，AI 的消费方式不同：

```text
公共协议层   §一 固定执行协议 · §二 当前与历史 · §三 Owner 路由 · §四 场景选择与导航
             → 所有场景共同适用，不随场景变化

场景层       场景 A ~ M
             → 用户按当前任务选择，并复制对应场景的「📋 发给 AI」

公共收口层   §十八 统一停线与交接规则 · §十九 最终纪律
             → 所有场景最终都受其约束
```

- **用户**：只复制目标场景的「📋 发给 AI」部分，不必手工拼接公共层。
- **AI 能访问本仓库时**：必须先读取公共协议层（§一~§四）与公共收口层（§十八、§十九），再执行用户指定的场景；不得因为「用户只给了场景 A」就只读 A。
- **场景层只负责调用**：触发停线或交接时按 §十八 处理，最终纪律由 §十九 承担；场景内不得重复公共层的正文。

## 一、所有 AI 必须遵守的固定执行协议

无论使用哪个工作场景，只要 AI 需要读取、分析、修改或执行项目内容，都必须遵守以下协议：

```text
1. 先读取 AGENTS.md，确认项目级入口和当前治理边界。
2. 判断当前任务属于哪个唯一 Owner，并读取该 Owner 的当前版本。
3. 不使用聊天记忆、旧版本、历史截图或常识代替当前 Owner。
4. 根据当前 Owner，把本次任务需要执行的节点临时整理为：
   节点 → 输入 → 要做什么 → 要产出什么 → 什么算完成 → 下一节点何时允许开始。
5. 读取当前仓库和已有产物，找到当前真实状态；不要假设用户之前说“已经完成”的部分一定真的完成。
6. 从第一个尚未满足完成条件的节点开始。
7. 前一节点没有完成，不得进入后一节点。
8. 不得自行新增、删除、压缩、合并、重排或替代 Owner 定义的长期规则。
9. 每次声称完成，都必须给出当前状态的真实证据：文件、命令输出、实际产物、关键帧、CI/Gate 或人工验收记录。
10. 触发停线或交接条件时，按 §十八《统一停线与交接规则》处理并报告，不在本协议内另行解释。
11. 没有完成条件和真实证据，不得使用“完成”“已落实”“已闭环”“CLOSED”。
```

### “完成”的统一含义

> **完成 = 当前 Owner 定义的完成条件全部满足 + 当前仓库存在可检查证据。**

以下情况都不能称为完成：

- 只读过文档；
- 只写了计划；
- 只改了文件但没有验证；
- 只跑过关键词扫描；
- 只引用历史 PASS；
- 只根据 AI 自己的描述判断；
- 后面的节点做了，但前面的节点没有完成；
- 真实产物还没有通过要求的验收。

## 二、当前与历史

- 当前规则：只认当前 Owner 的当前版本。
- 当前状态：优先认当前脚本、当前文件、当前产物和当前 CI/Gate。
- 历史：只用于知道过去发生过什么、为什么改过、检查重复和追溯证据。
- `outputs/archive/` 和 `changelog` 不得成为当前规则入口，不得反向指挥当前方案。

## 三、Owner 路由

```text
用户方向 / 用户拍板
        ↓
AGENTS.md
        ↓
当前任务唯一 Owner
        ↓
当前 Owner 定义的执行节点
        ↓
当前产物
        ↓
Gate / 真实产物 / 用户最终验收
```

主要 Owner：

- 视频生产：`SKILL.md`
- 产品事实：`APPLET / spec / R10`
- 业务流程解释：`R2`
- Remotion 技术：`R3`
- UI 视觉证据：`R6`
- 工程组件：`R7`
- 全局变更治理：`R8`
- 视觉导演：`R9`
- 资源职责边界：`docs/internal/内容资源调用协议.md`
- 整改前置侦察：`docs/internal/整改作战总纲.md`
- 历史证据：`changelog / archive`

如果同一个概念出现两个当前 Owner：**停止执行，先解决 Owner 冲突。**

## 四、场景选择与导航

**先判断任务复杂度，再选场景。**

复杂任务（满足任一）→ **先执行【场景 M｜全局统筹 · 战场侦察】**，完成侦察与布阵后，再回到对应执行场景：

- 涉及两个以上 Owner；
- 涉及两个以上流程或共享组件；
- 涉及全局规则、跨行业规则或规则替代；
- 跨多个产物（文档 / 脚本 / 模板 / 视频 / 事务）。

单 Owner 内的连续任务 → 直接按下方导航选用场景。

> 本判据只在本节定义；其他场景只引用本节，不复述。

| 场景 | 用途 | 默认是否改文件 |
|---|---|---|
| **M** | **前置：先建立全局战场认知，再决定怎么执行复杂任务** | 否 |
| A | 新会话接班并继续当前任务 | 是 |
| B | 只做当前时间点选题 | 否 |
| C | 把复杂任务拆成可执行节点 | 否 |
| D | 只调查问题，不修改 | 否 |
| E | 正式执行一次全局整改 | 是 |
| F | 重新验证之前是否真的完成 | 否 |
| G | 正式生产视频 | 是 |
| H | 只修一个已经明确的问题 | 是 |
| I | 只做 Remotion 工程实现 | 是 |
| J | 只做完成证据复核 | 否 |
| K | 只评估一个想法，不执行 | 否 |
| L | 做一次全局审计，不整改 | 否 |

> 「默认是否改文件」只是选用场景时的提示，不是治理规则。

## 五、场景 A｜新会话接班并继续当前任务

### 📋 发给 AI

```text
现在正式接手“券到卡包”项目。

不要让我重新介绍项目。
不要根据聊天记忆猜当前规则。
不要把历史产物直接当成当前方案。

先按 §四 判断任务复杂度：
如果属于复杂任务，先执行【场景 M】完成侦察与布阵，再回到本场景继续。

请按以下顺序执行：
1. 读取 AGENTS.md。
2. 找到当前任务对应的唯一 Owner，并读取该 Owner 当前版本。
3. 根据 Owner 当前规则，判断当前任务做到哪个节点。
4. 读取当前仓库和已有产物，核对这个节点是否真的已经完成。
5. 找到第一个尚未满足完成条件的节点。
6. 直接从这个节点继续执行。
7. 每个完成结论都必须给出当前真实证据。

需要我决定方向、审美、产品取舍或发布时，按 §十八 B 类交接。
其他情况下不要只做总结，直接继续执行。

开始时先报告：
当前任务：
当前 Owner：
当前节点：
已经完成的部分及证据：
本次准备做什么：
需要我决定：无 / 具体事项
```

新片视觉隔离按 `AGENTS.md` §九（新会话视觉隔离）执行。

## 六、场景 B｜只做当前时间点选题

### 📋 发给 AI

```text
现在只做选题，不生产视频，不写代码，不进入后续制作。

先读取：
1. AGENTS.md
2. 当前生产 Owner
3. Product Truth
4. 当前已产出状态

然后核实当前时间、节日、消费节点、行业经营周期和近期有效信息。

至少给我 5 个候选方向。
每个候选都必须写清：
行业 / 经营时刻 / 老板痛点 / 当前做法 / 产品真实能力 / 可研究玩法 / 视频切口 / 与已有内容的冲突。

不得伪造数据。
不得把历史信息当成当前信息。
不得替我决定最终选哪个方向。

做到“候选方向已经形成、但用户还没有拍板”就停止。
```

## 七、场景 C｜复杂任务拆解

### 📋 发给 AI

```text
现在先拆任务，不要直接大规模修改。

先读取本次任务对应的唯一 Owner，并确认 Owner 当前版本。
然后把任务拆成一张临时执行表：

节点 / 输入 / 输出 / 前置条件 / 是否可以并行 / 验证方法 / 用户介入点。

同时明确：
1. 哪些节点必须按顺序执行；
2. 哪些节点可以并行；
3. 哪些节点必须等待用户决定；
4. 哪些节点不能修改，只能读取和验证。

只有当执行顺序和依赖关系已经清楚后，才开始执行。
不要为了“多模型”而强行拆模型。
```

## 八、场景 D｜只调查问题，不修改

### 📋 发给 AI

```text
现在只调查，不修改任何文件。

我要知道这个问题到底发生在哪里，以及它属于谁。

请检查：
1. 涉及哪些 Owner；
2. 当前入口是什么；
3. 当前规则是什么；
4. 历史规则是什么；
5. 有没有重复规则；
6. 有没有冲突或第二 Owner；
7. 有没有第二入口；
8. 有没有当前/历史混淆；
9. 影响哪些文件、脚本、流程或产物；
10. 这是局部问题还是全局问题。

不要顺手修复。
不要因为“看起来应该这样”就修改。

最后只报告：
问题 / 证据 / Owner / 影响面 / 当前与历史边界 / 建议是否进入整改 / 需要我决定什么。
```

## 九、场景 E｜正式执行一次全局整改

### 📋 发给 AI

```text
现在进入正式全局整改。

不要直接改文件。

第一步：读取 docs/internal/整改作战总纲.md。
第二步：读取 docs/internal/R8-变更收敛协议.md。
第三步：先完成全局侦察，再开始 Migration。

侦察至少确认：
资源地图 / Owner 地图 / 入口地图 / 当前与历史边界 / 冲突 / 重复 / 断档 / 依赖 / 影响面。

然后建立 Change Contract 和 Impact Map。

执行时必须做到：
1. 先确定唯一 Owner；
2. 再确定唯一入口；
3. 明确哪些规则被替代；
4. 清理旧口径或登记为历史不可回用；
5. 再执行 Migration；
6. 再执行 Verification；
7. 最后才形成 Closure Report。

禁止：
- 新增第二 Owner；
- 新增第二入口；
- 新增一个平行流程来绕开旧问题；
- 没有证据就写 CLOSED。

任何结构性前提发生变化，都先停下来重新侦察，不要继续硬改。
```

## 十、场景 F｜重新验证之前是否真的完成

### 📋 发给 AI

```text
不要相信之前任何“已经完成”“已经落实”“CLOSED”的结论。

现在从当前仓库重新验证，不修改文件。

逐项检查：
Owner / 入口 / 影响面 / 新旧口径 / 当前产物 / 机械检查 / 负向测试 / 语义反例 / Real Output / CI 或 Gate / 人工验收证据。

每一项都必须回答：
当前证据是什么？
证据是否来自当前状态？
是否真的满足完成条件？

任何一项缺证据，都标记为“未完成”，不要帮它补证据。
最后只报告：已证明完成 / 尚未证明完成 / 缺什么证据。
```

## 十一、场景 G｜正式生产视频

### 📋 发给 AI

```text
现在正式生产视频。

只认当前 SKILL.md。
不要根据聊天记忆、旧视频或本提示词自行重写视频生产流程。

开始前：
1. 读取 AGENTS.md；
2. 找到当前生产 Owner；
3. 读取 SKILL.md 当前版本；
4. 把当前生产流程临时整理成“节点 / 输入 / 输出 / 完成条件”；
5. 检查当前已有产物，找到第一个未完成节点。

然后从第一个未完成节点继续。
前一个节点没有完成，不得进入下一个节点。

产品事实必须回 Product Truth。
资源职责必须遵守 docs/internal/内容资源调用协议.md。

需要我拍板方向、审美或发布时，按 §十八 B 类交接；其他情况下直接继续。
```

## 十二、场景 H｜只修一个已经明确的问题

### 📋 发给 AI

```text
现在只修一个已经明确的问题。

先确认：
1. 问题是什么；
2. 当前唯一 Owner 是谁；
3. 要修改的当前产物在哪里。

只修改解决这个问题必需的文件。
不要顺手整理其他文档。
不要修改全局规则。
不要趁机重构无关代码。

如果你发现这个问题实际上涉及流程、共享组件、跨行业规则、多个 Owner 或多个入口：
按 §十八 A 类停线并报告“这个问题已经升级为全局变更”，不要继续局部打补丁。

修复后必须运行对应验证，并报告修改了什么、验证了什么、结果是什么。
```

## 十三、场景 I｜只做 Remotion 工程实现

### 📋 发给 AI

```text
现在只做当前视频的 Remotion 工程实现。

先读取：
SKILL.md / R3 / R7 / R9 / 当前分镜 / 当前 R9 产物。

不要重新决定已经批准的内容方向。
新片视觉隔离按 `AGENTS.md` §九 执行。

先确认：
1. 当前视频要实现哪些屏；
2. 每屏对应什么数据；
3. 需要复用哪些已经登记的公共组件；
4. 哪些组件必须为当前片专属；
5. 当前工程依赖和验证方式是什么。

完成编码后，按当前 Owner 的完成条件进行渲染和验证。
没有通过要求的验证，不得说工程节点已经完成。
```

## 十四、场景 J｜只做完成证据复核

### 📋 发给 AI

```text
现在只做完成证据复核，不修改任何文件。

不要引用之前会话的总结作为证据。

逐项检查：
实际文件 / 实际命令 / 实际产物 / 当前 CI 或 Gate / 关键帧或截图 / 人工验收记录。

对每个“完成”结论回答：
1. 证据是什么；
2. 证据在哪里；
3. 证据是否属于当前最终状态；
4. 证据是否真正满足当前 Owner 的完成条件。

缺任何一项，就标记“证据不足”。
不得补写不存在的证据，不得拿历史 PASS 代替当前证据。
```

## 十五、场景 K｜只评估一个想法，不执行

### 📋 发给 AI

```text
现在只评估这个想法，不修改任何文件。

我的想法是：
[把想法写在这里]

请先判断：
1. 这是局部任务、局部修复，还是全局变更；
2. 如果执行，应该由哪个 Owner 负责；
3. 会影响哪些已有规则、入口、流程、脚本或产物；
4. 是否与当前规则冲突；
5. 是否会产生第二 Owner、第二入口或新旧规则并存；
6. 真正需要改动的最小范围是什么。

最后给我：
事实 / 风险 / 影响面 / 应落 Owner / 建议下一步。

没有得到执行决定前，不要修改仓库。
```

## 十六、场景 L｜全局审计，不整改

### 📋 发给 AI

```text
现在只做全局审计，不修改文件。

先读取 docs/internal/整改作战总纲.md，并按当前 Owner 体系检查整个项目。

重点检查：
1. 第二 Owner；
2. 第二入口；
3. 新旧口径同时存在；
4. 历史内容重新进入当前指挥链；
5. 一个规则被多个文档重复定义；
6. 有规则但没有 Owner；
7. 检查器通过但真实语义不成立；
8. 当前流程与 Owner 不一致；
9. 模板、提示词和 Owner 的当前规则不一致。

每个问题都给出：证据 / 所属 Owner / 影响面 / 严重程度 / 是否需要全局整改。

不要顺手修复。
```

## 十七、场景 M｜全局统筹 / 战场侦察（前置模式）

### 📋 发给 AI

```text
现在进入【全局统筹 / 战场侦察模式】。

你现在不是马上修改我指定的文件。
你的第一任务是：根据当前仓库真实状态，建立一张“临时作战地图”，让我确认你已经理解当前项目的整体结构、规则归属、当前状态与执行路线。

本次范围：
- 我还没有给出具体任务 → 做全局盘面侦察。
- 我已经给出任务 → 以该任务为范围做针对性侦察。

第一步 · 读取本领域的侦察要求
1. 读取 AGENTS.md，确认项目级治理规则与当前入口。
2. 判断本次范围属于哪一类，并读取对应 Owner 当前的侦察 / 前置要求：
   - 整改 / 治理 / 收口 / 清理 / 架构整理 / 全局一致性 → docs/internal/整改作战总纲.md
   - 视频生产 → SKILL.md
   - 规则替代 / 跨对象变更 → docs/internal/R8-变更收敛协议.md
   - 其他 → 该领域当前 Owner
3. 如果当前 Owner 没有定义侦察要求 → 明确报告“当前 Owner 未定义侦察要求”，再按第二步执行；不要自行发明长期规则。

第二步 · 通用侦察项（任何领域都必须回答）
1. 本次范围会碰到哪些资源、脚本、模板、当前产物、Change Contract 和历史证据？
2. 哪些属于当前规则，哪些只属于历史证据？
3. 是否存在第二 Owner、第二入口、重复规则、新旧口径并存、历史越权、无 Owner 规则，或检查器与真实语义冲突？
4. 这是局部问题还是全局问题？实际影响面是什么？
5. 哪些必须先做、哪些可以并行、哪些必须等我决定？
6. 如果本次涉及全局变更、整改或规则替代 → 先按 R8 确定动作类型（默认不是 ADD）。
7. 如果我之前给过一份整改方案 → 不要照着执行；先核验该方案的前提在当前仓库是否仍然成立。

第三步 · 输出临时作战地图

本次范围：全局盘面 / 指定任务
当前唯一 Owner：
相关入口：
当前规则：
历史证据：
已有产物：
未完成事务：
动作类型（按 R8 语义填写；非全局变更可留空）：
主要冲突或风险：
影响面：
必须先做：
可以并行：
必须等待：
需要用户拍板：

重要纪律：
- 这张作战地图只用于当前会话，不新建长期治理文档，也不成为第二状态源。
- 发现结构性冲突时，不要继续局部修改，按 §十八 停线并报告。
- 不要因为侦察发现问题就顺手整改。
- 侦察完成后，按 §四 决定进入哪个执行场景。
```

## 十八、统一停线与交接规则

AI 遇到下列三种状态时，处理方式不同：**A 类必须停线；B 类正常交接；C 类标记证据不足。**

### A. 必须停线（结构性冲突，不得继续修改）

```text
1. 发现两个当前 Owner；
2. 发现两个当前入口；
3. 发现当前规则和历史规则无法区分；
4. 发现批准方案与当前仓库事实不一致；
5. 发现任务影响面比原先声明的大很多；
6. Owner 没有定义必要步骤；
7. 发现检查器与真实语义明显冲突。
```

停线时必须写出：

```text
停止原因：
已经确认的事实：
缺少什么：
下一步允许继续的条件：
```

### B. 正常交接（不是出问题，等你拍板）

```text
8. 需要用户决定方向、审美、产品取舍或发布。
```

交接时必须写出：

```text
需要用户决定什么：
可选方案及各自影响：
我的建议：
在你决定前我不会修改的范围：
```

### C. 证据不足（动作已做，但还不能证明完成）

```text
9. 完成条件没有真实证据。
```

标记为「未完成」，写明缺什么证据、如何取得。不得补写不存在的证据，也不得用历史 PASS 代替当前证据。

## 十九、最终纪律

> **本节仅作最终纪律性强调，不新增规则；具体执行规则以 §一～§四 及 §十八 为准。**
>
> **启动指令不定义业务规则，只规定 AI 如何进入正确工作模式。业务规则变化只改对应 Owner。**
>
> **复杂任务先全局统筹，再确定 Owner、影响面、执行顺序，最后才修改文件；局部问题不得借机变成无边界整理。**
>
> **任何完成结论都必须基于当前状态的真实证据；任何历史都不得重新进入当前指挥链。**

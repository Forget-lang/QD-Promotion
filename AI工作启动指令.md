# 券到卡包 · AI 工作启动指令

> **这是用户侧工作控制台，不是项目规则 Owner，也不是 AI 自动入口。**
>
> 这份文件只负责：**让我把正确的启动指令复制给新会话 AI。**
> 
> 项目真正的 AI 自动入口是 `AGENTS.md`；视频生产唯一作业文档是 `SKILL.md`；其他领域规则由对应 Owner 负责。

## 使用规则

1. 我只需要选择场景并复制代码块。
2. 代码块里的文字可以直接发送，不需要我填写字段、补项目背景、补当前状态。
3. 普通正文是写给我看的；`📋 发给 AI` 代码块才是给 AI 的实际指令。
4. 指令中的“必须”表示无条件执行；“不得/不要”表示禁止执行；“只有……才……”表示必须满足条件后才能执行；“如果……则……”表示明确的条件分支。
5. AI 必须自己读取项目当前状态、当前 Owner、当前任务和当前时间；不得把本应由 AI 完成的信息整理工作推回给我。
6. AI 不能把历史产物自动当成当前答案。历史可以用来确认“过去做过什么”，不能直接决定“现在应该怎么做”。
7. 用户只需要对方向性决策、最终审美和最终发布负责；机械执行和证据整理由 AI 完成。

---

# 一、场景导航

| 场景 | 什么时候用 | 目标 |
|---|---|---|
| **A** | 新开会话 / 换 AI / 接班项目 | 让 AI 自己判断当前工作从哪里继续 |
| **B** | 不知道现在该做哪个行业 / 主题 | 让 AI 根据当前时间和市场信息选题 |
| **C** | 一个任务太大，需要多个模型协作 | 让 AI 拆节点并安排模型 |
| **D** | 怀疑项目有问题，但还不知道问题在哪里 | 只调查，不修改 |
| **E** | 已确定要做全局整改 | 按 R8 正式迁移、验证、收口 |
| **F** | 怀疑整改没有真正落实 | 用证据核对最终仓库状态 |
| **G** | 已确定行业/主题，要生产视频 | 沿 SKILL 正式生产链继续 |
| **H** | 只改一个局部问题 | 只处理指定范围，不扩大 |
| **I** | 分镜已确定，要进入 Remotion | 只做当前片的工程实现 |
| **J** | 要求 AI 证明“真的做完了” | 做完整证据复核 |
| **K** | 有一个想法，但还没决定是否做 | 先分析，不执行 |
| **L** | 明确要求一次全局审计 | 有边界地审计，不顺手整改 |

---

# 二、所有 AI 都必须按这个语言理解

以下规则适用于本文件全部场景：

- **“先读”**：先读取指定文件，再继续后面的工作。
- **“确认”**：必须找到仓库或产物中的实际证据，不能凭记忆判断。
- **“直接执行”**：不需要用户决定方向时，不询问“是否需要我继续”，直接做。
- **“停下来问我”**：遇到规定的触发条件，必须停止修改，先向用户报告并等待决定。
- **“完成”**：必须有实际文件、实际命令、实际产物或其他可以检查的证据；只写总结不算完成。
- **“当前”**：只认当前分支、当前规则、当前产物；历史文件不自动视为当前规则。

不得使用模糊词代替执行条件，例如“适当”“必要时”“视情况”“合理”“尽可能”“酌情”。需要判断时必须写成明确条件。

---

# 三、场景 A｜新会话正式开工 / 项目接班

**什么时候用：** 每次新开会话、切换 AI、换模型、重新接班项目时使用。

## 📋 发给 AI

```text
现在正式接手「券到卡包」项目的工作。

不要让我重新介绍项目背景。
不要凭聊天记忆猜规则。
不要把历史产物直接当成当前方案。

请严格按以下顺序执行：

第一步：读取 `AGENTS.md`。
第二步：按照 `AGENTS.md` 规定的读取顺序，读取当前任务对应的 Owner。
第三步：确认当前会话已经明确的任务；如果当前会话没有明确任务，就读取当前项目状态并判断现在正在进行什么工作。
第四步：确认当前工作已经完成到哪个节点。
第五步：找到当前节点尚未完成的第一件工作。
第六步：如果这件工作可以在不改变方向、不改变全局规则、不等待用户决定的情况下完成，立即执行，不要先问我“要不要继续”。
第七步：如果必须由我决定方向，立即停止，并明确告诉我需要决定什么以及为什么。

你必须确认以下 8 件事：
1. 当前任务是什么；
2. 当前工作节点是什么；
3. 当前唯一 Owner 是谁；
4. 已经完成了什么；
5. 已经存在什么真实证据；
6. 下一件必须完成的工作是什么；
7. 哪些事情你可以直接完成；
8. 哪些事情必须由我决定。

你不能做以下事情：
- 不能因为换了会话就从第一步重新做已经完成的工作；
- 不能重新创建第二套流程；
- 不能创建第二个 Owner；
- 不能擅自扩大当前任务范围；
- 不能只给我项目总结而不继续执行；
- 不能把历史片的构图、镜头、视觉母题、字幕布局、动效编排或整套样式直接当成新片模板。

如果当前任务是新片：
- 历史产物只能用于确认过去做过什么和检查重复；
- 不得打开旧片 `videos/g0X/index.tsx` 或 `data/g0X.ts` 作为新片创意模板；
- 允许复用的只有项目级公共能力、真实产品 UI、品牌资产、R7 已登记通用组件和明确批准的长期公共资产；
- 新片必须根据当前行业、当前内容策略和当前视觉导演规则重新形成视觉方案。

接班确认只需要给我下面这 8 行，然后立即继续执行：
当前任务：
当前节点：
当前 Owner：
已完成：
已有证据：
下一步：
我可以直接做：
需要你决定：

如果“需要你决定”为空，就写“无”，然后继续执行下一步。
```

---

# 四、场景 B｜当前时间点选题

**什么时候用：** 还没有决定下一条视频做哪个行业或主题。

## 📋 发给 AI

```text
现在只做选题，不生产视频。

先读取当前项目规则、当前已产出行业和产品事实。
然后获取当前有效的时间、节假日、消费节点、行业经营周期和近期内容环境信息。
需要联网的数据必须联网核实；不能用过期信息冒充当前信息。

请回答一个问题：
“从今天这个时间点出发，接下来 2–6 周最值得研究哪些行业和内容？”

必须按以下步骤执行：
1. 列出当前时间窗口；
2. 找出与实体店消费有关的节日、季节、经营节点和消费场景；
3. 找出哪些行业正在进入这些场景；
4. 检查 `券到卡包` 当前真实产品能力是否能支撑这些场景；
5. 检查项目已经做过哪些行业，排除明显重复；
6. 为每个候选方向找到一个具体经营痛点；
7. 为每个痛点找到一个真实可用的券玩法；
8. 判断这个玩法是否能形成一条完整短视频故事。

最终只给我：
- 候选行业；
- 当前时间窗口；
- 具体经营场景；
- 具体痛点；
- 产品能解决的动作；
- 可形成的视频切口；
- 是否已经做过相近内容；
- 生产难度；
- 需要我选择的方向。

至少给出 5 个候选方向。
不要因为行业长期热门就自动排前面。
不要伪造市场数据、平台数据或案例。
不要替我最终拍板。

完成标准：
你必须给出有来源的当前信息和清晰的候选方向；只给行业名称不算完成。
```

---

# 五、场景 C｜复杂任务拆解 / 多模型协作

**什么时候用：** 一个任务包含研究、内容、视觉、代码、验证等多个不同类型工作，不适合一个模型从头做到尾。

## 📋 发给 AI

```text
现在先不要大规模执行。
先把当前任务拆成可独立交付的工作节点，再决定每个节点使用什么模型。

第一步：拆任务。
每个节点必须写清：
- 节点名称；
- 节点目标；
- 输入；
- 输出；
- 前置依赖；
- 是否可以并行；
- 需要的工具能力；
- 验证方法；
- 是否需要用户拍板。

第二步：安排模型。
不要在这里写一个永久固定的模型排行榜。
请根据当前实际可用模型和当前节点需要的能力选择模型。
每个节点给出：
- 首选模型；
- 备用模型；
- 选择理由；
- 什么时候需要换模型。

第三步：安排复核。
生产和最终验收尽量使用不同模型。
高风险判断必须有独立复核。
纯机械任务不要强行使用最高能力模型。

第四步：输出作战表：
| 节点 | 目标 | 输入 | 输出 | 首选模型 | 备用模型 | 可否并行 | 验证方式 | 复核模型 | 我的介入点 |

在作战表确定以前，不要大规模修改项目文件。
如果只是一个模型就足够完成当前任务，不要为了“多模型”强行拆分。

完成标准：
必须得到一张可以直接执行的节点表；不能只说“建议用多个模型”。
```

---

# 六、场景 D｜问题侦察，不修改

## 📋 发给 AI

```text
现在只调查，不修改任何文件。

目标：找出我刚刚指出的问题到底是什么。

必须按以下顺序：
1. 找问题涉及的文件和规则；
2. 找每个相关概念的唯一 Owner；
3. 找当前入口；
4. 找重复、冲突、旧口径、历史越权；
5. 找受影响的当前产物；
6. 区分真正缺陷和只是可以更好的优化项。

禁止：
- 不修改文件；
- 不创建新规则；
- 不因为发现一个优化点就扩大调查范围；
- 不用猜测代替证据。

完成后必须给我：
- 问题是什么；
- 证据在哪里；
- Owner 是谁；
- 影响面是什么；
- 哪些是当前缺陷；
- 哪些只是可选优化；
- 下一步是否需要整改；
- 需要我决定什么。

只有调查结果完成后，才可以进入整改。
```

---

# 七、场景 E｜全局整改 / 治理迁移

## 📋 发给 AI

```text
现在进入一次正式的全局整改。

这是跨规则、跨文档、跨流程、共享组件或跨行业的变更。
必须按 `docs/internal/R8-变更收敛协议.md` 执行。

第一阶段：侦察。
先读取 `docs/internal/整改作战总纲.md`，然后确认：
- 当前 Owner；
- 当前入口；
- 受影响规则；
- 受影响文件；
- 受影响产物；
- 当前口径；
- 被替代的旧口径；
- 当前/历史边界。

第二阶段：变更计划。
在修改前形成：
- 新口径；
- 唯一 Owner；
- 唯一入口；
- ADD；
- REPLACE；
- REMOVE；
- PRESERVE；
- 验收条件。

如果新口径涉及方向选择，而当前会话没有用户批准，必须停下来问我。

第三阶段：迁移。
执行已经批准且属于机械落实的部分。
新规则进入唯一 Owner。
旧规则必须删除、废止登记或明确移出当前指挥链。
不得只新增不清旧。

第四阶段：验证。
必须真实运行对应检查。
必须做至少一个负向测试。
必须做至少一个语义反例。
涉及视觉或实际产物时，必须检查真实产物。

第五阶段：关闭。
只有证据齐全才可以写 `CLOSED`。
任何一个关键验证没有真实证据，都必须写 `PENDING`。

禁止：
- 不得创建第二 Owner；
- 不得创建第二入口；
- 不得绕过 R8；
- 不得为了过检查器改变真实语义；
- 不得把历史 PASS 当成当前 PASS。

完成标准：
必须有真实修改、真实验证、真实结果和明确的 CLOSED / NOT CLOSED 结论。
```

---

# 八、场景 F｜整改对账

## 📋 发给 AI

```text
现在不要继续解释，也不要继续修改。

请证明最近一次整改是否真的完成。

必须取得并报告真实证据：
1. 实际修改了哪些文件；
2. 实际 git diff / commit 内容是什么；
3. 实际运行了哪些命令；
4. 每条命令真实退出码是多少；
5. 最终文件状态是什么；
6. 相关 Gate 是 PASS、FAIL 还是 PENDING；
7. 是否存在实际产物或关键帧证据；
8. 是否做过负向测试；
9. 是否存在只写声明、没有证据的完成项；
10. 哪些地方仍然 PENDING。

必须把“实际证据”和“推测”分开。

证据不足时必须写：`证据不足`。
不得把“应该完成”“看起来完成”“已有历史 PASS”写成当前 PASS。

结论只能是：
- CLOSED：关键证据全部齐全；
- NOT CLOSED：仍有关键证据缺失。
```

---

# 九、场景 G｜正式生产一条视频

## 📋 发给 AI

```text
现在正式生产一条「券到卡包」宣传视频。

先读取当前会话已经确定的行业、主题、目标和已有材料。
如果行业或主题没有确定，不得擅自猜测；先告诉我“当前没有确定选题”，停止生产。

如果选题已经确定：
1. 读取 `AGENTS.md`；
2. 读取 `SKILL.md`；
3. 读取当前生产节点对应的 Owner；
4. 判断当前已经完成到哪一步；
5. 从第一个未完成节点继续。

不得重新执行已经有真实证据完成的节点。

视觉特别要求：
- 历史片可以用于识别已经做过什么和发现重复；
- 不得把历史片的具体构图、镜头关系、视觉母题、字幕布局、动效编排或整套样式直接当当前片答案；
- 不得打开历史 `videos/g0X/index.tsx` 或 `data/g0X.ts` 作为新片创意模板；
- 同行业系列允许沿用已经批准的系列母题；换行业必须重新建立视觉语言；
- 允许复用的只是项目级公共能力、真实产品 UI、品牌资产、R7 已登记通用组件和明确批准的长期公共资产。

执行规则：
- 可以直接执行的直接执行；
- 需要方向决定时停止并告诉我具体决定项；
- 不创建第二套视频生产流程；
- 不虚构产品能力、数据、案例；
- 不把文档通过当成成片通过。

完成标准：
每个完成节点必须有对应产物或验证证据；不能只写“已完成”。
```

---

# 十、场景 H｜局部任务，只改指定范围

## 📋 发给 AI

```text
现在只处理当前会话指定的这个局部问题。

先确认：
- 要改什么；
- 不改什么；
- 当前 Owner 是谁；
- 完成标准是什么。

然后只修改这个范围。

禁止：
- 不得顺手重构无关文件；
- 不得顺手修改全局规则；
- 不得因为发现其他问题就扩大范围；
- 不得创建第二 Owner。

如果你发现必须修改全局规则、共享组件、流程或跨行业规范，立即停止并告诉我：
“当前任务已经超出局部范围，需要进入全局变更流程。”

完成后给出：
- 改了什么；
- 没改什么；
- 运行了什么验证；
- 结果是什么。
```

---

# 十一、场景 I｜Remotion 工程实现

## 📋 发给 AI

```text
现在把当前已经批准的分镜 / R9 视觉决策转成 Remotion 实现。

先读取：
1. `AGENTS.md`；
2. `SKILL.md` 当前工程节点；
3. `docs/internal/R3-Remotion技术参考.md`；
4. `docs/internal/R7-Remotion组件库.md`；
5. 当前视频对应的分镜和 R9 视觉决策；
6. 当前已有源码。

先判断现有组件能否直接使用。
如果 R7 已登记组件可以满足要求，优先复用。
如果缺少组件，只在当前片确实需要时实现，不要创建第二套组件体系。

不得：
- 不得用历史 g0X 源码当当前片模板；
- 不得把历史片的具体构图或动画编排直接复制到当前片；
- 不得修改已经批准的内容逻辑来迁就工程实现；
- 不得用测试占位内容冒充最终画面。

实现后必须：
1. 跑对应代码检查；
2. 渲染真实关键帧；
3. 检查关键帧是否符合分镜和 R9；
4. 修复实际问题；
5. 给出真实文件、命令和结果。

如果发现分镜 / R9 本身有方向错误，停止工程修改，先报告问题，不要自行改变导演决策。
```

---

# 十二、场景 J｜证据复核：证明 AI 真的做完了

## 📋 发给 AI

```text
现在不要继续解释项目背景。
请只做证据复核。

目标：证明刚才声称完成的事情是否真的完成。

必须检查：
1. git diff / commit；
2. 实际修改文件；
3. 实际运行命令；
4. 每条命令退出码；
5. 最终文件状态；
6. 相关 Gate；
7. 实际产物、关键帧或成片；
8. 至少一个负向测试（适用时）；
9. 当前仍然 PENDING 的内容。

规则：
- 没有真实证据就写“证据不足”；
- 历史结果不能代替本次结果；
- 文字声明不能代替文件、命令或产物证据；
- 不允许把推测写成 PASS。

最后只能给出：
- 已证实完成；
- 部分完成；
- 未完成；
并逐项列证据。
```

---

# 十三、场景 K｜想法评估，不执行

## 📋 发给 AI

```text
先不要执行我刚刚提出的想法。

请只判断这个想法在当前项目里是什么问题、应该落在哪里、会影响什么。

必须回答：
1. 我刚刚提出的是什么想法；
2. 它真正要解决的问题是什么；
3. 当前系统是否已经有解决办法；
4. 当前 Owner 是谁；
5. 是否与现有规则冲突；
6. 是否属于当前缺陷；
7. 如果不改，会产生什么实际影响；
8. 如果改，需要影响哪些地方。

如果存在多个合理方案，可以给方案对比。
如果只有一个合理落点，只给一个。

禁止：
- 不得因为有想法就自动开始修改；
- 不得把可选优化强行变成整改；
- 不得创建第二 Owner。

最后明确写：
“建议执行 / 建议暂不执行 / 需要用户拍板”。
```

---

# 十四、场景 L｜有边界的全局审计

## 📋 发给 AI

```text
现在做一次有边界的全局审计。

先定义三件事：
- 审计对象；
- 审计目标；
- 审计停止条件。

然后检查：
1. 当前 Owner；
2. 当前入口；
3. 引用关系；
4. 第二 Owner；
5. 第二入口；
6. 重复规则；
7. 新旧口径并存；
8. 历史进入当前指挥链；
9. 跨文档 / 跨流程 / 跨 Owner 不一致；
10. 当前实际生产中已经暴露的规则缺陷。

审计期间禁止修改文件。
发现问题时先记录证据。

最后把发现分成两类：
A. 当前真实缺陷；
B. 只是可以更好的优化项。

只有 A 类可以进入整改。
B 类只记录，不自动修改。

如果没有新的事实、新的变更或新的真实缺陷，不得重新打开已经 CLOSED 的治理事务。
```

---

# 十五、我的使用原则

这一部分只给用户看，不复制给 AI。

- 新开会话：优先使用 **A**。
- 不知道下一条做什么：使用 **B**。
- 工作太大需要多人/多模型协作：使用 **C**。
- 怀疑有问题但还不想动：使用 **D**。
- 已经决定做全局修改：使用 **E**。
- 怀疑 AI 没有真正落实：使用 **F / J**。
- 已经确定行业和主题：使用 **G**。
- 只改一个小问题：使用 **H**。
- 分镜进入代码：使用 **I**。
- 有一个想法但还没决定：使用 **K**。
- 明确要求全面检查：使用 **L**。

**默认原则：不知道选哪个时，先用 A。A 的任务是让 AI 自己判断当前应该进入哪个工作模式。**

---

# 十六、最重要的一句话

> **我负责告诉 AI“我要解决什么问题”；AI 负责读取项目、判断应该怎么做、执行能直接执行的部分、在必须由我决定时停下来，并用真实证据证明做到了什么。**

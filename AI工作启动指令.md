# 券到卡包 · AI 工作启动指令

> **这是给我自己用的工作控制台，不是项目规则文档。**
> 用途：每次开启新工作、切换 AI、切换任务时，直接复制这里的对应指令，让 AI 先把工作方式、任务边界和接手动作理清楚，再开始干活。
> 本文件不承担任何规则 Owner，不要求其他权威文档引用它；规则一律由 AI 自己进入仓库后回到当前 Owner 判断。
>
> **文件位置：仓库根目录。** 你平时主要从这里复制“开工口令”。
> 配套使用说明见：`docs/AI使用手册.md`。那份手册负责解释“怎么和 AI 协作”；本文件负责“直接拿什么口令开工”。

---

## 一、怎么用

我不需要把项目背景、规则、历史一遍遍重新讲给 AI。

我只需要告诉 AI 三件事：

```text
我要做什么
当前已经有什么 / 卡在哪里
这次最重要的要求是什么
```

然后把下面最接近的启动指令复制给 AI，再把 `【】` 里的内容补上。

### 我的使用原则

- **先让 AI 接手和梳理，再让 AI 执行。**
- **复杂任务先看全局，不要让 AI 一上来只改眼前文件。**
- **我要的是结果，不是让我自己充当项目管理员。**
- AI 自己负责找正确的规则、Owner、依赖、历史边界和验证方法。
- AI 自己负责说明：做了什么、为什么这么做、还缺什么证据。
- 我只在需要我拍板、提供素材、看真图或做最终决定时介入。

### 我的全局工作方法：先统筹，再排兵布阵

这里的“行军打仗 / 排兵布阵”只是我的工作隐喻，不是项目新增规则，也不替代 `AGENTS.md` 或任何正式 Owner。

- **先察全局，后定阵型。** 先看目标、当前态势、入口、Owner、依赖、历史边界和影响面，再动局部。
- **先立中军，后分兵执行。** 先确定谁负责定义规则、谁负责执行、谁负责验证，再拆具体动作。
- **先清旧阵，后布新阵。** 新口径替代旧口径时，要同步处理旧规则、旧入口、旧引用和旧状态，不能新旧并存。
- **局部任务不因为“顺手还能优化”就自动升级成全局整改。** 发现可优化点，要先判断它是不是当前任务的真实阻塞或冲突。
- **已经 CLOSED 的事情，不因为“再确认一下”就重新开战。** 除非出现新的事实、新的变更或明确的新缺陷。

### 全局审计什么时候做

全局审计不是每次开工的默认动作，更不是为了“彻底”而无限扫查整个仓库。

只有出现明确触发条件时，才做一次有边界的全局审计，例如：

- 我明确要求“全局审计 / 全面复查”；
- 新增或修改全局规则、Owner、入口或正式生产流程；
- 出现第二 Owner、第二入口、新旧口径并存、历史重新进入当前链路等治理信号；
- CI / Gate 暴露出跨文档、跨流程或跨 Owner 的一致性问题；
- 实际生产发现现有规则无法执行，或出现明确的当前缺陷。

做全局审计时：

1. 先定义审计对象、目标和停止条件；
2. 先找当前 Owner / Entry，再查引用、冲突、重复和历史边界；
3. 区分“当前真实缺陷”和“可以更好但不影响当前系统”的优化建议；
4. 只有当前真实缺陷进入整改；纯优化项不要擅自扩大本轮范围；
5. 审计完成后给出证据和结论；没有新的事实依据，不重新打开已经 CLOSED 的治理任务。

**一句话原则：全局审计的目的，是证明当前阵型是否成立；不是为了不断发现新的可优化点。**

---

## 二、最常用：开一个新工作

```text
你现在开始接手「券到卡包」项目的这项工作：

【我要做的事】
【当前背景 / 已有材料】
【我最关心的结果】

先不要急着改文件，也不要凭聊天记忆直接执行。
先自己接手项目并判断这项工作属于什么类型。

请先完成：
1. 进入项目后，按项目当前入口接手；
2. 判断本次任务需要读取哪些 Owner，不要无目的遍历全部文档；
3. 如果属于整改、治理、收口、架构整理或全局变化，先做完整的全局侦察和影响面判断；
4. 找出本次任务的唯一 Owner、相关入口、受影响对象和历史边界；
5. 判断哪些事情需要我拍板，哪些事情你可以自己完成；
6. 给我一份简洁的「接手回执」，然后直接进入已获授权的执行部分。

接手回执至少告诉我：
- 你理解的任务是什么
- 本次任务类型
- 当前唯一 Owner
- 主要影响面
- 需要我决定的事项（没有就写“无”）
- 你准备怎么推进
- 第一项验证动作

不要只说“收到”。
```

---

## 三、让我 AI 梳理一个问题，而不是马上修改

适用于：我感觉项目哪里不对、文档很乱、规则可能打架，但还不知道具体问题在哪。

```text
先不要修改任何文件。

我要你帮我把这个问题梳理清楚：

【问题 / 我的直觉】
【我为什么觉得它有问题】
【相关文件或上下文，如有】

请不要直接给局部修补方案，先把整个问题看明白。

请完成一次「战场侦察」：
1. 这个问题涉及哪些文档、规则、脚本、产物或流程；
2. 每个相关概念目前真正的 Owner 是谁；
3. 当前入口、工作入口、状态入口分别是什么；
4. 是否存在重复 Owner、重复入口、重复规则、新旧口径并存或历史越权；
5. 哪些内容只是解释/引用，哪些内容真的在定义规则；
6. 哪些东西应该保留、替换、删除或只做指针；
7. 哪些风险是 P0，哪些可以延后。

最后给我：
- 你的问题判断
- 全局关系图（文字即可）
- 真正需要解决的核心矛盾
- 推荐的收敛方向
- 需要我拍板的地方

先梳理清楚，再决定要不要动手。
```

---

## 四、让我 AI 执行一次全局整改 / 治理

适用于：规则重构、文档治理、架构收口、清理旧口径、迁移 Owner、全局流程变化。

```text
这是一次全局整改，不是局部改文档。

【目标】
【为什么要改】
【我已经明确拍板的方向】

按“先统筹全局、后局部执行”的方式工作。

执行顺序固定为：
1. 全局侦察；
2. Owner / 入口确认；
3. 冲突、重复、断档与历史边界检查；
4. 影响面布阵；
5. 明确哪些是 ADD / REPLACE / REMOVE / PRESERVE；
6. 再进入 Migration；
7. 机械检查；
8. 负向测试；
9. 语义反例；
10. 涉及视觉/产物时做真实产物验证；
11. 给出整改对账单；
12. 只有证据齐全后才允许宣布完成。

最重要的要求：
- 不要通过新增文档制造第二 Owner；
- 不要让历史材料重新进入当前指挥链；
- 不要只加新规则而留下旧规则；
- 不要为了让检查器变绿而迁就错误语义；
- 不要让我自己逐条检查你有没有写进去。

你负责把已经拍板的事情一次落实干净，并用真实证据证明。

开始前先给我：
- 全局侦察摘要
- Owner / 入口阵型
- 影响面
- 迁移动作

我确认方向后，机械执行部分一次完成。
```

---

## 五、让我 AI 做一次“整改对账”

适用于：我已经让 AI 改过东西，现在我要判断“到底有没有真正落实”。

```text
不要重新替我解释项目。

请针对刚才这次整改，做一份真正的「整改对账单」。

目标不是说服我“应该已经完成”，而是证明最终仓库状态已经落实。

请逐项核对：
1. 实际修改了哪些文件；
2. 每项改动对应哪个 Owner；
3. 有没有第二 Owner / 第二入口 / 重复规则；
4. 旧口径是否已经清理或明确废止；
5. 有没有历史材料继续参与当前工作；
6. 相关引用是否仍指向正确 Owner；
7. 运行了哪些检查，真实退出码是什么；
8. 至少一个负向测试是否证明检查器真的能拦住坏例子；
9. 是否存在只靠文字声明、没有实际证据的“完成项”；
10. 还有哪些未完成 / PENDING。

最后只给我事实结果，不要用“基本完成”“应该没问题”这种模糊结论。

固定输出：
- 改动文件
- Owner 对账
- 旧口径清理
- 引用对账
- 机器验证
- 负向测试
- 语义验证
- 未完成项
- 结论：CLOSED / NOT CLOSED
```

---

## 六、让我 AI 开始正式生产一条视频

```text
现在开始做一条视频。

【行业 / 主题】
【已有内容，如有】
【参考图 / 视觉方向，如有】
【素材，如有】
【这次特别希望解决的问题】

请按项目当前正式生产流程接手，不要自己发明另一套流程。

开始时：
1. 先接入项目当前入口；
2. 按任务读取正式生产流程和相关 Owner；
3. 查看当前资产、当前状态和最新验证结果；
4. 判断从哪一步开始，而不是机械从第一步重做。

执行要求：
- 能自己完成的不要反复问我；
- 到必须我拍板/提供素材/审真图的节点明确停下来；
- 不用“文档都对了”代替真实产物质量；
- 最终给出真实产物、真实检查结果和真实退出码。

我的反馈如果是：
“这屏很空 / 很乱 / 不高级 / 不像我要的”
你负责把感受翻译成具体设计问题和修改动作，不要只机械放大字体或卡片。
```

---

## 七、让我 AI 接手一项局部工作

适用于：只改某一屏、某一句、某个数据文件、某个组件，不希望它扩大范围。

```text
这次只处理下面这个局部任务：

【具体对象】
【我要达到的效果】
【不能改变的东西】

请先确认这是不是一个真正的“局部任务”。
如果只是局部修改，就严格控制影响面，不要顺手重构全局。

如果你发现它实际上会影响全局规则、共享组件、跨行业规范或当前 Owner，先停下来说明原因和影响面，不要偷偷扩大修改范围。

完成后只需要告诉我：
- 改了什么
- 影响了什么
- 跑了什么检查
- 是否产生了新的全局问题
```

---

## 八、让我 AI 接手代码 / 设计稿 → Remotion

```text
现在接手这个视频编码任务：

【视频 / 行业】
【设计稿位置】
【需要实现的部分】
【特别需要注意的问题】

先读取项目当前入口，再读取本任务真正需要的生产流程、技术 Owner、组件 Owner 和当前设计稿。

不要自己重新定义视觉方向；设计稿负责“画什么”，技术层负责“怎么正确实现”。

请完成：
- 读取当前实现与实时组件清单；
- 判断可复用组件；
- 实现代码；
- typecheck；
- still / 关键帧检查；
- 运动/相似度等适用 Gate；
- 最后把真实结果交给我。

如果设计稿本身存在结构性问题，先指出问题，不要为了能写代码而偷偷改掉设计意图。
```

---

## 九、我怀疑 AI 没有真正落实

```text
现在不要继续解释。

我怀疑你刚才只是“说已经完成”，没有真正落实。

请自己证明，不要让我替你找：
1. git diff / 实际修改内容；
2. 实际运行的命令；
3. 每条命令真实退出码；
4. 最终文件状态；
5. 相关 Gate 结果；
6. 至少一个负向测试（如适用）；
7. 哪些地方仍然只是 PENDING。

如果证据不足，就明确告诉我“证据不足”，不要把推测写成 PASS。
```

---

## 十、我想让 AI “先给我看懂，再决定做不做”

```text
先不要执行。

请把这件事先讲清楚：

【我的想法】

我希望你从项目整体出发判断：
- 这是什么问题；
- 它真正影响什么；
- 应该落在哪里；
- 谁是 Owner；
- 会不会和现有东西打架；
- 有没有更简单的收敛方法；
- 如果不改，会留下什么风险。

最后只给我：
1. 问题判断；
2. 推荐落点；
3. 影响面；
4. 可选方案（如确实存在多个）；
5. 你建议我拍板什么。

不要先替我执行。
```

---

## 十一、AI 每次开工后，我希望看到的最小回执

我不需要长篇汇报，但必须知道它真的接住了：

```text
已接手。

任务：{一句话}
类型：{局部 / 生产 / 整改 / 治理 / 审计 / 编码 / 其他}
Owner：{当前唯一 Owner}
影响面：{简述}
已检查：{真实命令 / 当前 Gate}
我需要决策：{无 / 具体事项}
下一步：{一个明确动作}
```

---

## 十二、最重要的一条

我负责：

> **提出目标、方向、感受和最终拍板。**

AI 负责：

> **理解全局、寻找 Owner、规划影响面、执行机械工作、验证结果、暴露风险。**

理想工作方式只有一句：

```text
说目标
→ AI 梳理
→ 我拍板必要事项
→ AI 一次执行
→ AI 自证
→ 我看结果
```

---

## 十三、两份用户文档怎么分工

- **现在要一句能直接贴给 AI 的话**：看本文件。
- **现在想知道项目应该怎么和 AI 协作**：看 `docs/AI使用手册.md`。
- **现在要找项目真正的规则**：不要在用户文档里猜，让 AI 从 `AGENTS.md` 开始找当前 Owner。

两份都属于你的用户侧工具，不构成项目第二规则系统。

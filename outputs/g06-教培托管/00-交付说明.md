# g06 教培托管 · 交付说明

> 首版交付：2026-08-30 睡眠期代办轮｜同日口径重做五轮，**现行状态见 §六、§七**（本文按轮次留痕：§一~§五 是首版~第四轮的历史，数字与屏数已过期，别引用）
> 授权范围不变：不确认中间节点、背景图从资源库选、**不做语音合成**，先出分屏静帧 + 无声视频。

## 〇、这轮为什么返工（用户原话要点）

1. 「其中兑的就是一个下午的托管，接校、点心、作业看护，这句话不通顺吧？点心是什么？」
2. 「去除不必要的废话，不是精简到语句意思不明了、靠猜；表达的语意还是要直白，听了能明白意思。」
3. 「实体店商家不可能很熟悉功能的使用，我们的目的是以行业场景为例子，教如何去使用，不能让人看不懂意思。」
4. 「比如 S4，你要说明在券到卡包中制作这样一张兑换券，然后兑换券的内容就按照项目中的实际数据字段名称来描述，录入什么数据根据当前行业场景来定，商家去制作时也能准确找到。」

**落地成三条硬口径**（已升进 `SKILL.md` 与 `07-片1-分镜稿.md`，后续片沿用；`02-分镜稿.md` 顶部那份已作废）：

- 口播每句听一遍就要懂，不许留需要猜的省略宾语；
- 画面里出现的字段名/提示语一律照 `applet/pages_coupon/coupon/create.vue` 的真实标签与 placeholder 原样写，值按托管场景填；
- 由前两条推出屏数 9 → 10：原「字段」单屏装不下十一个真实字段，拆成 S4（券面内容）+ S5（期限与发放）两屏制券表单。

## 一、交付物清单

> **当前状态（2026-09-01 有声版）**：**现行权威文件是 `07-片1-分镜稿.md`**——片 1 为 12 屏数据（`video/src/data/g06.ts`），样式冻结只换内容。阶段二已完成：逐屏 TTS → loudnorm → atempo 1.2 → ffprobe 实测回填（dur / 48 句字幕帧位 / voiceDur / voiceOffset 全为测量值），**有声成片 `g06-教培托管-有声成片.mp4`（5501 帧 / 183.4s）在盘**，12 张实测帧号峰值静帧在 `frames/片1/`（无声验收期旧套图已按 2026-09-01 用户明令清理出交付目录、单套现行，git 历史可查）。
> **第三轮计划（`06-重做计划-操作链版.md`）已作废**——它把"讲透"做成了把制券页、券详情、领取页、核销页逐屏走一遍，那是页面顺序不是攻略。
> **已裁（2026-09-01 用户审片）**：① 盲听/看片通过——"目前能看得过去"，有声成片按现状交付；② 效果尺子静止 67% / 帧差 0.08 红灯**认**——长时制券阅读屏的安静属设计意图，不加强化微动、不为过线加晃动（此红转为已裁项，见 §三表末行）；③ S8 69% 占用旧挂起随本片一并放行。**用户前瞻要求：以后的产出要注意这个细节**——阅读屏"刻意安静"与"死寂"的取舍要在做屏时有意识裁决，别默认最小心智负担的慢漂移、等尺子报红了才补判（原台账条目已缺失）。

| 件 | 路径 | 说明 |
|---|---|---|
| 内容输入包 | `01-内容输入包.md` | 三券配置已全量改用制券页真实字段名（含「兑换内容上限 10 字，服务明细写使用须知」这条根因） |
| ~~母题+口播+分镜~~ | `02-分镜稿.md`（已退役 · 2026-09-02 移废纸篓） | 首版 10 屏 / 38 句整片作废；唯一仍有效的 §一「母题一页」已并入 `07-片1-分镜稿.md` §五，本文件已清理 |
| 分屏真图 | **`frames/片1/S1…S12-still.png`（唯一现行套图，实测帧号 214/423/832/1385/2078/2418/2972/3447/3910/4468/4980/5413）** | 取样帧 = 本屏末句起始帧 + 骨架 TAIL（`scripts/.tmp-frames.mjs` 从 g06.ts 现算）；第八轮起**每屏另抽入场/中段帧**，峰值帧单独干净不作数。旧无声套图与第二轮旧帧均已清理（git 可查、不得回流当现行） |
| **有声成片（现行）** | `g06-教培托管-有声成片.mp4` | 12 屏 / 5501 帧 / 183.4s，2026-09-01 阶段二产物：实测回填 + atempo 1.2 + J-cut 淡出。**待盲听**；效果尺子数字见 §三 |
| 三平台封面 | `covers/封面-抖音-9x16.png`、`封面-小红书-3x4.png`、`封面-搜狐-16x9.png` | 代码渲染文字于本片背景（BG-ABS-001），非 AI 文生图；小红书封面三张回执已改真实券名 |
| 发布稿 | `03-发布稿-抖音.md`、`04-发布稿-小红书.md`、`05-发布稿-搜狐.md` | 抖音文案 / 小红书图文 / 搜狐长文（长文已改成"逐格照着填"的教程式写法） |

## 二、关键口径与决策

- 行业与窗口：教培托管 × 开学季（用户圈选 #5）。
- 背景图：A 级素材库 BG-ABS-001 暖奶油卡片底（登记「可用」，bgBlur 3）；BG-ABS-006 因商用授权待确认未用。
- 视觉母题：回执单卡 / 打孔齿 / 盖章对勾 / 荧光抹带 / 作业本横线；节拍由字幕帧驱动。
- **可读性调参**：讲过的区域透明度地板 0.55 → **0.72**（首版"讲过的太暗"是「看也看不明白，感觉有压力」的直接来源）；S4 底部脚注上移到 `bottom:250`，避开两行字幕带（字幕容器 bottom:60，两行上沿到 y≈1757）。
- 布局来源（第二轮·10 屏-era，现行 12 屏见 `07` §一）：S1 全新 / S2 anchor-pain / S3 ref-13 / **S4+S5 制券表单自绘（仿表单，非截图，S4 为一屏标杆）** / S6 ref-16 / S7 ref-17 / S8 ref-11 / S9 ref-10 / S10 全新。结论条全片仅 2 次（S7 焦糖棕、S10 印章红）。
- 语音：按指示未做。字幕帧已按语速排布，后续配 TTS 只需回填 voiceDur。
- 官网：本轮未抓取（遵守「先别抓」限制）。

## 三、闸门结果（实测数字）

| 闸门 | 结果 | 数字 |
|---|---|---|
| 红线（画面+口播硬禁） | ✅ 通过 | 画面层 0 命中；文案层 0 命中——**本文件改写后复扫曾命中绝对化「最」字类用词 2 处，已改词后归零**（注：把禁词原样写进说明文字本身也会触闸，故此处不复述）；③ 层 5 处为产品署名「券到卡包」，规则允许 |
| 文档引用 | ✅ 通过 | 失效引用 0 处 |
| 事实（资产/素材对账） | ✅ 通过 | 关键资产在位，素材登记与磁盘一致 |
| 相似度 | ✅ 通过 | 12 屏结构指纹（type+ui）与已产出视频零同构；9 个绑定 ui 均有实组件（原无屏绑定的 `g06-compare` 已于 2026-08-31 第十四轮删除，注册表现收 9 键）。**边界：这道机检只防"照抄上一片的 ui 名"，换前缀重做同款骨架它拦不住（SKILL §三）** |
| tsc --noEmit | ✅ 通过 | 零错误 |
| 语速（人工尺，无机检） | ✅ 通过 | **有声实测口径（2026-09-01）**：12 屏 atempo 1.2 后各屏 5.1~5.7 字/秒，全部 ≤6、零超线（无声估算的逐句峰值 5.89 已被实测取代）；口播无阿拉伯数字与字母占位（"N 天"改说"几天内"，画面那行仍保留产品原话 `自领取日起N天内有效`） |
| 画面占用率（静帧） | ⚠️ 11/12（无声套图历史数字） | 69%–82% 为无声版套图实测（该套图已清理出交付目录，数字与图见 git 历史）；**现行有声成片整片占用 77% ✅**。S8-ledger = 69% 的旧挂起项仍在（不加装饰凑数，SKILL §四），随二批底部清理后未复测单屏——终审时如仍刺眼再议 |
| 效果尺子（成片） | ⚖️ **未达标·已裁接受（2026-09-01 用户）** | 有声成片 12 屏实测：**静止占比 67%｜中位帧间差 0.08｜占用率 77%✅**。静止/帧差落 G04 差片量级，根因＝5 屏长时制券阅读态句间只剩慢漂移。用户裁：本片安静可接受、按现状交付，不加微动不凑数。**留档警示后续产出**：同型红再现时，做屏阶段就主动裁"安静 vs 死寂"，不留到验收才判。旧 9 屏版数字（42%/0.54/75%）作废 |

> 时效：上表为 **2026-09-01 二批**（TTS 实测回填 + 底部小字清理 + 成片重渲后）复跑；gate-all 除效果尺子外全绿、tsc 零错误。

## 四、事实自查（这轮逐条回查产品代码，不是照抄首版）

- 「兑换内容」上限只有 10 个字 → 服务明细不能塞这里，改写进「使用须知」（≤500 字）。这是首版那句「接校、点心、作业看护」不成立的根因。
- 「到期提醒」的送达**不用挂订阅前提**（2026-08-30 同日再确认：该功能支持，可直接说）→ 片 1 与三篇稿已去掉"订阅过才收得到"，直接说"到期前会提醒家长"；产品原描述照下一条，仍不引用。
- 「每人限领总量」要先打开「限领总量」开关 → 画面 hint 已注明。
- 「转赠奖励券」只能选「私密发放」类型的有效优惠券 → S8 note 用产品原话；不承诺"一定发到她卡包"。
- 「指定手机号可领取」面板原话：每行一个手机号、最多 500 个、重复自动跳过、仅输入的手机号可领取 → S9 与长文均按原话。
- 海报页名为「海报发放」；家长领取按钮文案为「领取卡券」；记录侧为「导出领券记录 / 导出核销记录」+「发券渠道统计」。
- 「微信通知」是产品对到期提醒的原话，但属红线禁词，画面与文案一律不引用。
- 上屏字段名机器比对：脚本取 `g06.ts` 里全部 `k:` 标签逐字查 `applet/pages_coupon/coupon/create.vue`，12 个表单字段全命中原文（另 3 条是 S3 原因卡标签，非产品字段）。可复现命令记在案：读 `src/data/g06.ts` 提取 `k:` → `includes()` 比对 create.vue。
- 发布稿字段名与口播已统一：抖音、小红书原写「每人限领 1 张」，已改为产品字段全称「每人限领总量填 1 张」。

## 五、第二轮结论（2026-08-30 13:20，用户再审后）

**判定：问题在结构，不在措辞。** 字段名对齐、句子听得住，都只是必要条件。用户原话要点：

- 「S4 必须明确成'在券到卡包里做一张兑换券'——那只是我举的例子，你也可以说'制作一张兑换券'，强调**制作**这个动作。」→ 不要把举例当条款硬编码。
- 「看完整体画面屏和口播稿，觉得内容讲得不清楚、不直白。**实体店商家对这款产品是 0 接触**，还不具备概念，看了听不明白、看得迷迷糊糊，达不到有意向去体验使用的效果。」
- 「视频不仅要把**使用场景分析透彻**，还要**教会如何去使用**，类似使用教程——只是不拿真实应用截图，而是用 remotion 绘制的画面呈现。这需要 AI 设计并规划好。」

**我的失效根因（自查，不是替它辩护）**：整片骨架是"场景论证"（钩子→痛点→思路→机制→收尾），制券两屏被当成**论据插图**用，不是**操作台阶**用。所以缺了 0 接触的人非看不可的四样东西：① 入口——从打开小程序到制券页要点哪几级；② 顺序——先填哪组后填哪组，为什么；③ 后果——按下「创建优惠券」之后出现什么，券在哪里能看见；④ 对面——顾客扫码后他手机上是什么，到店出示什么，店员扫完双方各自看到什么。这四项一个都没有，画面再准也只是"一张像后台的图"。

## 六、第四轮：样式冻结，只做内容（2026-08-30 晚）

用户验收原话：「我刚才看了修改后的 s4 和 s5 这种 UI 样式表现的挺好的，而且其他屏的 UI 样式都挺好看的，现在主要就是内容的优化。」→ **渲染层与视觉样式一律不动**，本轮只改 `video/src/data/g06.ts` 的屏序、payload 行与值、口播帧表；骨架从九副现成组件里挑，不新增动画。

- **成片规模**：12 屏 / 5748 帧 / 191.6 秒；48 句口播去标点后逐句峰值 **5.89 字/秒**（线 ≤6），零超速。（此条原写 5.61 系旧错值，与逐句字数注记自相矛盾，2026-08-30 第十三轮在 `07-片1-分镜稿.md` 已改 5.89、此处漏改，2026-09-01 审计改齐。）
- **闸门实测**：红线 ✅、文档引用 ✅（失效引用 0）、事实 ✅、相似度 ✅（零同构屏）、上屏真实性 ✅（全部 `k:` 字段回源码逐字命中）；效果尺子 ⏳ 仍红——盘上成片是作废的 9 屏版，比 `video/src` 旧 392 分钟，按节奏待内容点头后重渲。
- **本轮抓出并修掉的三处内容错**（都是"没吃透真值就写"的老毛病，已回写进真值表防复发）：
  1. S5 把「到期提醒」挂在「领券顾客信息」组标题下 → 源码里它是平级独立分组（`create.vue:451`）。真值表新增 `createGroups`：制券页 15 块分组、各自真名与行归属全表登记。
  2. 片尾说"已领取／已核销" → 统计页没有这两个标签，真词是「有效发放张数 · 核销率 · 导出领券记录」（`stat/store_overview.vue:30-33`、`receive/list.vue:128`）。
  3. S11 副标题写"这四步"而时间线只有 3 个节点 → 改"这三步"；同屏页脚「次卡只有转赠没有奖励」已核（`转赠奖励` 全库只在 `pages_coupon/**`，`pages_card/card/create.vue:183` 只有「允许转赠」）。
- **另修**：S4 兑换内容提示语原来混用了类型页例子，改回 `create.vue:110` 原话「例：冷饮一杯，最多10个字」；S5 页脚与「创建优惠券」按钮几何重叠 → 页脚留空，对照改由 S7 页脚与 S8 右栏承担；`scripts/.tmp-frames.mjs` 的取样偏移改为按骨架给（`g06-make-rules` 需 +80 帧才拍到页脚带）。

## 七、第五~八轮：两条服务端口径 + S5 过程帧返工（2026-08-30 深夜）

### 7.1 你给的两条口径（前端查不到，只有你知道），已落成表内行

| 口径 | 落到哪 | 内容侧结论 |
|---|---|---|
| 订阅消息共六类：卡券领取成功通知 / 卡券到期提醒 / 转赠奖励到账通知 / 核销奖励到账通知 / 次卡领取成功通知 / 券包领取成功通知；**其他不确定的不用说** | `spec/coupon-fields.json` → `messageReminders.types`（六条各带 `trigger` 与 `evidence`；前端可证的引 `applet/config.js:10-16`，到账两条标「2026-08-30 用户服务端口径」）。`spec/facts.json` 的 `subscriptionMessages` 已补齐同六类并加了 `subscriptionMessagesRule` 指明**以 `messageReminders.types` 为准** | 片 1 直接说：「到期提醒是开的，提前 3 天提醒家长一次」「B 一核销，A 的奖励券自动到账，A 也会收到到账通知」。**到账类不再回避**——你同日再确认送达是产品支持的能力，不用挂「顾客先订阅才收得到」这层保留说法（旧稿那样写已全面改回）。仍守的唯一边界：措辞不带站外平台名，只说「提醒」「通知」，所以产品原描述那句仍不引用 |
| 「转赠中可随时取消转赠」**不重要，不需要说** | `spec/coupon-fields.json` → `offScript.items[0]`（带 `detail.vue:109,132` / `transfer_list.vue:128` 源码位）；`00-系列规划.md` 第三招、`07` S11 表、`允许转赠.playLeverage` 均已改成「仅首位领取好友可获得本券」 | 不上屏、不念。**保留例外**：「优惠券转赠中，暂不可核销」是商家真会撞上的核销拦截提示，按 `verifyPageKeyInfo` 保留 |

### 7.2 S5 返工（你贴帧指出「这一屏的帧都是乱的」）

根因两条，都不在样式：① **容量**——`g06-make-rules` 节拍表只有 6 个行槽，我排了三组七行，最后一组被顶出可用高度；② **入场时序**——`FormCard` 容器一挂就出现、行按拍擦入，中段是两张只有打孔齿的空白回执（frame 1700/1750 实测）。

修法分三层：① 内容压到六行五句（「填写手机号」移交 S8 私密发放屏）＋组名回归页面原生「⑤ 到期提醒」；② **真根因**——每一行在轮到自己之前就占了整行高度，卡因此挂成只有打孔齿的空框；`FormLine` 改成 `f < delay` 时 `return null` 不占位，卡片随内容长高，组标题与卡的入场取本组首行那一拍前 10 帧；③ 全程只复用该文件里现成的 `HandIn` / `RowWipe` / `GroupHead` / `FormCard`，未新增任何视觉。**我第一版只给容器套 `HandIn` 是半修**（S4 frame 1050 仍空 2.3 秒），是你再贴帧才逼出第②层。**我的验收失效点**：此前只抽峰值帧，静帧全绿掩盖了过程帧的空框——已把「每屏至少入场/中段/峰值三帧」写进 `SKILL.md`，并在 `07` §〇·续 留了返工记录。

> 附带勘误：`src/skeletons/` 这个目录在本库**不存在**（全库只有 `applet/uni_modules/tdesign-uniapp/components/skeleton`）。九副骨架实际都在 `video/src/videos/g06/index.tsx` 内联，`video/src/components/ui.tsx` 头注记录 08-29 已删掉 9 个"统一外观"业务组件。所以"复用现成组件"只能取该文件里的 `HandIn` / `RowWipe` / `GroupHead` / `FormCard`，不能再去找一个 skeletons 目录。

### 7.3 机检升级

`scripts/check-ui-truth.mjs` 从两层升到四层：① `k:` 字段名逐字（原有）② 「」声称的界面说法（原有）③ **分组归属**——组标题若照抄页面原生 8 个组名之一，其下每一行必须真在该组里，否则**硬失败**（S5 那个错字段名全对、① 拦不住）；④ 表外字段提醒回写。同时修掉一个键名边界 bug（`mark:` / `peak:` 会被当成 `k:` 吞掉值）。`spec/coupon-fields.json` 的 `createGroupsRule` 勘误：原生组名是 **8 个不是 7 个**（`领券顾客信息` 的 `title` 换行写在 468 行，之前数漏）。

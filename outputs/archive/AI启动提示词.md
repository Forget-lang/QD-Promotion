# 券到卡包 · AI 启动提示词

> 更新：2026-09-03（变更史只记 changelog，本文不复述；原则：提示词只指路，不复述规则）
> 用途：开新会话 / 换 AI 工具时，粘贴对应场景的提示词，让 AI 30 秒接手。
> 原则：**提示词只指路，不复述规则**——所有口径以 `AGENTS.md`（入口，含真源清单）、`SKILL.md`（七步法）、`spec/` 机器可读真源为准。
> 全局地图：新会话如需快速建立文档结构认知，可先读 `outputs/archive/文档架构总览.md`（2026-09-02 快照，只作路由指路，与权威文档冲突时以权威文档为准），再按开工三动作执行。

---

## 场景 A：文档审计 / 工作流验证

```
你是「券到卡包」内容运营项目的 AI 助手，工作区为 quandao/promotion。

【任务】文档审计（或工作流验证，二者同标准：验证就用正式产出来验）

第一步：读 AGENTS.md → SKILL.md → `spec/` 各真源（清单见 AGENTS.md §二）；跑 node scripts/gate-all.mjs --tsc。
第二步：检查（逐项输出，每项带磁盘/命令证据，不凭记忆）：
1. 断链与失效引用：以 check-doc-references 输出为准（已含反引号资源路径存在性）
2. 文档与实现是否一致：SKILL §三工程约定、R3 组件表逐条回 video/src/ 源码核对；文档描述的实现若已删除，指路必须改
3. 文档间口径冲突：同一参数在 SKILL/spec/R2/R3/R6 是否一致；红线一律以 spec/redlines.json 为准
4. 自觉条款残留：找"只能靠 AI 打勾、没有机检也没有真图可判"的规则——这种条款应当删或改成数字
5. 历史产物不回溯：outputs/ 已交付内容只报告不改动
6. 拍板对账：读 changelog 顶部近期条目，逐条对照 SKILL/spec/R 文档/本提示词的现行文字，列出"拍板了没落实"或"文档还写着已作废路线/旧数量"的项（语义过时不断链，机器查不出，必须人肉逐条对）
7. 代码卫生：查 video/src 的死代码与退化旁路——零引用的业务/外观组件、types 里挂着的已删渲染器专属字段与旧类型词表、任何"不声明 ui 也能渲染出来"的回退路径、共享层里不该存在的成品模板（2026-08-29 两轮拆除即此检查的成果基线）
8. 闸门自身健康：验证闸门不接管道、看真实退出码；近期新增/修改的检查层做负向测试（植入一个坏例子确认报红，移除后转绿）——绿灯本身要先证明是可信的
【协作规则】只读不改，先出问题清单；用户确认后再动手；改动权威文档后跑 gate-all + 记 changelog。
```

## 场景 B：正式产出一条视频（最常用）

```
你是「券到卡包」内容运营项目的 AI 助手，工作区为 quandao/promotion。

【任务】（二选一填这里）
- 按 SKILL 七步法选行业并端到端产出一条视频 + 三平台发布稿
- 做 XX 行业（内容现成则从第 2 步视觉母题开始）

第一步（开工三动作，AGENTS.md §三）：
  node scripts/gate-all.mjs   →   node scripts/list-assets.mjs   →  读 outputs/archive/changelog.md 顶部 10 条
红灯不产出、不交付。
第二步：**通读 SKILL.md 全文**（七步法、工程约定、清零重启否决清单、背景素材入库都在里面）——本任务一切口径以 SKILL 为准，不凭记忆、不凭习惯、不以本提示词为准。
第三步：严格照 SKILL.md 七步法执行，每进入一步重读 SKILL 对应章节确认不走样；SKILL 与任何其他材料冲突时，以 SKILL 为准。
【人工环节】方向拍板、一屏标杆、逐屏真图确认、素材提供（三确认节点见 AI使用手册 §六）——到点明确告诉我需要什么，等我完成再继续。
【我的表达习惯】我说"这屏很空/很乱/不像"，你翻译成具体画面动作重渲给我看，不许只做放大字号式敷衍。
```

## 场景 C：设计稿 → 编码（交给编程 AI，如 Cursor）

```
你是「券到卡包」Remotion 视频编码 AI，工作区为 quandao/promotion/video。

【输入】读根目录 SKILL.md（第 4~6 步）+ docs/internal/R3-Remotion技术参考.md（§五 扩展指南、§七 制作硬规则）
+ outputs/gXX-行业/ 下本片分镜稿与内容输入包（交付物清单以 SKILL §三为准）+ node scripts/list-assets.mjs 实时组件清单。
【任务】按设计稿实现专属屏组件（video/src/videos/gXX/）与数据文件（src/data/gXX.ts，屏上写 ui:'gXX-名字'，
在 scenes/index.tsx 的 VIDEO_RENDERERS 注册）。
【硬性】禁止 CSS animation/transition，全部 useCurrentFrame + interpolate/spring；屏间必须 TransitionSeries；
文案/颜色全从数据读，禁止硬编码；渲染必带 NODE_OPTIONS=""，浏览器用 Chrome Headless Shell
（npx remotion browser ensure 后指 node_modules/.remotion/... 路径，本机系统 Chrome headless 会因显示链接挂死）。
【验收】tsc 零错误 → 逐屏峰值帧 still → check-similarity 绿 → check-motion 达标（阈值以脚本输出为准——占用率只是防"空和死"的下限，高于它不代表更好，禁止为过线堆装饰，见 SKILL §四否决清单"对着指标凑画面"条）→ 交真图给用户。
```

## 场景 D：背景图入库验证（用户找图后逐张验收）

```
你是「券到卡包」素材管理 AI，工作区为 quandao/promotion。

【任务】用户提供候选背景图，逐张验收，合格即收录（此流程常态化独立运行，风格家族多样，质感必须精致）。
【第一步】读 SKILL.md §五（背景素材入库三关验收：风格家族、三关判据、入库流程全以该节为准）+ spec/assets.json（已入库清单 / 家族×色系缺口 / 下一序号）。
【第二步】逐张按 SKILL §五 过三关（任一不过 = 拒收，说清违反哪条）。
【第三步】合格者按 SKILL §五 入库流程执行（裁剪 → 命名 → 登记 → 前景兼容验证 → check-facts 对账）。
【纪律】不删用户原图；改 assets.json 后跑 gate-all + 记 changelog。
```

---

## 新文档准入三问（新建任何 .md 前必答，答不出先问用户）

1. **定性**：权威规则 / 时点快照 / 结构地图，三选一。判据：这份文件过期时该"改它"还是"重做一份"？改 = 权威；重做 = 快照（outputs/ 交付物，只在生产时点有效，不回溯对齐）；地图只载结构不载口径。
2. **口径**：要写数字/阈值/区间/规则吗？要 → 只写指向属主的指针，不抄数值（复述属主外的已登记口径 = gate 硬失败）；自荐当新口径属主的，先确认该口径在别处无家（属主唯一），并在 `scripts/ref-registry.json` 的 `caliberOwners` 登记 pattern + owner。
3. **入户**：权威性质且不在 `docs/internal/` 下 → 登记 `scripts/ref-registry.json`（extraDocs/aliases），纳入闸门扫描面；落盘后必跑 `gate-all` + 记 changelog。

## AI 工具选型（要点）

| 阶段 | 选什么 | 为什么 |
|---|---|---|
| 攻略 / 母题 / 审计判断层 | 旗舰档（GLM-5.3；国产池可测 DeepSeek-V4-Pro） | 唯一无机检兜底的节点 + 跨文档全局一致性，错误发布后才暴露 |
| 口播 / 分镜对表 / 数据复盘 | qwen-3.7-plus | 硬约束多的细致活，要服从性不要发挥 |
| Remotion 编码 / 闸门脚本 / 渲染修帧 | deepseek-v4-flash（Agent 形态） | 错误被 tsc/闸门当场抓，高频循环快且省（实测首选） |
| 发布稿短文案 / 标题候选批量 | seed-2.1-turbo | 平台语感短文案，低风险高频 |

- **选档判断尺（2026-09-03 用户拍板）**：看**错误被谁抓住、多快被抓住**——错误有硬校验当场抓的活用 flash 档；错误要发布后才暴露、或需跨文档全局一致性的活（攻略、文档架构、审计判断层）用旗舰档。梳理类拆两层：判断层旗舰、按已定清单搬运 flash。一句经济账：低频高价值买旗舰，高频循环买 flash。
- **形态比模型重要**：编码/渲染/量数必须在 Agent 形态（能读代码库、改文件、跑命令、看真图）里跑，纯对话形态做不了。
- **验证纪律、真值纪律对任何模型不豁免**：AI 声称"已修改并验证"时，一律用 `git diff` + 重跑命令复核（本项目遇到过伪造验证的会话）；产品能力一律回 `spec/` 真源，模型再强不凭参数记忆写功能。

### 宣传节点 × 常见大模型适配总表（国内外，2026-09 快照）

> 型号与排名季度级变动，此表只作选型对照起点；换用前按「选档判断尺」+ 小样实测定标。排序不分先后、同梯队内按当前公认表现。

| 宣传节点 | 第一梯队 | 第二梯队（性价比/特定强项） | 备注 |
|---|---|---|---|
| 行业攻略 / 选题策划 / 深挖 | Claude 旗舰系、GPT-5 系、Gemini Pro 系 | DeepSeek-V4-Pro、GLM-5.3、Kimi K2、Qwen-Max | 长文档综合+结构化+克制；Gemini 长上下文适合整站素材同读 |
| 文档审计 / 架构 / 口径对账 | Claude 旗舰系、GPT-5 系 | DeepSeek-V4-Pro（Thinking）、GLM-5.3、Kimi K2 | 跨文档全局一致性，语义敏感度优先 |
| 视觉母题 / 设计方向推演 | Claude 旗舰系、GPT-5 系 | GLM-5.3、DeepSeek-V4-Pro | 设计推演本质是结构化+对照分析 |
| 口播稿 / 中文口语叙事 | Qwen-Plus 档、豆包 Seed 旗舰档 | GLM-5.3、Kimi | 硬约束多要服从性；豆包系口语语感好 |
| 三平台发布稿（抖音/小红书/搜狐） | 豆包 Seed 系（抖音同源语感） | Qwen、GLM、Kimi、GPT-5 系 | 低风险短文案，可多模型出候选人来挑 |
| Remotion 编码 / 技术执行（Agent） | Claude 系（编码公认第一档） | DeepSeek-V4-Pro/Flash、GPT-5 系、GLM-5.3、Qwen-Coder | 必须 Agent 形态；项目实测 flash 档够用且省 |
| 看真图审片 / 多模态验收 | Gemini Pro 系、GPT-5 系 | Claude 视觉、Qwen-VL | 对照分镜核画面信息密度 |
| 数据复盘 / 结构化分析 | GPT-5 系、DeepSeek-V4-Pro | Qwen-Plus、GLM-5.3 | 克制结论，不编数据 |
| 背景图生成（素材线，须标注 AI 生成） | 即梦/Seedream、Midjourney | 通义万相、Recraft、Gemini Image | 仅抽象背景/纹理；具象人物插画禁（C-01） |
| TTS 语音合成 | 豆包 seed-tts（项目已定稿组合） | MiniMax Audio、火山其余音色 | 变速与语速口径见 SKILL 第 6 步 |

> 上表不含的常见模型（Llama 系开源、混元、Step 等）：可作私有化/备份位，无突出项。

## 启动后 AI 的一句话复述（示例）

> "已跑 gate-all（通过项与跳过项见脚本输出，不在此复述条数）+ list-assets + changelog 顶 10 条。当前状态以脚本输出与 changelog 最新条目为准。我将按 SKILL 七步法执行【任务】，到人工环节会停下等你。"

---

*注：本文件只是启动模板；规则细节一律以 `SKILL.md` 与 `spec/` 为准，冲突时以权威文档为准。*

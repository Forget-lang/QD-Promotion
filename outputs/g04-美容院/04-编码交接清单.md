# G04 编码交接清单（M2 阶段一 · 画面验证）

> 用途：把「设计稿→代码→画面验证」编码环节交接给 Cursor/Trae 本地版执行（按 AI 启动提示词「场景 D：设计稿→编码执行」）
> 交接日期：2026-08-25 ｜ 交接方：策划 AI ｜ 执行方：Cursor/Trae（编程 AI）

---

## 一、输入文件（执行前全部读一遍）

| 文件 | 用途 |
|---|---|
| `outputs/g04-美容院/02-视频设计稿.md` | **可执行规格**：8 屏分镜/风格五维/动画时序/画面展示内容（编码依据） |
| `outputs/g04-美容院/01-券组合与四件套文案.md` | 画面展示内容来源 + 口播稿全文（⑤，598 字，字幕断句用） |
| `docs/internal/M2-视频制作手册.md` | 怎么做（技术硬规则/两阶段流程） |
| `docs/internal/R3-Remotion技术参考.md` | 原理/组件 API |
| `video/src/` 全部代码 | 现有组件库（scenes/components/types/palette） |

## 二、已完成的前置工作（不要重复做）

1. **TransferScene 已修复 darkText 适配**：`video/src/scenes/TransferScene.tsx` 人物标签（老客/新客）与底部说明卡文字已改为 `scene.darkText ? p.ink : '#fff'`（G04 浅紫背景需要）
2. **背景图已就位**：`video/public/backgrounds/g04/bg.png`（= BG-ABS-003 粉雾网格渐变底，素材索引表 A 级，浅底 → 全片 `darkText: true`）
3. **S2 文案已修正**：`video/src/data/g04.ts` S2 leftItems 已改无绝对化表述（"一提办卡，不少顾客就打退堂鼓 / 办了卡充完钱就很少再来 / 推销太多顾客更警惕"）——已按此渲染 S2 still 验证通过；本次重渲染时以现有 g04.ts 为准，勿回退

## 三、编码步骤

1. **读设计稿 + 代码库**：重点理解 S1 contrast 钩子 + 手写高亮（HighLightText new）、S2 编号列表（PainScene numbered-list 变体 new）、**S3 cardface 新积木**（new:CardFaceScene，规格 R3 §5.4）、**S4 括号分组**（new:BracketGroupScene，R3 §5.6）、S5 多层分区（GridScene multi-section 变体 new）、S6 transfer、字幕数据（subtitles 字段）
2. **新建 `video/src/data/g04.ts`**：
   ```yaml
   id: G04-Beauty-SendCard
   style:
     palette: berry-purple
     motion: snappy
     typography: friendly
     transition: slide
     hookStyle: contrast
     bgImage: backgrounds/g04/bg.png
     bgBlur: 0
   scenes: 8 屏（hook→pain→**cardface**→**bracket-group**→grid→transfer→panel→cta），全部 darkText: true
   dur 预估（设计稿值，最终 TTS 实测回填）:
     S1 12.2s | S2 12.5s | S3 15.5s | S4 18.4s | S5 16.4s | S6 12.7s | S7 13.7s | S8 9.7s
   subtitles: 口播全文（01 文档⑤）按语义断句，每行 10-20 字，无声版时间轴均匀分布（占位）
   ```
3. **【先做】新积木 CardFaceScene**（2026-08-25 新增，S3 专用，禁止回落 solution 白卡）：
   - 规格：`docs/internal/R3-Remotion技术参考.md §5.4`（五要素齐全：视觉结构/数据字段/差异说明/动画时序/验收）
   - 实现顺序（R3 §5.2 步骤 3）：types.ts 的 SceneType 加 `'cardface'` + Scene 加 cardName/cardType/fields/brand 可选字段 → `scenes/CardFaceScene.tsx` 新建渲染器 → `scenes/index.tsx` SceneRenderer switch 注册 → tsc + 1 帧 still 验证（深色卡面白字清晰、字段值 ≥32px、与 solution 白卡肉眼可辨）
4. **注册**：在 `video/src/data/index.ts` 导出 g04
5. **类型检查**：`npx tsc --noEmit` 零错误
6. **still 验证**：`npx remotion still src/index.tsx G04-Beauty-SendCard /tmp/g04-s1.png --frame=NN --scale=0.3`——每屏 ≥1 帧，重点验证：
   - S1 contrast 红绿卡 + 副标题（对比度，darkText）
   - S3 cardface 深色卡面：次数大字是焦点（≥100px）、字段值 ≥32px 可读、光晕+渐变蒙层磁卡质感（V-R09 + 视觉观感差异化）
   - S4 括号分组：左竖排标签 + 大括号 + 右侧字段明细全部可见、可截图抄作业
   - S5 多层分区：虚线分区 + 丝带标题 + 数字锚点（3 天/6 次/90 天）可见
   - S6 transfer 浅底白字问题已修复（人物标签应为深色 ink）
   - 内容左右在安全区（x:80-1000）
7. **出逐屏关键帧单张（2026-08-25 改：不渲染无声版、不拼图）**：逐屏峰值帧（起始帧 + dur×FPS×0.3）still 单张输出 → `outputs/g04-美容院/g04-s1.png` ~ `g04-s8.png`；对照 M2 §7.1 检查表逐项自检
8. **红线闸门**：`cd .. && node scripts/check-redlines.mjs`（video/src 层零命中）
9. **视觉观感比对（M2 §2.3 新增）**：逐屏关键帧 vs G03 关键帧——S3 深色卡面与 G03 白卡列表肉眼可辨，骨架不雷同

## 四、新增规则提醒（2026-08-25 生效，影响数据文件）

- **字幕 = 口播全文**：照抄 01 文档⑤口播稿（598 字），不做摘要不缩写（E-013/E-014）；口播稿已通过绝对化检查，不要改写
- **左右安全区 ≥80px**（M2 §3.3 新口径）：如组件布局贴边，按安全区收紧
- **时长深度结果论**：dur 按设计稿预估即可，最终 TTS 实测回填，不要为对齐区间调 dur

## 五、验收标准

- [ ] `npx tsc --noEmit` 零错误
- [ ] 首帧符合设计稿 hero 视觉（S1 contrast + 副标题）
- [ ] 每屏布局模式/视觉参数/画面展示内容符合设计稿
- [ ] 动画时序无嵌套冲突（M2 §3.1）
- [ ] 字幕占位 = 口播全文（语义断句）
- [ ] 红线闸门 video/src 层零命中

## 六、输出（交回主会话）

- 逐屏关键帧单张：`outputs/g04-美容院/g04-s1.png` ~ `g04-s8.png`（每屏峰值帧 1 帧，供用户确认画面质量——人工环节 3）
- 问题清单（如有）：哪些设计稿要求无法满足 + 原因 + 替代方案
- 本阶段不输出无声版 mp4（2026-08-25 定）

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-059`
- 标题：文档面残尾清理——风格包内假顶层节 ＋ 历史片登记表反向指针
- 负责人：项目 AI（执行）
- 范围：`global`
- 状态：`CLOSED`
- 来源：2026-09-24 复盘审计未清理项 **U10**（原列在 056 收口报告 P-4 批次、执行时漏项）＋ 本轮回盘自查新发现 1 处（056 引入的登记表缺反向指针）。

## 二、Goal

两处"看起来没问题、但会让下一个读的人走错"的文档残尾：① `remotion-components` 包内 §⑧A 底下顶着一个 `## 三、工程约定` 的**假顶层节**（逐字搬迁时把旧章节号一起搬进来，与本包 ①~⑩ 的编号体系并列，会被读成"包还有第十一节叫三"）；② 056 新建的 `outputs/LEGACY-ISOLATION.md` **只有单向引用**——登记表指向 `AGENTS.md` §九，而 §九 不指向登记表，从宪法入口进来的会话**看不到这份登记表存在**（隔离标记因此可能日后被当作冗余清理掉）。

## 三、新口径 New Policy

1. **下沉进风格包的正文不得携带旧文档的顶层章节号**；来源在 §A 标题的括注里写明即可，正文一律挂在包自己的编号栏下（本包只有 ①~⑩ 与派生栏 ③A/③B/③C/⑦B/⑧A/⑧B）。
2. **登记表与规则本体必须双向可达**：规则 Owner（`AGENTS.md` §九）加一行指向登记表与机器声明源；登记表继续声明"规则本体属 §九"。**只有单向引用＝新会话从权威入口进来看不到登记件**。
3. `AGENTS.md` 顶部「最后校验」随本次实际读取与改动更新为 2026-09-24。

## 四、Replace

| 被替代 | 说明 |
|---|---|
| 包 §⑧A 内的 `## 三、工程约定` 顶层标题 | 删除；迁入说明改为"旧章节号不再保留，本节即 §⑧A 正文"（避免包内出现第二套并列编号） |
| `AGENTS.md` §九 末条之后的缺口 | 补一行：登记表＝`outputs/LEGACY-ISOLATION.md`＋机器声明源＝`ref-registry.legacyPieces`＋对账方＝`check-style-rotation`，并重申"规则本体仍属本节" |
| `AGENTS.md` 顶部校验日期 | 2026-09-23 → 2026-09-24 |

## 五、Remove

- 包内那个与 ①~⑩ 并列的假顶层章节号。

## 六、Preserve

- §⑧A 的**正文逐字不动**（只处理标题行）；`SKILL.md` §三 指针节不动（它是指向包的入口，不是重复正文）。
- 包内小节按「迁移波次」排列（③A→③B→⑧A→③C→⑦B→⑧B，标题已各自写明第几波）**故意不重排**——按数字重排属无收益的整文件挪动，且波次顺序本身就是这六节的来历说明。
- `legacyPieces` 声明、6 条标记、对账机检、其余闸门与事务全部不动。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `video/styles/remotion-components/SKILL.md` | 风格包 | REPLACE（去假顶层节；正文零改动） | `check-doc-references` ＋ diff 行数核对 | DONE |
| `AGENTS.md` | 治理宪法 | REPLACE（§九 补反向指针；校验日期） | 同上 | DONE |
| `outputs/LEGACY-ISOLATION.md` | 登记件 | PRESERVE（其"规则本体属 §九"句不改，双向闭合由 §九 侧补） | 人工核对 | DONE |

## 八、Migration Plan

1. 包 §⑧A：删 `## 三、工程约定`，改写迁入说明。
2. `AGENTS.md` §九：补反向指针一行；顶部校验日期更新。
3. 验证：引用闸门（AGENTS 与新路径在扫描面内）→ `gate-all` 零漂移 → 契约 → 关闭。

## 九、Mechanical Checks

- `node scripts/check-doc-references.mjs`：0 硬失败（AGENTS 里反引号路径 `outputs/LEGACY-ISOLATION.md` 须真实存在）。
- `node scripts/gate-all.mjs`：与 058 基线逐行零漂移。
- `node scripts/check-change-contract.mjs`。
- 正文零改动核对：`git diff --stat` 应只见标题/说明/一行新增，不出现 §⑧A 正文行变动。

## 十、Negative / Semantic Counterexample

**语义反例（绕过关键词仍须被拦的判据）**：反向指针若只写在登记表一侧，新会话从 `AGENTS.md` 进入仍然看不见登记件——**"引用存在"不等于"双向可达"**。检验方法＝从 §九 出发能否一步走到登记表与它的机器声明源（本次由 §九 新增行闭合）；反向从登记表也走得到 §九（056 已写）。

同理，包内假顶层节的判据不是"标题写错了"，而是**它会让包的编号体系出现第二套并列编号**——即便把 `三、工程约定` 改成 `⑧A-1` 之类仍是错的（又造一个新编号），正解是取消该编号、正文归入 §⑧A。

## 十一、Real Output Verification

- Real Output 声明：`not-applicable`
- 理由：纯文档结构与引用方向修正，不触碰任何视觉/内容判据、阈值或产物形态；证据形态为真实命令输出（引用闸门、gate-all、diff 行数核对），不产生关键帧或成片。

- 真实关键帧/短片：无
- 人工验收判据：从 `AGENTS.md` §九 一步可达登记表；包内不再出现与 ①~⑩ 并列的章节号
- 结果：N/A

## 十二、Closure Report

- 已修改：`video/styles/remotion-components/SKILL.md`（§⑧A 去假顶层节 `## 三、工程约定`，迁入说明改写）、`AGENTS.md`（§九 补历史片登记表反向指针；顶部校验日期 2026-09-23 → 2026-09-24）。
- 已废止：下沉正文携带旧章节号的做法。
- 已保留：§⑧A 正文逐字不动；包内按迁移波次排列（故意不重排，理由见 §六）；`legacyPieces` 与 6 条标记及对账机检；其余闸门与事务。
- 影响面：`3 / 3`
- 旧口径扫描：PASS（`check-doc-references` 0 硬失败；本事务未新增登记话头）
- 机械检查：PASS（`gate-all` 14/16＋2 项已裁跳过、**exit 0**，与 058 基线逐行零漂移；`check-doc-references` 0 硬失败；diff 行数核对＝AGENTS 2 增 1 删／包 1 增 2 删 ⇒ §⑧A **正文逐字未动**）
- 负向测试：PASS（临时把 `outputs/LEGACY-ISOLATION.md` 改名 → `check-doc-references` **硬失败并点名 `AGENTS.md:149`「文档指路的资源路径在磁盘上不存在」** → 还原后复绿。含义：反向指针受机检守护，登记件日后不会被静默删掉。**如实标注**：该次为读输出接了 `sed` 管道，node 退出码被管道掩盖，故判据取闸门打印的硬失败行为、不取该退出码）
- 语义反例：PASS（「引用存在 ≠ 双向可达」——此前只有登记表单向指 §九，从宪法入口进来看不到登记件；本次由 §九 补行闭合双向，且反向路径同时进 `check-doc-references` 的路径存在性检查。包内假编号同理：正解是**取消编号**、把正文归入 §⑧A，而不是再造一个新编号）
- 真图/成片：N/A（Real Output 声明＝`not-applicable`）
- 本次新增红：0

**结论：CLOSED**

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260918-026`
- 标题：g11 S3 制券屏「金额揭示前置 ＋ 时点重排」——只调出场顺序，不加元素
- 负责人：AI（用户 2026-09-18 15:00「按照你的建议来执行吧」，即批准路线 A：金额揭示提前 ＋ 压掉尾部静置）
- 范围：`local`（仅 g11 片内 `video/src/videos/g11/index.tsx` 的 S3 时点参数 ＋ 对应文档同步；**不涉**共享层、闸门、真值源、其他屏）
- 状态：`CLOSED`

## 二、Goal

外部清单把「S3 手气券生成」列为 P0，诉求是让观众看出「**系统正在生成一张券**」而不是「在展示一张已经做好的券」；清单自定的手段是**只调出场时间**（WriteIn / BigChalk / RowLine / NoteBand 的时点），且明令不改券字段、金额、使用条件与 payload 结构。

经核，025 已把 S3 判为**不在其范围**（用户 2026-09-18 11:44 拍板不动 S3，理由：S3 是**已验收的一屏标杆**，且当时反对的是"为机器指标加装饰"）。本事务按用户 15:00 批准执行**路线 A**：

1. **金额揭示前置**（清单的核心诉求）：现状是「标题 → 三行字段（规则）→ 金额」，金额要等到 **第 7 秒**才出现，前面 6 秒全是文字行；改为「标题 → **金额揭示** → 规则补齐」，让本屏最有张力的元素（面额大数字）成为**第一个事件**。
2. **压掉尾部静置**：现状 `tip` 在 15.7s 出现、其后 **4.3 秒无事件**；改为把事件更均匀铺开、末事件推到约 17.3s，尾部静置压到 ~2.7s。

**为什么这条路不需要重新做质感验收**：本事务**不改任何元素、样式、位置、版式、时长、`layoutKind`**——只改它们的**出场时间参数**。S3 的视觉语言（深夜黑板牌 + 粉笔字 + 面额大数字 + 金线下划线）与已验收质感**完全不变**；但出场顺序变化需**用户看真图确认观感**（见 §十一）。

## 三、新口径 New Policy

1. **制券屏的事件顺序**＝「**标题 → 金额揭示（核心事件） → 规则补齐 → 解释（批注） → 提示收口**」——把"这张券最关键的信息"放在最前，规则与解释随后补齐；不得让核心信息晚于规则细节出现。
2. **单屏尾部静置上限**：一屏最后一个事件距该屏片尾 **≤ 3 秒**（本屏目标 ~2.7s）。超出即视为"尾部空转"，须靠时点重排（**不是**加装饰）解决。
3. **"生成感"的合法手段**：仅限 §四 的时点重排；**新增"券面容器/空框"等可见元素属越界**（须先补 `07-片1-分镜稿` 走 C-14，属另一条路线，不在本事务）。

## 四、Replace

| 旧口径 | 位置 | 新口径 |
|---|---|---|
| 券类型行 `start = 30` | `video/src/videos/g11/index.tsx`（`G11FormFace`） | `start = 18` |
| 面额街区：标签 `180` / 大数字 `196`、`214` / 金线下划线 `224` / note `232`（**晚于三行字段**） | 同上 | 标签 `60` / 大数字 `78`、`104` / 金线下划线 `126` / note `142`（**早于三行字段**） |
| 三行字段 `RowLine start = 64 / 86 / 108`（**早于金额**） | 同上 | `start = 240 / 272 / 304`（**晚于金额**，间隔由 22 帧拉至 32 帧） |
| 批注带 `NoteBand start = 330` | 同上 | `start = 400` |
| 提示条 `tip WriteIn start = 470`（其后静置 4.3s） | 同上 | `start = 520`（其后静置 ~2.7s） |

## 五、Remove

- **不新增**任何可见元素（不加"券面容器/空框"、不加图形、不加粒子/光效/抖动）。
- **不改**元素样式、字体、字号、颜色、位置、内边距、板高（`Board h=1150 top=300` 不动）、屏时长（600 帧不动）、`layoutKind`。
- **不改** `types.ts` 与 `data/g11.ts`（payload 结构、券字段、金额取值、消费门槛、使用条件一律不动）。
- **不改**共享层（`VTemplate.tsx` / `components/**` / `scenes/**` / `palette.ts`）。
- **不改**闸门与检查器（`scripts/**` 零改动）。
- **不改**其他屏（S1 / S2 / S4 / S5 / S6 / S7）与其他片（g06~g10）。
- **不含** S3-02 / S3-03 的形态改动（本轮只动出场时序，引线与提示条本身不变）。

## 六、Preserve

- S3 一屏标杆的全部视觉语言与已验收质感（黑板牌、粉笔字、手绘线、面额大数字、金线下划线、板下余温光）。
- S3 的 20s 时长、`layoutKind`、板尺寸与版式结构。
- S3 三镜的设计意图与 `State Change` 语义（券从无到有 / 填了什么值→为什么这么填 / 会设→别越界）。
- 其余各屏实现与产物；`g11-烧烤-无声成片.mp4` 的规格（1080×1920 / 96.66s）。
- 025 已闭环的全部结论（S4 / S5 事件化、游离文件清除、红线闸门解除状态）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `video/src/videos/g11/index.tsx`（`G11FormFace` 的 6 组 `start` 参数） | 片内工程 | REPLACE（9 个数值） | V1（金额三项 `start` 小于字段行 `start`，且金额首现 ≤ 90 帧）＋ V2（tipline `start` ≥ 500）＋ V3（元素与版式零新增）＋ `tsc` 零错 | DONE |
| `outputs/g11-烧烤/07-片1-分镜稿.md`（S3 段「入场」） | 片产物（分镜稿） | REPLACE | V4：S3 入场段与实现一致（顺序＝标题→金额→字段），并附本事务出处 | DONE |
| `outputs/g11-烧烤/08-R9视觉决策卡.md`（S3-01 / S3-02 / S3-03 的 `Motion` / `Peak Frame`） | 片产物（Shot 表） | REPLACE | V4：`check-visual-shot-contract` PASS；峰值描述与实现一致 | DONE |
| `outputs/g11-烧烤/frames/S3-制券①屏.png` | 片产物（交付帧） | PRESERVE（已按新实现重渲，**结果与改前逐字节一致**——屏尾全内容帧在两版时点下内容相同） | 重渲后 md5 与改前相同；目视核对全元素齐备 | DONE |
| `outputs/g11-烧烤/g11-烧烤-无声成片.mp4` | 片产物（成片） | REPLACE（重渲） | mtime 晚于 `video/src/**`；`check-motion` 实读 | DONE |
| 全闸门基线 | 机制 | PRESERVE | V5：`gate-all --tsc` 红灯构成**不超改前**（探针假红 ＋ 尺子设计阶段红；红线闸门维持已解除状态） | DONE |

## 八、Migration Plan

1. 立本契约（`APPROVED`）。
2. 改 `G11FormFace` 的 9 个 `start` 数值（仅时点，不动任何元素与样式）→ `tsc` 零错。
3. 渲 S3 关键帧自检（金额首现帧 / 字段补齐帧 / 屏尾帧），确认观感为「先揭金额、后补规则」，无折行与溢出。
4. 同步 `07-片1-分镜稿`（S3 入场段 ＋ 本事务出处）与 `08-R9视觉决策卡`（S3 三镜的 `Motion` / `Peak Frame`）。
5. 重渲 S3 交付帧与无声成片。
6. 跑 V1~V5 ＋ `gate-all --tsc` ＋ `check-change-contract`。
7. 填 §十二 闭包并移档 `closed/`。

## 九、Mechanical Checks

- `tsc --noEmit`：零错。
- `check-visual-shot-contract`：PASS。
- `check-change-contract`：PASS。
- `gate-all --tsc`：红灯构成不超改前。
- **验证 / 负向测试**：
  - **V1（金额前置成立）**：`G11FormFace` 内「面额标签 / 大数字 / 下划线 / note」四项 `start` **均小于**三行 `RowLine` 的 `start`；且面额区首个 `start` ≤ 90 帧（≤3s）。
  - **V2（尾部静置受控）**：`tip` 的 `start` ≥ 500（距片尾 600 帧 ≤ 100 帧，即 ≤3.3s）。
  - **V3（零新增元素）**：`git diff` 仅出现 `start` 数值变化，**不出现**新增 JSX 元素 / 新样式属性 / 新 import / 新组件；元素清单与改前逐一对应。
  - **V4（文档同步）**：`07` S3 入场段顺序＝标题→金额→字段补齐且含本事务出处；`08` 的 S3-01~03 的 `Motion` / `Peak Frame` 与实现一致。
  - **V5（闸门不劣化）**：`gate-all --tsc` 红灯 ≤ 改前。

## 十、Negative / Semantic Counterexample

- **反例①（借机加元素）**：为了让"生成感"更足而加"空白券面/空框/券轮廓"——**由 §三.3 与 §五 拦**（属路线 B，须先补稿走 C-14，本事务明令不含）。
- **反例②（借机加装饰）**：给 S3 加粒子、呼吸光、镜头推拉以抬高 `check-motion` 指标——**由 §五 拦**（本事务只调时点；且仓库既有"禁为过线加装饰"纪律）。
- **反例③（改内容或取值）**：把 `start` 重排顺带改了字段文案、金额、门槛——**由 §五 拦**（`data/g11.ts` 与 `types.ts` 零改动）。
- **边界如实声明**：本事务**不声称**"生成感"已被完全满足——真正的"空白券面→完整券"需要新增容器（路线 B），**未做亦不评价其必要性**；本事务只完成"金额揭示前置 ＋ 尾部不空转"两件可验证的事。观感是否达标以**用户看真图**为准（§十一）。

## 十一、Real Output Verification

```text
- Real Output 声明：`applicable`
- 适用证据：
  - `outputs/g11-烧烤/frames/S3-制券①屏.png`：按新时点重渲的屏尾全内容帧（应呈「金额已揭示在前、三行字段已补齐、批注带与提示条均已出现」）
  - 关键帧自检件：金额首现帧 / 字段补齐帧（渲于 /tmp，用于自检不出交付物）
  - `outputs/g11-烧烤/g11-烧烤-无声成片.mp4`：重渲后 mtime 晚于 `video/src/**`；`check-motion` 实读 S3 本屏三项（与改前 54% / 0.23 对比）
```

- 真实关键帧/短片：见上（S3 交付帧 ＋ 无声成片）。
- 人工验收判据：① 金额（面额大数字）是否**明显早于**三行字段出现；② 屏尾是否不再有 4 秒空转；③ S3 的视觉语言与改前**逐项一致**（无任何新元素）；④ 其余屏画面与改前一致。
- 结果：PASS（`applicable`）

## 十二、Closure Report

- 已修改：`video/src/videos/g11/index.tsx`（`G11FormFace` 的 **9 个 `start` 数值**：券类型 30→18；面额标签 180→60、大数字 196/214→78/104、金线下划线 224→126、note 232→142；三行字段 64/86/108→240/272/304（间隔 22→32）；批注带 330→400；提示条 470→520）；`outputs/g11-烧烤/07-片1-分镜稿.md`（S3「入场」段重写 ＋ 本事务出处段）；`outputs/g11-烧烤/08-R9视觉决策卡.md`（S3-01 / S3-02 / S3-03 的 `Motion` 与 `Peak Frame` 共 6 格）；`outputs/g11-烧烤/g11-烧烤-无声成片.mp4`（重渲，22.5MB / 2898 帧）
- 已废止：旧时点序列（「标题 → 三行字段 → 金额」，金额迟至第 7 秒；尾部 `tip` 后 4.3 秒无事件）
- 已保留：S3 全部元素 / 样式 / 字号 / 位置 / 板高（`h=1150 top=300`）/ 屏时长（600 帧）/ `layoutKind`（`board-form`）；`types.ts` 与 `data/g11.ts` 零改动（payload、券字段、金额、门槛、使用条件未动）；其余六屏（S1 / S2 / S4 / S5 / S6 / S7）零改动；共享层与闸门零改动（`scripts/**` 无变化）
- 影响面：`6 / 6`
- 旧口径扫描：PASS（附边界声明）—— 本条属**时序口径**（事件出场顺序），无字面话头可 grep，机检部分不可得；替代证据＝V1（`start` 值比较）与 V3（diff 逐行核对）两项**结构化实证**，边界已如实标注
- 机械检查：PASS —— `tsc --noEmit` **零错**；`check-visual-shot-contract` PASS；`check-change-contract` PASS（28 笔）；`gate-all --tsc` **14 绿 / 16**，红灯 2 道**均非本事务引入**（① 文字安全区探针＝背景纹理假红 ② 效果尺子＝设计阶段红）；**本次新增红 0**
- 负向测试：PASS —— **V1**：面额区五项 `start`（60 / 78 / 104 / 126 / 142）**全部小于**三行字段最小 `start`（240），且面额区首现 = 60 帧（2.0s ≤ 3s）；**V2**：`tip` `start = 520`（距屏尾 80 帧 ≈ 2.7s ≤ 3s）；**V3**：`G11FormFace` 段 diff 共 8 组、**只含 `start` 数值差异**（逐行核过），全文件 diff 中无本事务引入的新增 JSX 元素 / import / 样式块；**V4**：`07` S3 入场段已改为「标题→金额→字段补齐」并含本事务出处，`08` S3 三镜 6 格已同步，`check-visual-shot-contract` PASS；**V5**：`gate-all --tsc` 红灯数 = 2，与改前一致（不劣化）
- 语义反例：PASS（三条边界如实声明：借机加"券面容器"、借机加装饰、顺带改内容取值——均**不由文本层拦**，靠契约 §五 与人工判据；此外**不声称**"生成感"已被完全满足，见下方执行期记录）
- 真图/成片：PASS（`applicable`）—— 关键帧自检件（金额首现 834 帧 / 字段补齐 1060 帧 / 屏尾 1330 帧，渲于 /tmp 仅作自检）＋ `g11-烧烤-无声成片.mp4`（2898 帧 / 22.5MB；mtime `15:05:10` 晚于源码 `15:01:13`）。**交付帧 `frames/S3-制券①屏.png` 重渲后与改前逐字节一致**（屏尾全内容帧在两版时点下内容相同），故未产生新的帧文件改动
- 本次新增红：**0**

**执行期如实记录（两项）**

1. **本事务达成了清单诉求，但未改善面板指标，且原因已查明**：改后 S3 本屏 `静止 54% ｜ 中位帧差 0.21`（改前 `54% ｜ 0.23`），全片 `49% / 0.32 / 48%`（与改前**完全一致**）。原因：**时点重排只改变"事件发生的时刻"，不改变"动的总时长"**；而 `静止占比` 量的是后者（连续采样样本帧间差 ≤0.3 的占比）。弹簧揭示动画本身仍是 16~26 帧（0.5~0.9s），9 个事件的总动时长未变；缩短的尾部 1.6 秒被中段新增的等长空档抵消。→ **结论：`check-motion` 三指标对"出场顺序"不敏感**，本项只能由"看真图"验收（清单诉求属观感层）。**不为改善此指标去拉长动画或加装饰**（违 §五 与既有纪律）。
2. **范围外发现（只记，未修）**：`07` 与 `08` 把 S3-02 的批注区称作「粉笔引线 / 两条引线」，而实现是 `NoteBand`（**两条带「–」前缀的文字行**），并非带连线的引线——属既有文档-实现命名偏差。为避免范围蔓延，本事务**保持既有命名、只改时点**；该偏差在此登记备查。

**结论：CLOSED**

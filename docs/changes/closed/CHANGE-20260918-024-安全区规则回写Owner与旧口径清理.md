# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260918-024`
- 标题：安全区规则回写 R3 Owner ＋ g11 旧口径注释清理 ＋ 023 证据等级收紧
- 负责人：AI（用户 2026-09-18 11:11 转呈外部审计意见，含三项收口；本事务不动任何闸门）
- 范围：`global`
- 状态：`CLOSED`

## 二、Goal

CHANGE-20260918-023 的技术修法已 CLOSED，但留下三处治理/口径残留，属"下一轮 AI 会被旧口径误导"的典型形态：

1. **旧口径残留（第二 Owner）**：`video/src/videos/g11/index.tsx` 文件头注释仍写「内容落在 x[120,960]（**板宽 840、left 120**）」，而实现已是 `BOARD_W = 820 / BOARD_LEFT = 130` → 注释与常量对不上，未来 AI 只扫文件头极易把它"改回去"。
2. **长期规则藏在历史事务里**：023 的 New Policy（「`sceneScale` 一律 1」「安全区达标不得靠整场景缩放」）其实是**技术层长期规则**，却只存在于已 CLOSED 的 023 中——**历史事务是证据，不应承担当前 Owner 职责**；而 `R3 §7.3` 作为安全区 Owner 只写了数值，没写"达标方式"。
3. **证据等级写得比实测覆盖高**：023 闭包把 N1 写成"证明 g06~g10 渲染行为未变"——实际只对 **g10** 做了同帧逐字节回归；g06~g09 是由"`video.id === 'g11'` 分支从未命中"这一**代码事实**支持，未逐一做像素级回归。

## 三、新口径 New Policy

1. **R3 §7.3 成为"安全区达标方式"的 Owner**：安全区**不得靠"整场景缩放"实现**——缩放会连带缩放字幕等独立安全区元素（023 实例：字幕被带出 y1760–1920 规格带）；正确做法是**主体容器自身落进 x120..960**，共享层 `sceneScale` 保持 1。
2. **片内参数不进 Owner**：`BOARD_LEFT / BOARD_W` 等属该片工程参数（`videos/gXX/`）＋变更事务，**不写进 R3**——Owner 只承载"为什么/规则"。
3. **证据等级表述纪律**：直接实证的与仅由代码事实支持的，必须分开写；不得用"全部/逐一"式措辞覆盖未做逐一项。

## 四、Replace

| 旧口径 | 位置 | 新口径 |
|---|---|---|
| 文件头注释「板宽 840、left 120」 | `video/src/videos/g11/index.tsx:4` | 与实现一致：板宽 820、left 130（含旋转 ±0.6° 角点外扩 ~6px 余量），并指向 R3 §7.3 与 023 |
| §7.3 只有数值、无"达标方式"规则 | `docs/internal/R3-Remotion技术参考.md §7.3` | 新增 1 行规则（安全区不得靠整场景缩放；主体容器自身满足；片内尺寸不进本表） |
| 023 §六 Preserve 与 §十二「N1…证明 g06~g10 渲染行为未变」 | `docs/changes/closed/CHANGE-20260918-023-*.md` | 收紧为「**g10 逐字节直接实证；g06~g09 由代码事实支持（未逐一回归）**」，并标注勘误来源 |

## 五、Remove

- 删除 g11 文件头旧尺寸注释（840/120）。
- **不改**任何闸门与检查器（`scripts/**` 零改动）；**不改** R3 既有安全区数值口径（左右 ≥120 / 顶 120 / 底 160 / 字幕 y1760–1920 / 活动区 y120–1760）。
- 不重写 023（只做勘误级收紧，不改其技术结论与状态）。

## 六、Preserve

- 023 的技术修法与全部产物（`sceneScale = 1`、`BOARD_LEFT 130 / BOARD_W 820`、7 屏真图、无声成片）。
- R3 §7.3 既有数值口径与 `G11SafeAreaProbe` 取证 Composition。
- 023 的事务状态与其余证据内容（`CLOSED` 不动）。
- g11 的实现参数本身（本事务只改注释，不改常量）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `video/src/videos/g11/index.tsx`（文件头注释） | 片内工程 | REPLACE（1 行注释） | V1：注释与常量一致；全仓 grep「板宽 840」＝0（V4）；`tsc` 零错 | DONE |
| `docs/internal/R3-Remotion技术参考.md §7.3` | 领域 Owner | ADD（新增 1 行规则） | V2：含"不得靠…整场景缩放"，且**不含** 820/130 片内参数 | DONE |
| `docs/changes/closed/CHANGE-20260918-023-*.md` | 变更事务（历史证据） | REPLACE（2 处措辞收紧） | V3：含"g06~g09 由代码事实支持"，不再出现"g06~g10 逐字节"式表述 | DONE |
| `docs/changes/template.md` | 契约结构 Owner | PRESERVE | 核查结论：不改契约结构，无同步事项 | DONE |
| 全闸门基线 | 机制 | PRESERVE | `gate-all --tsc` 红灯构成不超改前（探针·背景纹理假红 ＋ 效果尺子·设计阶段红），本次新增红 0 | DONE |

## 八、Migration Plan

1. 改 g11 文件头注释（1 行）→ `tsc` 零错。
2. R3 §7.3 新增"安全区达标方式"1 行。
3. 收紧 023 的两处证据表述（加勘误标注）。
4. 跑 V1~V4 与 `gate-all --tsc` + `check-change-contract`。
5. 填 §十二 并移档。

## 九、Mechanical Checks

- 旧口径扫描：全仓 grep「板宽 840」→ 改前 1 处（g11 文件头）→ 改后 **0 处**。
- 文档引用检查：`check-doc-references` 无新增命中（R3 在扫描面内）。
- 其他闸门：`check-change-contract` PASS；`gate-all --tsc` 红灯不超改前。
- **验证/负向测试**：
  - **V1（注释＝实现）**：g11 文件头注释与 `BOARD_W = 820 / BOARD_LEFT = 130` 一致（同文件内 grep 双向命中）。
  - **V2（Owner 边界干净）**：R3 §7.3 含"不得靠…整场景缩放"且**不含** `820`/`130` 片内参数。
  - **V3（证据等级收紧）**：023 内出现"g06~g09 由代码事实支持"，且不再出现"g06~g10…逐字节"式合并表述。
  - **V4（防回流）**：全仓 grep「板宽 840」＝0 处。

## 十、Negative / Semantic Counterexample

- **反例**：注释与常量的漂移**没有自动判据**（`video/src/**` 不在任何闸门扫描面；`deprecatedTerms` 只扫文档正文）——本事务只做一次性对齐，**不声称已获得机器保护**，如实声明该边界。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务只改「工程文件头注释 + 领域 Owner 一行规则 + 已关闭事务的两处措辞收紧」，不产出、不修改任何画面/分镜/渲染件，也不改代码行为（常量未动）。其有效性由 V1~V4 的 grep 实证承担（属 §九 机械检查项）。
```

- 真实关键帧/短片：不适用（023 的真图/成片已在其自身事务内验收，本事务不改其字节）
- 人工验收判据：V1~V4 实测结果 + `gate-all` 红灯构成不变
- 结果：PASS（not-applicable）

## 十二、Closure Report

- 已修改：`video/src/videos/g11/index.tsx`（文件头注释 1 行，与常量对齐）；`docs/internal/R3-Remotion技术参考.md §7.3`（新增「安全区达标方式」1 行）；`docs/changes/closed/CHANGE-20260918-023-*.md`（2 处证据等级收紧＋勘误标注）；`outputs/g11-烧烤/g11-烧烤-无声成片.mp4`（为使效果尺子证据不过期而重渲，画面内容与改前一致）
- 已废止：g11 文件头旧尺寸注释（板宽 840 / left 120）；「长期技术规则只存在于已关闭事务」的状态
- 已保留：023 的技术修法与其全部产物、R3 §7.3 既有数值口径、g11 实现参数（**常量未动，只改注释**）、023 的事务状态（`CLOSED` 不动）、`G11SafeAreaProbe`
- 影响面：`5 / 5`
- 旧口径扫描：PASS —— 全仓 grep「板宽 840」＝ **1 处**，且**仅本事务契约 §四 对旧口径的引用**（契约格式要求列出旧口径以备对照；`docs/changes/**` 不在任何闸门扫描面）→ 代码 / 领域 Owner / 片产物侧**零残留**
- 机械检查：PASS —— `check-doc-references` PASS；`check-change-contract` PASS（26 笔）；`gate-all --tsc` **14 通过 / 16**，红灯 2 道**均非本事务引入**：① 文字安全区探针＝背景纹理假红（机制定位见 023 §十二）② 效果尺子＝设计阶段红（静止 53% / 中位帧差 0.26 / 占用率 48%，属实测现状而非本次改动所致）；**本次新增红 0**
- 负向测试：PASS —— **V1**：g11 注释「板宽 820、left 130」×1 与常量 `BOARD_W = 820` ×1 双向命中一致；**V2**：R3 §7.3 含「不得靠…整场景缩放」×1 且**不含** `820`（0 处，片内参数未进 Owner）；**V3**：023 内「g06~g09 由…」×2（§六／§十二各一处，勘误到位）；**V4**：见上（残留仅本事务契约自身）
- 语义反例：PASS（边界如实声明：注释↔常量的漂移**无自动判据**，本事务只做一次性对齐，不声称已获机器保护）
- 真图/成片：N/A（`not-applicable`，见 §十一；023 的真图/成片已在其自身事务内验收，本事务不改其内容）
- 本次新增红：**0**

**结论：CLOSED**

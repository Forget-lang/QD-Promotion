# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260923-042`
- 标题：文档面整理收尾——MAP/手册/README 对齐双线与风格层、豁免查找去硬编码
- 负责人：用户（方向）／AI-B（侦察、契约、迁移、验证）
- 范围：`global`
- 状态：`CLOSED`
- 触发：用户 2026-09-23 批复「按照你建议来执行，现在要完全整理好文档」（批 C ① 档三项）

## 二、Goal

一句话：041 补齐了治理面，但**索引与用户面文档**还剩最后一次残留——`MAP` 单线世界观与"Remotion 专属"表述、`AI使用手册` 的"唯一作业流程"、`爆款技能包/README` 同句；另有一处**代码硬编码**（豁免表查找写死 `g\d{2}`，f 线取不到豁免）。本事务把这四处收干净。

- 边界：只改索引/说明/查表逻辑；**不改任何闸门判据、不改业务规则**。台账落点迁移（P0 #1）另立事务，不并入本批。

## 三、新口径 New Policy

1. **索引与手册跟架构**：凡描述"生产链/主链/接班链"处，一律按**双内容线**（先判线→取该线唯一 Owner）与**六层模型**描述（导演层＝R9；风格层＝风格包）。
2. **豁免查找按内容线声明**：豁免表（motion/safeArea）的片号 key 由**内容线声明的 `outputPrefix`** 提取，不写死 `g`——与判线同源（`scripts/ref-registry.json` 的 `contentLines`）。

## 四、Replace

| # | 位置 | 旧 | 新 |
|---|---|---|---|
| 1 | `PROJECT-CONSISTENCY-MAP.md`:7 | "再按当前任务读取 `SKILL.md` 与对应 Owner" | "再按当前任务**先判内容线、取该线唯一 Owner**（行业线＝`SKILL.md`／教程线＝`docs/internal/产品功能教程作业规范.md`）或对应领域 Owner" |
| 2 | 同上 §1 主链 | "R9 视觉导演方法 → R9 视觉映射表 → Visual Shot Contract → 分镜 / Remotion 实现" | "导演层（R9 通用判据＋逐片《导演稿》）→ 风格层（所选风格包：视觉语言；视觉映射表＝通用默认起手式）→ Visual Shot Contract → 实现（Remotion／风格化引擎／后期，按所选风格包）" |
| 3 | 同上 §2 表"视觉方法"行 | "R9…视觉导演与审美决策.md ｜ 镜头级视觉决策原则" | "导演层 ｜ 同上 ｜ 为什么这样讲（叙事六项）＋视觉决策原则" |
| 4 | 同上 §2 表"视觉映射"行 | "R9-视觉映射表.md ｜ 叙事关系 → Remotion 视觉关系/组件" | "…｜ 叙事关系 → 表达方式默认起手式（工具无关；构件名为 `remotion-components` 举例）" |
| 5 | 同上 §2 表（新增行） | （无风格层） | "风格层 ｜ `video/styles/` 下每风格一个自包含目录（清单与逐闸门适用性＝`scripts/ref-registry.json` 的 `styles`）｜ 用什么视觉语言表达" |
| 6 | 同上 §2 表"字段登记"行 | 只列 `coupon-fields`／`card-fields` | 补 `point-fields.json`／`member-fields.json`（与 `check-ui-truth`／`check-redlines` 消费面一致） |
| 7 | 同上 §6 接班链 | "`… → AGENTS.md → 当前 Owner / SKILL.md → outputs → Gate`" | "`… → AGENTS.md → 该线唯一 Owner（行业线＝SKILL.md）→ outputs → Gate`" |
| 8 | `docs/AI使用手册.md` 文档分工表 `SKILL.md` 行 | "视频生产唯一作业流程" | "行业线（行业场景视频）唯一作业流程" |
| 9 | 同上（新增行） | （缺教程线） | "`docs/internal/产品功能教程作业规范.md` ｜ AI ｜ 教程线唯一方法 Owner ｜ 只能管教程线" |
| 10 | `knowledge/爆款文案技能包/README.md`:3 | "视频生产唯一作业流程仍是 `SKILL.md`" | "行业线（行业场景视频）唯一作业流程仍是 `SKILL.md`（教程线见其方法 Owner）" |
| 11 | `scripts/gate-all.mjs` 两处豁免查找 | `const m = /g\d{2}/i.exec(...)` 硬编码行业前缀 | 改用共享 `pieceKeyOf()`（按 `contentLines` 的 `outputPrefix` 提取；未命中返回 null＝维持原"无豁免"行为） |
| 12 | `scripts/content-lines.mjs` | （无此函数） | ADD `pieceKeyOf(token, lines)`——路径分隔边界匹配，禁止裸子串（防 `config2` 误命中 `g2`） |

## 五、Remove

**Remove**：上表 12 处的旧表述与硬编码；改后 §九 复扫须归零。

**不并入本事务（保持挂账）**：
- **发布后验台账落点迁移**（`check-release-feedback` 读 `outputs/archive/`，P0 #1）→ **另立 043**（用户已认可方向：迁到 `outputs/` 下非 archive 的独立位置）。
- CI 冻结 g11＋引用不存在的脚本 → 未获口径指示，继续挂账。
- APPLET 快照、教程线视觉语言、风格化机检覆盖、白板 motion 重定标、第 4 步模板参数化 → 各自独立事务。

## 六、Preserve

- `outputs/archive/*`、`docs/changes/*`、`outputs/g06–g11`、`video/styles/*/probe`：零改动。
- MAP 的"不提供实时状态/不承担入口"边界句、§3/§4 已闭环登记：保留。
- 041 已定的口径结论（交付规格授权复述、R9-S 定位句）不动。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `docs/internal/PROJECT-CONSISTENCY-MAP.md`（7 处） | 恢复索引 | REPLACE＋ADD | 复扫＋闸门＋双线/六层核对 | 批 1 |
| `docs/AI使用手册.md`（2 处） | 用户面 | REPLACE＋ADD | 同上 | 批 1 |
| `knowledge/爆款文案技能包/README.md`（1 处） | 研究资源门面 | REPLACE | 同上 | 批 1 |
| `scripts/content-lines.mjs`（新增 `pieceKeyOf`） | 共享判定 | ADD | 单测（构造路径）＋闸门 | 批 1 |
| `scripts/gate-all.mjs`（2 处调用点） | 总闸门 | REPLACE | 行为核对（g11 豁免仍生效）＋负向 | 批 1 |

## 八、Migration Plan

1. 批 1：`content-lines.mjs` 加 `pieceKeyOf` → `gate-all` 两处改用它 → 跑 `gate-all` 核对 g11 豁免行不变。
2. 批 1：MAP 7 处、手册 2 处、README 1 处。
3. 复跑 `check-doc-references`／`check-change-contract`／`gate-all`；关键词复扫；负向测试（构造 `f90`／含 `config2` 的路径各验一次）。

## 九、Mechanical Checks

- 文档引用检查：`check-doc-references` 零硬失败。
- 闸门：`check-change-contract` PASS；`gate-all` 与 042 前逐行一致（豁免行文案不变、红绿不变）。
- 关键词复扫：`视频生产唯一作业流程` → 0（现行链）；`当前 Owner / SKILL.md` → 0；`Remotion 视觉关系` → 0。
- **`pieceKeyOf` 实测**：`…/outputs/g11-烧烤/x.mp4` → `g11`；`…/outputs/f90-功能/x.png` → `f90`；`…/video/src/data/config2.ts` → **不命中**（防裸子串误伤）。

## 十、Negative / Semantic Counterexample

- **负向（放过假 red / 制造假绿）**：为了让 f 线"看起来能豁免"而把 key 提取放宽成任意 `\w+\d+`——会把不存在的片号也拿去查豁免表，等于给豁免开后门。拦截：§九 实测三项（含"不命中"用例）＋未命中仍返回 null（行为与旧版一致）。
- **语义反例**：把 MAP 主链写成"内容 → 风格包 → 导演稿"（风格包先于导演决策）——与 039 冻结的"导演层定为什么这样讲、风格层定用什么表达"相反，且违反"换包不换导演稿"。拦截：主链节点逐条与 §三.1/§三.2 对账。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝索引/手册/README 表述与豁免查找去硬编码，不产出关键帧或成片；判据本体零改动。
```

## 十二、Closure Report

- 已修改：`docs/internal/PROJECT-CONSISTENCY-MAP.md`（7 处：接班纪律／§1 主链三节点／§2 导演层行＋视觉映射行＋新增风格层行＋字段登记补 point/member／§6 接班链）、`docs/AI使用手册.md`（分工表 SKILL 行措辞＋新增教程线行；两处接班链句）、`knowledge/爆款文案技能包/README.md`:3、`scripts/content-lines.mjs`（ADD `pieceKeyOf`）、`scripts/gate-all.mjs`（motion/safeArea 两处豁免查找改用它）
- 已废止："视频生产唯一作业流程"的单线说法；MAP 的"R9 视觉导演方法 → R9 视觉映射表 → 分镜/Remotion 实现"单线主链与"叙事关系 → Remotion 视觉关系/组件"表述；豁免查找的 `g` 前缀硬编码
- 已保留：MAP 的"不提供实时状态/不承担入口"边界句与 §3/§4 已闭环登记；`outputs/archive`、`docs/changes`、历史注记零改动；041 口径结论不动
- 影响面：`5 / 5 已处理`（§七 逐行）
- Owner/入口冲突：**PASS**（风格层行只登记权威与声明源；MAP 仍不承担入口）
- 旧口径扫描：**PASS**（`视频生产唯一作业流程`／`当前 Owner / SKILL.md`／`Remotion 视觉关系`／`g\d{2}/i.exec` 复扫各 0 命中）
- 机械检查：**PASS**（`check-doc-references` 0 硬失败；`check-change-contract` PASS(44)；`gate-all` 13/15、2 项跳过、无红灯，与 041 基线**行级零漂移**——含 g11 两条已裁豁免行的文案与红绿均不变）
- 负向测试：**PASS**（`pieceKeyOf` 四例：`…/outputs/g11-烧烤/x.mp4`→`g11`、`…/outputs/f90-功能/x.png`→`f90`、`…/video/src/data/config2.ts`→**null**、`…/outputs/archive/发布后验台账.json`→**null**；未命中保持 null＝旧行为，未放宽成任意 `\w+\d+`）
- 语义反例：**PASS**（MAP 主链节点顺序＝内容 → 导演 → 风格 → 实现，与 039 §三.1/§三.2 逐条对账一致；未写成"风格包先于导演决策"）
- 真图/成片：N/A（Real Output 声明为 `not-applicable`）
- 本次新增红：**0**

**结论：CLOSED**

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260915-001`
- 标题：建立全局变更收敛机制，降低用户监督成本
- 负责人：内容与视觉方法论负责人
- 范围：`global`
- 状态：`CLOSED`

## 二、Goal

把“用户提出全局想法”从普通文档修改升级为可追踪、可验证、可关闭的迁移事务，避免新旧口径并存、规则只新增不清理、完成状态只能靠用户反复追问确认。

## 三、新口径 New Policy

以后所有跨片、跨行业、共享组件、全局方法、规则或流程变更，都进入 `Change Contract → Impact Map → Migration → Verification → Closure` 生命周期。

用户负责方向与最终视觉；执行 AI 负责迁移与自证；机器负责机械约束；涉及视觉时提供真实产物证据。

## 四、Replace

- “改几个文档 + 跑一次 gate + AI 口头说已落实”的非事务式变更方式。
- 只新增新规则、不检查旧规则是否回潮的做法。
- 用户通过重复追问确认执行是否完整的工作方式。

## 五、Remove

- 将“已完成”定义为“AI 自评完成”。
- 将单次 grep 命中为零视为语义迁移完成。

## 六、Preserve

- `AGENTS.md` 的决策层/执行层分离与整改对账单。
- `scripts/ref-registry.json` 的唯一口径、废止话头与领域真源职责；不重复承载事务状态。
- `docs/AI使用手册.md` 的用户口令与协作说明；全局变更的唯一治理属主由 AGENTS + R8 承担，避免再造第二套流程。
- 历史视频与 changelog 不回溯重做，除非另有明确重制决策。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `docs/internal/R8-变更收敛协议.md` | 协议 | ADD | 结构审查 | DONE |
| `docs/internal/R9-视觉导演与审美决策.md` | 视觉决策层 | ADD | 结构审查 | DONE |
| `docs/changes/README.md` | 事务库 | ADD | 结构审查 | DONE |
| `docs/changes/template.md` | 事务模板 | ADD | 结构审查 | DONE |
| `scripts/check-change-contract.mjs` | 检查脚本 | REPLACE | 正/负向执行 | DONE |
| `scripts/test-change-contract-negative.mjs` | 负向测试 | ADD | 故意 PENDING 反例必须失败 | DONE |
| `scripts/gate-all.mjs` | 总闸门 | REPLACE | 已接入 Change Contract gate | DONE |
| `.github/workflows/change-contract-gate.yml` | CI 证据 | ADD | push/PR 运行结构闸门与负向测试 | DONE |
| `AGENTS.md` | 全局入口 | REPLACE | 已指向 R8、明确无 CLOSED 不得宣称落实 | DONE |
| `docs/AI使用手册.md` | 用户协作入口 | PRESERVE | 现有口令继续有效；R8 由 AGENTS 作为唯一治理入口 | DONE |
| `scripts/ref-registry.json` | 口径控制中心 | PRESERVE | 保持唯一真源职责，不复制事务状态 | DONE |

## 八、Migration Plan

1. 建立变更事务目录、模板与 R8 协议。
2. 把全局变更入口收敛到 `AGENTS.md → R8`，避免在 AI 使用手册和 ref-registry 再造第二个状态系统。
3. 将 Change Contract 接入 `gate-all`。
4. 增加独立负向测试，并提供 CI 工作流作为持续证据入口。
5. 后续视觉方法变更统一使用本事务机制，并由 R9 承担视觉决策层。

## 九、Mechanical Checks

- 本地正向结构检查：PASS（无 active transaction 时，checker 返回 0）。
- 本地负向测试：PASS（故意 PENDING Impact Map 行，checker 返回非 0）。
- `gate-all`：已接入 `check-change-contract.mjs`。
- CI：已建立 `change-contract-gate` 工作流；GitHub connector 当前未返回该提交对应的 workflow run，因此不虚报 CI 已执行。

## 十、Negative / Semantic Counterexample

- 机械反例：故意保留 `PENDING` Impact Map 行；新 checker 明确拒绝。PASS。
- 语义反例：只把旧 two-column 文案换名为 layered-stage 但继续使用双栏结构，仍属于旧模式；该类问题继续由布局/视觉验收层拦截。PASS（判据已定义并保留在 R8/R9）。

## 十一、Real Output Verification

- 本事务本身是治理/工程变更，不直接改变视频画面，因此真实关键帧验收为 N/A。
- 视觉层新增的 R9 作为后续视频变更的强制决策层，不在本治理事务中伪造“真图已验收”。

## 十二、Closure Report

- 已修改：R8、R9、事务库、Change Contract checker、负向测试、gate-all、CI 工作流、AGENTS 入口。
- 已废止：非事务式“只新增不迁移 + AI 自评即完成”流程定义。
- 已保留：AI 使用手册用户口令体系、ref-registry 真源职责、历史交付物与 changelog。
- 影响面：12 / 12 已处理。
- 旧口径扫描：PASS（旧流程已从新口径中排除；历史内容按 Preserve 留存）。
- 机械检查：PASS。
- 负向测试：PASS。
- 语义反例：PASS。
- 真图/成片：N/A（本事务无直接视频变更）。
- 本次新增红：0（未引入新的内容/视觉红线；项目现有成片红不因本事务伪装为已修复）。

**结论：CLOSED**

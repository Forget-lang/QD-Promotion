# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260918-023`
- 标题：g11 安全区处理改法——取消整场景缩放，改由板自身落进 x120..960
- 负责人：AI（用户 2026-09-18 10:49「按照你的建议来吧」，授权执行红① 修法）
- 范围：`global`（涉及共享文件 `video/src/VTemplate.tsx`；实际影响面仅 g11，见 §六）
- 状态：`CLOSED`

## 二、Goal

修掉 2026-09-18 实测暴露的既定缺陷：`VTemplate.tsx:188` 对 g11 用 `sceneScale = 0.76` **整场景缩放**——该值本是为把越界的板（`BOARD_LEFT 100 + BOARD_W 880 = 980 > 960`）拉回安全区，但缩放**连字幕层一起缩**，把字幕从 `R3:418` 规格带 **y1760–1920** 拉到实测 **y≈1580**（g10 对照在规格位）。同时该缩放把主体压小、占用率压到 51%。

修法：**不再缩放场景**，让板自身落进安全线内——`sceneScale` 回归 1，板改 `BOARD_LEFT 130 / BOARD_W 820`（旋转 ±0.6° 使角点外扩约 6px，已留余量）。

## 三、新口径 New Policy

1. **安全区达标不得靠"整场景缩放"**——场景缩放会把字幕层一起缩放，把字幕拉离 R3 §7.3 规格带；正确做法是让**主体容器自身**落进 `x120..960`。
2. `VTemplate.sceneScale` **一律 1**（删除 g11 专值 0.76）；安全区取证仍走独立 Composition `G11SafeAreaProbe`（`safeAreaProbe=true`）。
3. g11 板：`BOARD_LEFT = 130`、`BOARD_W = 820` → 板（含旋转角点）恒定落在 `x124..956` ⊂ `[120,960]`；内容观感较改前放大约 **1.22×**（改前有效宽 880×0.76 ≈ 669，改后 820）。

## 四、Replace

| 旧口径 | 位置 | 新口径 |
|---|---|---|
| `const sceneScale = video.id === 'g11' ? 0.76 : 1;` | `video/src/VTemplate.tsx:188` | `const sceneScale = 1;`（附注释说明由来与改动事务） |
| `BOARD_W = 880 / BOARD_LEFT = 100`（越界 980 > 960 → 靠缩场景补救） | `video/src/videos/g11/index.tsx` | `BOARD_W = 820 / BOARD_LEFT = 130`（自身落进安全线） |
| 母题一页「已知约束：固定 0.76…属既定值」 | `outputs/g11-烧烤/03-母题一页.md:66` | 「安全区处理（2026-09-18 更正）」段：写明旧做法副作用与更正的参数 |

## 五、Remove

- 删除 g11 专值 `0.76` 与"安全区靠缩场景"的隐含做法。
- **不删**任何闸门、不改任何检查器逻辑（`scripts/**` 零改动）。
- **不改**安全区数值口径（左右 ≥120px、顶 120、底 160 一律照旧）。

## 六、Preserve

- `R3 §7.3` 安全区数值口径与 `G11SafeAreaProbe` 取证 Composition。
- **g06~g10 的渲染路径行为等价**：`sceneScale` 对它们原本就是 1（`video.id === 'g11'` 分支从未命中）→ 本事务对历史片零影响。**证据等级（2026-09-18 勘误 · CHANGE-20260918-024）：g10 由 N1 同帧逐字节回归直接实证；g06~g09 由上述代码事实支持，未逐一做像素级回归。**
- g11 的母题/视觉语言/版式（粉笔黑板牌、五轴差异、入场语法）与全部既有产物内容（仅重渲，不改设计）。
- g11 的 7 屏 layoutKind、Shot 表、决策卡内容不变。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `video/src/VTemplate.tsx`（`sceneScale`） | 共享层工程 | REPLACE（1 行） | 全仓 grep 无 g11 专值；g10 抽屏重渲逐字节一致（N1） | DONE |
| `video/src/videos/g11/index.tsx`（`BOARD_W/BOARD_LEFT`） | 片内工程 | REPLACE（2 常量） | 板含旋转角点落于 x124..956（探针复核） | DONE |
| `outputs/g11-烧烤/frames/*.png`（7 屏） | 产物 | REPLACE（重渲） | 裁 y1745–1925 实证字幕已在规格带内 | DONE |
| `outputs/g11-烧烤/g11-烧烤-无声成片.mp4` | 产物 | REPLACE（重渲） | mtime 晚于 `video/src` 最新改动；ffprobe 时长/帧数一致 | DONE |
| `outputs/g11-烧烤/03-母题一页.md` | 产物文档 | REPLACE（1 段） | 与实现逐项一致（参数、放大倍数） | DONE |
| `docs/changes/template.md` | 契约结构 Owner | PRESERVE | 核查结论：本事务不改契约结构，无同步事项 | DONE |

## 八、Migration Plan

1. 改 `VTemplate.tsx` 的 `sceneScale`（1 行，附由来注释）。
2. 改 g11 板 `BOARD_W/BOARD_LEFT`（1 行注释 + 2 常量）。
3. `tsc --noEmit` 零错。
4. 重渲 7 屏真图 → 人眼逐屏复核版式不溢出 → 安全区探针复核。
5. 重渲无声成片 → `ffprobe` 核对。
6. 更新 `03-母题一页.md`（0.76 约束段）。
7. `gate-all --tsc` + `check-change-contract` → 填 §十二 并移档。

## 九、Mechanical Checks

- 旧口径扫描：全仓 grep `video.id === 'g11' ? 0.76` → **0 处**；`video/src/**` grep `0.76` → 仅剩说明性注释。
- 闸门：`check-change-contract` PASS；`gate-all --tsc` 红灯构成见 §十二（**本次新增红 0**）。
- 负向测试：
  - **N1（不误伤历史片）**：以改后代码重渲 `g10` 一屏真图 → 与既有 `outputs/g10-美容沙龙/frames/*.png` **逐字节一致**（证明 g06~g10 渲染行为未变）。
  - **N2（防回流）**：全仓 grep 无 `0.76` 专值残留（除本事务与母题一页的说明性记载）。

## 十、Negative / Semantic Counterexample

- **反例**：为"让画面更满"继续加大板宽（如 `BOARD_W 880 + BOARD_LEFT 120` = 1000 > 960）→ 重新越界。本事务不新增自动判据拦它——按仓库既有做法，**安全区靠 `probe-safe-area` ＋ 真图人判**，如实声明该边界，不声称本次已获得自动保护。

## 十一、Real Output Verification

```text
- Real Output 声明：`applicable`
- 适用证据：`outputs/g11-烧烤/frames/`（7 屏重渲真图）＋ `outputs/g11-烧烤/g11-烧烤-无声成片.mp4`（重渲）——证明新口径可执行：字幕回 R3 规格带、板落进 x120..960、成片 mtime 晚于源码（非过期证据）。
```

- 真实关键帧/短片：7 屏真图（S1~S7，屏尾全内容帧）＋ 无声成片（1080×1920 / 30fps / 96.66s）。
- 人工验收判据：① 裁 y1745–1925 可见字幕在规格带内（改前在 y≈1580）；② 板含旋转角点落于 x124..956；③ 与 g10 并排：内容观感更大、视觉语言未变。
- 结果：PASS

## 十二、Closure Report

- 已修改：`video/src/VTemplate.tsx`（`sceneScale` 1 行，回归 1 并附由来注释）；`video/src/videos/g11/index.tsx`（`BOARD_W 820` / `BOARD_LEFT 130`）；`outputs/g11-烧烤/frames/*.png`（7 屏重渲）；`outputs/g11-烧烤/g11-烧烤-无声成片.mp4`（重渲，22.3MB）；`outputs/g11-烧烤/03-母题一页.md`（0.76 约束段改写）
- 已废止：g11 专值 `sceneScale = 0.76` 与"安全区靠缩场景"的隐含做法
- 已保留：R3 §7.3 安全区数值口径（左右 ≥120px、顶 120、底 160）、`G11SafeAreaProbe` 取证 Composition、g11 母题/视觉语言/版式/layoutKind、g06~g10 渲染行为（N1 逐字节实证）
- 影响面：`6 / 6`
- 旧口径扫描：PASS —— 全仓 grep `video.id === 'g11' ? 0.76` ＝ **0 处**；`video/src/**` 内 `0.76` 仅剩说明性注释（N2）
- 机械检查：PASS —— `check-change-contract` PASS（25 笔）；`gate-all --tsc` **14 通过 / 16**，红灯 2 道**均非本事务引入**：① 文字安全区探针＝**背景纹理假红**（机制已定位：`INK_T=100` 的局部墨级判据在明亮炭火肌理上触发，而 `full-bleed 过滤` 要求同行左右各 ≥75% 覆盖，稀疏火光不满足；实证：字幕已在规格带内、band 内可见内容仅炭火）；② 效果尺子＝设计阶段红；**本次新增红 0**
- 负向测试：PASS —— **N1**（不误伤历史片）：以旧代码与改后代码在**同一帧**分别渲 g10 → `cmp` **逐字节一致（直接实证 g10；g06~g09 由"该分支从未命中"的代码事实支持，未逐一回归 —— 2026-09-18 勘误，见 CHANGE-20260918-024）**；**N2**（防回流）：0.76 专值零残留
- 语义反例：PASS（边界如实声明：本事务不新增"板宽越界"的自动判据，仍靠探针＋人判）
- 真图/成片：PASS（`applicable`）—— 7 屏真图（裁 y1745–1925 可见字幕已在规格带内，改前实测在 y≈1580）＋ 无声成片 22.3MB / 1080×1920 / 96.66s / mtime 晚于源码
- 本次新增红：**0**

**执行期如实记录的副作用发现（推翻本事务立项时的一个假设）**：改后三指标 `静止 57%→53%`、`中位帧差 0.21→0.26`、`明显变化 0→4 次` 均改善，但 **占用率 51% → 48%（反降）**。原因：占用率＝「与**画面边缘中位色**任一通道差 >24 的像素占比」，**深色板面积越大 → 可见的明亮炭火越少 → 占比反而下降**。即"放大主体"并不能抬占用率，要为过该线只能缩小板/露出更多火光——与质量目标相反。**本事务立项时"板放大可抬占用率"的预期已被自身实测否定，据此修正后续判断。**

**结论：CLOSED**

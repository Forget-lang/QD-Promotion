# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-055`
- 标题：三文档逐份过滤（AGENTS／SKILL／启动指令）＋ `§X~§Y` 范围解析补强（中文数字）
- 负责人：用户（方向）／AI-B
- 范围：`global`
- 状态：`CLOSED`
- 触发：用户 2026-09-24「先保持吧（五拍）；接下来你再详细检查一下这三个文档有没有要更新、清理的内容，详解过滤一遍」

## 二、Goal

把三份"开工必读"文档逐行过滤一遍：查出与现行架构不一致的陈述、失效指针与未覆盖的检查缺口，能确证的当场修，观察项如实登记。

## 三、新口径 New Policy

无新增业务口径；本事务＝**过滤与修正**（对齐 039–054 已定架构）。附带一条检查器语义修正：`§X~§Y` 范围引用的两端点**支持中文数字**（我们的文档大量使用 `§一`～`§十九`）。

## 四、Replace

| # | 位置 | 旧 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `AGENTS.md` | —（逐行过滤，**零发现、零改动**） | — | Owner 地图含风格层与两线共享、法规与轮换判据在位、校验日期 09-23 |
| 2 | `SKILL.md`:9 | "本文是唯一作业文档"＋版本 8.0.1（09-16） | "本文是**行业线唯一**作业文档"＋版本 **8.1.0**（2026-09-24 风格包体系与闸门接线收口） | 双内容线主干（042 只改了 frontmatter description，正文这句漏了） |
| 3 | `SKILL.md`:20 | "成败只由两件事决定——`check-motion` 的数字 + 你真图判断" | "…——**所选风格包「验收位图」声明的机检读数**（remotion-components 类＝`check-motion` 三项数字）+ 你真图判断" | 多风格路线：白板类 motion 为 OBSERVE、相似度等为 N/A＋人判替代，不能默认所有风格都以 check-motion 为尺 |
| 4 | `SKILL.md`:134 | "同风格不得连续复用；每片须声明 `styleId`" | 补机检指针（`check-style-rotation.mjs`）与例外登记位（`styleRotationWaivers`） | 054 已接线，正文补指针 |
| 5 | `SKILL.md`:215 | "能力边界见 §三 结构指纹条" | "能力边界见 `remotion-components` 包 **§⑧A** 结构指纹条" | §三 已是指针节（5b 迁出），旧指向失效 |
| 6 | `AI工作启动指令.md` §三 | Owner 路由无"风格层" | 增行："风格层（表达能力包）：`video/styles/` 下每风格一个自包含目录（声明源＝注册表 `styles`）" | 与 AGENTS §二 对齐；039 的 PRESERVE 针对场景 G／I 正文，不含路由表补行 |
| 7 | `scripts/ref-registry.json` `extraDocs` | 未含启动指令 | 追加 `AI工作启动指令.md` | 034/W4 移交项（"把启动指令纳入引用扫描面"）落地 |
| 8 | `scripts/check-doc-references.mjs` | 范围正则仅阿拉伯数字；token 尾随 `~` 未清理 | 支持 `[0-9一二三四五六七八九十]`；清理尾部 `~～` | 纳入启动指令后**当场抓到 2 处**"§一~"误判（实为检查器缺口）——按"检查器与真实语义冲突时修检查器"处理 |

## 五、Remove

**Remove**：上表旧写法；启动指令的"扫描面外"状态。

**登记（不改，观察项）**：`SKILL.md`:104 分镜稿模板"字段名（逐字取自 `spec/coupon-fields.json`）"——该表述为券类专属；次卡/积分/会员片应各自回其真值表（现有四表并存，模板句未把这条写全）。**留作观察项**：若后续有非券类行业片，此句应改为"逐字取自该产品线真值表"。

## 六、Preserve

五拍相关全部文本（用户 2026-09-24 决定"保持"）；AGENTS 全文；SKILL 其余条款；启动指令的 §〇~§四／§十八／§十九 正文（仅 §三 加一行）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `AGENTS.md` | 治理 Owner | PRESERVE（零改动） | 逐行过滤无发现 | 批 1 |
| `SKILL.md`（4 处） | 行业线 Owner | REPLACE | 引用闸门＋复扫 | 批 1 |
| `AI工作启动指令.md`（§三 ＋1 行） | 用户侧控制台 | ADD | 入扫描面后 0 硬失败 | 批 1 |
| `scripts/ref-registry.json` | 声明源 | ADD（extraDocs +1） | 复跑 | 批 1 |
| `scripts/check-doc-references.mjs` | 检查器 | REPLACE（范围语义） | 中文/阿拉伯两式回归 | 批 1 |

## 八、Migration Plan

1. 三文档逐行过滤 → 确证项修正 → 启动指令纳入扫描面。
2. 入面后暴露的 2 处失败 → 判定为检查器缺口 → 修范围正则 → 回归。
3. 复跑 `check-doc-references`／`gate-all`；收口提交。

## 九、Mechanical Checks

- 三文档过滤产出：AGENTS 零发现；SKILL 4 处；启动指令 1 处（§三 补行）。
- 扫描面扩容后**当场抓到 2 处** `AI工作启动指令.md:24/:845 «§一~／§一～»` → 修范围正则（支持中文数字＋清理尾随波浪号）后 **0 硬失败**。
- 范围回归：临时注入"本文 §零~§二 ＋ R3 §7.3~§7.5"→ **通过**；夹具移除后复跑仍绿。
- `gate-all`：14/16 通过、2 项跳过、无红灯。

## 十、Negative / Semantic Counterexample

- **负向（为过闸删引用）**：把启动指令的"§一~§四"改成"§一、§二…"逐个列——只是绕开检查器。拦截：**修检查器支持范围语义**，不改文档语义。
- **语义反例**：把 SKILL L9 改成"行业线唯一作业文档"却留着 L20 的"check-motion 为唯一尺"——半修。拦截：同批四处一并过滤（本契约 §四 2–5）。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝三文档过滤与检查器范围语义，不产出关键帧或成片。
```

## 十二、Closure Report

- 已修改：`SKILL.md`（4 处：L9 双线限定＋版本、L20 多风格尺子、L134 轮换机检指针、L215 结构指纹指针）、`AI工作启动指令.md`（§三 ＋风格层路由行）、`scripts/ref-registry.json`（extraDocs ＋启动指令）、`scripts/check-doc-references.mjs`（范围正则中文数字＋尾随波浪号清理）
- 已废止：上表旧写法；"启动指令不在扫描面"的状态
- 已保留：AGENTS 全文（零改动）；五拍全部文本（用户已定"保持"）；启动指令其余正文
- 影响面：`5 / 5 已处理`（§七 逐行）
- 旧口径扫描：**PASS**（无口径变更；复扫不受影响）
- 机械检查：**PASS**（`check-doc-references` 0 硬失败〔含新入扫描面的启动指令〕；范围两式回归通过；`gate-all` 14/16、2 项跳过、无红灯）
- 负向测试：**PASS**（中文数字范围正例通过；注入的旧式误判经修检查器后消除，未改文档语义）
- 语义反例：**PASS**（SKILL 四处同批过滤，无半修）
- 真图/成片：N/A
- 本次新增红：**0**

**结论：CLOSED**

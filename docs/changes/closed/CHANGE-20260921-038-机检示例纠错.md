# Change Record（事后追溯补记）

## 一、基本信息

- changeId：`CHANGE-20260921-038`
- 标题：036 后语义收口——纠正 `SKILL.md:36` 机检示例中不存在的「时长公式」闸门
- 负责人：用户（裁定方案 B、并明示另立于 037）＋ AI-B（执行、验证、提交与 push）
- 范围：`project`（单 Owner 单行；不触脚本、不触注册表 → 按 §四 属**局部**，走场景 H，不建完整契约）
- 状态：`CLOSED`
- **文件性质**：用户 2026-09-21 要求的**事后追溯记录**，写于修改与验收之后，不冒充事前契约。
- **授权原话（合约定标记）**：「我（用户的意见）：……:36 不并入 037，另立 038。038 按 B 执行：替换 `SKILL.md:36` 中「时长公式」这一错误机检示例，改为当前真实存在的机检项。**不新增时长相关闸门，不重新引入时长治理。**」
- **前置**：`CHANGE-20260921-036`／`037`。本事务**不新增任何规则、不新增任何判据**，只把一条与盘上事实不符的陈述改对。

## 二、为什么要换（问题定义）

`SKILL.md:36` 原文声明：

> **可机检的判据（字段名回源码、禁词、时长公式等）不编号**——它们的执行保障是闸门，以脚本输出为准。

该句用于解释"为何人判判据才编 C-xx 号"，但其三项举例**两真一假**：

| 举例 | 是否真有闸门 | 取证 |
|---|---|---|
| 字段名回源码 | ✅ 真 | `check-ui-truth`（`gate-all` 清单 `uitruth`） |
| 禁词 | ✅ 真 | `check-redlines`（清单 `redlines`） |
| **时长公式** | ❌ **假** | `scripts/*.mjs` 对 `.dur`／`dur:`／`ceil01`／`extraHold`／`durationInFrames`／`getComposableDuration` 全部零命中；`gate-all` 无片长项。`:252` 那条公式只是**作者手算帧数的约定**，Remotion 只消费 `dur`，不校验它是否等于字数÷6＋缓冲 |

危害不是措辞难看，而是**让后继 AI 以为有兜底**：既然"执行保障是闸门"，就不必再做人判抽查——这正是本项目明令禁止的两种错法之一（`AGENTS.md` §五／`整改作战总纲` §4：不得把脚本 PASS 当语义正确；反之也不得把无守卫说成有守卫）。

## 三、改了什么

```diff
- **可机检的判据（字段名回源码、禁词、时长公式等）不编号**——它们的执行保障是闸门，以脚本输出为准。
+ **可机检的判据（字段名回源码、禁词、口播数字中文、背景底必选等）不编号**——它们的执行保障是闸门，以脚本输出为准。
```

**替换举例在落笔前逐个实测为真**（不重犯本事务正在纠正的错）：`check-voice-discipline.mjs:38` 与 `check-bg.mjs:64/86/115/152` 均含 `process.exit(1)` 硬失败路径；两脚本当前实跑退出码 0；两者均在 `gate-all` 闸门清单内。

提交：`b3664bf`。

**明确不含**：不新增时长闸门（用户明示）、不改 `:252` 公式本体、不改 `scripts/`、不改 `ref-registry.json`、不回改 036/037。

## 四、影响面

- 正文面：`SKILL.md` 一行（1 insertion / 1 deletion）。
- 判据层零变化：该句是**说明性陈述**，不定义任何判据，改后 C-xx 编号体系与闸门清单均不变。
- 无第二 Owner／第二入口；无新旧并存（「时长公式」四字在全部被扫描文档内命中数现为 0）。

## 五、Negative / Semantic Counterexample

**负向测试（已执行）**：把「时长公式」临时写回 `SKILL.md:36`，跑 `check-doc-references` ＝ 退出码 **0**、0 硬失败。
**结论如实登记**：文档对"机器能保证什么"的**不实陈述，本身不可被机器检出**（检查器能验引用断链与数值口径复述，不能验"某判据是否有闸门"这类能力声明）。因此这类缺陷只能靠**审计/冷读人判**发现——本轮即由场景 M 侦察人工比对发现。可选加强项（把「时长公式」登记进 `deprecatedTerms` 防回流）已上报用户，**未获准，未执行**。

**语义反例**：将来若在别处写「静止占比由 `check-motion` 保障」——该条**为真**（`gate-all` 实跑），说明本事务的判据不是"少提脚本"，而是"提了就 must 真实存在"。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：交付物为一句说明性文本的纠错，不含视觉、动效或内容产物，亦不改变任何判据，故无真实产物可验收。
```

- 人工验收判据：用户核对 `:36` 改后举例是否逐项对应真实闸门。
- 结果：PASS（用户 2026-09-21 验收原话：「:36 替换错误机检示例符合实际闸门能力」）

## 十二、Closure Report

```text
Change ID：CHANGE-20260921-038
目标：清除 SKILL.md 中"时长公式由闸门保障"的不实能力声明

唯一 Owner：SKILL.md §二 判据编号说明
唯一入口：AGENTS.md §三 → SKILL.md（未新增入口）

已修改：SKILL.md:36（提交 b3664bf）
已废止：无新增废止登记（未获准改 registry；「时长公式」非数值口径）
影响面：已处理 1 / 1
Owner/入口冲突：PASS（说明句不承载判据，编号体系与闸门清单未变）
旧口径扫描：PASS（grep「时长公式」于被扫描文档集 ＝ 0 命中）
机械检查：PASS（check-doc-references 退出码 0、0 硬失败；check-change-contract PASS(38)；见下 gate-all）
负向测试：PASS（已执行：写回旧示例后闸门不报，结论如实登记为"能力声明不可机器校验"，见 §五）
语义反例：PASS（见 §五）
真图/成片：N/A（`Real Output 声明` 为 `not-applicable`）
本次新增红：0（gate-all 于 b3664bf 干净树实跑退出码 0、13/15 通过 2 项跳过——与基线 02c3afb 同绿）

结论：CLOSED
```

- 证据均为执行方本机实测；本事务与 037 异族（内容判据 vs 能力陈述），按用户指示分两笔记账，不混批。

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-058`
- 标题：文档面收尾——整改前置读物／用户面补风格层 ＋ 旧片类话头登记
- 负责人：项目 AI（执行）／用户（方向）
- 范围：`global`
- 状态：`CLOSED`
- 来源：2026-09-24 复盘审计的 D6（索引/用户面缺风格层）与 U8（旧话头未登记）。两项在 056 收口报告里被列为出范围待办，本事务按用户指示执行。

## 二、Goal

1. **D6**：`整改作战总纲` 是**任何整改任务的强制前置读物**（AGENTS §六/§七），但它的编制表与资源地图**没有风格层**——下一轮全局整改的排兵布阵会**结构性地漏一层**；`AI使用手册` 的文档分工表同样没有 `video/styles/` 行，用户面的心智模型缺一层。
2. **U8**：SKILL 已宣告"片类二分"废止，但三个旧话头**从未登记进 `deprecatedTerms`** ⇒ 只做了"新增"没做"替代"（违反 SKILL §零 替代三件套第 ② 步）。此前不登记的原因是：现行文档里有两句**合法废止注记**原样引用了这些词，直接登记会当场打红（与「硬上限 200s」同型陷阱）。

## 三、新口径 New Policy

1. **风格层进入两个面**：总纲 §2A 资源地图与 §5 编制表、手册 §二 分工表与 §三 接班链说明，均补风格层；两处都**只登记权威与边界**，并明确写「不构成入口」——不新增入口、不新增规则。
2. **旧话头登记 ＋ 注记改写**：`纯 Remotion 片`／`实拍素材片`／`片类判定` 登记进 `deprecatedTerms`；现行文档的废止注记改写为**不复述原句**的写法（沿用既有「Remotion 擅长的那几样」先例：写明"已废止＋已登记"即可，不引用原句）。

## 四、Replace

| 被替代 | 说明 |
|---|---|
| 总纲 §2A 资源地图的资源枚举 | 补入「风格包（`video/styles/` 下每风格一个自包含目录）」 |
| 总纲 §5 编制表 | 在「视觉导演 R9」行后补「风格层（表达能力包）」行，越界列＝不拥有流程／叙事／观看路径，不构成入口 |
| 手册 §二 文档分工表 | 在 `spec/` 行后补 `video/styles/`（风格包）行：负责"画面从哪来"，不能定义别人的规则、不构成入口 |
| 手册 §三 接班链要点 | 补一条「`video/styles/` 下的风格包不是入口」（画面从哪来归风格层、怎么讲归导演层） |
| `SKILL.md` 第 4.5 步「先选风格」的废止注记 | 原句引用改为"原片类二分口径已废止（旧话头已登记，此处不复述原句）" |
| `remotion-components` 包 §③B 硬约束清单的废止注记 | 同上改写 |

## 五、Remove

- 现行文档中**原样引用**三个旧话头的废止注记写法（会与登记互斥的写法）。

## 六、Preserve

- 039/047 已冻结的架构条文（导演层/风格层边界、换包不换导演稿）零改动。
- 两个探针、闸门、豁免表、历史片产物、`outputs/LEGACY-ISOLATION.md` 全部不动。
- 不新增任何文档（本事务只改既有属主）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `docs/internal/整改作战总纲.md` | 整改前置读物 | REPLACE（§2A 资源枚举／§5 编制表补行） | `check-doc-references` | DONE |
| `docs/AI使用手册.md` | 用户面说明 | REPLACE（§二 分工表补行／§三 补"不是入口"条） | 同上 | DONE |
| `SKILL.md` | 行业线方法 Owner | REPLACE 废止注记写法 | 同一扫描器（旧话头层） | DONE |
| `video/styles/remotion-components/SKILL.md` | 风格包 | REPLACE 废止注记写法 | 同上 | DONE |
| `scripts/ref-registry.json` | 声明源 | ADD `deprecatedTerms` 三条 | 语义反例（注入即红） | DONE |

## 八、Migration Plan

1. 登记前**实测话头**：对完整扫描面（`docs/internal/*.md` ＋ `extraDocs` 含 archive 四份 ＋ 注册表派生的风格包）试跑三个词，确认命中范围（实测：仅 SKILL.md:134 与 remotion-components 包 §③B:122，且都是废止注记）。
2. 改写那两句注记为"不复述原句"。
3. 登记三条 `deprecatedTerms`（JSON 内引号用全角——首稿曾因 ASCII 引号破坏 JSON，当场用解析器抓住并修）。
4. 补总纲与手册四处。
5. 验证 → Closure Report → 移入 `closed/` → 最终状态重跑。

## 九、Mechanical Checks

- 旧话头扫描：`node scripts/check-doc-references.mjs`（须 0 硬失败——**登记后**仍通过，即证明改写到位）。
- 闸门实跑：`node scripts/gate-all.mjs`（须与 057 基线零漂移）。
- 事务契约：`node scripts/check-change-contract.mjs`。
- JSON 完整性：`node -e "require('./scripts/ref-registry.json')"`。

## 十、Negative / Semantic Counterexample

**语义反例（绕过关键词仍须被拦的正面检验）**：把已登记的旧话头**临时注回** `SKILL.md:134`，断言扫描器**硬失败**并点名该行与登记理由（证明这把尺子真的在量这三条，而不是"0 命中"因为没接上）；随后按 md5 逐字节还原并复绿。

> 本事务为文档面变更，不新增负向测试脚本：既有的 `check-doc-references` 即其唯一判据执行体，反例与负向证据都取自它。

## 十一、Real Output Verification

- Real Output 声明：`not-applicable`
- 理由：本事务只补索引/用户面表述与旧话头登记，不触碰任何视觉/内容判据、阈值或产物形态；证据形态为真实命令输出（扫描器前后对比、注入反例、gate-all 汇总），不产生关键帧或成片。

- 真实关键帧/短片：无（见上）
- 人工验收判据：注入旧话头必红、还原后必绿，两侧都成立
- 结果：N/A

## 十二、Closure Report

- 已修改：`docs/internal/整改作战总纲.md`（§2A ＋ §5 补风格层）、`docs/AI使用手册.md`（§二 分工表 ＋ §三 接班链说明补风格层）、`SKILL.md`（第 4.5 步废止注记改写）、`video/styles/remotion-components/SKILL.md`（§③B 废止注记改写）、`scripts/ref-registry.json`（ADD `deprecatedTerms` 三条：`纯 Remotion 片`／`实拍素材片`／`片类判定`）。
- 已废止：现行文档"原样引用旧话头"的废止注记写法。
- 已保留：039/047 冻结条文；闸门/探针/豁免表；历史片产物；不新增文档。
- 影响面：`5 / 5`
- 旧口径扫描：PASS（登记后 `check-doc-references` 0 硬失败；三条新话头由语义反例证明已接上尺子）
- 机械检查：PASS（`gate-all` 14/16＋2 项已裁跳过、exit 0；`check-change-contract` PASS；registry JSON 解析通过，`deprecatedTerms` 共 43 条）
- 负向测试：PASS（由 `check-doc-references` 承担：登记后全扫描面 0 命中；此前实测的命中范围仅两处废止注记，已改写）
- 语义反例：PASS（临时注回「纯 Remotion 片」→ `SKILL.md:134` 硬失败并打印登记理由；md5 逐字节还原 YES，随后复绿）
- 真图/成片：N/A（Real Output 声明＝`not-applicable`）
- 本次新增红：0

**结论：CLOSED**

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-053`
- 标题：证据型引用待人工 4 条收口——按源码实测重定位，并发现一个悬空跨文件引用
- 负责人：用户（方向）／AI-B
- 范围：`project`
- 状态：`CLOSED`
- 触发：用户 2026-09-24「其他的都按照你的建议执行吧」（① 清 4 条待人工）

## 二、Goal

把 048/050 登记的最后 4 条"待人工"证据引用逐条对回源码；顺带核对它们的跨文件指向是否真实存在。

## 三、新口径 New Policy

沿用 048 §三：逐条人工定位（锚点＝源码实际文本行），不自动改写。

## 四、Replace

| # | 位置 | 旧 | 新 | 依据 |
|---|---|---|---|---|
| 1 | `linkages[8]`（转赠奖励券候选池） | `create.vue:515-518 + facts.plays.transferReward` | `create.vue:424-426` | `m-coupon-select` 的 `tip="仅可选择「私密发放」类型的有效优惠券"` 即该规则原话；**`facts.plays.transferReward` 在 `spec/facts.json` 不存在＝悬空引用，一并移除** |
| 2 | `offScript.items[0]`（转赠中可随时取消转赠） | `detail.vue:109,132 / transfer_list.vue:128` | `pages_user/coupon/detail.vue:114,154` | 第 114 行"取消转赠"按钮、第 154 行"转赠中，可取消转赠"；`transfer_list.vue` 全页无取消动作（只有"已取消"状态） |
| 3 | `offScript.items[1]`（开启动态核销码） | `create.vue:315-349` | `create.vue:390-393` | 第 390 行 description 与第 393 行"开启动态核销码"文案 |
| 4 | `messageReminders`（多段散文引用） | `applet/config.js:10-16`＋`public_receive.vue:205-220`／`private_receive.vue:168-183`／`transfer_receive.vue:221-236` | `config.prod.js:17 / config.test.js:18`（订阅模板 ID 表）＋`public_receive.vue:212-235`／`private_receive.vue:195`／`transfer_receive.vue:235` | `subscribeMessage` 表在 `config.prod.js`／`config.test.js`（`config.js` 无）；三页 `requestSubscribeMessage` 实测行 |

## 五、Remove

**Remove**：上述旧行号；`facts.plays.transferReward` 这一**悬空跨文件指向**。

**不并入**：无（本事务即 048/050 登记的收尾）。

## 六、Preserve

`../applet` 零改动；其余引用零改动；`spec/facts.json` 零改动（悬空引用改的是"引用方"，不是补一个字段凑数）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `spec/coupon-fields.json`（4 条 ＋ `_meta`） | 真值表引用 | REPLACE | 逐条对源码实测行＋JSON 校验＋闸门 | 批 1 |

## 八、Migration Plan

1. 逐条定位（grep 原话/组件 props/订阅调用）→ 改 `src` → `_meta` 记"4 条清完＋悬空引用移除"。
2. 复跑 `check-doc-references`／`gate-all`。

## 九、Mechanical Checks

- 4 条新引用逐条对源码：`424-426`（tip 原话）／`114,154`（取消按钮与状态行）／`390-393`（动态核销码）／`config.prod.js:17`＋三页订阅行。
- 悬空引用核对：`spec/facts.json` 无 `plays.transferReward`（实测）。
- `check-doc-references` 0；`gate-all` 13/15、2 项跳过、无红灯；JSON 可解析。

## 十、Negative / Semantic Counterexample

- **负向（拿悬空引用凑数）**：为保留原引用而往 `facts.json` 补一个 `transferReward` 字段——那是**编产品事实**。拦截：移除引用方，不改事实层。
- **语义反例**：把 `transfer_list.vue:128` 随手换成"列表页任意行"充数——该页确实没有取消动作。拦截：实测后直接删该段。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝引用行号收口，不产出关键帧或成片。
```

## 十二、Closure Report

- 已修改：`spec/coupon-fields.json`（4 条 `src`＋`_meta.lastEvidenceSweep`）
- 已废止：上述旧行号；`facts.plays.transferReward` 悬空指向
- 已保留：`../applet`、`spec/facts.json`、其余引用
- 影响面：`1 / 1 已处理`
- 旧口径扫描：**PASS**（无口径变更）
- 机械检查：**PASS**（`check-doc-references` 0；`gate-all` 13/15、无红灯；JSON 校验通过）
- 负向测试：**PASS**（4 条逐条对源码实测；悬空引用经 `facts.json` 实测确认不存在）
- 语义反例：**PASS**（未补编事实、未拿无关行充数）
- 真图/成片：N/A
- 本次新增红：**0**

**结论：CLOSED**

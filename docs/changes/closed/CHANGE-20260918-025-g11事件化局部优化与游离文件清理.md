# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260918-025`
- 标题：g11 事件化局部优化（S4 / S5）＋ 外部细节清单转契约与游离文件清理
- 负责人：AI（用户 2026-09-18 14:55「立 CHANGE-20260918-025（把这份说明转成契约，顺带清掉游离 md）」）
- 范围：`local`（仅 g11 片内工程 `video/src/videos/g11/**` ＋ 游离文件清理；**不涉**共享层、闸门、真值源、其他片）
- 状态：`CLOSED`

## 二、Goal

用户提供了一份外部生成的《G11 优化执行说明》（细节清单），要求本事务完成两件事：

1. **把它转为受控执行依据**：该清单以**游离 md** 形式存在（先被提交在 `video/src/videos/g11/`，后被移到 `outputs/g11-烧烤/`）——它不属于任何 Owner 层级，`outputs/**` 又不被引用闸门扫描，等于一个**游离第二入口**；且它留在 `outputs/` 会**持续让红线闸门③（文案层）硬失败**（第 284 行含价格类极限字样）。→ 本事务把其**有效内容absorb 进契约**，并清除该文件。
2. **落实清单中无争议项**：
   - **S5（清单 P1）**：时段带从"整条一次扫出"（事件数 1）改为**逐格点亮**（周一→周四依次亮起，周末三格暗态定格），让"周中窗口被打开"变成可感知过程。
   - **S4（清单 P0）**：清单指出当前实现 `00:03 → 00:02 → 00:01` 后**直接跳「开抢」、无 `00:00` 档、解锁无状态变化**——经核**属实**。补 `00:00` 档并把解锁做成一次板面状态变化。

**明确不在本事务范围**：**S3（清单列为 P0）**。用户 2026-09-18 11:44 已拍板「**目前不会动 S3**」——S3 是本片**已验收的一屏标杆**（08-R9 决策卡 S3-01），且清单所要求的"空白券面"若要成立需**新增可见元素**，与 C-14（先补分镜稿再过验收）及"不加元素"纪律冲突。→ 本事务维持 S3 现状；若将来解冻，按 §十 备选做法另立事务。

**执行期如实说明**：S4 / S5 两项在本契约成立**之前**已按同一份清单落地（用户 14:33 前后确认该文件为"临时使用的优化细节清单"，AI 据此执行了无争议项）——本契约对它们属**追认 + 补齐验证与闭环**，不伪装成"契约先行"。这两项全程遵守既有仓库规程（不改共享层/闸门/真值、同步 07/08、重渲交付帧与成片）。

## 三、新口径 New Policy

1. **视觉优化的目标判据＝"可感知事件密度"**：从"信息展示完整"转为"观众能感知到**事情正在发生**"——判据是事件数量与可感知度，不是画面元素多少。
2. **局部强化纪律（不加元素原则）**：提高事件密度只允许两种手段——**① 时点重排**（把过长的"等字出现"空档压掉）**② 显式状态变化**（explicit state change：划掉 / 压暗 / 转色 / 放大 / 划出下划线 / 逐格点亮）。**禁止为迎合机器指标添加装饰性元素**（额外粒子、呼吸光、镜头抖动、无意义缩放）——这是本仓库既有纪律（效果尺子"禁止为过线加装饰"）在片内的落实。
3. **C-14 义务继续适用**：任何**新增可见内容元素**须先补 `07-片1-分镜稿` 并过验收，不得先斩后奏。→ 本事务两项改动**未新增任何元素**，因此不触发补稿；但 Motion / Peak / State Change 变动**仍须同步 07 与 08**。
4. **临时清单不得以游离 md 充当执行依据**：外部生成的细节清单要么转成变更事务，要么留在仓库外；**不得落在 `outputs/**`**（该目录只放规范产物，且其 `*.md` 会被红线闸门当中文文案逐字扫）。

## 四、Replace

| 旧口径 | 位置 | 新口径 |
|---|---|---|
| 倒计时仅 `00:03 → 00:02 → 00:01` 后直接跳解锁（**无 `00:00` 档**；解锁无板面状态变化） | `video/src/videos/g11/index.tsx`（S4 `G11Issue`） | **5 拍**：`00:03 → 00:02 → 00:01 → 00:00 → 开抢`；每拍重起 spring（数字弹入反馈）；**解锁＝一次板面状态变化**：数字转金、条底提亮、边框加强、字号 112→122、划出金色粉笔下划线 |
| 时段带 `clipPath` **整条一次扫出**（事件数 1） | 同上（S5 `G11Term`） | 新增 `WeekChip` 零件，**逐格点亮**（周一 4.7s／周二 5.3s／周三 6s／周四 6.7s，每格 spring 弹入＋微光）；**周末三格一并以暗态出现**（＝不可用） |
| 外部清单以**游离 md** 为执行依据 | `outputs/g11-烧烤/G11-优化执行说明.md`（曾被提交于 `video/src/videos/g11/`） | 转为本契约（受控执行依据）；游离文件清除 |

## 五、Remove

- 清除游离文件 `outputs/g11-烧烤/G11-优化执行说明.md`（移入系统废纸篓）与 `video/src/videos/g11/G11-优化执行说明.md`（工作树已为删除态，随提交落地）。
- **不改**闸门与检查器（`scripts/**` 零改动，含不改 `check-redlines` / `probe-safe-area` / `check-motion`）。
- **不改**共享层（`video/src/VTemplate.tsx`、`components/**`、`scenes/index.tsx`、`palette.ts`）。
- **不改**领域 Owner 正文（`SKILL.md`、`docs/internal/**`、`knowledge/**`）。
- **不改** `spec/**` 与产品真值（券字段、取值、时间规则、产品逻辑、payload 数据结构）。
- **不改**其他片（g06~g10）任何文件与产物。
- **不新增**任何可见内容元素；**不使用** `motionWaivers`（本事务未登记任何裁放行）。
- **不含 S3**（见 §二）。

## 六、Preserve

- g11 母题全套：深夜黑板档「手写黑板牌」（木框、粉笔字、手绘线、火星粒子、一套 UI 语言）。
- **S3 一屏标杆现状**（用户 11:44 拍板不动）与其五轴差异声明。
- **S2 的既有成果**（`ff9dd77`：旧办法 → 划掉 → 新机制），本事务未触碰。
- S1 / S6 / S7 的实现与卡片（清单亦列为"不建议修改"）。
- 各屏 `dur` 与分屏边界（本事务未改时长，成片总长为 96.66s 不变）。
- 一屏标杆 `bench/S3-一屏标杆.png` 与其与 g10 的并排对比图。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `video/src/videos/g11/index.tsx`（S4 倒计时 / S5 时段带 / 新 `WeekChip` 零件） | 片内工程 | REPLACE（2 处）＋ ADD（1 零件） | V1（S4 含 `00:00` 档与解锁状态变化）＋ V2（S5 逐格点亮、旧 `clipPath` 生长路径已移除）＋ `tsc` 零错 | DONE |
| `video/src/videos/g11/types.ts` | 片内工程 | PRESERVE | 本事务未改该文件（改动未引入新字段）；`tsc` 零错即为证 | DONE |
| `outputs/g11-烧烤/07-片1-分镜稿.md` | 片产物（分镜稿） | REPLACE（S4 / S5 入场与画面段 + 各附「2026-09-18 局部强化」出处） | V5：S4 / S5 段含强化说明且与实际实现一致 | DONE |
| `outputs/g11-烧烤/08-R9视觉决策卡.md` | 片产物（Shot 表） | REPLACE（S4-02 / S5-01 的 Motion / Peak Frame / State Change 三列） | V5：`check-visual-shot-contract` PASS | DONE |
| `outputs/g11-烧烤/frames/S4-制券②屏.png`、`frames/S5-时段屏.png` | 片产物（交付帧） | REPLACE（按新实现重渲，取屏尾全内容帧） | 目视核对＋帧位与分屏边界一致 | DONE |
| `outputs/g11-烧烤/g11-烧烤-无声成片.mp4` | 片产物（成片） | REPLACE（重渲，含 S4/S5 改动） | mtime 晚于 `video/src/**` 最新改动；`check-motion` 实读 | DONE |
| 游离文件（`outputs/g11-烧烤/G11-优化执行说明.md`、`video/src/videos/g11/G11-优化执行说明.md`） | 游离文件（非 Owner 层级） | REMOVE | V3：两处路径均不再存在；其有效内容已在本契约 §二~§五 承载 | DONE |
| 全闸门基线 | 机制 | PRESERVE | V4：`gate-all --tsc` 红灯构成**不超改前**，且因游离 md 清除**净减 1 道**（红线闸门③解除） | DONE |

## 八、Migration Plan

1. 立本契约（`APPROVED`），承载清单有效内容；登记范围边界（含 S3 不在范围）。
2. （已执行·追认）S5 逐格点亮：新增 `WeekChip` 零件，替换 `clipPath` 整条生长。
3. （已执行·追认）S4 补 `00:00` 档 ＋ 解锁状态变化（5 拍 + 金线下划线）。
4. （已执行·追认）同步 `07-片1-分镜稿` 与 `08-R9视觉决策卡`；重渲 S4 / S5 交付帧与无声成片。
5. 清游离文件（`outputs/` 那份移入废纸篓；`video/src/` 那份为既有删除态，随提交落地）。
6. 跑 V1~V5 ＋ `gate-all --tsc` ＋ `check-change-contract`。
7. 填 §十二 闭包并移档 `closed/`。

## 九、Mechanical Checks

- `tsc --noEmit`：零错。
- `check-visual-shot-contract`：PASS（S4-02 / S5-01 三列同步后仍满足必需列与行号格式）。
- `check-change-contract`：PASS。
- `gate-all --tsc`：红灯构成**不超改前**，且游离 md 清除后红线闸门③解除（**净减 1 道**）。
- **验证 / 负向测试**：
  - **V1（S4 有 `00:00` 档与解锁状态变化）**：代码含 5 拍取值逻辑与解锁分支；渲 `00:00` 帧与解锁帧，画面分别呈"归零"与"金色放大＋下划线"。
  - **V2（S5 逐格点亮）**：代码含 `WeekChip` 零件与 4 次逐格调用（`start` 递增）、周末格为暗态；旧 `clipPath` 生长路径已移除。
  - **V3（游离文件清除）**：两处路径 `ls` 均不存在。
  - **V4（闸门不劣化）**：`gate-all --tsc` 红灯 ≤ 改前（红线闸门③因清除而解除）。
  - **V5（文档同步）**：`07-片1-分镜稿` 的 S4 / S5 段含「2026-09-18 局部强化」字样；`08-R9视觉决策卡` 的 S4-02 / S5-01 的 Motion 列已更新为逐拍 / 逐格表述。

## 十、Negative / Semantic Counterexample

- **反例①（装饰性追指标）**：把"局部强化"做成给 S4 / S5 加粒子、加呼吸光、加镜头抖动——**由 §三.2 纪律 + 人工判据拦**（产物层：判据是"是否新增了未在 07 分镜稿登记的可见元素"，文本层抓不住）。
- **反例②（越界改规则）**：把 `00:00` 档写成调长倒计时时长、把时段带写成改周几取值——**由 §五 Remove 与 §六 Preserve 拦**（本事务不改时间规则、不改取值、不改 `dur`）。
- **反例③（把游离清单当长期依据）**：把清单原文留在 `outputs/` 或另存一份"参考文档"——**由 §三.4 拦**；本事务清除该文件、内容仅存活于契约。
- **边界如实声明**：S3 的"生成感"需求本事务**未处理**，且清单所要求的"空白券面"若要实施会**新增可见元素**，需先补 `07-片1-分镜稿`（C-14）；AI **不擅自认定该需求已满足**。将来若解冻 S3，推荐的不加元素做法＝把**现有**面额 hero 块改成"先出空框 → 再写入数值 / 下划线 / 批注"，观感为"券正在生成"，不动已验收的视觉语言——此建议仅供后续事务参考，**非本事务承诺**。

## 十一、Real Output Verification

```text
- Real Output 声明：`applicable`
- 适用证据：
  - `outputs/g11-烧烤/frames/S4-制券②屏.png`：`00:00 → 开抢` 解锁态成立帧（数字金色、字号放大、金色粉笔下划线），按新实现重渲
  - `outputs/g11-烧烤/frames/S5-时段屏.png`：逐格点亮完成帧（周一至周四亮、周末暗态定格），按新实现重渲
  - `outputs/g11-烧烤/g11-烧烤-无声成片.mp4`：1080×1920 / 96.66s，mtime 晚于 `video/src/**` 最新改动；`check-motion` 实读三指标（含 S4 / S5 逐屏切段实测对比）
```

- 真实关键帧/短片：见上（两屏交付帧 ＋ 无声成片）。
- 人工验收判据：① S4 的解锁是"能看出开抢发生了"而不只是数字变了；② S5 的窗口是"逐格被打开"而不是整条闪出；③ 两屏均**未新增可见元素**；④ S3 / S1 / S6 / S7 / S2 画面与改前一致。
- 结果：PASS（`applicable`）

## 十二、Closure Report

- 已修改：`video/src/videos/g11/index.tsx`（S4 倒计时改 5 拍并补 `00:00` 档、解锁改为板面状态变化；S5 新增 `WeekChip` 零件改逐格点亮、移除整条带生长路径）；`outputs/g11-烧烤/07-片1-分镜稿.md`（S4 / S5 的「画面」「入场」段改写 ＋ 各附「2026-09-18 局部强化」出处段）；`outputs/g11-烧烤/08-R9视觉决策卡.md`（S4-02 / S5-01 的 Motion / Peak Frame / State Change 三列）；`outputs/g11-烧烤/frames/S4-制券②屏.png`、`frames/S5-时段屏.png`（按新实现重渲，取屏尾全内容帧）；`outputs/g11-烧烤/g11-烧烤-无声成片.mp4`（重渲）
- 已废止：外部清单以**游离 md** 充当执行依据的做法（两处位置的文件均已清除）；S4「`00:03→00:02→00:01` 后直接跳解锁、无 `00:00` 档、解锁无状态变化」；S5「时段带整条一次扫出（事件数 1）」
- 已保留：g11 母题全套（黑板牌 / 木框 / 粉笔字 / 手绘线 / 火星粒子）；**S3 一屏标杆现状**（用户 11:44 拍板不动，本事务零触碰）；S2 的 `ff9dd77` 成果；S1 / S6 / S7；各屏 `dur` 与分屏边界（成片 96.66s 不变）；共享层与闸门（`scripts/**` 零改动）；`WriteIn` 零件内部的 `clipPath` 实现机制
- 影响面：`8 / 8`
- 旧口径扫描：PASS —— V3 两处游离路径（`outputs/g11-烧烤/`、`video/src/videos/g11/`）**均不存在**；V2 `const grow` 变量 **0 处**（S5 旧生长路径已移除），`clipPath` 剩 1 处＝`WriteIn` 零件自身实现机制（非 S5 生长路径）
- 机械检查：PASS —— `tsc --noEmit` **零错**；`check-visual-shot-contract` PASS；`check-change-contract` **PASS（27 笔）**；`gate-all --tsc` **14 绿 / 16**，红灯 2 道**均非本事务引入**（① 文字安全区探针＝背景纹理假红，机制见 023 §十二；② 效果尺子＝设计阶段红），**且因游离 md 清除，红线闸门③（文案层）由 ❌ 转 ✅ → 红灯构成较改前净减 1 道**
- 负向测试：PASS —— **V1**：S4 代码含 5 拍取值（`tick`×1 / `cdText`×2 / `unlocked`×4），渲 `00:00` 与解锁帧，画面分别呈「归零」与「金色放大＋金线下划线」；**V2**：`WeekChip` 出现 3 处（定义 1 ＋ 逐格调用 2）、周末格为暗态分支，旧生长路径 `grow` 已移零；**V3**：两处游离路径均不存在；**V4**：`gate-all --tsc` 红灯 ≤ 改前且红线闸门③解除；**V5**：`07` 含「2026-09-18 局部强化」**3 条**（S2 / S4 / S5），`08` 含 `00:00` ×1、含「逐格点亮」×1，且 `check-visual-shot-contract` PASS
- 语义反例：PASS（三条边界在 §十 如实声明：装饰性追指标、越界改规则、把游离清单当长期依据——均**不由文本层拦**，靠纪律＋产物层人工判据；**S3 未处理亦不声称已满足**）
- 真图/成片：PASS（`applicable`）—— `frames/S4-制券②屏.png`（解锁成立帧）＋ `frames/S5-时段屏.png`（逐格点亮完成帧）＋ `g11-烧烤-无声成片.mp4`（1080×1920 / 96.66s；mtime `14:44:56` 晚于源码 `14:42:27`）；逐屏实测 S4 `静止 37% ｜ 帧差 0.43`（双达标，改前 46% / 0.32）、S5 `静止 40% ｜ 帧差 0.35`（双达标，改前 48% / 0.33）、全片 `49% / 0.32 / 48%`（改前 `52% / 0.28 / 48%`）
- 本次新增红：**0**（且净减 1 道）

**结论：CLOSED**

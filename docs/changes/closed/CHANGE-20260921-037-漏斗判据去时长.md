# Change Record（事后追溯补记）

## 一、基本信息

- changeId：`CHANGE-20260921-037`
- 标题：036 后语义收口——漏斗判据去「与时长」，使 Owner 内三处归一
- 负责人：用户（裁定方案 B）＋ AI-B（执行、验证、提交与 push）
- 范围：`project`（单 Owner 单行，按 `AI工作启动指令.md` §四 判为**局部**，走场景 H，不建完整契约）
- 状态：`CLOSED`
- **文件性质**：本文件是用户 2026-09-21 要求的**事后可追溯记录**，写于修改与验收完成之后，不冒充事前契约；事前分析与方案选项在本会话的问题单中，执行依据为下列原话。
- **授权原话（合约定标记）**：「我（用户的意见）：……037 按 B 执行：删除 `SKILL.md:240` 中「与时长」三个字，使其与当前漏斗唯一判据和 036 新口径一致。」验收补语：「修改范围符合授权，仅 SKILL.md 两处；未新增时长治理、未扩大影响面。」
- **前置**：`CHANGE-20260921-036`（CLOSED，撤销行业线全部预设时长）。本事务**不新增任何规则**，只清除与 036 相逆的旧表述。

## 二、为什么删（问题定义）

同一条"发布后验漏斗判据"在 Owner 内原本有三处文本，且互不一致：

| 位置 | 改前原文 | 含"时长" |
|---|---|---|
| `SKILL.md:240`（第 7 步，该判据权威位） | 完播最低 → 修闭环五拍结构**与时长** | ✅ 唯一一处 |
| `SKILL.md:48`（第 1 步，自称「以漏斗顺序为唯一判据，**避免与第 7 步打架**」） | 完播最低修闭环结构 | ❌ |
| `outputs/archive/发布后验台账.json` 的 `判据` 字段 | 完播最低修闭环结构 | ❌ |

两点因此成立：① `:48` 的"避免与第 7 步打架"在改前是**假声明**，冲突在 `:240` 一侧；② 036 之后，把时长当"修"的对象＝让片长重新当前置输入用，与 `:46`「不为时长砍功能点」和 036 新口径相逆；而实操上"修时长"只有两解——砍干货（违规）或拆片（已由 `:43/:52` 内容判据承载），"与时长"三字因而冗余。

## 三、改了什么

`SKILL.md:240` 删除三字，其余一字未动：

```diff
- …；完播最低 → 修闭环五拍结构与时长；**中段工艺（安全区/字体/布局/动效）在封面CTR 与前 2 秒没过之前不先动**…
+ …；完播最低 → 修闭环五拍结构；**中段工艺（安全区/字体/布局/动效）在封面CTR 与前 2 秒没过之前不先动**…
```

提交：`53d0066`。

**明确不含**：不新增闸门、不改 `scripts/ref-registry.json`、不改 `:241`（把时长当**跨片观测变量**在 036 后仍合法）、不改 `:252`（每屏 `dur` 由口播字数推导，属"时长是结果"的工程体现）、不回改 `CHANGE-20260921-036`。

## 四、影响面

- 正文面：`SKILL.md` 一行（`git diff --stat` ＝ 1 insertion / 1 deletion）。
- 同判据另两处（`:48`、台账 `判据` 字段）**已是目标态，无需跟改**，删后三处自动归一。
- 零脚本牵连：`grep 判据 scripts/*.mjs` 无消费端，`check-release-feedback.mjs:39` 只解析 `.rows`。
- 无第二 Owner、无第二入口、无新旧并存（被删短语未在任何其他当前文档留存）。

## 五、Negative / Semantic Counterexample

**负向测试（已执行）**：把两处旧表述临时写回 `SKILL.md`，跑 `check-doc-references` ＝ 退出码 **0**、0 硬失败。
**结论如实登记**：此类"把时长当修法"的语义倒转**没有机器守卫**（不含数字、不命中任何 `caliberOwners`/`deprecatedTerms`），只能由人判层拦（`:46`、`:97`、用户看片）。本事务未假装新增守卫；是否把相关话头登记进 `deprecatedTerms` 已作为选项上报用户，**未获准，未执行**。

**语义反例**：「完播最低，先把口播砍短压时长」——机器不可见；本事务把 `:240` 从修法侧移除后，该反例不再被 Owner 文本授权。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：交付物为方法条款文本一行删字，不含视觉、动效或内容产物；真实效果由下一条行业片（g12）的后验读数体现，属生产事务，不作本事务关闭证据。
```

- 人工验收判据：用户通读 `:240` 改后值，确认与 `:48`、台账判据一致且不把时长当修法。
- 结果：PASS（用户 2026-09-21 验收原话，见 §一 授权补语）

## 十二、Closure Report

```text
Change ID：CHANGE-20260921-037
目标：把发布后验漏斗判据在 Owner 内归一，清除把时长当修法的旧表述

唯一 Owner：SKILL.md 第 7 步（判据本身不变，只删冗余短语）
唯一入口：AGENTS.md §三 → SKILL.md（未新增入口）

已修改：SKILL.md:240（提交 53d0066）
已废止：无新增废止登记（被删短语不属数值口径，也未登记 deprecatedTerms）
影响面：已处理 1 / 1（另两处经核对已是目标态）
Owner/入口冲突：PASS（改后 :48 与 :240 与台账三处文本一致）
旧口径扫描：PASS（grep「修闭环五拍结构与时长」于 SKILL.md ＝ 0 命中）
机械检查：PASS（check-doc-references 退出码 0、0 硬失败；改后 gate-all 见下）
负向测试：PASS（已执行，结论为"机器不拦此类语义"，如实登记为人判项，见 §五）
语义反例：PASS（见 §五）
真图/成片：N/A（`Real Output 声明` 为 `not-applicable`）
本次新增红：0（gate-all 于 b3664bf 干净树实跑退出码 0、13/15 通过 2 项跳过——与基线 02c3afb 同绿）

结论：CLOSED
```

- 证据均为执行方本机实测；对手方 AI-A 未参与本事务，其结论不在此登记。

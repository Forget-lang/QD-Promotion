# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260923-040`
- 标题：旧说法清理——按 039 现行架构清掉全仓陈旧表述与漂移指针
- 负责人：用户（方向）／AI-B（侦察、契约、迁移、验证）
- 范围：`global`
- 状态：`CLOSED`
- 触发：用户 2026-09-23 原话「不仅要清理这个，你要检查一遍还有哪些要清理的，严格按照现在的架构来，旧的说法都清理掉，这个你来评估和建议并执行」

## 二、Goal

一句话：039 已把架构改成「六层模型 ＋ 两条内容线 ＋ 风格包 ＋ 单一声明源」，但全仓还残留一批**按旧架构写的说法**（旧步骤名、旧术语、指向已迁出正文的旧落点）——它们不是规则冲突，却会让新会话/新 AI 按旧地图找路。本事务把这些**说法层**残留一次清完，**不动任何判据本体**。

- 边界：**只改"说法/指针"，不改规则语义**。判据、阈值、流程本体一律不动。
- 与 039 的关系：039 已 `CLOSED`（R8 §十一：关闭后不得承接新范围）⇒ 另立本事务；039 的产出是本次的判断基准。
- 与既有登记 P0 的关系：登记在项目记忆的 6 项 P0 中，属"结构性修复"的 4 项**不并入**本事务（见 §五），只在本事务登记。

## 三、新口径 New Policy

判断基准（一处说法是否"旧"，按下列现行架构判）：

1. **步骤名**：第 2 步＝「视觉语言（由所选风格包定义）」；第 4.5 步＝「视觉转换（导演层定"怎么讲"，风格包定"怎么画"）」；第 5／6 步的阈值与渲染参数属主＝`remotion-components` 包 §③C／§⑦B／§⑧B。
2. **术语**：「纯 Remotion 片／实拍素材片」二分已废止；风格化产出按 `scripts/ref-registry.json` 的 `styles` 声明；「母题／视觉定位卡／母题一页」是 `remotion-components` 风格包**内部概念**（包 §③A），不是项目级步骤。
3. **落点**：正文在哪，指针就指哪——已迁入包的内容不得再指 `SKILL.md` 的原步骤号。
4. **内容线**：两线并行（行业线＝`SKILL.md`／教程线＝`产品功能教程作业规范.md`），"唯一作业文档"类表述必须限线。

## 四、Replace

旧说法 → 新说法（下表为本事务的逐处改动单）：

| # | 位置 | 旧说法（要点） | 新说法 |
|---|---|---|---|
| 1 | `SKILL.md`:3（frontmatter description） | "行业选型→**视觉母题**→口播去冗→…" | "行业选型→**视觉语言**→口播去冗→…" |
| 2 | `SKILL.md`:46 | "跨片复用的是开场签名、**视觉母题**、已做好的界面组件" | "…开场签名、**所选风格包的视觉语言（续用同风格时）**、已做好的界面组件" |
| 3 | `SKILL.md`:56 | "③ **母题**与开场签名（一次定，系列内沿用）" | "③ **视觉语言**与开场签名（由所选风格包定；同风格续用时沿用）" |
| 4 | `SKILL.md`:110（第 4 步分镜稿模板） | "入场语法：…必须属**本片母题**" | "入场语法：…必须属**所选风格包的视觉语言**" |
| 5 | `R3`:186 | "文字保锐＋主体静止红线详见 **SKILL 第 5 步**" | "…详见 `remotion-components` 包 **§③C**" |
| 6 | `R3`:198 | "（口径见 **SKILL 第 5 步**）" | "（口径见 `remotion-components` 包 **§③C**）" |
| 7 | `R3`:236 | "（**SKILL 第 2 步·行业视觉母题**）／能力边界见 **SKILL §三**" | "（`remotion-components` 包 **§③A**·行业视觉母题）／能力边界见该包 **§⑧A**" |
| 8 | `R3`:350 | "见 **SKILL §三**" | "见 `remotion-components` 包 **§⑧A**" |
| 9 | `R3`:381 | "操作流程（**行业母题**/分镜稿/…）" | "操作流程（**视觉语言（由所选风格包定义）**/分镜稿/…）" |
| 10 | `R3`:433／435／443／445 | "**SKILL 第 4.5 步**"（三层视觉结构／语义动效／色彩基准） | "`remotion-components` 包 **§③B**" |
| 11 | `R7`:10 | "见 `remotion-components` 包 ③ 与 **`SKILL.md` 第 4.5 步** / `R3` §7.5" | "见该包 **§③B** / `R3` §7.5" |
| 12 | `R7`:69 | "遵 **SKILL C-15** 复工隔离" | "遵该包 **§③A** 的 C-15 开工隔离" |
| 13 | `remotion-components` 包 §③A:93 | "落在**第 4.5 步**（视觉转换）" | "落在**本包 §③B**" |
| 14 | 同上 §③A:109 | "定义属主在**本次第 2 步**上两条" | "定义属主在**本栏**上两条" |
| 15 | 同上 §③A:129 | "（**纯片**默认，行业专属以**第 2 步母题**为准）" | "（**本风格**默认；行业专属以**本包 §③A 母题**为准）" |
| 16 | 同上 §③B:122 | 标题"**纯 Remotion 片**硬约束清单" | "**本风格**硬约束清单" |
| 17 | `docs/战略简报.md`:19 | "（2026-09-14 定，**SKILL 第 2 步**强制）" | "（`remotion-components` 风格包强制，见其 §③A；风格化片按所选风格包「验收位图」声明）" |
| 18 | `docs/战略简报.md`:47 | "**唯一作业文档**：`SKILL.md`（八步法）" | "**行业线唯一作业文档**：`SKILL.md`（八步法）" |
| 19 | `PROJECT-CONSISTENCY-MAP.md`:84 | "R9 视觉导演与视觉映射表为现行**视觉决策层**" | "R9 为**导演决策 Owner（视觉导演＋叙事导演）**" |
| 20 | `check-bg.mjs`:3／57／150 | "（**SKILL 第 2 步** / §五…）" | "（`remotion-components` 包 §③A「背景底必选」＋ `SKILL.md` §五 入库；本闸门按风格「验收位图」声明取值）" |
| 21 | `check-motif-card.mjs`:3／6／14／66／87／145／156／159／164 | 自述与输出文本通篇"**SKILL 第 2 步**" | 改指 `remotion-components` 包 **§③A**（母题一页定义属主）＋保留"按风格声明取目标"的现行机制说明 |
| 22 | `check-similarity.mjs`:3／76／226／268／315／325 | "**SKILL 第 2 步**主判据／**SKILL §三**工程约定" | 改指 `remotion-components` 包 **§③A／§⑧A** |
| 23 | `check-layout-diversity.mjs`:3／162 | "（**SKILL 第2步 C-16** / §三 布局指纹）"／"见 **SKILL C-16**" | 改指 `remotion-components` 包 **§③A** |
| 24 | `check-motion.mjs`:13 | "指标与合格线（**SKILL 第 6 步**）" | "…（阈值属主＝`remotion-components` 包 §⑦B）" |
| 25 | `gate-all.mjs`:58 | 标签"母题卡闸门（视觉定位卡四栏+素材张数）" | "母题卡闸门（按风格声明取值：视觉定位卡四栏+素材张数）" |
| 26 | `video/src/types.ts`:47 | 注释"（g11 起**纯 Remotion 片**）" | "（g11 起 `remotion-components` 风格）" |

## 五、Remove

**Remove**：上表 26 处的旧字面；改后须保证 §九 的关键词复扫中"项目级口吻的旧说法"归零。

**不并入本事务（已登记 P0 或多族结构问题，只登记不执行）**：

| 项 | 为什么不并入 |
|---|---|
| `PROJECT-CONSISTENCY-MAP.md` §1 主链／:7／:94 的**单线世界观** | 登记 P0（结构修复，需覆盖教程线；不是一句措辞） |
| `docs/AI使用手册.md`:69、`knowledge/爆款文案技能包/README.md`:3 的"视频生产唯一作业流程" | 035 已移交 S3 族：登记 `deprecatedTerms` 会与 archive 正文互斥，须先做扫描面分层 |
| `check-release-feedback.mjs` 读 `outputs/archive/` 台账（P0 #1）／APPLET 快照漂移（P0 #2）／CI 冻结 g11（P0 #5） | 结构性修复，各自需独立事务 |
| 039 登记的"第 4 步分镜稿模板未按风格参数化" | 属"接口参数化"，随首个风格化正式片事务定；本次只清其**用词**（上表 #4） |

## 六、Preserve

- **历史注记**（"替代旧 C-01／C-04""原'纯 Remotion 片／实拍素材片'二分已废止"等）＝ 变更史，保留。
- **`deprecatedTerms` 登记条目**（含 `g11 历史 8 问验收清单` 等）＝ 机器登记，保留。
- **`outputs/g06–g11`、`video/styles/*/probe/`、`docs/changes/closed|active/`** ＝ 历史产物/事务文本，零改动。
- **"母题"在 `remotion-components` 风格包内的全部用法**（§③A 正文、⑨ 产物清单、历史片代码注释）＝ 该风格自己的概念，保留。
- **`AGENTS.md`:140「视觉母题」**（视觉隔离的通用词）、**教程规范/启动指令中的"不继承行业线母题卡"** ＝ 边界声明，保留。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `SKILL.md`（6 处措辞） | 方法 Owner | REPLACE | 复扫归零＋闸门 | 批 1 |
| `R3`（6 组指针） | 技术参考 | REPLACE | 复扫归零＋闸门 | 批 1 |
| `R7`（2 处指针） | 组件目录 | REPLACE | 复扫归零＋闸门 | 批 1 |
| `remotion-components` 包（§③A 3 处、§③B 1 处） | 风格包 | REPLACE | 复扫归零＋闸门 | 批 1 |
| `docs/战略简报.md`（2 处） | 用户面文档 | REPLACE | 复扫归零＋闸门 | 批 1 |
| `PROJECT-CONSISTENCY-MAP.md`（1 处 R9 定位） | 恢复索引 | REPLACE | 复扫归零＋闸门 | 批 1 |
| `scripts/check-bg / check-motif-card / check-similarity / check-layout-diversity / check-motion / gate-all`（自述与输出文本） | 闸门脚本 | REPLACE（文本，不改判据） | 复跑闸门，行为零漂移 | 批 2 |
| `video/src/types.ts`（1 行注释） | 工程注释 | REPLACE | 复扫归零 | 批 2 |
| `outputs/g06–g11`／`docs/changes/*`／`probe/` | 历史 | PRESERVE | 零改动 | 全批 |

## 八、Migration Plan

1. 批 1：文档层（SKILL 4 处／R3 6 组／R7 2 处／包 4 处／战略简报 2 处／MAP 1 处）。
2. 批 2：脚本文本层（5 个闸门脚本 ＋ gate-all 标签）＋`video/src/types.ts` 注释。
3. 复跑 `check-doc-references`／`check-change-contract`／`gate-all`；关键词复扫（见 §九）；行为零漂移核对（闸门红绿与 040 前一致）。

## 九、Mechanical Checks

- 旧说法复扫（现行链，排除 `outputs/`、`docs/changes/`、`.workbuddy/`）：关键词 `SKILL 第 2 步`（除合法落点）／`SKILL 第 4.5 步`／`SKILL §三`（除合法）／`纯 Remotion 片`／`实拍素材片`／`视觉决策层`／`一次定，系列内沿用` → 0 命中或仅命中 §四 允许的注记。
- 文档引用检查：`check-doc-references` 零硬失败。
- 闸门：`check-change-contract` PASS；`gate-all` 与 040 前逐行一致（除 gate-all 标签一行的声明性变化）。
- 旧术语/旧口径扫描：039 的四条旧口径（C-01 话头／两条正路／默认假设没有任何外部图片素材／Remotion 擅长的那几样）继续 0 命中（本事务不得回流任何一条）。

## 十、Negative / Semantic Counterexample

- **负向（换词不换形）**：把"母题"批量替换成"视觉母题卡"就算清完——仍是把 `remotion-components` 的内部概念当项目级步骤。拦截层：§三.2 术语基准 ＋ 人判（本项目级出现处一律写"所选风格包的视觉语言"）。
- **负向（误删注记）**：把"替代旧 C-01／C-04"这类变更史注记当旧说法删掉——抹掉替换证据。拦截层：§六 Preserve ＋ 复扫结果须逐条人过目。
- **语义反例**：把 `R3` 的指针从错误落点（SKILL 第 5 步）改成另一个也存在但**语义不对**的落点（如包 §③A）——文档引用闸门只验"该文档有该节"，不验语义。拦截层：逐条人工核对目标节的实际内容（本事务每条改动都在 §四 表里写明目标节）。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝说法与指针层清理（文档、脚本文本、注释），不产出关键帧或成片；判据本体零改动。
```

## 十二、Closure Report

- 已修改：`SKILL.md`（6 处：description／第 1 步两处／第 2 步适用范围／第 4 步分镜稿模板／第 4.5 步指针标题）、`docs/internal/R3-Remotion技术参考.md`（6 组指针）、`docs/internal/R7-Remotion组件库.md`（2 处）、`video/styles/remotion-components/SKILL.md`（§③A 3 处／§③B 1 处）、`docs/战略简报.md`（2 处）、`docs/internal/PROJECT-CONSISTENCY-MAP.md`（1 处）、`scripts/check-bg.mjs`／`check-motif-card.mjs`／`check-similarity.mjs`／`check-layout-diversity.mjs`／`check-motion.mjs`（自述与输出文本）、`scripts/gate-all.mjs`（标签）、`video/src/types.ts`（1 行注释）
- 已废止：旧步骤名"行业视觉母题"作为项目级步骤（改为"由所选风格包定义"）；"纯 Remotion 片／实拍素材片"的现行用法；指向已迁出正文的旧落点（`SKILL 第 2／4.5／5 步`、`SKILL §三`）；"视觉决策层"作为 R9 定位
- 已保留：变更史注记 3 处（pack §③B"原'纯 Remotion 片'口径已废止"／R9"由'视觉决策层'演进"／SKILL 第 4.5 步"原二分废止"）；`deprecatedTerms` 登记；`outputs/g06–g11` 与 probe 产物；"母题"在 `remotion-components` 包内的全部用法；`AGENTS.md`:140 与教程规范／启动指令中的边界声明
- 影响面：`9 / 9 已处理`（§七 逐行）
- Owner/入口冲突：**PASS**（未新增 Owner／入口；改后各指针指向真实属主）
- 旧口径扫描：**PASS**（关键词复扫：`SKILL 第 2 步`／`SKILL 第2步`／`SKILL 第 4.5 步`／`SKILL 第 5 步`／`SKILL §三`／`实拍素材片` 0 命中；`纯 Remotion 片`／`视觉决策层` 命中均为**变更史注记**（逐条人过目相符）；039 四条旧口径继续 0 命中）
- 机械检查：**PASS**（`check-doc-references` 0 硬失败；`check-change-contract` PASS(42)；`gate-all` 13/15、2 项跳过、无红灯；与 040 前行级 diff 仅两处已声明差异——变更事务计数 41→42、母题卡闸门标签文案）
- 负向测试：**PASS**（注入测试：向 `SKILL.md` 临时写入`见 SKILL 第 5 步`→ 复扫命中 1；还原后 0。语义反例拦截见下行）
- 语义反例：**PASS**（新指针逐条核到目标节的实际内容，不只验锚点存在：§③A 母题设计法／§③B 画面骨架与硬约束／§③C 三层运动与动效纪律（"文字保锐／主体静止"命中）／§⑦B 效果尺阈值与定标／§⑧A 工程约定（"结构指纹"命中）／§⑧B 渲染参数）
- 真图/成片：N/A（Real Output 声明为 `not-applicable`）
- 本次新增红：**0**

**结论：CLOSED**

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260915-002`
- 标题：把 R9 视觉导演决策真正接入组件选择与文案→视觉转换
- 负责人：内容与视觉方法论负责人
- 范围：`global`
- 状态：`CLOSED`

## 二、Goal

让“高级感 / 不像 PPT / 动效有意义”不再只停留在原则文档，而是成为每个 Shot 在进入 Remotion 实现前必须回答的视觉决策字段，并让组件库按语义选型而不是反过来决定镜头。

## 三、新口径 New Policy

`R9` 负责视觉决策，`R9-视觉映射表`提供默认选型，`R7`负责组件目录，`R3`负责实现原理；任何关键镜头必须具备 `Visual Subject / Visual Metaphor / Peak Frame / State Change / Exit` 五个视觉字段后才能进入代码。

## 四、Replace

- “先看有什么组件，再把镜头拼出来”的组件驱动画面方式。
- 只用三层视觉结构描述“怎么画”，但没有明确说明“为什么选这个视觉语言”的做法。
- 文案分镜直接进入代码、缺少视觉隐喻与 peak frame 决策的中间阶段。

## 五、Remove

- 以“更丰富”“更有动感”作为组件新增的单一理由。
- 以左右双栏、统计卡、流程卡等现成组件形态默认承载抽象痛点/洞察。

## 六、Preserve

- SKILL 第 4.5 步作为正式视觉转换关。
- R3 继续负责 Remotion API、帧驱动、动画参数与工程实现。
- R7 继续负责组件目录和状态登记。
- 历史视频不回溯重做。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `docs/internal/R9-视觉导演与审美决策.md` | 视觉决策协议 | PRESERVE | 作为唯一审美原则属主 | DONE |
| `docs/internal/R9-视觉映射表.md` | 视觉选型辅助表 | ADD | 结构审查 | DONE |
| `docs/internal/R7-Remotion组件库.md` | 组件目录 | REPLACE | R9 语义选型接入 | DONE |
| `SKILL.md` | 作业流程 | PRESERVE | 现有第 4.5 步已是视觉转换入口 | DONE |
| `docs/internal/R3-Remotion技术参考.md` | 工程原理 | PRESERVE | 不复制 R9 决策规则 | DONE |
| `outputs/g11-烧烤/07-片1-分镜稿.md` | Shot 表 | REPLACE | 关键镜头已进入 R9 五字段流程 | DONE |
| `outputs/g11-烧烤/08-R9视觉决策卡.md` | 视觉导演台账 | ADD | 20/20 Shot 五字段完整 | DONE |
| `scripts/check-visual-shot-contract.mjs` | 机械闸门 | ADD | 缺字段负向测试必须失败 | DONE |
| `.github/workflows/visual-shot-contract.yml` | CI | ADD | CI 执行视觉契约与全闸门 | DONE |

## 八、Migration / Verification

1. 已建立 R9 视觉映射表。
2. 已把 R9 选型规则接入 R7 组件目录。
3. g11 Shot 表已增加并通过 `Visual Subject / Visual Metaphor / Peak Frame / State Change / Exit` 五字段检查。
4. 已增加 `check-visual-shot-contract.mjs` 与负向测试；缺少关键字段的反例会被拒绝。
5. CI 已在视觉契约检查前 materialize `APPLET` Product Truth，避免产品源码路径差异导致假失败。

## 九、Mechanical Checks

- R7 指向 R9：PASS。
- R9 视觉映射表存在：PASS。
- g11 视觉决策卡 20/20 Shot 五字段完整：PASS。
- 负向测试：PASS。
- Change Contract checker：PASS。

## 十、Negative / Semantic Counterexample

机械反例：删除任一 Shot 的 `Visual Metaphor` 或 `Peak Frame`，`check-visual-shot-contract` 必须非零退出；已验证。

语义反例：把“转赠”改写为“二次触达”，但画面仍使用五个卡片节点水平连接；文字换了、视觉关系没换，仍属于旧模式，应拒绝。该反例已登记于 g11 R9 决策卡。

## 十一、Real Output Verification

本事务改变的是视觉导演决策层、Shot 前置契约和机械闸门，不是历史成片本身；历史视频按不回溯原则不重渲。

g11 已完成 20 Shot 的 R9 决策台账，并作为后续 Remotion 实现的前置契约。真实成片审美验收仍属于 g11 交付阶段，不冒充本事务已经完成。

## 十二、Closure Report

- 新口径已进入：R9 + R9-视觉映射表 + R7 语义选型 + g11 R9 决策卡。
- 旧口径已替代：组件驱动镜头、信息框排列默认承载抽象洞察。
- 机械约束已落地：视觉 Shot Contract + negative test + CI workflow。
- 影响面：全部 9 项完成。
- 本事务无 PENDING Impact Map 项。
- 真实成片审美验收：不属于本事务的完成条件；由 g11 后续交付验收承担。
- 结论：**CLOSED**

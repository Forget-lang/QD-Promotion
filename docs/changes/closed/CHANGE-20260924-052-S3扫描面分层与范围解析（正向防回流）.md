# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-052`
- 标题：S3 批——扫描面分层（archive 只查"历史污染当前"）、`§X~§Y` 范围解析、正向防回流清单
- 负责人：用户（方向）／AI-B
- 范围：`global`
- 状态：`CLOSED`
- 触发：用户 2026-09-24「剩下的都按照你的建议来执行吧」（S3 为其最后一项；034/035 曾两次移交，047 §五 登记）

## 二、Goal

解开 034 实测的互斥：**登记旧话头会让 archive 正文硬失败** ⇒ 引入扫描面分层——现行面照旧全检，**archive 只查"历史污染当前"类**；顺带补 `§X~§Y` 范围引用解析与正向防回流模式清单。

## 三、新口径 New Policy

1. **扫描面分层**（`check-doc-references`）：
   - **现行面**（`docs/internal` ＋ `extraDocs` 里的当前文档 ＋ 风格包）：原检查组全部保留（引用断链／路径存在／章节锚点／计数型复述／`caliberOwners`／`deprecatedTerms`）。
   - **archive**（`outputs/archive/`）：**只跑「历史污染当前」类**（新增 `archiveNormativePatterns`）；**豁免**其余全部——历史正文允许与当前状态不一致（034 裁定：不回填），也允许含已废止话头（历史豁免）。
2. **`§X~§Y` 范围引用**：解析为两个端点分别校验锚点（规约成「§A §B」交同一套校验）。
3. **正向防回流清单**（登记在 `ref-registry.archiveNormativePatterns`，6 条）：`以本表…为准`／`参考本表了解当前状态`（变体）／`长期有效成文在此`／`每片照清单做/跑`／`固定机制`／`本表即当前…`——命中 archive 即硬失败，指向"改指针或降级为历史叙述"。

## 四、Replace

| # | 位置 | 旧 | 新 |
|---|---|---|---|
| 1 | `scripts/check-doc-references.mjs` | 三处 `for (const [file,path] of allDocs)` 循环对 archive 与现行面同检 | 各循环加 `ARCHIVE_DOC(file) → continue`；文末（报告前）新增 archive 专项检查 |
| 2 | 同上 章节引用解析 | `§7.3~§7.5` 会被切成 `7.3~` 非法 token | 范围先规约成 `§7.3 §7.5` 再走锚点校验 |
| 3 | `scripts/ref-registry.json` | 无 archive 模式清单 | 新增 `archiveNormativePatterns`（6 条，含 desc） |
| 4 | `outputs/archive/官网资源库落地规划.md`:79 | "（当时约定：每片照清单跑）" | 删该括注（新检查当场抓到的真残留） |

## 五、Remove

**Remove**：上表旧写法。

**不并入**：4 条待人工引用（048/050 登记）；教程线/风格化等随生产片项。

## 六、Preserve

`outputs/archive/**` 历史正文与数据（除 §四#4 一处规范性括注）；`docs/changes/**`；现行面各文档零改动。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `scripts/check-doc-references.mjs` | 检查器 | REPLACE（分层＋范围） | 四组测试（见 §九） | 批 1 |
| `scripts/ref-registry.json` | 声明源 | ADD（模式清单） | 预检＋负向 | 批 1 |
| `outputs/archive/官网资源库落地规划.md` | 历史证据 | REPLACE（1 处括注） | 复扫归零 | 批 1 |

## 八、Migration Plan

1. 实现分层与范围解析 → 预检（现行 archive 须干净）。
2. 测试四组：① archive 注入指挥句→须红；② archive 注入已登记旧话头→**须不报**；③ 存在/不存在的 `§X~§Y`→通过/红；④ 正向检查抓真残留并清理后回绿。
3. 复跑 `check-doc-references`／`gate-all`／`check-change-contract`。

## 九、Mechanical Checks

- ① 负向（指挥句）：向 `outputs/archive/挂账总览.md` 注入"以本表为准" → **EXIT=1**，点名 `挂账总览.md:37  «…以本表为准…» — archive 规范性残留` ✓
- ② 历史豁免：向同文档注入已登记话头「五角色素材库」 → **EXIT=0、0 命中**（archive 不查 deprecatedTerms）✓
- ③ 范围解析：`R3 §7.3~§7.5` → 通过；`R3 §7.3~§9.9` → **EXIT=1** 并点名「§9.9」✓
- ④ 真残留：向 `官网资源库落地规划.md:79` 命中「每片照清单跑」→ 清理 → 回绿 ✓
- 全闸门：`check-doc-references` 0；`gate-all` 13/15、2 项跳过、无红灯；`check-change-contract` PASS(53)（模式清单入库后计数）

## 十、Negative / Semantic Counterexample

- **负向（分层被读成"archive 免检一切"）**：有人据此认为"archive 里写什么都行"——错：archive **仍查**「历史污染当前」（指挥句/规范框架）。拦截：§三.3 清单＋负向①。
- **语义反例**：把"以脚本输出为准"这类**合法现行句**也塞进模式——会误伤现行面。拦截：清单只在 archive 面生效，且模式带 `[^。；\n]{0,8}` 限长；预检现行 archive 0 命中。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝检查器语义分层与测试，不产出关键帧或成片。
```

## 十二、Closure Report

- 已修改：`scripts/check-doc-references.mjs`（archive 分层＋`§X~§Y` 范围＋archive 专项检查）、`scripts/ref-registry.json`（`archiveNormativePatterns` 6 条）、`outputs/archive/官网资源库落地规划.md`（1 处规范性括注）
- 已废止：archive 与现行面"同检"的旧语义；`§X~§Y` 被解析为非法 token 的旧行为
- 已保留：archive 历史正文其余零改动；现行面各文档零改动；`deprecatedTerms` 对现行面照旧生效
- 影响面：`3 / 3 已处理`
- 旧口径扫描：**PASS**（archive 专项检查新增即抓到 1 处真残留并已清；现行面复扫不受影响）
- 机械检查：**PASS**（§九 四组测试全过；`check-doc-references` 0；`gate-all` 13/15 无红灯；`check-change-contract` PASS(53)）
- 负向测试：**PASS**（① archive 指挥句→红；② archive 旧话头→不报；③ 范围两端点正负各一）
- 语义反例：**PASS**（模式仅作用于 archive 面；预检 0 误伤）
- 真图/成片：N/A
- 本次新增红：**0**

**结论：CLOSED**

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260918-027`
- 标题：抖音图文首篇试点（烧烤手气券）——交付结构定稿 ＋ 台账字段 ＋ 机检接入
- 负责人：AI（用户 2026-09-18 16:00「继续执行吧」，承接 15:40 起「按建议顺序执行」的下一步）
- 范围：`local`（图文首篇产物登记进 `outputs/g11-烧烤/`；`video/src/graphic/**` 新建图文组件；**改检查器一项待用户点头**，见 §三.4）
- 状态：`CLOSED`

## 二、Goal

按 `CHANGE-20260917-019` 立的图文线口径（§三 2~8 条），产出抖音图文首篇，并落地 019 明确留给"试点事务"的三件事：

1. **首篇图文产物**：与 g11-烧烤**同行业同玩法**（周中夜宵手气券），复用其选题、真值表与字段取证；结构按图文形态**重写**（大标题＋步骤清单），**禁止搬运口播稿/视频文案**。
2. **交付结构定稿**（019 §三.8「交付结构随首篇试点定」）：图文图落 `outputs/gXX-行业/graphic/`；发布稿 `06-发布稿-抖音图文.md`。
3. **台账字段**（019 §三.6）：图文条目用**进入率 / 翻完率 / 赞藏评**替代视频三指标，`发布后验台账.json` 类型标注「图文」。

## 三、新口径 New Policy

1. **图文交付结构（定稿）**：① 图：`outputs/gXX-行业/graphic/` 下 N 张 PNG，**统一 3:4（1080×1440）**（首图即封面）；② 发布稿：`outputs/gXX-行业/06-发布稿-抖音图文.md`；③ 图文数据文件：`video/src/graphic/<gXX>.ts`（供机检逐字核对图内字段名）。
2. **图内字段名口径**：图文图内的产品字段名/界面名**必须逐字取自 `spec/*-fields.json` 并回 applet 源码取证**（与视频线同口径）；数值只能来自 spec 真值。
3. **品牌名口径（不放宽）**：画面/首图/话题标签**不出现品牌名**；产品名只在**标题或正文文字区客观提一次**（`rules.brand_display` 原文不动）。
4. **机检接入（本事务内实现数据文件；扫入检查器属改检查器，待用户点头后执行）**：019 §三.5 要求"图文数据文件入 `check-ui-truth` 扫描面"。本事务**先让数据文件成型**（结构对齐视频线 `data/gXX.ts` 的 `k:` 逐字登记法）；**修改 `scripts/check-ui-truth.mjs` 使其扫描图文数据文件**这一步，按用户 2026-09-17 定的边界（改检查器须用户点头 ＋ 同步 `docs/changes/template.md` ＋ 补负向测试）**不擅动**，在 §十二 如实标注其状态并移交。
5. **台账口径（定义）＋ 字段扩展与加行（待授权）**：图文条目**不用**视频四数，改用**进入率（封面点击）／翻完率／赞藏评**。现状障碍（实测）：台账 `rows` 的字段是固定的视频四数（`播放量/封面CTR/2秒跳出/完播`），而 `scripts/check-release-feedback.mjs` 正是读这四数判定——**要给图文加独立指标键，就等于改检查器**，按 §三.4 用户边界**不擅动**。同时台账既有约定是「**片进已发布时**才加行」，本事务产出尚未发布。⇒ 本事务**只定义口径、不加行、不改检查器**；两件（字段扩展 + 加行）在 §十二 如实标注为移交项。

## 四、Replace

| 旧口径 | 位置 | 新口径 |
|---|---|---|
| 抖音只有短视频一条线的产品结构（`03-发布稿-抖音.md` 单份） | `outputs/g11-烧烤/` | 增图文线产物：`graphic/` 图 ＋ `06-发布稿-抖音图文.md`（短视频线产物不动） |
| 图文交付结构未定（019 留白） | —（019 §三.8 的待定项） | 本契约 §三.1 定稿三件（图目录 / 发布稿命名 / 数据文件路径） |

## 五、Remove

- **不改**任何检查器与闸门（`scripts/**` 零改动；机检接入待授权）。
- 不改 `SKILL.md` / `AGENTS.md` / `spec/**` / `docs/internal/**`（019 已把图文线口径写进 SKILL，本事务只执行）。
- 不改 g11 视频侧任何产物（`data/g11.ts` / `videos/g11/**` / `frames/**` / `covers/**` / 两版成片）。
- **不搬运**视频口播稿或视频文案到图文（019 §三.8 明令）；图文结构独立重写。
- 不新增图文线之外的表单/台账（复用既有 `发布后验台账.json`）。
- 不加任何二维码/小程序码；图内不出现品牌名。

## 六、Preserve

- 019 确立的图文线全部口径（形态、版式、品牌名、上屏真实性、后验指标、伴生关系）。
- g11 视频侧全部产物与已验收结论（含有声成片 3188 帧 / 106.27s、封面 4 张、三份发布稿）。
- `rules.brand_display` 原文；短视频线发布稿原文。
- 既有 `check-ui-truth` 对视频线的扫描行为逐字不变。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `video/src/graphic/G11Graphic.tsx` | 图文组件（新建） | ADD | V1：组件可渲、`tsc` 零错；5 张均为 1080×1440 | DONE |
| `video/src/index.tsx`（注册图文 Composition） | 工程注册 | ADD（5 行） | V1：`g11-graphic-1..5` 渲图成功 | DONE |
| `outputs/g11-烧烤/graphic/*.png` | 图文产物 | ADD（5 张） | V2：逐张目视核对（信息流小图可读、无品牌名、无二维码） | DONE |
| `outputs/g11-烧烤/06-发布稿-抖音图文.md` | 发布稿 | ADD | V3：标题 ≤30 字且钩子与视频/小红书/搜狐**不同构**；正文 ≥5 信息单元；话题 3~5 不带品牌词 | DONE |
| `video/src/graphic/g11.ts`（图文数据文件） | 机检数据源（新建） | ADD | V4：图内字段名 `k:` 逐字回 `spec/coupon-fields.json` ＋ applet 源码；与分镜稿取证一致 | DONE |
| `outputs/archive/发布后验台账.json` | 台账 | DEFERRED（口径已定，加行与字段扩展待授权） | V5：复核表明确认**未加行、未改检查器**；图文指标口径（进入率/翻完率/赞藏评）已写入本契约 §三.5 | DONE |
| `docs/changes/template.md` | 契约结构 Owner | PRESERVE | 核查结论：本事务未改检查器，无同步事项 | DONE |
| 全闸门基线 | 机制 | PRESERVE | V6：`gate-all --tsc` 红灯构成**不超改前**（探针假红 ＋ 尺子设计阶段红），本次新增红 0 | DONE |

## 八、Migration Plan

1. 立本契约（`APPROVED`），定交付结构三件。
2. 建 `video/src/graphic/G11Graphic.tsx`（5 屏：首图＋四张清单图）与 `g11.ts` 数据文件；在 `index.tsx` 注册 5 个 3:4 Composition。
3. 渲 5 张 PNG 至 `outputs/g11-烧烤/graphic/`；逐张自检（小图可读、零品牌名、零二维码、字段名取证）。
4. 写 `06-发布稿-抖音图文.md`（标题钩子与其余三平台不同构；正文按图文形态重写）。
5. 台账加「图文」类型条目。
6. 跑 V1~V6 ＋ `gate-all --tsc` ＋ `check-change-contract`；因改了 `video/src` 需重渲 g11 成片恢复"证据有效"。
7. 填 §十二 闭包并移档 `closed/`；**机检接入（改 `check-ui-truth`）在 §十二 如实标注并移交**。

## 九、Mechanical Checks

- `tsc --noEmit`：零错。
- `check-change-contract`：PASS。
- `gate-all --tsc`：红灯构成不超改前。
- **验证 / 负向测试**：
  - **V1**：5 个图文 Composition 渲图成功、尺寸均 1080×1440。
  - **V2**：5 张图逐张目视＋缩到 360px 宽复看；图内无品牌名、无二维码。
  - **V3**：`06-发布稿-抖音图文.md` 标题 ≤30 字；与 `03-发布稿-抖音`／`04-小红书`／`05-搜狐` 的钩子**不同构**（逐对比对）；正文信息单元 ≥5。
  - **V4**：图内出现的字段名逐字命中 `spec/coupon-fields.json` 且与 `07-片1-分镜稿.md` 的取证一致。
  - **V5**：台账**未新增行**（未发布）、`check-release-feedback.mjs` **零改动**；图文指标口径已落在本契约 §三.5。
  - **V6**：`gate-all --tsc` 红灯 ≤ 改前。

## 十、Negative / Semantic Counterexample

- **反例①（搬运视频文案）**：把口播稿句子直接搬进图文 → **由 §五 与 V3 拦**（逐句比对 `02-口播文案.md`，重合句不得出现）。
- **反例②（图内点名工具名）**：借"图文是新形态"在图内写产品名 → **由 §三.3 拦**（brand_display 原样适用，只允许正文文字区提一次）。
- **反例③（顺手改检查器）**：本事务就把 `check-ui-truth` 改成扫图文 → **由 §三.4 拦**（改检查器须用户点头，本事务不擅动）。
- **反例④（后验指标混用）**：图文条目按视频三指标回填 → **由 §三.5 拦**。
- **边界如实声明**：图内字段名的**机检覆盖尚不可得**（数据文件已成型，但检查器未接入扫描）——本事务**不声称**已获机器保护，如实移交。

## 十一、Real Output Verification

```text
- Real Output 声明：`applicable`
- 适用证据：
  - `outputs/g11-烧烤/graphic/g11-graphic-1..5.png`：5 张图文图（1080×1440，首图即封面），逐张自检＋360px 小图复看
  - `outputs/g11-烧烤/06-发布稿-抖音图文.md`：图文形态发布稿（标题/正文/话题/配图说明）
  - `video/src/graphic/g11.ts`：图文数据文件（图内字段名逐字登记，供后续机检接入）
```

- 真实关键帧/短片：图文形态无成片，证据为 5 张静态图（符合 019 §三.3「零视频渲染」）。
- 人工验收判据：① 5 张图信息流小图可读；② 图内零品牌名、零二维码；③ 标题钩子与其余三平台不同构；④ 正文未搬运口播；⑤ 字段名回源码取证。
- 结果：PASS（`applicable`）

## 十二、Closure Report

- 已修改：`video/src/graphic/G11Graphic.tsx`（新建，5 图组件）；`video/src/graphic/g11.ts`（新建，图内字段名逐字登记）；`video/src/index.tsx`（1 行 import ＋ 5 个 3:4 Composition 注册）；`outputs/g11-烧烤/graphic/*.png`（新建 5 张）；`outputs/g11-烧烤/06-发布稿-抖音图文.md`（新建）；`video/public/xhs/BG-XHS-02-点阵网格.jpg`（素材映射：从 `背景素材/小红书风格/2.jpg` 复制，家族目录 `xhs/`）；`outputs/g11-烧烤/g11-烧烤-有声成片.mp4`（重渲以恢复"最新成片"证据有效性——改 `video/src` 会使其判过期）
- 已废止：无（首篇新建；无旧口径被推翻）
- 已保留：019 确立的图文线全部口径；g11 视频侧全部产物与已验收结论；`rules.brand_display` 原文；`check-ui-truth` 对视频线的扫描行为逐字不变；台账原文（**未加行**）
- 影响面：`8 / 8`
- 旧口径扫描：PASS —— 本事务为新建，无旧口径话头可扫；另核实 `outputs/g11-烧烤/graphic/` 与 `06-发布稿-抖音图文.md` 均不与既有产物重名冲突
- 机械检查：PASS —— `tsc --noEmit` **零错**；`check-change-contract` **PASS（29 笔）**；`gate-all --tsc` **14 绿 / 16**，红灯 2 道**均非本事务引入**（① 文字安全区探针＝背景纹理假红 ② 效果尺子＝设计阶段红）；**本次新增红 0**
- 负向测试：PASS —— **V1**：5 个 Composition 渲图成功、尺寸均 1080×1440；**V2**：逐张目视 ＋ 缩到 300px 小图复看（首图「周一没人？」与内页正文均可读）；图内**零品牌名、零二维码**；**V3**：`06-发布稿-抖音图文.md` 标题 23 字（≤30），钩子＝**数字清单型**（「5 步」前置），与短视频线（身份＋问句）／小红书（身份＋痛点＋方案＋数字）／搜狐（问题词＋结果词）**四平台各不相同**；正文 5 个步骤单元 ≥5；**V4**：图内 5 个字段名（`手气券`／`每人限领总量`／`领取开始时间`／`可用时段`／`券码`）逐字命中 `spec/coupon-fields.json` 与 applet 源码（`每人限领总量`＝create.vue:246、`领取开始时间`＝275、`可用时段`＝185；`券码` 命中 13 个源码文件）；**V5**：台账**未新增行**（g11 尚未发布）、`check-release-feedback.mjs` 零改动；**V6**：`gate-all --tsc` 红灯数 = 2，与改前一致
- 语义反例：PASS（四条边界如实声明；另**移交两项**，见下）
- 真图/成片：PASS（`applicable`）—— `outputs/g11-烧烤/graphic/` 5 张（1080×1440）＋ `g11-烧烤-有声成片.mp4`（重渲，3188 帧 / 23.9MB）
- 本次新增红：**0**

**执行期如实记录（两项移交，均因触及用户"改检查器须点头"边界而本事务不擅动）**

1. **图文数据文件的机检接入**：019 §三.5 要求"图文数据文件入 `check-ui-truth` 扫描面"。本事务已让数据文件成型（`video/src/graphic/g11.ts`，图内字段名逐字登记＋取证对照），但**把该文件扫入检查器＝改 `scripts/check-ui-truth.mjs`**，未做。
2. **台账字段扩展与加行**：019 §三.6 要求"台账字段与机检接入随首篇试点落地"。实测障碍：台账 `rows` 字段是固定的**视频四数**（`播放量/封面CTR/2秒跳出/完播`），而 `check-release-feedback.mjs` 正是读这四数判定 → 给图文加**独立指标键**等于改检查器（未做）；且台账既有约定是「**片进已发布时**才加行」，本事务产物尚未发布。⇒ 本事务只把**图文指标口径**（进入率／翻完率／赞藏评）写入 §三.5，**不加行、不改检查器**。

**结论：CLOSED**

---

## 关闭后修订（2026-09-18 16:2x · 用户指令）

- **修订内容**：首图 `graphic/01-首图-周一没人.png` 的背景**由珊瑚橙底（CV-001）改为点阵网格纸（BG-XHS-02）**，与其余 4 张**保持一致**；配色随之改为**深墨大字 ＋ 珊瑚橙强调**（原 `coverKit` 的白/金字在白底上不可读，故首图不再引用 `coverKit`，其 import 已移除）。
- **依据**：用户 2026-09-18 16:22「第一张图片的背景图保持和其他屏一致，不要单独用一张」。
- **未变**：5 张尺寸（3:4 / 1080×1440）、文案、字段名取证、交付结构（§三.1）、§三 全部口径、两项移交；`graphic/g11.ts` 无需改（未记背景）。
- **同步**：`06-发布稿-抖音图文.md` 配图说明第 1 条已改；因改 `video/src/graphic/**` 会令"最新成片"判过期，已重渲 `g11-烧烤-有声成片.mp4` 恢复证据有效。
- **性质**：产物级视觉调整，**不改任何规则口径**，故不重开事务。

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-054`
- 标题：风格轮换机检接线——「同风格不得连续复用」从文本规则变为可验闸门
- 负责人：用户（方向）／AI-B
- 范围：`global`
- 状态：`CLOSED`
- 触发：用户 2026-09-24「其他的都按照你的建议执行吧」（③ 把轮换规则接上机检）

## 二、Goal

039 §三.6 立「同风格不得连续复用」时定为"**先记录、后机检**"——记录（styleId 声明、各包 ⑩ 条款）已在，**机检一直缺**（实测 `scripts/` 全目录无此检查）。本事务把机检补上，让下一条片（g12）起该规则可验。

## 三、新口径 New Policy

1. **片风格认领**：片目录内声明 `styleId` 即视为已认领（与导演稿闸门同口径）；历史片（无 styleId）不回溯。
2. **判据**：最新两条已声明 `styleId` 的行业片**同风格 ⇒ 硬失败**；确有理由连用者，逐条登记 `styleRotationWaivers{video, styleId, reason(非空), approvedBy}`（＝"片首声明连续复用例外＋理由"的机器落点）。
3. 已声明片不足两条 ⇒ ⏭️（接线就位、待对照）。

## 四、Replace

| # | 位置 | 旧 | 新 |
|---|---|---|---|
| 1 | `scripts/` | 无轮换检查 | 新增 `check-style-rotation.mjs` |
| 2 | `scripts/gate-all.mjs` | 无此行 | GATES 增 `stylerotation`（排在相似度闸门之后） |
| 3 | `scripts/ref-registry.json` | 无豁免位 | 增 `styleRotationWaivers: []`；`styles._说明` 补机检说明 |

## 五、Remove

**Remove**：规则"只有文本、没有机检"的状态。

**不并入**：g12 生产（用户明示"先不着急"）；风格化机检覆盖（另议）。

## 六、Preserve

各风格包 ⑩ 条款、`SKILL.md` 第 4.5 步、`_template`：零改动（本事务只补机检）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `scripts/check-style-rotation.mjs` | 闸门 | ADD | 三组测试（同风格红／换风格绿／登记例外放行） | 批 1 |
| `scripts/gate-all.mjs` | 总闸门 | ADD（一行） | 新行出现在总览 | 批 1 |
| `scripts/ref-registry.json` | 声明源 | ADD（豁免位＋说明） | 复跑 | 批 1 |

## 八、Migration Plan

1. 写闸门 → 接 `gate-all` → 注册表补豁免位。
2. 三组夹具测试 → 清夹具 → 复跑全闸门。

## 九、Mechanical Checks

- ① 同风格：夹具 g98/g99 均 `styleId=whiteboard` → **EXIT=1** 并点名两条片与风格名 ✓
- ② 换风格：g99 改 `remotion-components` → **EXIT=0**（"换用了新风格"）✓
- ③ 登记例外：`styleRotationWaivers` 加 g99 一条 → **EXIT=0**（列出批准人与理由）✓
- 现量：0 条声明 styleId 的片 → ⏭️「接线就位」（历史片不回溯）✓
- `gate-all`：新增一行 `风格轮换闸门（同风格不得连续复用）`；其余行与 053 基线一致、无红灯。

## 十、Negative / Semantic Counterexample

- **负向（豁免变成后门）**：把豁免条目的 `reason` 留空即算通过——已被闸门拒（要求 `reason` 非空）。真正的防线是"逐条登记＋批准人"，与 `motionWaivers` 等同构。
- **语义反例**：只比较"上一条片"而忽略中间的历史片（无 styleId）——历史片未声明风格，无从比较，属**如实跳过**（并在输出里写明"历史片不回溯"），不是漏检。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝闸门接线与测试，不产出关键帧或成片（规则将在 g12 上首次实弹生效）。
```

## 十二、Closure Report

- 已修改：`scripts/check-style-rotation.mjs`（新增）、`scripts/gate-all.mjs`（+1 行）、`scripts/ref-registry.json`（`styleRotationWaivers`＋说明）
- 已废止：规则的"无机检"状态
- 已保留：各包 ⑩／SKILL 第 4.5 步／`_template` 原文
- 影响面：`3 / 3 已处理`
- 旧口径扫描：**PASS**（无口径变更）
- 机械检查：**PASS**（三组测试全过、夹具已清；`gate-all` 无红灯；`check-change-contract` PASS）
- 负向测试：**PASS**（同风格连用被抓；空 reason 不放行）
- 语义反例：**PASS**（历史片跳过已在输出中明示）
- 真图/成片：N/A（首次实弹在 g12）
- 本次新增红：**0**

**结论：CLOSED**

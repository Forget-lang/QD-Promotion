# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260918-030`
- 标题：红线功能字段名豁免（用户声明）＋ 图文机检接入 check-ui-truth
- 负责人：AI（WorkBuddy）
- 范围：`project`
- 状态：`CLOSED`

## 二、Goal

两件"Owner 已要求/已声明、机制接不上"的缺口一次补实：① **闸门交汇冲突从根解除**——用户 2026-09-18 17:27 声明「手气券的最大金额、最小金额是功能级的，这个使用没问题」，即 spec 登记的产品字段名属功能级命名，红线极限词扫描不应把字段名当广告极限词；据此 `check-redlines` 对 data 文件先剥离功能字段名再扫极限词，**⑥ 券种↔面额配对规则零改动**、未来片可正常"声明券种＋写真名"，不再需要语义代号绕行（该方案就此作废）。② **图文机检接入**（CHANGE-20260917-019 §三.5 最后一项移交）——`video/src/graphic/*.ts` 纳入 `check-ui-truth` 扫描面，图内字段名逐字回源。

## 三、新口径 New Policy

1. **红线闸门·功能字段名剥离**：`check-redlines.mjs` 扫描 `video/src/data/**/*.ts` 前，先把 **spec 四表（coupon/card/point/member-fields.json）登记的产品字段名**（`fields[].label` ＋ `createGroups[].rows`）从扫描文本中剥离——功能级字段名不构成极限词违规（依据＝用户 2026-09-18 声明）。剥离清单从 spec 自动派生（单一真源，不手维护白名单）。**剥离只作用于 data 目录**：`outputs/**/*.md`（对外发布文案）与其余扫描全量照旧。
2. **上屏真实性闸门·⑦ 图文分支**：`video/src/graphic/*.ts` 纳入扫描——图内字段名（`images[].k` 数组与 `k:` 单值）必须满足其一：逐字命中 spec 四表字段名/组行名；或为券种名（`couponTypes.label`）；或逐字命中 applet 源码（归一化 grep 实证，对应 evidence 的"非 spec 表内条目"类，如「券码」）。**`onScreen:false` 的字段名出现在图内 → ❌ 硬失败**（"行名不上屏"原则对图文同样生效）。
3. ⑥ 券种↔面额配对规则**原文不动**；g11 已发布冻结、其绕行态保持原样（分镜稿备注更新为"已解除"留痕）。
4. 语义代号方案（`randomMin/randomMax` 间接层）**废止不再实施**——用户声明使直连真名成为可能，更简单且少一层映射。

## 四、Replace

- 旧状态「data 文件写手气券面额行名必触红线 → 只能绕行（不声明券种、不登字段名）」→ 可安全写真名，配对保护恢复。
- 旧状态「图内字段名靠人眼逐个核对（027 人工抓出 2 处不逐字）」→ 开工前机检硬拦。
- 027 §十二 移交项①（图文数据文件入扫描面）就此销账。

## 五、Remove

- 语义代号间接层方案（未实施过，仅存在于 027/讨论记录——正式废止，不得再提出作为实现）。
- 禁止恢复项：① 把剥离豁免扩到 `outputs/**/*.md` 或 spec/redlines.json 词表本身（词表一个字不动）；② 把 `onScreen:false` 名写上图内/发布文案；③ 借本事务改 ⑥ 配对规则或词表内容。

## 六、Preserve

- `spec/redlines.json` 全部词表与分层结构**逐字不动**（豁免在检查器逻辑层，不动数据源）。
- `check-ui-truth` ①~⑥ 全部规则与输出格式；`retiredRows`/`allRows` 既有派生。
- g11 已发布产物、绕行态 data、后验台账两行待回填。
- 019/027 图文线其余口径；`video/src/graphic/g11.ts` 现状（T7 回归必须全绿）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `scripts/check-redlines.mjs` | 检查器 | ADD（功能字段名剥离：spec 派生清单＋data 目录限定） | T1/T2/T3/T4 | DONE |
| `scripts/check-ui-truth.mjs` | 检查器 | ADD（⑦ 图文分支：逐字回源＋onScreen:false 反向硬拦） | T4/T5/T6/T7 | DONE |
| `outputs/g11-烧烤/07-片1-分镜稿.md` | 片产物 | REPLACE（「闸门交汇冲突」备注 → 已解除留痕） | grep 命中 | DONE |
| `.workbuddy/memory/` | 记忆 | REPLACE（待办③④销账＋新口径登记） | 复核命中 | DONE |

## 八、Migration Plan

1. `check-redlines.mjs`：顶部派生功能字段名清单（spec 四表）→ `scan()` 对 data 目录文件先剥离再匹配 → 头部注释补豁免说明。
2. `check-ui-truth.mjs`：新增 `GRAPHIC_DIR` ＋ ⑦ 图文分支（提取/校验/输出/退出接线）→ 头部注释补 ⑦。
3. 负向测试 T1~T7（临时文件跑完即删）。
4. `07-片1-分镜稿` 备注更新；记忆同步。
5. `gate-all --tsc` 全量 ＋ `check-change-contract`；提交推送。

## 九、Mechanical Checks

- 旧术语/旧口径扫描：`grep -n "randomMin" video/src scripts docs` ＝ **0 处**（语义代号方案未落地且废止）。
- 文档引用检查：`check-doc-references` PASS。
- 其他相关闸门：`gate-all --tsc` 总览 0 红（探针/尺子两行维持 ⏭️ 已裁）。
- **负向测试**（临时文件跑完即删，删后复跑闸门回基线）：
  - **T1** data 临时文件写面额行名真名（`k: '随机最大金额'`）→ `check-redlines` **exit 0**（剥离生效；改前必炸）。
  - **T2** data 临时文件字幕含真极限词文案（如「全场最大折扣」）→ **exit 1**（真文案仍全量拦截）。
  - **T3** outputs 临时 md 写面额行名 → 观察并记录（豁免**不扩散**到对外文案的实证）。
  - **T4**（端到端）data 临时场景声明 `couponType: '手气券'` ＋ 面额两行名全写 → `check-ui-truth` ⑥ **不报 typeMissing/typeMismatch/undeclared** ＋ `check-redlines` **exit 0**（冲突解除实证）。
  - **T5** graphic 临时文件造 spec/applet 均查无的字段名 → `check-ui-truth` **exit 1**（⑦ graphicBad）。
  - **T6** graphic 临时文件写 `onScreen:false` 名 → **exit 1**（⑦ graphicHidden 反向硬拦）。
  - **T7** g11 图文数据现状回归 → `check-ui-truth` **整体 exit 0**。

## 十、Negative / Semantic Counterexample

- 反例①「豁免＝极限词规则失效」：以为功能字段名豁免后文案可写极限词——T2 拦（真文案仍硬失败，剥离只吃 spec 登记的完整字段名）。
- 反例②「豁免扩散到发布文案」：发布稿里写极限词也想借豁免过关——剥离按目录限定（仅 `video/src/data`），T3 实证 outputs 不豁免。
- 反例③「图内借用功能字段名上屏」：把 `onScreen:false` 行名印到图文里——⑦ 反向硬拦（T6）。
- 反例④「假字段名混进图文」：编一个产品里不存在的名字——⑦ 逐字回源（T5）。

## 十一、Real Output Verification

**声明（二选一，机器可读，必填）**：本事务到底需要什么真实产物，由本事务自己声明，机器不猜。

```text
- Real Output 声明：`applicable`
- 适用证据：
  - 负向测试 T1~T7 实测退出码与命中明细（临时文件跑完即删、删后闸门回基线）
  - `check-ui-truth` 输出含「⑦ 图文机检」段且 g11 图文数据回归通过
  - `check-redlines` 对 data 目录剥离生效、对 outputs 不剥离的对照结果
（证据清单到此为止。）
```

- 真实关键帧/短片：N/A——本事务不产画面（真实产物＝检查器行为与机检结果）。
- 人工验收判据：① spec 四表词表文件 diff 为空；② ⑥ 规则文本 diff 为空；③ g11 图文数据文件未被改动（T7 回归通过即证）。
- 结果：见 §十二

## 十二、Closure Report

- 已修改：`scripts/check-redlines.mjs`（新增 `FUNC_LABELS` 派生——spec 四表 `fields[].label`＋`createGroups[].rows`，`existsSync` 补导入；`scan()` 对 `video/src/data/**` 文件先剥离功能字段名再匹配；头部注释补豁免说明）；`scripts/check-ui-truth.mjs`（头部「六层」→「七层」＋⑦ 说明；新增 `GRAPHIC_DIR`/`extractGraphicNames`/⑦ 校验与输出；⑦ 硬拦接入退出链；✅ 文案扩图文计数）；`outputs/g11-烧烤/07-片1-分镜稿.md`（备注标题改「已解除」＋解除段留痕）；`.workbuddy/memory/`（待办销账＋新口径登记）
- 已废止：语义代号间接层方案（`randomMin/randomMax`——未实施即废止，027/讨论记录中的提案正式关闭）；「手气券面额行名不可写进 data」的绕行必然性；「图内字段名只能人眼核对」
- 已保留：`spec/redlines.json` 全部词表与分层**逐字不动**；`check-ui-truth` ①~⑥ 规则与输出原文；g11 已发布产物与绕行态（分镜稿备注留痕记录机制演进）；`video/src/graphic/g11.ts` 零改动（T7 回归通过即证）；019/027 图文线其余口径
- 影响面：`4 / 4`
- 旧口径扫描：PASS —— `grep -rn "randomMin" video/src scripts docs` ＝ **0 处**；`07` 备注含「已解除（CHANGE-20260918-030」**1 处**；词表文件 diff 为空（`git diff --stat spec/redlines.json` 空）
- 机械检查：PASS —— `check-change-contract` **PASS（32 笔）**；`gate-all --tsc` 总览 **0 红**（探针/尺子维持 ⏭️ 已裁）；基线 `check-redlines` exit 0、`check-ui-truth` exit 0（⑥ 基线＝6 个声明券种屏）
- 负向测试：PASS —— **T1**（隔离重测）data 临时文件写真名 `k:'随机最大金额'` → redlines **exit 0**、临时文件 0 命中（剥离生效；首轮与其它临时文件同场读数被污染，隔离重测为准，已如实留档）；**T2** data 真极限词文案「全场最大折扣」→ **exit 1** 命中«最大»；**T3** outputs md 写面额行名 → **exit 1** 命中«最大»（豁免不扩散实证）；**T4**（端到端）临时屏声明 `couponType:'手气券'`＋两行真名 → ui-truth **exit 0** 且 ⑥ 计入「7 个声明屏…对得上」（基线 6＋临时 1）、redlines **exit 0**（冲突解除实证）；**T5** graphic 编造字段名 → **exit 1** `graphicBad` 命中；**T6** graphic 写 `onScreen:false` 名 → **exit 1** `graphicHidden` 命中；**T7** g11 图文数据回归 → ⑦ 正式输出「图文机检 ✅ 5 个图内字段名全过」。临时文件全部删除，删后基线双绿
- 语义反例：PASS —— 反例①（豁免＝规则失效）T2 拦；反例②（扩散到发布文案）T3 拦；反例③（onScreen:false 名上图文）T6 拦；反例④（编造字段名）T5 拦——四条全部机检实证，非人工判据
- 真图/成片：PASS（`applicable`）—— 真实产物＝检查器行为与机检结果（⑦ 段正式输出、T1~T7 退出码）；本事务不产画面
- 本次新增红：**0**

**结论：CLOSED**

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260917-008`
- 标题：口径与指针纠错包（总账去硬编码 / 注册表自述同步 / 归档旧路径）
- 负责人：用户 + 本地 Agent
- 范围：`global`
- 状态：`CLOSED`
- 基线：`HEAD = f0b9a74`
- **定位说明**：三处都是「陈述了错误事实」的指针/口径纠错，改动互不依赖、各自可独立验证；因触达 3 个对象，按 `AGENTS.md` §一 升级为全局变更走 R8。**同类打包声明**：三项同属"口径纠错"一族，故合并为一个事务；若日后其中一项再现问题，可凭 Impact Map 逐行归因。

## 二、Goal

清掉三处**会误导后续会话**的错误陈述（都是"说的事实已经不对"，不是"规则没写"）：

1. `PROJECT-CONSISTENCY-MAP.md` 硬编码「`CHANGE-20260916-004` 为当前唯一 active 事务」——实测当前 active 为 **005 + 006 + 007**，该行已过期。
2. `scripts/ref-registry.json` 的 `description` 自称只被 `check-doc-references` / `check-facts` 读取——实测**另有 4 个脚本读取**，且它已承载 4 类历史豁免登记。
3. `outputs/archive/AI启动提示词.md` 第 4、15 行称"当前操作模板在根目录 `AI启动提示词.md`"——**根目录无此文件**，当前文件是 `AI工作启动指令.md`；同目录 `README.md` 已写对，仅该副本自身没改。

## 三、新口径 New Policy

1. **能机器推导的状态，不人工硬编码。** 总账不登记"当前哪些事务 active/closed"，改为指向 `docs/changes/active/` 与 `docs/changes/closed/` 目录，并重申实时状态以脚本输出与当前产物为准（`AGENTS.md` §十一 既有口径）。
2. **注册表的自述必须与它被读取的事实一致**；读者清单以实际代码为准，**不在注册表里复述条数**（避免下次再漂移）。
3. **归档文件内的"当前状态陈述"必须指向真实存在的对象**；归档快照本身的内容不回填。

## 四、Replace

- 总账「变更事务」行状态列：硬编码的 004/003 陈述 → 指向目录与脚本输出。
- `ref-registry.json` 的 `description`：误述的 2 个读者 → 如实描述实际读者面与承载职责。
- 归档副本第 4、15 行：`AI启动提示词.md` → `AI工作启动指令.md`。

## 五、Remove

- 移除总账中对"哪一份是当前唯一 active 事务"的人工断言（该断言必然漂移）。
- 本事务**不删除任何文件、不删除任何章节**。

## 六、Preserve

- `docs/internal/R10-产品事实源解析协议.md` 第 67 行的约束**继续遵守**：注册表**不复制事务状态**（本事务只改自述文字，不新增任何键）。
- `outputs/archive/AI启动提示词.md` 的**历史内容快照不回填**：只改两处指向当前对象的路径，其历史正文保持原样。
- g11 线（文档引用 5 处断链、母题卡红、g10 母题页即存缺口）**整条挂起**，等用户落实文档后重建。
- 已确认的重复项（`AI工作启动指令.md` 公共层 ↔ `AGENTS.md`；`AGENTS.md` §一 ↔ `整改作战总纲.md` §0）**只记不修**。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `docs/internal/PROJECT-CONSISTENCY-MAP.md`「变更事务」行 | 手工总账 | REPLACE | `grep` 确认该行不再含硬编码事务号，改为指向目录 | DONE（`grep -c "为当前唯一 active"` = 0；已改为指向 `docs/changes/{active,closed}/`） |
| `scripts/ref-registry.json` `description` | 注册表自述 | REPLACE | JSON 仍可解析；`description` 不再声称只有 2 个读者 | DONE（`node -e require(...)` 可解析、顶层键仍 12 个；自述已改为"读者面以实际代码为准、不写死条数"） |
| `outputs/archive/AI启动提示词.md` 第 4、15 行 | 归档内旧路径 | REPLACE | 两行均指向 `AI工作启动指令.md`；且**改动行数只准为 2** | DONE（`git diff --stat` = 2 insertions / 2 deletions；`grep -c "根目录 \`AI启动提示词.md\`"` = 0） |
| `docs/changes/active/CHANGE-20260917-008-*.md` | 本事务 | ADD | 本文件 | DONE |

## 八、Migration Plan

1. 改总账该行为"指向目录 + 以脚本输出为准"。
2. 改 `ref-registry.json` 的 `description`（只改字符串，不动任何键）。
3. 改归档副本第 4、15 行路径；核对历史正文未被改动。
4. 机械检查 + 语义反例 + 零新增红核验；回填 §九/§十二。

## 九、Mechanical Checks

- 旧术语/旧口径扫描：总账该行 `grep -c "为当前唯一 active"` 应为 0；归档副本 `grep -c "根目录 \`AI启动提示词.md\`"` 应为 0。
- 旧组件/旧布局扫描：不适用。
- 文档引用检查：`check-doc-references` 本事务新增断链 0；基线 5 处 g11 断链属 `6117bbb`。
- 其他相关闸门：`gate-all` 与迁移前逐项一致；`check-change-contract` PASS。
- **注册表可解析性**：`node -e "require('./scripts/ref-registry.json')"` 必须成功（改字符串不得破坏 JSON）。
- **读者面实证**：`grep -rln "ref-registry" scripts/ .github/` 的实际命中数须与新 `description` 不再矛盾（新自述不写死条数，故不会因读者增减而失真）。

## 十、Negative / Semantic Counterexample

**反例 1（换汤不换药）**：把总账那一行改成「以目录为准」，但同一张表的其他行仍硬编码事务状态。此时单行 `grep` 全绿，漂移源只是换了一格。
**拦截层**：人工通读该表，确认**整表不再出现事务号**；本事务的 §九 判据只覆盖已定位的那一行，故此处明确标注为"需人工确认项"，不假装机检能全覆盖。

**反例 2（把归档当现行规则回填）**：顺手把 `outputs/archive/AI启动提示词.md` 的旧规则正文也改成现行口径。形式上是"纠正错误"，实质违反「历史只作证据、不得回填」。
**拦截层**：§六 Preserve 明确只改两处路径；改动后 `git diff --stat` 必须显示该文件**只动 2 行**——行数即判据。

**反例 3（注册表自述又写死条数）**：新 `description` 写成"被 6 个脚本读取"。今天对、明天错。
**拦截层**：§三 第 2 条要求自述**不写死条数**；§九 的人工复核确认 `description` 中不含具体数量断言。

## 十一、Real Output Verification

**声明（二选一，机器可读，必填）**：本事务到底需要什么真实产物，由本事务自己声明，机器不猜。

```text
- Real Output 声明：`not-applicable`
- 理由：本事务只改三处陈述性文字（一处总账状态列、一处注册表自述、两行归档内路径），不产出、不修改任何视频画面 / 分镜 / Remotion 组件 / 渲染产物，不存在可验收的真实画面。
```

- 真实关键帧/短片：不适用
- 人工验收判据：三处新陈述都指向真实存在的对象；归档文件 `git diff --stat` 只动 2 行
- 结果：PASS（not-applicable）

## 十二、Closure Report

- 已修改：`docs/internal/PROJECT-CONSISTENCY-MAP.md`「变更事务」行；`scripts/ref-registry.json` 的 `description`；`outputs/archive/AI启动提示词.md` 第 4、15 行
- 已废止：总账中「004 为当前唯一 active」的人工断言；注册表「仅被文档引用与事实闸门读取」的误述；归档中指向已不存在文件的路径
- 已保留：注册表全部 12 个顶层键与数据（只改自述字符串）；归档历史正文（仅动 2 行）；`R10` 第 67 行约束继续遵守
- 影响面：`0 / 0`
- 旧口径扫描：PASS —— `grep -c "为当前唯一 active" docs/internal/PROJECT-CONSISTENCY-MAP.md` = 0；`grep -c "根目录 \`AI启动提示词.md\`" "outputs/archive/AI启动提示词.md"` = 0；注册表 `description` 已不再写死读者条数
- 机械检查：PASS —— `node scripts/check-change-contract.mjs` PASS；`check-doc-references` 本事务新增断链 0；`gate-all` 与迁移前逐项一致；`node -e "require('./scripts/ref-registry.json')"` 成功（顶层键 12）
- 负向测试：PASS（**行数即判据**）—— 归档文件 `git diff --stat` = 2 insertions / 2 deletions，证明未回填历史正文，§十 反例 2 可被检出
- 语义反例：PASS —— §十 三个反例已构造；其中反例 1（同表其他行仍硬编码事务状态）**明确标注为需人工确认项**，未假装机检可全覆盖
- 真图/成片：N/A（not-applicable，见 §十一）
- 本次新增红：0 —— 基线 2 道红（文档引用 / 母题卡）归属 `6117bbb` 的 g11 删除

**结论：CLOSED**

**关闭依据（2026-09-17 15:05，用户确认「都处理掉」）**：机械验证按 `CHANGE-20260917-012` 修定的口径执行——在候选状态 `f065a35` 的干净检出（工作树 0 改动、APPLET 快照 298 文件）上按两个工作流原样步骤重跑：`change-contract-gate` 全绿；`visual-shot-contract` 仅硬步骤 `gate-all` 红（2/15，两道红逐条归因 g11 线：文档引用 5 处断链 + 母题卡 g10 缺口），**本笔新增红 0**。移档后的最终状态另行复验一次（见当日日志）。

**未完成事项**：
1. 等用户确认。
2. 未做（只记）：`check-doc-references.mjs` 按 basename 解析、不校验路径这一**解析粒度缺陷**（"路径错但文件名存在"抓不到）——本事务只纠正了三处**实例**，未修**成因**；修它属独立事务（且 `CHANGE-20260916-005` 未关闭，其 §十二 证据依赖当前检查器行为，不宜同期改动）。
3. g11 线整条挂起，等用户落实文档后重建。

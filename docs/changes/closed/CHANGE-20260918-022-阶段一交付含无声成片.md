# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260918-022`
- 标题：阶段一交付＝逐屏单张 ＋ 一版无声成片（SKILL:216 口径收敛）
- 负责人：AI（用户 2026-09-18 10:33 拍板：先立这笔极小事务，把 SKILL:216 改实，改完 gate-all 验）
- 范围：`global`
- 状态：`CLOSED`

## 二、Goal

消除 `SKILL.md` 第 6 步交付行（第 216 行）「**不渲无声版**」与同段其余正文之间的内部冲突——该句诞生于 2026-08-29「质量整改 / SKILL v6 收敛」（`2607161`，与效果尺子同一提交），其后 2026-09-03（`552cc57`）、2026-09-07（`9759284`）两次修订只改「取哪一帧」，**未见任何拍板撤销它**；而它同段并存的口径都要求能看整片：

- 零条 §3：「一条视频的成败只由两件事决定——`check-motion` 的数字 + 你看真图的判断」；
- 第 6 步叙事 8 问第 **⑤** 问：「全片 100 秒内有无持续变化？」；
- 第 6 步量化表定标样本用的是「**G04 成片** 63%」「参考片 95%」，`gate-all` 效果尺子的机检对象也是「**最新成片**」。

→ 把阶段一交付写实为「逐屏单张 ＋ 一版无声成片」，使量化与 8 问第⑤问有可测对象，验收链闭环。

## 三、新口径 New Policy

1. **阶段一（无声）交付物＝逐屏单张真图 ＋ 一版无声成片**（2026-09-18 用户拍板："无声版视频这个还是要渲染的，我要看整体效果"）。
2. 无声成片归档路径：`outputs/gXX-行业/gXX-行业-无声成片.mp4`（沿用 g10 既有 `-有声成片.mp4` 命名族）。
3. **量化与 8 问跑在这一版上**：`check-motion` 三项（静止占比／中位帧间差／画面占用率）与叙事 8 问第 ⑤ 问（全片持续变化）都以无声成片为对象，**不得以"只有静帧"推迟**。
4. **仍不拼九宫格**——九宫格不能替代单张（单张才能逐屏判排版/层级）；此禁令保留。
5. **无声成片不是发布终态**：发布终态仍是有声成片，属阶段二（逐句 TTS→loudnorm→`atempo 1.2`→回填 dur）。
6. 交付帧仍取屏尾「全内容帧」（C-06 不变）；C-14（交付帧即发布稿）不变。

## 四、Replace

| 旧口径 | 位置 | 新口径 |
|---|---|---|
| 「无声版阶段渲逐屏单张交你看（不拼九宫格、**不渲无声版**）」 | `SKILL.md:216`（第 6 步交付行） | 「无声版阶段渲**逐屏单张 ＋ 一版无声成片**交你看（**不拼九宫格**——九宫格不能替代单张）」＋ 新增一条：无声成片为整体节奏验收对象（量化＋8 问⑤跑在它上面），且非发布终态 |

## 五、Remove

- 删除「**不渲无声版**」这一**禁止性**表述（含其后的旧话头）；同句改为正面规定。
- 旧话头「不渲无声版」登记进 `scripts/ref-registry.json` 的 `deprecatedTerms`（防回流机检，消费端由 CHANGE-20260917-021 接入）。
- **不删**「不拼九宫格」（仍禁止九宫格拼图）。
- **不新增**任何闸门 / 脚本；`scripts/**` 代码零改动。

## 六、Preserve

- C-06（交付帧取屏尾全内容帧）、C-14（交付帧即发布稿）、C-07 ②（入场/中段过程帧逐屏抽查）。
- 阶段二全流程与参数（逐句 TTS → loudnorm → ffprobe 实测 → `atempo 1.2` → dur/字幕回填 → 盲听）。
- 量化三线阈值（静止 ≤45%／中位帧间差 ≥0.35／占用率 ≥70%）及其定标纪律（换机器/改采样须重新定标）。
- 渲染规程（`NODE_OPTIONS=""` ＋ `--browser-executable` 指向 Chrome Headless Shell）。
- g10 及更早片的历史产物与命名；`outputs/g11-烧烤/` 既有产物与真图。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `SKILL.md` 第 6 步交付行 | 流程 Owner | REPLACE（1 处表述） | 全仓 grep「不渲无声版」＝0；`check-doc-references` 硬失败 0（SKILL 在扫描面内） | DONE |
| `scripts/ref-registry.json` `deprecatedTerms` | 检查器数据 | ADD（1 条登记：旧话头） | 负向测试 V4：扫描面写回旧话头即硬失败；全扫描面 0 误报 | DONE |
| `outputs/g11-烧烤/g11-烧烤-无声成片.mp4` | 产物 | PRESERVE（本事务只将其由"越界产物"转为合规交付物，不改字节） | 时间戳晚于 `video/src/**` 最新改动；`ffprobe` 1080×1920 / 96.66s / 2898 帧 | DONE |
| `docs/changes/template.md` | 契约结构 Owner | PRESERVE | 核查结论：本事务不改契约结构，无同步事项 | DONE |
| 全闸门基线 | 机制 | PRESERVE | `gate-all` 红灯构成不超改前（2 道：文字安全区探针·背景纹理假红 ＋ 效果尺子·设计阶段红），本次新增红 0 | DONE |

## 八、Migration Plan

1. 改 `SKILL.md` 第 6 步交付行（§四 所列 1 处）＋ 追加无声成片条款 1 条。
2. `scripts/ref-registry.json` 登记旧话头 1 条。
3. 全仓 grep 复核：「不渲无声版」零残留；「不拼九宫格」「check-motion」「atempo 1.2」等保留项仍在位。
4. 跑负向测试 V1~V4 并记录退出码；跑 `gate-all`（含 `--tsc`）与 `check-change-contract`。
5. 填 §十二 Closure Report 后移档 `closed/`。

## 九、Mechanical Checks

- 旧口径扫描：全仓 grep「不渲无声版」——**改前 1 处（`SKILL.md:216`）→ 改后 0 处**；登记表新增 1 条 term，由 021 的消费端机检。
- 旧组件/旧布局扫描：不适用（不动视觉与工程代码）。
- 文档引用检查：`check-doc-references` 无新增非 g11 类命中（g11 相关命中已于 4/4.5 步清零）。
- 其他相关闸门：`check-change-contract` PASS；`gate-all` 红灯不超改前。
- **验证/负向测试**：
  - **V1（正面，新口径在位）**：`SKILL.md` 内「无声成片」命中 ≥1 且与「逐屏单张」同句。
  - **V2（不误删）**：`SKILL.md` 内「不拼九宫格」仍在位（证明只删了该删的）。
  - **V3（保留项未动）**：`SKILL.md` 内 `check-motion`、`atempo 1.2`、C-06/C-14 引用仍在位。
  - **V4（防回流）**：在扫描面（`docs/internal/`）临时文件写回旧话头 → `check-doc-references` 硬失败并命中该文件；随后删除临时文件复跑，回到基线。

## 十、Negative / Semantic Counterexample

- **反例①（同义改写绕拦）**：把「不渲无声版」改写成「阶段一暂不出片」「先不渲」等同义表述 → 本层按 term 字面拦，**抓不住**；由人工交付判据（**是否能看到整片**）覆盖。本节如实声明该边界，不声称字面层可拦一切变体。
- **反例②（把无声成片当发布终态）**：渲了无声成片后直接当成品发布、跳过阶段二与 TTS → **本事务明确「无声成片≠发布终态」**（§三.5），由阶段二流程与发布稿流程承担；字面层拦不住，如实声明。

## 十一、Real Output Verification

```text
- Real Output 声明：`applicable`
- 适用证据：`outputs/g11-烧烤/g11-烧烤-无声成片.mp4`——本事务新口径的首个实例（既有产物，2026-09-18 10:08 渲；只被"合法化"，字节未改）。证明新口径可执行且交付物真实存在：`ffprobe` = 1080×1920 / h264 / 30fps / 96.66s（2898 帧）/ 22.2MB；文件 mtime 晚于 `video/src/**` 最新源码改动（成片非过期证据）。
```

- 真实关键帧/短片：`outputs/g11-烧烤/frames/` 7 张逐屏单张（S1~S7，屏尾全内容帧）＋ 本事务依据的无声成片。
- 人工验收判据：① `SKILL.md:216` 新句四要素齐备（逐屏单张／无声成片／量化与 8 问⑤跑在这一版／有声留阶段二）；② 全仓旧话头零残留且登记可机检拦回流；③ 无声成片可播放、时长与源码 dur 一致。
- 结果：PASS

## 十二、Closure Report

- 已修改：`SKILL.md` 第 6 步交付行（1 处 REPLACE：旧句「…（不拼九宫格、不渲无声版）…」→「…**逐屏单张 ＋ 一版无声成片**…（**不拼九宫格**——九宫格不能替代单张）…」）＋ 追加「阶段一那版无声成片」条款 1 条；`scripts/ref-registry.json`（`deprecatedTerms` 新增 1 条＝「不渲无声版」）
- 已废止：「不渲无声版」旧交付口径（2026-08-29 `2607161` 质量整改批写入；已登记进 `deprecatedTerms`，由 021 的消费端机检拦回流）
- 已保留：C-06 / C-14 / C-07 ②、「不拼九宫格」禁令、阶段二全流程（逐句 TTS→loudnorm→ffprobe→`atempo 1.2`→回填）、量化三线阈值与定标纪律、渲染规程（`NODE_OPTIONS=""` ＋ `--browser-executable`）、g10 及更早片历史产物
- 影响面：`5 / 5`
- 旧口径扫描：PASS —— 全扫描面（`docs/internal/*.md` ＋ 11 份 `extraDocs`）「不渲无声版」残留 **0 处**（改前 1 处＝`SKILL.md:216`）
- 机械检查：PASS —— `check-change-contract` PASS（24 笔）；`gate-all --tsc` **14 通过 / 16**，红灯 2 道**均为本事务之前既存且已定性**：① 文字安全区探针（g11 高对比炭火纹理被判墨级像素＝**假红**，实证：裁 y1740-1940 区间无任何字形）；② 效果尺子（g11 无声成片实测 静止 57%／中位帧差 0.21／占用率 51%，属**设计阶段红**，按尺子自身条文「设计阶段可带此红继续，交付前必须重渲重测」可继续）；**本次新增红 0**
- 负向测试：PASS —— V1「无声成片」命中 2 处且与「逐屏单张」同句；V2「不拼九宫格」命中 1 处在位（只删该删的）；V3 `check-motion`／`atempo 1.2`／C-06／C-14 引用命中 10 处在位；V4 扫描面写回旧话头 → `check-doc-references` **硬失败（exit 1，命中 `docs/internal/_tmp-022-neg.md:3  «不渲无声版» — 旧口径话头回流`）**，删临时文件复跑回 PASS
- 语义反例：PASS（两条边界如实声明：同义改写绕拦、无声成片被当发布终态——见 §十，均不声称字面层可拦）
- 真图/成片：PASS（`applicable`）—— `outputs/g11-烧烤/g11-烧烤-无声成片.mp4`：1080×1920 / h264 / 30fps / 96.66s / 2898 帧 / 22.2MB，mtime 晚于 `video/src/**` 最新源码改动
- 本次新增红：**0**

**结论：CLOSED**

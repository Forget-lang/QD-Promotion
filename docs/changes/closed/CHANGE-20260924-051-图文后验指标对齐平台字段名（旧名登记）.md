# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-051`
- 标题：图文后验指标按平台字段名对齐（封面点击率／文案完读率）＋ g11 图文行「已回填」＋ 7d 复盘结论入台账
- 负责人：用户（方向）／AI-B
- 范围：`project`
- 状态：`CLOSED`
- 触发：用户 2026-09-24「剩下的都按照你的建议来执行吧」（含对"进入率↔封面点击率、翻完率↔文案完读率"映射的认可）

## 二、Goal

把图文后验指标从**我们自造的名字**换成**平台能直接读到的字段名**，让"回填"这件事在抖音面板上可执行；同时把 g11 的 7d 复盘结论落进台账。

## 三、新口径 New Policy

图文后验三指标＝**封面点击率／文案完读率／赞藏评**（＝平台「流量分析」页字段名；`SKILL.md` 第 7 步为属主）。旧名（进入率（封面点击）／翻完率）自本事务起**登记废止**，不得在现行文档出现。

## 四、Replace

| # | 位置 | 旧 | 新 |
|---|---|---|---|
| 1 | `SKILL.md` 第 7 步 图文段 | "**后验指标（图文版）**：进入率（封面点击）/翻完率/赞藏评" | "**封面点击率／文案完读率／赞藏评**（＝平台「流量分析」页字段名，2026-09-24 对齐）" |
| 2 | `scripts/check-release-feedback.mjs`（注释＋`METRIC_FIELDS`） | 图文＝`['进入率','翻完率','赞藏评']` | 图文＝`['封面点击率','文案完读率','赞藏评']` |
| 3 | `outputs/发布后验台账.json` 图文行 | 键名 `进入率`／`翻完率`；状态"已发布-待回填" | 键名 `封面点击率`（0.00%）／`文案完读率`（16.67%）；状态**已回填**；新增 `复盘结论` |
| 4 | 同上 视频行 | 无 `复盘结论` | 新增 `复盘结论`（首个瓶颈＝前 2 秒；封面CTR 平台未解锁） |
| 5 | `scripts/ref-registry.json` `deprecatedTerms` | 无 | 新增两条：`进入率（封面点击）`／`翻完率`（含废止说明与指向） |

## 五、Remove

**Remove**：旧指标名在三处正文（SKILL／闸门／台账键）。

**不并入**：S3 批（下一笔）；4 条待人工引用。

## 六、Preserve

`spec/` 其它字段；`../applet`；`docs/changes/**`；视频行的四数（封面CTR 保持缺省＝平台未解锁，不伪填）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `SKILL.md`（1 行） | 方法 Owner | REPLACE | 复扫旧名归零＋闸门 | 批 1 |
| `check-release-feedback.mjs` | 闸门 | REPLACE | 复跑：图文行按新键校验 | 批 1 |
| `发布后验台账.json` | 状态源 | REPLACE＋ADD | 闸门输出"已回填 1／待回填 1" | 批 1 |
| `ref-registry.json`（2 条登记） | 声明源 | ADD | 负向：旧名写回扫描面 → 硬失败 | 批 1 |

## 八、Migration Plan

1. 改 SKILL／闸门／台账／注册表 → 跑 `check-release-feedback` ＋ `check-doc-references`。
2. 负向测试：把旧名写回 `SKILL.md` → 闸门须硬失败 → 还原。
3. 复跑 `gate-all`；收口提交。

## 九、Mechanical Checks

- `check-release-feedback`：**3 行（历史豁免 1／已回填 1／待回填 1）**——图文行完成回填。
- 登记预检：旧名在扫描面 0 命中（登记前实测）。
- 负向：`SKILL.md:178` 写回「翻完率」→ `check-doc-references` **EXIT=1** 并点名"旧口径话头回流"；还原后回绿。
- `gate-all` 13/15、2 项跳过、无红灯。

## 十、Negative / Semantic Counterexample

- **负向（换名不换实）**：只把台账键改名、SKILL 不改 → 两处口径分叉。拦截：§四 三处同批改＋复扫。
- **语义反例**：为凑"已回填"而给视频行编一个封面CTR 值——平台明确"播放量超过200后展示"，当前 50。拦截：视频行保持缺省＋备注写明平台门槛。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝指标命名对齐与台账回填，不产出关键帧或成片。
```

## 十二、Closure Report

- 已修改：`SKILL.md`（第 7 步图文指标名）、`scripts/check-release-feedback.mjs`（注释＋`METRIC_FIELDS`）、`outputs/发布后验台账.json`（图文行键名与值＋两行 `复盘结论`＋图文行置`已回填`）、`scripts/ref-registry.json`（`deprecatedTerms` ＋2 条）
- 已废止：「进入率（封面点击）」「翻完率」作为图文后验指标名
- 已保留：视频行四数缺省（封面CTR 平台未解锁）；`../applet`、`spec/` 其它字段、`docs/changes/**`
- 影响面：`4 / 4 已处理`
- 旧口径扫描：**PASS**（旧名在扫描面 0 命中；负向测试证明登记生效）
- 机械检查：**PASS**（`check-release-feedback` 3 行〔豁免1／已回填1／待回填1〕；`check-doc-references` 0；`gate-all` 13/15、无红灯）
- 负向测试：**PASS**（旧名写回 → EXIT=1 点名；还原 → 绿）
- 语义反例：**PASS**（视频封面CTR 未伪填，备注写明平台门槛）
- 真图/成片：N/A
- 本次新增红：**0**

**结论：CLOSED**

# CHANGE-20260916-003 · 全局治理阵型收口

## 基本信息

- 状态：`MIGRATING`
- Issue：#3
- Owner：`AGENTS.md`（全局治理）；本事务本身由 Change Contract 承载，不新增治理 Owner。
- 唯一 AI 自动入口：`AGENTS.md`
- 用户工作控制台：`AI工作启动指令.md`（个人操作工具，不是规则 Owner，不要求任何权威文档引用）
- 目标：把“先统筹全局、后局部执行”的原则升级为仓库最高治理戒律，并修正文档角色误判，降低维护成本。

## Goal

把“规则 / Owner / 入口 / 当前 / 历史”五类边界彻底厘清，并明确“用户怎样启动 AI 工作”与“AI 进入仓库后怎样接管”是两个不同角色：前者只是个人工作控制台，后者才是仓库自动入口。

## 新口径 New Policy

> **先察全局，后定阵型；先立中军，后分兵执行；先清旧阵，后布新阵；任何局部修改，不得破坏全局唯一性、边界、Owner、入口和历史/当前分层。**

本事务确认：

1. 任何规则必须有明确落点。
2. 任何落点只能有一个 Owner。
3. 任何引用只能指向 Owner；非 Owner 只能解释、引用、导航或提供证据。
4. 任何替代必须清理旧阵；不能长期新旧并存。
5. 任何历史必须退出当前指挥链。
6. 任何全局改动必须先完成战场侦察与影响面布阵，再进入 Migration。
7. `AI工作启动指令.md` 是**用户自己的工作控制台**，只提供开工口令与任务模板，不定义项目规则，也不承担项目入口职责；`AGENTS.md` 是**AI 进入仓库后的唯一自动入口**。

## Replace

- 将“`outputs/archive/AI启动提示词.md` 是当前启动模板 / 当前接班入口”的旧角色认知，替换为“根目录 `AI工作启动指令.md` 是用户个人工作控制台”。
- 将“用户启动模板”和“AI 项目入口属于同一类”的混淆，替换为“个人操作工具 vs AI 自动入口”。
- 将 `docs/AI使用手册.md` 中旧的“启动提示词已退休”口径，改为明确区分：归档副本只用于历史追溯；根目录当前控制台供用户使用。

## Remove

- 删除 `docs/changes/active/CHANGE-20260916-003-全局治理阵型收口-说明.md` 重复事务副本。
- 删除当前文档与归档说明中把用户控制台当成 AI 自动入口的表述。
- 删除“当前用户必须依赖权威文档反向引用启动模板”的设计要求。
- 不删除历史正文证据；历史通过 Git 可追溯。

## Preserve

- `AGENTS.md` 仍是 AI 自动入口。
- `SKILL.md` 仍是唯一视频生产作业流程。
- R2–R10、spec 与其他现有 Owner 不改职责。
- `PROJECT-CONSISTENCY-MAP.md` 仍只作状态总账。
- `outputs/archive/` 与 changelog 仍承担历史证据与变更记录。
- R8 / 整改总纲仍是全局变更的治理覆盖层，不新增第二治理体系。

## Impact Map

| 资产 | 动作 | 结果 |
|---|---|---|
| `AI工作启动指令.md` | ADD / RENAME | 当前用户个人工作控制台；不进入自动入口链，不成为规则 Owner |
| `docs/AI使用手册.md` | REPLACE | 明确个人工作控制台 vs AI 自动入口的职责边界 |
| `outputs/archive/AI启动提示词.md` | PRESERVE | 保留历史前身快照，不进入当前指挥链 |
| `outputs/archive/README.md` | REPLACE | 说明旧名文件与当前控制台的历史关系 |
| `CHANGE-20260916-003-全局治理阵型收口-说明.md` | REMOVE | 删除重复事务控制面 |
| `AGENTS.md` | PRESERVE | 自动入口保持唯一，不要求引用个人工作控制台 |
| `SKILL.md` / R2–R10 / `spec/` | PRESERVE | 现有 Owner 职责不变 |
| `PROJECT-CONSISTENCY-MAP.md` | PRESERVE | 仍为状态索引 |
| `outputs/archive/changelog.md` | REPLACE | 本轮最终收口时补充变更记录 |

## Migration Plan

1. 完成文档角色全局侦察：确认用户、AI、规则 Owner、状态账、历史证据的不同职责。
2. 将原“AI启动提示词”当前接口重命名为 `AI工作启动指令.md`，内容重写为真正的个人工作控制台。
3. 明确控制台只提供“怎么叫 AI 开工”的模板，不复述项目规则，不要求权威文档反向引用。
4. 重写 `docs/AI使用手册.md` 的角色分工与换会话说明。
5. 将归档旧名文件保留为历史前身，并同步 archive README。
6. 删除重复 Change 说明文件。
7. 跑机械 Gate、负向测试与语义反例。
8. 补 changelog；根据最终证据生成 Closure Report。

## Mechanical Checks

- `node scripts/check-change-contract.mjs`
- `node scripts/check-doc-references.mjs`
- `node scripts/gate-all.mjs`
- 全库搜索当前入口类表述，确认没有把 `AI工作启动指令.md` 当作 AI 自动入口或规则 Owner。
- 检查 `AI工作启动指令.md` 不包含 Owner 规则正文，只包含用户任务模板、启动动作和自证要求。
- 检查 `outputs/archive/AI启动提示词.md` 只作为历史前身保留。

## Negative / Semantic Counterexample

- Negative test 1：Change Contract 对故意缺失结构 / PENDING Impact Map 的伪事务必须失败；现有 negative test 已验证。
- Negative test 2：把 `AI工作启动指令.md` 描述成“项目规则唯一真源”应被文档角色审计判为错误，因为它没有规则 Owner 权限。
- Negative test 3：把 `outputs/archive/AI启动提示词.md` 当成当前工作入口应被历史边界检查判为错误。
- Semantic counterexample：故意把个人工作控制台改名为“AI作业规范”，但内容仍只是“给 AI 的工作口令”；名称变化不能让它获得规则 Owner 权限，必须仍回当前 Owner 判断。

## Real Output Verification

- 本事务涉及的是协作接口与文档角色，不直接改变视频成片视觉语义。
- 仍必须通过当前 `gate-all`；涉及生产回归时，以最新真实关键帧/成片 Gate 结果为准。

## Closure Report

当前尚未 CLOSED。

关闭前必须记录：

```text
Change ID：CHANGE-20260916-003
目标：全局治理阵型与文档角色收口

唯一 AI 自动入口：AGENTS.md
用户工作控制台：AI工作启动指令.md

已修改：待最终对账
已废止：archive 中 Prompt 当前入口角色、重复 Change 说明副本、控制台必须被权威文档反向引用的旧设计
影响面：待最终 fresh 对账
Owner/入口冲突：待 fresh 验证
旧口径扫描：待 fresh 验证
机械检查：待 fresh 验证
负向测试：PASS / 待补语义样例
语义反例：待验证
真图/成片：不适用本次文档角色变更；Gate 仍需 fresh
本次新增红：待 fresh 验证

结论：NOT CLOSED
```

# CHANGE-20260916-003 · 全局治理阵型收口

## 基本信息

- 状态：`MIGRATING`
- Issue：#3
- Owner：`AGENTS.md`（全局治理）；本事务本身由 Change Contract 承载，不新增治理 Owner。
- 唯一 AI 自动入口：`AGENTS.md`
- 用户操作接口：`AI启动提示词.md`（只负责指挥模板，不是规则 Owner）
- 目标：把“先统筹全局、后局部执行”的原则升级为仓库最高治理戒律，并修正文档角色误判，降低维护成本。

## Goal

把“规则 / Owner / 入口 / 当前 / 历史”五类边界彻底厘清，并让用户指挥 AI 的 Prompt 模板与 AI 进入仓库后的自动入口形成明确的上下游关系，而不是互相竞争或被误判为历史文件。

## 新口径 New Policy

> **先察全局，后定阵型；先立中军，后分兵执行；先清旧阵，后布新阵；任何局部修改，不得破坏全局唯一性、边界、Owner、入口和历史/当前分层。**

本事务确认：

1. 任何规则必须有明确落点。
2. 任何落点只能有一个 Owner。
3. 任何引用只能指向 Owner；非 Owner 只能解释、引用、导航或提供证据。
4. 任何替代必须清理旧阵；不能长期新旧并存。
5. 任何历史必须退出当前指挥链。
6. 任何全局改动必须先完成战场侦察与影响面布阵，再进入 Migration。
7. `AI启动提示词.md` 是**用户当前操作接口**，只指路，不定义规则；`AGENTS.md` 是**AI 进入仓库后的唯一自动入口**。

## Replace

- 将“`outputs/archive/AI启动提示词.md` 是启动提示词 / 当前接班入口”的旧角色认知替换为“根目录 `AI启动提示词.md` 是用户操作接口”。
- 将“AI 启动提示词与 AI 项目入口属于同一类”的混淆替换为“用户侧操作接口 vs AI 侧自动入口”。
- 将 `docs/AI使用手册.md` 中“归档启动提示词不作为当前作业入口”的单一口径，改为明确区分：归档副本不作当前入口，但根目录当前 Prompt 模板供用户使用。

## Remove

- 删除 `docs/changes/active/CHANGE-20260916-003-全局治理阵型收口-说明.md` 重复事务副本。
- 删除当前文档与归档文件中会把历史 Prompt 当作当前操作入口的表述。
- 不删除历史正文证据；历史通过 Git 可追溯。

## Preserve

- `AGENTS.md` 仍是 AI 自动入口。
- `SKILL.md` 仍是唯一视频生产作业流程。
- R2–R10、spec 与其他现有 Owner 不改职责。
- `PROJECT-CONSISTENCY-MAP.md` 仍只作状态总账。
- `outputs/archive/` 与 changelog 仍承担历史证据与变更记录。
- R8 / 整改总纲仍是全局变更的治理覆盖层，不新增第二治理体系。

## Impact Map

| 资产 | 动作 | 结果 |
|---|---|---|
| `AI启动提示词.md` | ADD / RESTORE | 恢复当前用户 Prompt 操作接口 |
| `docs/AI使用手册.md` | REPLACE | 消除入口/角色混淆 |
| `outputs/archive/AI启动提示词.md` | REPLACE | 固化为历史快照，不进入当前指挥链 |
| `outputs/archive/README.md` | REPLACE | 明确根目录当前模板 vs 归档历史快照 |
| `CHANGE-20260916-003-全局治理阵型收口-说明.md` | REMOVE | 删除重复事务控制面 |
| `AGENTS.md` | PRESERVE | 自动入口保持唯一；后续只需补用户接口指针 |
| `SKILL.md` / R2–R10 / `spec/` | PRESERVE | 现有 Owner 职责不变 |
| `PROJECT-CONSISTENCY-MAP.md` | PRESERVE | 仍为状态索引 |
| `outputs/archive/changelog.md` | REPLACE | 本轮最终收口时补充变更记录 |

## Migration Plan

1. 完成文档角色全局侦察：确认用户、AI、规则 Owner、状态账、历史证据的不同职责。
2. 恢复根目录 `AI启动提示词.md`，内容只承担场景路由与用户操作模板。
3. 重写 `docs/AI使用手册.md` 的角色分工与换会话说明。
4. 将归档同名文件降级为历史快照并同步 archive README。
5. 删除重复 Change 说明文件。
6. 补足 `AGENTS.md` 对当前用户操作接口的指针说明；不改变唯一 AI 自动入口。
7. 跑机械 Gate、负向测试与语义反例。
8. 补 changelog；根据最终证据生成 Closure Report。

## Mechanical Checks

- `node scripts/check-change-contract.mjs`
- `node scripts/check-doc-references.mjs`
- `node scripts/gate-all.mjs`
- 全库搜索旧表述：`outputs/archive/AI启动提示词.md` + “当前入口 / 当前启动入口 / 启动模板”组合命中必须只剩历史说明或迁移证据。
- 检查根目录 `AI启动提示词.md` 不包含 Owner 规则正文，只包含路由/操作模板。

## Negative / Semantic Counterexample

- Negative test 1：Change Contract 对故意缺失结构 / PENDING Impact Map 的伪事务必须失败；当前既有 negative test 已 PASS。
- Negative test 2：把 `AI启动提示词.md` 描述成“项目规则唯一真源”应被文档角色审计判为错误，因为规则必须回 Owner。
- Negative test 3：把 `outputs/archive/AI启动提示词.md` 当成当前启动入口应被历史边界检查判为错误。
- Semantic counterexample：故意把 Prompt 模板改名为“AI 作业规范”，但内容仍只是一组用户口令；名称变化不能改变其“用户操作接口”角色，也不能让它取得规则 Owner 权限。

## Real Output Verification

- 文档角色变更本身不改变视频成片，不新增视觉语义。
- 仍必须通过当前 `gate-all`；若涉及生产回归，以最新真实关键帧/成片 Gate 结果为准。

## Closure Report

当前尚未 CLOSED。

关闭前必须记录：

```text
Change ID：CHANGE-20260916-003
目标：全局治理阵型与文档角色收口

唯一 AI 自动入口：AGENTS.md
用户操作接口：AI启动提示词.md

已修改：待最终对账
已废止：archive 中 Prompt 当前入口角色、重复 Change 说明副本
影响面：待最终 fresh 对账
Owner/入口冲突：待 fresh 验证
旧口径扫描：待 fresh 验证
机械检查：待 fresh 验证
负向测试：PASS / 待补其余语义样例
语义反例：待验证
真图/成片：不适用本次文档角色变更；Gate 仍需 fresh
本次新增红：待 fresh 验证

结论：NOT CLOSED
```

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-066`
- 标题：贴图方法章节去重、路由顺序纠正与启动场景标题收口
- 负责人：项目 AI
- 范围：`global`
- 状态：`VERIFYING`

## 二、Goal

清理当前文档冷读发现的三个结构性/表述残留：独立贴图方法文档重复定义同一 `〇-B`、`〇-A` 与 `〇-B` 阅读顺序仍不符合既定路由、启动指令场景 G 标题仍保留“按内容线”旧措辞，使当前文档状态与已生效的内容职责模型完全一致。

## 三、新口径 New Policy

1. `docs/internal/贴图宣传作业规范.md` 中“与既有抖音图文线的边界”必须先于“方法层与风格层的边界”，阅读顺序为 `〇-A → 〇-B`。
2. 独立贴图方法/风格边界只保留一个当前正文位置，不存在第二份 `〇-B`。
3. `AI工作启动指令.md` 场景 G 标题使用“当前内容职责”而非已过时的“内容线”表述；正文路由已采用内容职责，不再制造标题/正文不一致。

## 四、Replace

| 资产 | 旧口径 | 新口径 |
|---|---|---|
| `docs/internal/贴图宣传作业规范.md` | `〇-B` 重复两次，且第一次位于 `〇-A` 前 | 只保留一份 `〇-B`，并将 `〇-A` 放在其前面 |
| `AI工作启动指令.md` §十一 | 场景 G 标题“按内容线进入其 Owner” | 场景 G 标题改为“按内容职责进入其 Owner” |

## 五、Remove

- 贴图方法文档第二份重复的 `〇-B` 正文。
- 启动指令场景 G 标题中的“按内容线”旧措辞。
- 不删除第一份合法 `〇-B` 的内容；不删除已存在的 `〇-A`。

## 六、Preserve

- `AGENTS.md` 唯一入口与当前 Owner 地图。
- 已关闭的 063、064、065 事务，不回写历史。
- 贴图方法 Owner 的具体规则与 `posterStyles` 声明源。
- 启动指令 A～O 场景编号、N/O 双 AI 机制及 §十八、§十九公共收口规则。
- 视频 `contentLines`、`video/styles/` 与 `poster/styles/` 的现有边界。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `docs/internal/贴图宣传作业规范.md` | 静态贴图方法 Owner | REPLACE | 章节计数 + 顺序冷读 + Owner/入口对账 | PLANNED |
| `AI工作启动指令.md` | 用户侧启动控制台 | REPLACE | 旧措辞扫描 + 场景 G 冷读 | PLANNED |
| `AGENTS.md` | 项目级入口/治理 | PRESERVE | 与两个文档交叉路由核对 | PLANNED |
| `docs/internal/R9-视觉导演与审美决策.md` | 导演决策 Owner | PRESERVE | 风格/导演边界核对 | PLANNED |
| `scripts/ref-registry.json` | 声明源 | PRESERVE | `posterStyles` / `contentLines` 不受影响 | PLANNED |

## 八、Migration Plan

1. 在当前 `main` 基线下定位两个重复 `〇-B` 与场景 G 标题。
2. 删除第二份重复 `〇-B`；保留并移动为当前唯一正文的那份内容，使文档顺序成为 `〇-A → 〇-B`。
3. 将启动指令场景 G 标题改为“按内容职责进入其 Owner”。
4. 冷读启动指令、贴图方法 Owner、AGENTS、R9、registry，确认没有产生新的第二 Owner/第二入口。
5. 机械扫描旧表述，并对章节计数和顺序做语义反例验证。
6. 跑现有 `change-contract-gate` 与 `visual-shot-contract`；不创建新 Gate。
7. 形成 Closure Report，按 R8 最终状态迁入 `closed/` 后再次执行最终状态机械验证。

## 九、Mechanical Checks

- `docs/internal/贴图宣传作业规范.md` 的 `## 〇-B、方法层与风格层的边界` 出现次数必须为 1。
- `## 〇-A、与既有抖音图文线的边界` 必须位于 `## 〇-B、方法层与风格层的边界` 之前。
- `AI工作启动指令.md` 不得保留场景 G 标题“按内容线进入其 Owner”。
- 不得新增第二入口/第二 Owner，不得把贴图重新纳入视频 `contentLines`。
- 当前 workflow：`change-contract-gate`、`visual-shot-contract`。

## 十、Negative / Semantic Counterexample

反例 1：把重复 `〇-B` 的标题改成同义词（如“贴图视觉边界”），但保留第二份同职责正文——仍是第二规则位置，应判失败。

反例 2：把场景 G 标题改掉，但正文重新要求贴图进入 `contentLines` / `SKILL.md`——关键词虽干净，路由语义仍错误，应判失败。

反例 3：删除重复章节后又把 `〇-A` 放回 `〇-B` 后面——数量正确但阅读路由仍反向，应判失败。

## 十一、Real Output Verification

- Real Output 声明：`not-applicable`
- 理由：本事务仅清理和对齐项目文档，不修改或生产视频、图片等真实内容产物。

## 十二、Closure Report

- 已修改：`docs/internal/贴图宣传作业规范.md`、`AI工作启动指令.md`
- 已废止：重复的 `〇-B` 正文、`〇-A` 在 `〇-B` 前的错误顺序、场景 G 标题中的“按内容线”旧措辞
- 已保留：`AGENTS.md`、R9、registry、既有场景骨架、已关闭历史事务
- 影响面：`5 / 5`
- Owner/入口冲突：PASS（启动控制台、贴图方法 Owner、R9 与 registry 路由一致）
- 旧口径扫描：PASS（当前候选未出现旧场景 G 标题；贴图方法仅有一份 〇-B，且 〇-A 在其前）
- 机械检查：PASS（`change-contract-gate` run `35983241098` / run number `268` success；最终候选工作流证据对应当前候选）
- 负向测试：PASS（同一 run 的 Negative test step success）
- 语义反例：PASS（当前候选冷读拒绝“改名但仍双定义”“标题改了但正文导回视频”“数量正确但顺序错误”三类变体）
- 真图/成片：N/A
- 本次新增红：0

**结论：NOT CLOSED（待迁入 closed/ 并在最终状态再次跑机械检查）**

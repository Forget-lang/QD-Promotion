# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260921-035`
- 标题：启动指令双内容职责路由收口（含场景 O 两阶段模型修正）
- 负责人：用户（边界与授权）＋ AI-B（唯一执行方）＋ AI-A（独立验收方）
- 范围：`global`
- 状态：`APPROVED`
- 批准记录：三段式，不合并推断。① 用户带标记的常设规则原文「我（用户）的意见：只有你们两边达成统一意见，你就直接修改文档，我基本就是大概过一遍，我也没有细致查看的」；② 用户本轮带标记的授权补充原文「我（用户）的补充：.workbuddy这个只是workbuddy自己存储记忆的文件……这个不用管……**其他问题你们来互相讨论商定**」；③ 双方对本契约边界与波次顺序已达成一致（AI-A 明示"同意建立 CHANGE-20260921-035，而且这不是可选项"）。本契约不代替用户对**具体改法文本**的确认。
- **前置**：`CHANGE-20260920-031`／`032`／`033` 均 `CLOSED`；`CHANGE-20260921-034` 保持 `VERIFYING` 且**不并入本事务**（其缺项为用户追认与异会话冷读，属另一条线）。
- **定位四问**：① 层＝用户侧操作协议层（`AI工作启动指令.md` 自述非 Owner，但为所有会话共用的入口路由）；② Owner＝两条内容职责各自的方法 Owner 不变（行业线 `SKILL.md`、教程线 `docs/internal/产品功能教程作业规范.md`），判线唯一声明源仍是 `scripts/ref-registry.json` 的 `contentLines`；③ 牵动＝单文件 7–8 个编辑点，**零脚本、零注册表、零 spec、零 Owner 正文、零闸门判据**；④ 旧口径＝"单一视频生产流程"世界模型在入口层的残留表述。

### 问题地图（侦察结论，均本机实测）

| # | 事实（证据行号） | 定性 |
|---|---|---|
| A1 | `AI工作启动指令.md:4` 「视频生产**唯一作业流程**是 `SKILL.md`」 | 与 `AGENTS.md` §二／§三 双内容职责主干冲突 |
| A2 | `:87` §三 Owner 路由只列「视频生产：`SKILL.md`」，无教程 Owner | Owner 地图落后 |
| A3 | `:505` 场景 M 分类表只列「视频生产 → SKILL.md」，教程落"其他" | 轻（通用路由仍可达），但入口层宜点名 |
| B1 | `:336-344` 场景 G 正文「只认当前 `SKILL.md`」「读取 `SKILL.md` 当前版本」 | 教程片被硬路由进行业线工作流 |
| B2 | `:392` 场景 I 强制「`SKILL.md` / R3 / R7 / **R9** / 当前分镜 / 当前 R9 产物」 | 与 `产品功能教程作业规范.md:36/:45/:46`（行业线视觉条款不继承、`check-bg`/`check-motif-card` 对本线 N/A、教程视觉语言待 f01 实测）冲突 → 入口层本身成了方法渗透通道 |
| B3 | `:205` 场景 B 八个候选必填字段中至少 5 项为行业线抽象（行业／经营时刻／老板痛点／当前做法／可研究玩法） | 结构缺陷：教程线无对应物（教程规范 §一：不用行业、不用经营痛点，一条教程＝一个使用任务）。删词不解决，须把"选什么"的定义让给内容线 Owner |
| C1 | 全文 `教程` 命中 **0 处** | 教程生产在用户侧控制台无自然入口 |
| C2 | 行业线专有动作名实测：`分镜`=1（`:392`）、`R9`=2（`:94` 属合法导航＋`:392`）、`母题`/`五拍`/`口播`/`爆款` 均 **0**；教程线专有词（`八段骨架`/`使用任务`/`current-verified`/`闸门矩阵`）均 **0** | 真实改动面窄于预估（≈7–8 处）；且启动指令并未复制两线方法正文 |
| E1 | `AI工作启动指令.md` 不在 `ref-registry.json` 的 `extraDocs`；沙箱真跑 `check-doc-references` 实验组：纳入后 **2 处硬失败**（`:24` «§一~»、`:823` «§一～」） | 失败原因是检查器**不解析 § 范围表达式**（与总纲 §4 判例同型），且 `📋` 口令块在 ```text 内被**整块跳过** → 纳入的实际收益远小于预期。**属检查器语义族** |
| D0 | 场景 O 自身 `:714`「不要把问题池攒成一份大方案再动手」 | 与用户确认的两阶段模型冲突（本轮由执行方引入，见 §三 W0） |

## 二、Goal

让 `AI工作启动指令.md` 从"单一视频生产控制台"升级为**多内容线的统一路由与工作控制台**：入口层只负责把用户送到正确 Owner，具体生产方法继续由各线唯一 Owner 持有。同时修正场景 O 的执行模型表述，使"理解与计划一次成形、执行与验收分批"成为该场景的正式口径。

**本事务不做**：不碰 `scripts/`、`ref-registry.json`、两线 Owner 正文、`AGENTS.md`；不治理 `.workbuddy`；不并入 034；不在本阶段决定"是否新增教程专属场景"。

## 三、新口径 New Policy

### 3.1 W0（执行契约修正，先于一切内容波次）

`AI工作启动指令.md:714` 替换为（**该句双方已一致，属已决定文本**）：

> **O 的出口是一份完整的问题地图与整改计划**：问题清单／分组／依赖／优先级／分批顺序／每批验收判据。**分批的只是执行与验收，不是理解。** 计划必须保留"执行期新暴露问题入池、队列重排"的通道；**执行期允许发现新问题，但不得静默改变原问题族的范围**——一旦新事实改变 Owner、影响面、依赖关系或事务边界，即暂停当前波次回 O／M 重侦察。O 不承诺在执行前预见一切。

### 3.2 W1–W2 改法方向（已定；**逐字文本尚未决定，故本契约不预先冻结**）

- **W1 统一路由句**：`:4`／`:87`／`:505` 三处必须复用**同一句**"先判内容线 → 进入该线唯一 Owner"的表述，禁止三处各写一份；表述内不得出现任何把两线压成单线的限定词。
- **W2-G**：由「只认 `SKILL.md`」改为"先识别当前内容线，再进入该线唯一 Owner 并按其执行"。
- **W2-I**：**拆分为两段**——公共工程动作（两线共用：`R3` 工程原理、`R7` 已登记组件、`video/` 注册链、渲染与验证方式）与**线专属前置**（行业线才需要的分镜稿／R9 决策材料）。**禁止把两侧一并抽象成"读当前线 Owner 及其适用参考"**（见 §十 反例 N3）。
- **W2-B**：候选必填字段不再由启动指令枚举，改为"字段由当前内容线 Owner 的选择标准定义"；启动指令只保留与线无关的通用产出要求（依据可验证、不得伪造、不替用户决定）。
- **D 原则（每波第 0 条）**：启动指令不得复制两线任何方法正文；两线专有词命中数在改造后须仍为 **0**。

### 3.3 W2-Q（后置判定，执行方 ≠ 验收方）

判据：只依据**改造后的** `AI工作启动指令.md`，走查用户说一句「我要做一条产品功能教程」，能否 ① 不靠 AI 猜测 ② 不绕道 G／I ③ 不错误继承行业线前置，直达 `docs/internal/产品功能教程作业规范.md`。PASS ⇒ 不新增场景；FAIL ⇒ 才讨论"新增场景"或"通用内容线选择入口"。
**该走查由 AI-A 独立执行并出 PASS／FAIL**，AI-B 不得自测（034 的教训：改者自验＝自证）。

## 四、Replace

| 旧口径 | 替代者 |
|---|---|
| 入口层"视频生产唯一作业流程＝SKILL.md"的单线世界模型 | 先判内容线再取该线唯一 Owner 的双线路由（W1） |
| G／I 把行业线方法与产物当所有视频的公共前置 | 公共工程动作与线专属前置分离（W2） |
| B 用行业线抽象定义候选必填字段 | 字段定义让给内容线 Owner（W2） |
| O「不要攒方案」＝理解也可分批 | 计划要全、执行与验收分批（W0） |

## 五、Remove

- 不删除任何文件、不删除任何 Owner、不新增第二入口。
- 移除的仅是入口层的**单线限定表述**与**线专属前置的跨线扩散**。
- 本事务**不**改 `extraDocs`、**不**改 `check-doc-references.mjs`。

## 六、Preserve

`AGENTS.md`（含 §二 双职责行与 §八 对启动指令"非 Owner"的定性）；`SKILL.md` 全文；`docs/internal/产品功能教程作业规范.md` 全文；`scripts/` 全部与 `ref-registry.json`；`spec/`；`docs/changes/template.md`；`docs/AI使用手册.md`；`outputs/` 与 `video/` 全部产物；场景 A／C／D／E／F／H／J／K／L／M 中与内容线无关的通用协议正文；`CHANGE-20260921-034`（保持独立 `VERIFYING`）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属波次 |
|---|---|---|---|---|
| `AI工作启动指令.md:714` | 场景 O 执行模型 | REPLACE | 与 §3.1 逐字一致；O 节内不再出现"理解不必成形"式表述 | W0 |
| 同文件 `:4`／`:87`／`:505` | 入口路由声明 | REPLACE（三处同批） | 三处复用同一句；单线限定词 grep＝0；未复制两线方法 | W1 |
| 同文件 `:336-344`（G）／`:392`（I）／`:205`（B） | 场景行为 | REPLACE | G 无写死单线 Owner；I 公共／专属两段且保留行业线具体清单；B 字段定义移交线 Owner | W2 |
| 教程入口走查 | 人判判据 | ADD（判定动作，非文件） | AI-A 独立走查出 PASS／FAIL，附其依据的行号 | W2-Q |
| `.workbuddy/**` | 单代理私有记忆层 | PRESERVE（**用户明示排除**） | 本事务与后续一致性治理均不追；其内容陈旧不构成启动指令缺陷 | 全阶段 |
| `scripts/check-doc-references.mjs`＋`ref-registry.json` | 检查器语义 | REPLACE／REMOVE 候选 → **移交 S3** | 本事务不触碰；移交记录见 §十四 | 全阶段 |
| 两线 Owner 正文／`AGENTS.md`／`spec/`／产物 | — | PRESERVE | `git diff --stat` 证明未触碰 | 全阶段 |

## 八、Migration Plan

1. **阶段 A · 基线**：留存改前 `check-doc-references`／`check-change-contract`／`gate-all` 输出（重定向落盘，修正 034 的取证瑕疵），并留存 §一 C2 的词频基线。
2. **W0** 独立落盘 → **提交并公布 HEAD SHA** → AI-A 按第 0 条＋8 条验收 → PASS 才进 W1。
3. **W1**（三处同批）→ 提交 → 验收。
4. **W2**（G／I／B）→ 提交 → 验收。
5. **W2-Q** 由 AI-A 独立走查 → PASS 关闭本事务的入口问题；FAIL 则停线回 O 讨论入口形态。
6. **阶段 D · 收口**：全部波次验收 PASS 且最终状态复跑机械检查后，方得 `CLOSED` 并移入 `closed/`。
7. **每波执行环（本契约内该工作流的唯一表述处）**：AI-B 改 → `commit` → `push` 远端 → 公布**远端 HEAD SHA** 与工作区状态 → AI-A 从该 SHA **只读**验收（不改动、不代提交、不代推送）→ PASS 进下波；FAIL 留本波，不重开 O。权属依据＝用户带标记指令「我（用户的补充：AI-B来执行修改文档，上传git；AI-A读取更改，把关，确认」，故 AI-A 建议的"中性化／逐次确认"写法不采纳（该建议提出于用户裁定之前），`push` 不再是逐波待确认事项。已知取舍：验收发生在已推送内容之上，未定稿会以提交形式出现在远端——此为用户选定的工作流，本事务不另行设闸。
8. **回 O／M 条件**：Owner 归属、影响面、依赖关系或事务边界发生结构性变化；或发现原问题族定义本身被推翻。

## 九、Mechanical Checks

- **第 0 条反证**：两线专有词在启动指令内命中数须为 0——行业线侧 `母题`／`五拍`／`口播`／`爆款`／`分镜` 恒 0（`分镜` 现 1，W2 后须 0），教程线侧 `八段骨架`／`使用任务`／`current-verified`／`闸门矩阵` 恒 0。
- **合法导航例外须显式列出**：`:94` 的 `R9` 属 §三 Owner 路由表，允许保留；不得为满足 grep 而删除路由。
- **单源检查**：W1 引入的统一路由句 grep 命中数＝3 且**逐字相同**；O 两阶段模型表述全文件命中＝1（不产生第二份）。
- **旧口径扫描**：`唯一作业流程`／`视频生产唯一`／"不要攒方案"式句子在文件内归零；`教程` 命中数由 0 变为 ≥1（路由点名），属预期新增。
- **文档引用**：`check-doc-references` 0 硬失败且与阶段 A **逐字一致**（启动指令不在扫描面，此项用于证明其余文档未受影响）。
- **契约闸门**：`check-change-contract` 通过（含本事务）。
- **总闸门**：`node scripts/gate-all.mjs` 0 红，且输出与阶段 A **逐字一致**。
- **口径唯一性**：新增文本不得命中 `deprecatedTerms`（30 条）与 `caliberOwners`（10 条）——本事务已用沙箱试算确认现文命中为 0。

## 十、Negative / Semantic Counterexample

1. **N1（换词不换形）**：把 `:4` 改成"视频生产的**主要**作业流程是 `SKILL.md`"——去掉了"唯一"，但仍是单线世界模型。拦阻层：W1 判据从"唯一作业流程"扩为**任何把两线压成单线的限定词**，并配 §九 的三处逐字一致性检查。
2. **N2（伪分流真复制）**：在 G 或 I 里加一句"若为教程线请改读教程规范"——看似完成分流，实际是把两线方法名与前置清单写进入口层，长期必然与两线 Owner 漂移。拦阻层：D 原则第 0 条 ＋ §九 两线专有词 grep 反证。
3. **N3（过度抽象反弹）**：为兼容教程线把 I 改写成"读取当前内容线 Owner 及其适用参考文档"——一个词都不剩，行业线执行者从此不知道该读 `R9`／分镜稿。拦阻层：§3.2 明文要求 I 拆成"公共工程动作＋线专属前置"两段，验收须确认行业线具体清单**仍然存在且更清晰**。
4. **N4（借道扩面）**：整改中途顺手把 `docs/AI使用手册.md:74`（`docs/changes/` 与 archive 并列为"不进入当前指挥链"）一起改掉——该处真实有问题但属另一族。拦阻层：W0 的"不得静默改变问题族范围"＋ §七 Impact Map 封闭列举。

## 十一、Real Output Verification

- Real Output 声明：`not-applicable`
- 理由：交付物为入口路由与场景文本编辑，不含视觉或内容产物；W2-Q 是路由走查判据（人判），不是真实产物，按 R8 §十 不假装机器可验。

- 真实关键帧/短片：本事务不存在内容产物（见上）
- 人工验收判据：每波由 AI-A 依 §八.7 的 8 条逐条回答；W2-Q 由 AI-A 独立走查并附行号依据。
- 结果：PENDING

## 十二、Closure Report

- 已修改：PENDING
- 已废止：PENDING
- 已保留：PENDING
- 影响面：`0 / 5`
- 旧口径扫描：PENDING
- 机械检查：PENDING
- 负向测试：PENDING
- 语义反例：PENDING
- 真图/成片：PENDING
- 本次新增红：PENDING

**结论：NOT CLOSED**

## 十三、事务边界（与同时进行的其它事务的关系）

- **034 保持独立**：本契约不承接其未决项（用户追认、异会话冷读），也不得在其 Closure Report 与本事务之间互相引为证据。
- **S3 批（待另立）**：接收 §一 E1 全部证据（检查器不解析 `§X~§Y` 范围、代码块整块跳过、archive 与 current 无身份分层、`deprecatedTerms` 无豁免字段、034 移交的旧话头登记互斥）。**本契约不实现任何 checker 改动。**
- **archive 规范性残留（待另立）**：`outputs/archive/官网资源库落地规划.md:7`／`:52` 与 Owner `内容资源调用协议.md` §3 的重叠，独立第三类事务。
- **多 active 并存合法性**：本仓库已有先例——`CHANGE-20260917-008:17` 记录当时 active 为 005＋006＋007 并存。
- **`.workbuddy` 永久排除**：单代理私有记忆层不作为项目公共控制面治理（用户明示）。

## 十四、移交登记（本事务不执行）

`AI工作启动指令.md` 是否纳入 `extraDocs`，须在 S3 批中一并决定，且**前置**是：先让 `check-doc-references` 正确解析 `§一~§四` 这类范围表达式，并明确 ```text 块内的结构性引用是否应受校验。已知证据：沙箱实验组纳入即出 2 处硬失败（`:24`、`:823`）。

## 十五、执行记录与裁定登记

### 15.1 W0 = PASS（AI-A 从 `f031301` 只读判定，AI-B 自评一致）
旧句归零、新句唯一命中、"不得静默改变问题族范围"落位、本波未触碰 `:4`/`:87`/`:505`、diff 仅 2 文件；`check-doc-references` 与阶段 A 逐字一致、`check-change-contract` PASS(37)、`gate-all` 13/15 无红且与基线逐字一致。AI-A 对"计划要全 vs 不承诺预见一切"的张力判定为不矛盾，AI-B 自评附问已消解。AI-A 同时撤回其上一轮"W0 证据不足"（因读取旧 SHA），AI-B 记账：双方各有一次读旧基线的失误。

### 15.2 提交权属（原 W0 附项，已由用户裁定关闭）
- 事实：`f031301` 等此前 6 笔提交均由用户侧执行，非执行方所为。
- AI-A 建议把本契约 §八.7 与场景 O 纪律 3 改为"与工作流无关的中性表述"，理由是"由谁 commit 属协作方式，不应由场景升格为硬规则"。**该批评对提出时点成立**——当时确无用户裁定，规则属执行方过度立法，AI-B 采纳并已降级。
- 用户随后带标记裁定「AI-B 来执行修改文档，上传 git；AI-A 读取更改，把关，确认」→ 决定权落定，故最终写法不是中性化，而是**登记生效约定并标明 Owner**：纪律 3 现文为「由谁执行 commit 属工作流约定，其 Owner 是用户——本场景不自行立法，只登记当前生效约定」；§八.7 同步。
- 保留的不变量（与谁提交无关）：验收方不得修改或提交执行方的在制品；每轮汇报必带 HEAD SHA 与工作区状态；已推送历史只能向前修正。
- 该工作流在本契约内的唯一表述处是 §八.7；本节不复述，也不设"是否 push"的逐波待确认项。

### 15.3 W0／权属修正的改动清单
`AI工作启动指令.md` 纪律 3（去过度立法＋标明决定权 Owner，1 行）＋ 本契约 §八.7 与 §15.2（登记用户裁定）。该节当时记为"未开始 W1"——W1 已执行，记录见 §15.4。

### 15.4 W1 执行记录（AI-A 验收前只登记事实，不自判 PASS）
改动：`AI工作启动指令.md` 三处路由声明（`:4` 顶部身份行、`:87` §三 Owner 路由、`:505` 场景 M 分类表），单文件 3 行替换。

统一路由句（三处逐字相同，作为 §九 单源判据）：
`先判内容线（唯一声明源：\`scripts/ref-registry.json\` 的 \`contentLines\`），再进入该线唯一 Owner——行业场景视频 → \`SKILL.md\`；产品功能教程 → \`docs/internal/产品功能教程作业规范.md\``

机械结果（本机实测）：CORE 命中 **3**（判据＝3 且逐字相同）；`唯一作业流程`／`视频生产唯一`／`- 视频生产：`／`视频生产 → SKILL` 均 **0**；`教程` 由 **0 → 3**；`deprecatedTerms`＋`caliberOwners`＋铁律一 试算 **0 命中**；`check-change-contract` PASS(37)；`check-doc-references` 与 `gate-all` 均与阶段 A **逐字一致**。

第 0 条反证：W2 目标行 `:338`「只认当前 SKILL.md。」与 `:392`「SKILL.md / R3 / R7 / R9 / 当前分镜 / 当前 R9 产物。」**原文未动**；`:205` 场景 B 字段未动 → W1 未提前吞做 W2。

设计取舍登记：三处复用同一长句会略增该行长度，但换取 §九 可用 grep 证明"未各写一份"；场景 M 分类表内因此与本节其它无反引号的条目风格不一致，属有意选择，不改。
### 15.5 当前问题族状态
问题族未被静默改变：A（路由）＝W1 已执行待验收；B（场景串线）／C（教程入口）＝W2、W2-Q 未开工；E（`.workbuddy`）＝按用户边界移出治理面，不追；F（扫描面与检查器语义）＝移交 S3。**034 仍独立 `VERIFYING`，未并入本事务。**

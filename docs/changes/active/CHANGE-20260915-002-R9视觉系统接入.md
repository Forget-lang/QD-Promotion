# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260915-002`
- 标题：把 R9 视觉导演决策真正接入组件选择与文案→视觉转换
- 负责人：内容与视觉方法论负责人
- 范围：`global`
- 状态：`MIGRATING`

## 二、Goal

让“高级感 / 不像 PPT / 动效有意义”不再只停留在原则文档，而是成为每个 Shot 在进入 Remotion 实现前必须回答的视觉决策字段，并让组件库按语义选型而不是反过来决定镜头。

## 三、新口径 New Policy

`R9` 负责视觉决策，`R9-视觉映射表`提供默认选型，`R7`负责组件目录，`R3`负责实现原理；任何关键镜头必须具备 `Visual Subject / Visual Metaphor / Peak Frame / State Change / Exit` 五个视觉字段后才能进入代码。

## 四、Replace

- “先看有什么组件，再把镜头拼出来”的组件驱动画面方式。
- 只用三层视觉结构描述“怎么画”，但没有明确说明“为什么选这个视觉语言”的做法。
- 文案分镜直接进入代码、缺少视觉隐喻与 peak frame 决策的中间阶段。

## 五、Remove

- 以“更丰富”“更有动感”作为组件新增的单一理由。
- 以左右双栏、统计卡、流程卡等现成组件形态默认承载抽象痛点/洞察。

## 六、Preserve

- SKILL 第 4.5 步作为正式视觉转换关。
- R3 继续负责 Remotion API、帧驱动、动画参数与工程实现。
- R7 继续负责组件目录和状态登记。
- 历史视频不回溯重做。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `docs/internal/R9-视觉导演与审美决策.md` | 视觉决策协议 | PRESERVE | 作为唯一审美原则属主 | DONE |
| `docs/internal/R9-视觉映射表.md` | 视觉选型辅助表 | ADD | 结构审查 | DONE |
| `docs/internal/R7-Remotion组件库.md` | 组件目录 | REPLACE | R9 语义选型接入 | DONE |
| `SKILL.md` | 作业流程 | PRESERVE | 现有第 4.5 步已是视觉转换入口 | DONE |
| `docs/internal/R3-Remotion技术参考.md` | 工程原理 | PRESERVE | 不复制 R9 决策规则 | DONE |
| 后续 Shot 表 | 产物格式 | REPLACE | 强制五个视觉字段 | PENDING |

## 八、Migration Plan

1. 建立 R9 视觉映射表。
2. 把 R9 选型规则接入 R7 组件目录。
3. 后续所有关键镜头 Shot 表使用 `Visual Subject / Visual Metaphor / Peak Frame / State Change / Exit`。
4. 用 g11 作为第一条验证片，检查“先语义、后组件、再代码”的流程是否真的改变画面。

## 九、Mechanical Checks

- R7 是否指向 R9：PASS。
- R9 视觉映射表是否存在：PASS。
- 当前事务结构完整：PENDING（待负向测试）。
- Shot 表强制字段是否已成为现有文件/检查器的硬约束：PENDING。

## 十、Negative / Semantic Counterexample

机械反例：一个 Shot 缺少 `Visual Metaphor` 或 `Peak Frame`，未来检查器必须拒绝进入“可实现”状态。

语义反例：把“转赠”改写为“二次触达”，但画面仍使用五个卡片节点水平连接；文字换了，视觉关系没有换，仍属于旧模式，应拒绝。

## 十一、Real Output Verification

- 本事务只更新视觉方法文档，不直接改变视频画面。
- g11 将作为下一阶段真实关键帧验证片。
- 结果：PENDING

## 十二、Closure Report

- 已修改：R9-视觉映射表、R7 组件库。
- 已废止：组件驱动画面的默认选型方式。
- 已保留：SKILL 第 4.5 步、R3 工程层、R9 原则层、历史视频不回溯。
- 影响面：2 / 6 已处理，g11 与 Shot 表硬约束待下一步。
- 旧口径扫描：PENDING
- 机械检查：PENDING
- 负向测试：PENDING
- 语义反例：DEFINED / PENDING
- 真图/成片：PENDING（下一阶段 g11）
- 本次新增红：PENDING

**结论：NOT CLOSED**

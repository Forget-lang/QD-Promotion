# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260921-036`
- 标题：行业线预设时长口径撤销——时长改为内容完成度的结果
- 负责人：用户（裁定方向）＋ AI-B（唯一执行方：契约、迁移、验证、提交与 push）＋ AI-A（只读验收方）
- 范围：`global`
- 状态：`VERIFYING`
- **前置事务**：`CHANGE-20260920-031`（CLOSED，曾在 :109 把 `60-90 / 90-120 / 120-150 / 硬上限 200s` 划归"继续只服务行业线"）；`CHANGE-20260921-034`（active，`VERIFYING`，明文不改 `ref-registry.json`／`scripts/`）；`CHANGE-20260921-035`（CLOSED，把检查器语义改造移交 S3）。本事务与 034 **不并批**：034 不触注册表，本事务必触注册表，两者互不依赖。
- **授权链（三段分开陈述，不合并推断；场景 O 纪律 1）**：
  ① **事实**：双 AI 收敛期间，双方一度推荐"保留数字、只补『参考』限定词"，理由是 `outputs/archive/changelog.md:1344-1349` 与提交 `902bc07`（待办文件 §4 ＋ `docs/战略简报.md` 注记）记录用户 2026-08-25／08-30 两次确认保留"三档参考 / 200s 上限"。该材料属 `outputs/archive/` 历史证据（`AGENTS.md` §十一），只证"过去拍过"，不证今日。
  ② **已裁定**：用户 2026-09-21 以「我（用户的补充）：」标记原话给出——「行业攻略不设预设时长，内容以一套攻略是否完整讲透为止；时长是结果，不是前置约束。」据此，上述历史口径自本事务生效起被撤销（含"参考档"这一身份）。
  ③ **已裁定**：用户同日再以同标记原话给出——「AI-B：唯一执行方——负责建立 Change Contract、Migration、提交与 push。」AI-A 为只读验收方。
  ④ **标记差异如实记**：本事务两项裁定所用标记为「我（用户的补充）：」，与场景 O 约定的「我（用户）的意见：」字面不同。判据取其实质——两处均出自用户本人消息、非转发件转述——故按裁定采用；此处登记差异，避免后续会话把"补充"读成不具授权力的材料。
- **本批不含（未获裁定，不静默扩大范围）**：`SKILL.md:240`「完播最低 → 修闭环五拍结构与时长」的处置（A 加限定句／B 清除时长作为修正变量／C 入池另批）尚未由用户选定；`SKILL.md:36`（把工程 duration 计算与时长治理闸门混为一谈）双方一致建议单独入池，本批不动。两项均登记于 §七 与 §八，不构成 §十二 的关闭条件。

## 二、Goal

消除 `SKILL.md` 行业线内部"不设固定区间"与"三档秒数＋200s 硬上限"并存的双时长语义，把片长的决定权从**前置数字约束**移交给**内容完成度判据**，并同步清掉注册表与非属主文档里对这组数字的现行指向，避免新会话继续把秒数当目标执行（已有先例：g10 的口播稿曾自加码"压到 85–95s"，后于 2026-09-10 记录为无依据并撤回）。

## 三、新口径 New Policy

行业线唯一时长口径（正文属主：`SKILL.md` §二 第 3 步）：

> **不设预设时长**：结束条件只有一个——本篇选中的那 2~4 个功能点连同其关联面是否讲到透。片长由内容完成度自然产生，本文不给任何档位、区间或上限（含"参考档"）。

配套不变式（本批保留，防连带误删）：

- `SKILL.md:46`「屏数与时长由这套攻略要讲透的功能点深浅决定，**不为时长砍功能点**」——新口径的同一句话，保持原位。
- 拆片判据唯一落在内容侧：`:43`（范围大过 2~4 个功能点 → 拆成两条痛点两篇）＋ `:52`（一行一套攻略，一套攻略一片）。**不新增拆片规则。**
- `:252` 每屏 `dur = ceil01(口播字数÷6) + extraHold` 属**工程侧由口播反推帧数**的计算，正是"时长是结果"的体现，**保留**。
- 语速 ≤6 字/秒、冗余句 ≤8% 是表达尺，不是片长档，**保留**（其数值口径属主不变）。

## 四、Replace

| 被替代的旧口径 | 现位置 | 替代为 |
|---|---|---|
| 「字数与时长不设固定区间」标题与正文三档秒数并存（同一行自相矛盾） | `SKILL.md:97` | §三 新口径整行；旧拍板戳（2026-08-25）与新裁定戳并列可追溯 |
| 「时长按复杂度自然落位（简单 60-90s / 标准 90-120s / 复杂 120-150s，硬上限 200s，超了拆下一片）」 | `SKILL.md:97` | 删除；"超了拆下一片"的触发条件由 `:43/:52` 内容判据单独承担 |
| 「落位区间与硬上限以 `SKILL.md` §二 第 3 步为准」——指向即将不存在的口径的活指针 | `docs/战略简报.md:32` | 改指 §三 新口径（内容完成度判据），并保留该文件原有的"按每片自己那套攻略判、不拿行业总产出凑档"一句 |

## 五、Remove

- `scripts/ref-registry.json` 的 `caliberOwners` 条目：`pattern = "(60-90|90-120|120-150)\\s*s|硬上限\\s*200\\s*s|200\\s*s\\s*上限"`、`owner = SKILL.md`、`desc = 时长落位区间与硬上限（SKILL §二第 3 步）`。**必须随本批撤除**——`check-doc-references.mjs:195` 只拦属主外复述、不校验属主内口径是否仍存在，只删正文会留下指向空规则的活口属主且闸门全绿。
- 同批在 `deprecatedTerms` 登记旧话头（防回流）。**登记前已用检查器自身正则对全部 23 份被扫描文档实测**（`docs/internal/*.md` ＋ `ref-registry.json:extraDocs`），命中面如下，故可安全登记这 5 条：`60-90s`、`90-120s`、`120-150s`、`硬上限 200s`、`时长按复杂度`（旧文本改写后全扫描集零命中）。
- **明确不登记**：`200s 上限`、`时长档`（两者会命中 `docs/internal/产品功能教程作业规范.md:11` 的正当禁令句）、`不设固定区间`、`自然落位`（会命中 `docs/战略简报.md:32`，且与新语义近似，登记等于自打）。理由登记进条目 `desc`，防后人补登记时打红别的 Owner。

## 六、Preserve

| 对象 | 处置 |
|---|---|
| `docs/internal/产品功能教程作业规范.md:11`（教程线"不设时长档、不设硬上限"） | 本批**零改动**。其句中"会命中数值口径的属主外复述判定"的理由随 `caliberOwners` 撤除而失效，登记为后续单行清理，不并入本批 |
| `outputs/g10-美容沙龙/02-口播文案.md:9/:52`（历史片口播稿曾复述"参考档/硬上限"） | 已交付产物的历史证据，不回改（先例：g06–g09 历史豁免）；且 `outputs/g*` 不在扫描集，不构成硬失败 |
| `CHANGE-20260920-031:109`、`outputs/archive/changelog.md`（git 历史内） | 历史证据，只解释"这组数字曾经有授权"，不得回用于当前产出，也不得被当作撤销本裁定的依据 |
| `SKILL.md:43/:46/:52/:252`、语速与冗余句数值口径 | 原样保留（见 §三 不变式） |
| 各片实测秒数（g06 187.8s/12 屏、g07 108.6s、g08 65.4s、g09 93.1s、g10 93.7s、g11 108.8s） | 作为"内容深浅决定片长"的历史证据继续存在，不再充当档位校准样本 |

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `SKILL.md:97` | 行业线方法正文（口径属主） | REPLACE | 新行不命中任何 `deprecatedTerms`；grep 三档秒数与 `硬上限 200s` 于 `SKILL.md` ＝ 0 命中；§三 不变式四条仍在原处 | 波 1 |
| `scripts/ref-registry.json`（`caliberOwners` 时长条 ＋ `deprecatedTerms` 5 条） | 共享闸门配置 | REMOVE ＋ ADD（登记项，非新规则） | `check-doc-references` 0 硬失败；`list-assets`／`content-lines.mjs` 枚举校验通过 | 波 1 |
| `docs/战略简报.md:32` | 被扫描的非属主文档 | REPLACE（仅指针措辞） | 该行不再指向"区间与硬上限"；全文数值口径属主外复述 0 命中 | 波 1 |
| `SKILL.md:240`（完播最低 → 修…与时长） | 用户 2026-09-14 拍板句 | 悬置（候选 REPLACE） | 本批不设关闭条件 | 待用户选 A/B/C |
| `SKILL.md:36`（把工程 duration 计算混同为时长治理闸门） | 检查器/工程语义族（与 035 移交的 S3 同族） | 本批不做 | 另立事务后由其自定 | 入池 |
| `outputs/g*`、`video/`、`spec/`、`AGENTS.md`、教程线 Owner、`scripts/*.mjs`、`docs/changes/template.md`、034 事务 | — | PRESERVE | `git status` 与 `git diff --stat` 证明未触碰 | 全阶段 |

## 八、Migration Plan

1. 用户确认本契约方向，并选定 `:240` 的处置（A／B／C）；在此之前只提交契约本身，不改任何规则正文。
2. 改 `SKILL.md:97` 为 §三 新口径整行；逐字复核 §三 四条不变式仍在原位原样。
3. 改 `docs/战略简报.md:32` 指针（不新增第二处正文，只留指向）。
4. 改 `scripts/ref-registry.json`：删 `caliberOwners` 时长条 → 加 §五 五条 `deprecatedTerms`（各带 `desc` 说明登记理由与不登记项的警示）。
5. 顺序说明：3 与 4 必须在 2 之后同批完成——若先删 registry 条目再改正文，中间态会出现"正文有数字、属主无登记"的窗口；若先改正文不撤条目，则留下 §五 所述活口属主。落盘按 2→3→4 一次提交，不产生可检出的中间态。
6. 跑 §九 全部机械检查 ＋ §十 反例；任一 FAIL 停在原事务继续改，不重开讨论。
7. 全绿后在 §十二 填证据、更新状态，按 R8 §十一 推荐顺序取证并关闭；本批不含 `:240`、`:36`。

## 九、Mechanical Checks

- 旧术语/旧数字扫描：`grep -n "60-90\|90-120\|120-150\|硬上限 200\|时长按复杂度" SKILL.md docs/战略简报.md docs/internal/产品功能教程作业规范.md knowledge/*.md` ＝ 0 命中；全仓扫描（排除 `outputs/`、`.workbuddy/`、`node_modules/`、本事务与 `closed/`）仅允许命中本契约文本。
- 文档引用闸门：`node scripts/check-doc-references.mjs` ＝ 0 硬失败、0 需人工确认。
- 变更收敛闸门：`node scripts/check-change-contract.mjs` ＝ PASS。
- 全量：`node scripts/gate-all.mjs` ＝ 相对基线（本事务建立前 HEAD `02c3afb` 实测 `check-doc-references` 0 硬失败、`check-change-contract` PASS(37)）**零新增红**；既存红若命中须点名归因，不得表述为通过。
- 属主唯一性：撤除后 `caliberOwners` 中不得再有任何指向片长数值的条目；`60-90` 等字面量在扫描集内不得以非废弃形态出现。
- 负向测试：临时在被扫描文档内插入一行含「参考档 90–120s」的非属主文本，期望 `check-doc-references` 硬失败；复测后删除并确认回到 0 硬失败。

## 十、Negative / Semantic Counterexample

**反例（关键词已改、本质仍违反新口径）**：下一片口播稿写「这套攻略内容多，那咱们把片子控制在一分半以内，讲不完的下次再说」。字面不含任何被禁秒数档，也不命中 `deprecatedTerms`，但把片长重新当前置约束使用、并为凑"讲完"砍内容——正是旧"硬上限 200s，超了拆下一片"的语义变体。

**由哪一层拦**：机器拦不住。`caliberOwners`/`deprecatedTerms` 只做字面与数值匹配，`scripts/` 不存在任何片长闸门（本事务侦察实测：`.mjs` 对 `ceil01`/`extraHold`/`.dur` 零命中）。拦截责任落在人判两处——① 第 1 步内容输入包的"功能点是否讲到透"（`:55`）；② 用户看真图与成片验收（`:46` 不为时长砍功能点）。**如实登记为能力边界**：本事务不假装新增机器守卫。

**语义反例 2（写法层）**：若把新口径写成"不受 200s 上限约束"——换词不换形且违反 §五 不登记项禁令，会当场打红教程线 Owner；正确写法是正面陈述"不给任何档位、区间或上限"（先例：`CHANGE-20260920-031:109` 对教程线的同一要求）。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：交付物为方法条款文本与注册表条目，不含视觉、动效或内容产物；新口径的真实效果由下一行业片（g12）在第 1 步内容输入包与成片验收中体现，属生产事务，不作为本事务的关闭证据。
```

- 真实关键帧/短片：本事务不存在内容产物（见上声明）。
- 人工验收判据：AI-A 从执行方公布的 HEAD SHA 读 diff，按场景 O 的 8 条判据逐条回答 PASS／FAIL；另需一条人判——`SKILL.md` 全文（尤其 `:43/:46/:52/:97/:252`）连读后，是否还能得出"存在任何片长预设"的结论。
- 结果：PROPOSED 阶段不填。

## 十二、Closure Report

```text
Change ID：CHANGE-20260921-036
目标：撤销行业线预设时长口径，片长改由内容完成度决定

唯一 Owner：SKILL.md §二 第 3 步（片长无预设值，故不再存在片长数值口径属主）
唯一入口：AGENTS.md §二/§三 → 行业线 SKILL.md（未新增入口）

已修改：SKILL.md:97、docs/战略简报.md:32、scripts/ref-registry.json（caliberOwners 10→9、deprecatedTerms 30→35）
已废止：三档秒数 ＋ 硬上限 200s ＋ "超了拆下一片"秒数触发（4 个字面登记进 deprecatedTerms；原 caliberOwners 条删除）
影响面：已处理 3 / 3（§七 波 1 三项；:240 与 :36 按 §一 未获裁定，不属本事务影响面）
Owner/入口冲突：PASS（片长概念现只有 SKILL.md 一处正文；战略简报只留指针；教程线 Owner 零改动）
旧口径扫描：PASS（五个旧字面在 SKILL.md／docs/internal/*.md／knowledge/*.md／AGENTS.md 命中 0）
机械检查：PASS（候选状态 d06fcfe 实测：check-doc-references 0 硬失败；check-change-contract PASS(38)；gate-all 13/15 通过、2 项跳过、无红灯，退出码 0）
负向测试：PASS（注入旧口径夹具后 4/4 在册字面命中「旧口径话头回流」硬失败；删夹具复验回 0）
语义反例：PASS（§十 两条：变体二已按正面书写规避；变体一如实登记为机器不可拦、由人判层承担，未假装加守卫）
真图/成片：N/A（`Real Output 声明` 为 `not-applicable`）
本次新增红：0（与基线 HEAD 02c3afb 同为无红灯；gate-all 输出的 29 处文案层待确认与 g11 两行待回填均为既存项，已点名归因，不表述为通过）

结论：NOT CLOSED
```

- **未达 CLOSED 的原因**：R8 §九 要求全部关闭条件均有最终证据。② AI-A 已于本轮声明从 `eebfb5c` 验收 PASS（等级＝对手方声明，见 §13.5.2）；仍缺 ③ **用户本人通读 `SKILL.md` 的人判确认**。机器证据全部齐备，不用改写机器结论来充数。
- 本契约于 `cf6f439` 建立为 `PROPOSED`；用户 2026-09-21 已给出方向裁定与执行方指定（见 §一 授权链②③），对手方 AI-A 对契约本体判 PASS，故本事务转 `VERIFYING`。`CLOSED` 仍须 §九／§十 全部证据齐备且完成人工验收（R8 §九：关闭是证据状态，不是文字状态）。
- 建议关闭后另开会话做异会话冷读复核（场景 F），其结论高于执行方自证与对手方复核。

## 十三、Migration 执行记录（含自纠）

### 13.1 为什么在未逐字确认措辞的情况下进入迁移

依据项目自己的在案规则，不新设解释：`scripts/ref-registry.json` 的 `deprecatedTerms` 条目「改动权威文档：一元一票否决」记录了 2026-09-14 用户把它拆为**决策层拍板 ＋ 执行层机器自证**两级——「决策层仍需用户明确点头；执行层改交『整改对账单 ＋ 负向测试』自证，不再默认逐条等确认」。本事务决策层已由用户 09-21 两句裁定给定，§三 新口径只是该决策的落地写法，属执行层，故进入迁移并以本 §十三 ＋ §九 证据自证。**决策未覆盖的两项（`:240`、`:36`）未被代入**，仍按 §一 排除在外。

### 13.2 落盘顺序与改动面

按 §八 顺序一次完成（`SKILL.md` → `docs/战略简报.md` → `scripts/ref-registry.json`），无中间态提交：

| 文件 | 实际改动 | 复核 |
|---|---|---|
| `SKILL.md` | 仅 :97 一行改写（1 insertion / 1 deletion） | `git diff --stat SKILL.md` ＝ `1 file changed, 1 insertion(+), 1 deletion(-)`；:43/:46/:52/:252 逐条复验零改动（§三 不变式成立） |
| `docs/战略简报.md` | 仅 :32 一行（旧数值指针 → 新判据指针） | diff 单行 |
| `scripts/ref-registry.json` | `caliberOwners` 删 1 条（10→9）；`deprecatedTerms` 加 5 条（30→35） | `JSON.parse` 通过；残留时长 caliber 条＝0 |

### 13.3 执行期自纠（如实记，不掩盖）

**第一次写入曾造成本事务自己定义的违规**：把被撤销的旧秒数当"变更注记"原样留在 `SKILL.md:97` 正文里（`撤销原「简单 60-90s / …硬上限 200s…」`）。这违反 §五——`deprecatedTerms` 扫描**不认属主、无豁免**（`check-doc-references.mjs:212-223`），一旦同批登记这些字面，Owner 自己就会硬失败；且属"换词不换形"的语义残留。

- **发现方式**：提交前逐字自查 `git diff`，非依赖闸门（该状态当时尚未登记话头，闸门尚不会报）。
- **修法**：改为只留日期与 `变更史见 docs/changes/` 的指针写法，正文字面不含任何被禁秒数。
- **教训登记**：Owner 内写"撤销了什么"也必须走指针，不得复述旧数值——此判据已在 §五 与 registry `desc` 双向留痕。

### 13.4 机械与语义证据（候选状态）

| 项 | 命令 | 结果 |
|---|---|---|
| 旧口径扫描 | `grep` 五个字面于 `SKILL.md`／`docs/战略简报.md`／`docs/internal/*.md`／`knowledge/*.md`／`AGENTS.md` | **0 命中** |
| 属主唯一性 | `caliberOwners` 残留时长条 | **0 条**（片长不再有数值口径，符合"时长是结果"） |
| 文档引用闸门 | `node scripts/check-doc-references.mjs` | ① 0 处 ② 无 ＝ **0 硬失败** |
| 变更收敛闸门 | `node scripts/check-change-contract.mjs` | `PASS: checked 38 transaction(s)` |
| 负向测试 | 临时在 `docs/internal/tmp-negtest-036.md` 注入旧口径句 → 跑闸门 | **4/4 在册字面全部命中「旧口径话头回流」硬失败**（夹具另触 `§九` 断链提示，属夹具自身文本）；删夹具后复验回 0 硬失败 |
| 语义反例 | §十 两条 | 变体一（"控制在一分半以内"）：**机器不可拦，人判层承担**，已如实登记能力边界；变体二（否定式复述"不受 200s 上限约束"）：正确写法已采用正面陈述，且 §五 禁止登记该话头 |
| 全量闸门 | `node scripts/gate-all.mjs` | 候选状态 `d06fcfe` 重跑＝ `✅ 13/15 通过、2 项跳过——无红灯`，退出码 0（与 §一 基线 `02c3afb` 同绿，本次新增红 0） |
| 既存非红项归因 | 同上 | 红线文案层 29 处「券到卡包／小程序码／活码／微信搜」＝ 规则说明句与 spec 真值，属既存项非本次新增；发布后验 g11 两行待回填＝既存挂账（g12 前须回），非本事务引入 |

### 13.5 关闭前还缺什么

1. ~~`node scripts/gate-all.mjs` 在**最终候选状态**上的重跑记录~~ ✅ 已于 `d06fcfe` 重跑并登记 §13.4。
2. ~~AI-A 从候选 HEAD 读 diff，按场景 O 八条判据逐条 PASS／FAIL~~ **对方已声明 PASS**（从 `eebfb5c` 回读，与基线 `02c3afb` 独立对比）。⚠ **证据等级如实标**：该结论出自用户转发的 AI-A 原文，属**对手方声明**，执行方只能登记其来源，不能代其为证据，也不构成 §十二 的自证升级；AI-A 报告里的独立计数（八项秒数字面＝0、caliber 时长条＝0、话头＋5）由其在自己会话实测，本事务未复核其运行过程。
3. **用户本人通读 `SKILL.md` §二 第 3 步**，确认"是否还读得出任何片长预设"——这是唯一仍未取得的人判项。
   **⚠ 边界澄清（防后继会话误当已满足）**：AI-A 一度把自身审查表述为"对 036 的**最终人判**：PASS"，执行方据此**未**关闭本事务；AI-A 次轮已更正，原话「还缺最后一个关闭条件：用户本人确认……AI-A 前面已经明确'不能替用户冒充这项确认'」。结论：**对手方复核无论多强，都不顶替本条**；本事务的 ③ 只有用户以本人原话给出才算取得。
4. 上述齐备后才填 §十二 结论并移入 `closed/`；`:240`、`:36` 另立事务，不并入本事务关闭条件（AI-A 同轮确认：这两项不得借 036 顺手处理）。

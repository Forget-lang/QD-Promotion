# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260917-011`
- 标题：历史账本全系退役与悬空指针清零
- 负责人：用户 + 本地 Agent
- 范围：`global`
- 状态：`VERIFYING`
- 基线：`HEAD = origin/main = d896110`
- **定位说明**：触及 `AGENTS.md`（宪法 / Owner 地图）、`SKILL.md`（生产 Owner）、3 份 `docs/internal` Owner、用户控制台 `AI工作启动指令.md`、归档说明、1 个检查器 → 跨 5 类权威对象 → 按 `AGENTS.md` §一 升级为全局变更走 R8。
- **前置归属**：本事务是 `CHANGE-20260917-009` 的**范围外后继**。009 已把「变更史的当前落点」统一到 `docs/changes/`，并在其 Impact Map 中把 2026-08 分卷登记为 `PRESERVE`（全文保留）；`d896110`（2026-09-17 12:14「删除changelog」）随后把该分卷整份删除（2474 行 / 132 条），**与 009 契约相反、未走 R8、且未触发 CI**（该文件不在任何工作流 `paths` 内）。本事务把这个既成事实收敛为唯一口径并清零指针，属 `AI工作启动指令.md` §十八 **A-4「批准方案与当前仓库事实不一致」** 的收口动作。
- **顺序偏差声明**：本契约在**用户给出方向之后、迁移之前**落盘；方向由用户 12:19 指令给出（"这些都不再维护了……就决定删除吧，更新引用"）。

## 二、Goal

让「变更史只在 `docs/changes/`」成为**仓库内无歧义的事实**：changelog 全系（主文件 + 2026-08 分卷）均不再维护、不再充当任何落点，因此把它在 9 个位置留下的引用全部清零，并让闸门**认得这两个已退役文件名**、不再放行 `changelog` 一词当指针。

## 三、新口径 New Policy

1. **变更史的唯一落点 = `docs/changes/{active,closed}/`**。仓库内**不维护任何 `changelog*.md`**，也不得以 `changelog` 一词充当"变更史指针"。
2. **引用退役资产时的写法：只删指针，不伪造新目标。** 原日期 / 规则事实仍成立的，保留事实、去掉指针（例：`（2026-08-25 用户拍板）`）；原指针是唯一案例载体的，改为指向 **git 历史**（唯一真实落点），**不得**改指 `docs/changes/`（该条目时间早于其起点 09-15，会制造新悬空指针）。
3. **闸门必须认得已退役文件名**：`changelog.md` 与 `changelog-2026-08.md` **都**进硬拦阻；同时收窄"历史轮次叙事"的豁免白名单——`changelog` 不再是合法指针目标，只剩 `docs/changes/`。
4. **不新增任何替代文档 / 账本**：除本契约外，本事务不 ADD 任何文件。

## 四、Replace

- 9 处指向 `outputs/archive/changelog-2026-08.md` 的引用 → 去指针（保留事实）。
- `check-doc-references.mjs` 的退役文件名拦阻：1 个名字 → 2 个名字；其提示语去掉"（2026-08 及更早见 `changelog-2026-08.md`）"。
- `check-doc-references.mjs` 历史轮次叙事的豁免白名单 `/changelog|docs\/changes/` → `/docs\/changes/`。
- `AI工作启动指令.md` §三 的历史证据行（去括注）。

## 五、Remove

- `outputs/archive/changelog-2026-08.md` 在仓库内的**存在性**（已由 `d896110` 事实删除；本事务**只承认与收口，不重复执行删除动作**）。
- 由它派生的 9 处引用；`docs/changes/active/CHANGE-20260917-009` 中已失效的 `PRESERVE` 断言（改为指向本事务的订正指针）。
- **不删除任何其他资产**；`outputs/archive/` 其余 8 个文件全部保留。

## 六、Preserve

- `outputs/archive/` 其余资产、`knowledge/`、`video/`、`spec/` 全部不动。
- `SKILL.md` **规则正文一字不减**——只去括注指针，`git diff` 需能证明减少的字全部是引用字样。
- `AGENTS.md` 除 §二 Owner 地图该行的括注外，其余一字不动。
- **可追溯性**：`changelog-2026-08.md` 全文仍可从 git 取回（`git show d03e606:outputs/archive/changelog-2026-08.md`）。
- g11 线与母题卡闸门红**不处理**（独立挂账，见 §十二 说明）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `AGENTS.md` §二 历史证据行 | 宪法 / Owner 地图 | REPLACE | 该行不再出现 `changelog-2026-08.md`；其余行零改动 | DONE（去括注，仅 1 行） |
| `SKILL.md`（3 处引用：97 / 130 / 257） | 生产 Owner | REPLACE | 断链清零；规则正文零减字（diff 核验） | DONE（3 处去指针；保留 08-25 / 08-29 日期事实） |
| `docs/internal/R9-视觉导演与审美决策.md:166` | 领域 Owner | REMOVE | "与其他文档的边界"列表中该行整行删除 | DONE |
| `docs/internal/整改作战总纲.md:79` | 领域 Owner | REPLACE | 编制表该格去括注 | DONE |
| `docs/internal/PROJECT-CONSISTENCY-MAP.md:94` | 状态总账 | REPLACE | 变更史一行改写为"以 `docs/changes/` 为准；历史账本已全系退役" | DONE |
| `AI工作启动指令.md:97` | 用户控制台（非 Owner） | REPLACE | 历史证据行去括注 | DONE |
| `outputs/archive/README.md:17` | 归档说明 | REMOVE | "目录内容说明"表中 changelog 那一行整行删除 | DONE |
| `outputs/g09-宠物店/00-交付说明.md:5` | 单片交付说明 | REPLACE | "后续状态与复盘一律归 changelog"改为指向 `outputs/archive/发布后验台账.json`（009 已裁定的落点），消除句内自相矛盾 | DONE（**闸门扫描面外**，由本轮全仓 `grep` 兜出） |
| `scripts/check-doc-references.mjs` | 检查器 | REPLACE | 退役名正则覆盖 2 个名字；白名单收窄为 `docs/changes/`；负向测试见 §十 | DONE |
| `docs/changes/active/CHANGE-20260917-009-*.md` | 未关闭事务 | REPLACE | 加 1 行范围外订正指针（不回填历史正文、不改其原有段落） | DONE |
| `docs/changes/active/CHANGE-20260917-011-*.md` | 本事务 | ADD | 本文件 | DONE |

## 八、Migration Plan

1. 登记事实基线：`d896110` 已删分卷；`find -name "changelog*"` 为空；无脚本读取其内容。
2. 清 9 处引用（8 个文件）。
3. 加强闸门：扩展退役名正则 ＋ 收窄白名单 ＋ 改提示语。
4. 在 009 加 1 行范围外订正指针。
5. 验证：`node scripts/check-doc-references.mjs` ＋ `node scripts/check-change-contract.mjs` ＋ `node scripts/gate-all.mjs`。
6. 负向测试（见 §十）。
7. 回填 §九 / §十二（**不写 CLOSED**）。

## 九、Mechanical Checks

- 旧术语 / 旧口径扫描：`grep -rn "changelog-2026-08"` 在 `docs/`、`outputs/`、`scripts/`（排除 `docs/changes/active/CHANGE-20260917-009` 与 011 自身）应为 **0**；`grep -rn "changelog"` 在本扫描面内只应剩 `check-doc-references.mjs` 的拦阻正则与 `ref-registry.json` 4 条"原条目已缺失"的历史注记。
- 文档引用检查：`check-doc-references` 硬失败数须由 **11 → 5**，且这 5 处全部属 g11 线（`PROJECT-CONSISTENCY-MAP.md:66–70`），与本事务无关。
- 其他相关闸门：`check-change-contract` 含本契约 PASS；`gate-all` 红数仍为 2，且两道红的构成回到基线（文档引用＝g11 线 5 处、母题卡＝g10 缺口）。
- 减字核验：`SKILL.md` 的删除字数须等于被删引用字样的字数，规则正文零减字。

## 十、Negative / Semantic Counterexample

**反例 1（改词不改义）**：把「案例见 `outputs/archive/changelog-2026-08.md`」改写成「案例见历史账本」——关键词没了，但仍然指着一个已退役、不存在于仓库的落点。
**拦截层**：§三 第 2 条要求"只删指针、不换同义空指针"；§九 的旧口径扫描按**语义落点**（是否仍指 changelog 系）判，不只看文件名。

**反例 2（把悬空指针静默改指 `docs/changes/`）**：把 08-25 / 08-29 / 08-30 三处引用改写成「详见对应变更事务」——这些日期**早于 `docs/changes/` 的起点（09-15），根本不存在对应事务**，等于制造新悬空指针。（与 009 §十 反例 2 同型）
**拦截层**：§三 第 2 条明写"不得改指 `docs/changes/`"；§九 核对改写后每一处的时间是否落在 `docs/changes/` 覆盖范围内。

**反例 3（借清理顺手删规则正文）**：删括注时连带删掉括注前面的规则内容。
**拦截层**：§六 ＋ §九 减字核验。

**反例 4（把白名单放宽当"兼容"）**：保留 `changelog` 在历史轮次豁免白名单里，等于允许「详见 changelog」继续当合法指针——这正是"闸门失去检查对象即默认放行"的复发形态。
**拦截层**：§三 第 3 条 ＋ §九（白名单必须只剩 `docs/changes/`）。

**反例 5（顺手扩大打击面）**：借本次把 `outputs/**` 加进 CI 触发面、或把 `AI工作启动指令.md` 注册进 `ref-registry.extraDocs`，从而"顺便"修掉闸门盲区。
**拦截层**：本次**不做**。两者都会改变闸门的触发频率 / 扫描面，属独立事务；已在 §十二 挂账。

## 十一、Real Output Verification

**声明（二选一，机器可读，必填）**：本事务到底需要什么真实产物，由本事务自己声明，机器不猜。

```text
- Real Output 声明：`not-applicable`
- 理由：本事务只改文档引用指针、归档说明表行与一个检查器的拦阻名单，不产出、不修改任何视频画面 / 分镜 / Remotion 组件 / 渲染产物，不存在可验收的真实画面。
```

- 真实关键帧/短片：不适用
- 人工验收判据：`check-doc-references` 断链由 11 回到基线 5；退役文件名写回即被拦
- 结果：PASS（not-applicable）

## 十二、Closure Report

- 已修改：`AGENTS.md`（§二 历史证据行 1 行）、`SKILL.md`（97 / 130 / 257 共 3 处引用字样）、`docs/internal/R9-视觉导演与审美决策.md`（删 1 行列表项）、`docs/internal/整改作战总纲.md`（§5 编制表 1 行）、`docs/internal/PROJECT-CONSISTENCY-MAP.md`（§4 变更史 1 行）、`AI工作启动指令.md`（§三 1 行）、`outputs/archive/README.md`（目录说明表删 1 行）、`outputs/g09-宠物店/00-交付说明.md`（1 行）、`scripts/check-doc-references.mjs`（3 处：白名单收窄 ＋ 退役名正则扩为 2 名 ＋ 提示语）、`docs/changes/active/CHANGE-20260917-009-*.md`（追加 §十三 范围外订正，正文零回填）
- 已废止：changelog 全系（`changelog.md` 由 `b581908` 退役、`changelog-2026-08.md` 由 `d896110` 事实删除；本事务只承认并收口）；由它派生的 9 处引用；009 契约中已失效的 `PRESERVE` 断言
- 已保留：`outputs/archive/` 其余资产；`SKILL.md` 全部规则正文（只去引用字样）；`AGENTS.md` 除该行括注外零改动；`changelog-2026-08.md` 全文仍可经 git 取回（`git show d03e606:...`）
- 影响面：`11 / 11`
- 旧口径扫描：PASS —— `grep -rn "changelog-2026-08"`（`docs/` `outputs/` `scripts/`，排除 009 / 011 事务本体）＝ **0 处**；裸 `changelog` 仅剩 3 类刻意保留：`check-doc-references.mjs` 的拦阻正则与注释（拦阻本体）、`ref-registry.json` 4 条"原引用的 changelog 条目已缺失"如实注记、`CHANGE-20260917-010` 的历史叙述
- 机械检查：PASS —— `check-doc-references` 硬失败 **18 → 5**（5 处全部属 g11 线，＝迁移前基线）；`check-change-contract` PASS（13 transactions，含本契约）；`gate-all` 仍 `2/15` 且两道红的构成与基线一致
- 负向测试：PASS —— 三条探针实测：**T1**（裸文件名 `changelog-2026-08.md`）被拦；**T2**（`第48轮` ＋ 以 `changelog` 当变更史指针）被拦（白名单收窄生效）；**T3**（`第12轮` ＋ 指向 `docs/changes/`）**不误报**。装拦阻但未清引用时为 18 处，清场后回到 5 处＝基线。**另证**：拦阻加装后当场抓出 `整改作战总纲.md:79`（裸文件名，旧检查的盲区）与 `outputs/g09-宠物店/00-交付说明.md:5`（扫描面外，由全仓 `grep` 兜出）
- 语义反例：PASS —— §十 五个反例均写明拦截层；反例 4（白名单放宽）已在本次实现中生效（T2 实测被拦），反例 2（静默改指 `docs/changes/`）已在 §三 第 2 条明令禁止，反例 5（顺手扩大打击面）已在本节"未做"中显式排除
- 真图/成片：N/A（not-applicable，见 §十一）
- 本次新增红：0 —— `gate-all` 仍为 2 道红，且构成与基线逐项一致（文档引用＝g11 线 5 处；母题卡＝g10 既有缺口）。**本次把文档引用闸门由 11 处降到 5 处**（净减 6），即把 `d896110` 引入的 6 处红全部清回。

**结论：NOT CLOSED**

**关闭前置**（按 `CHANGE-20260917-012` 修定为「候选状态上的等价重现」，不再要求 GitHub CI 记录）：在候选状态的干净检出上按两个工作流的原样步骤重跑并逐条记录退出码，＋用户人工验收。实测记录见 `CHANGE-20260917-012` §十二。

**未做（有意）**：
1. `check-doc-references.mjs:62` 的 basename 解析改造（爆炸半径大，属独立事务）。
2. `AI工作启动指令.md` 未注册进 `ref-registry.extraDocs` → 该文件目前**不在文档引用闸门的扫描面内**；本事务只清理其内容，不扩扫描面。
3. `outputs/**` 未加入 CI 触发面 → 引用型文件被删仍可能不触发 CI（`d896110` 即此例）。
4. g11 线与母题卡闸门红不处理。
5. `ref-registry.json` 内 4 条"原引用的 changelog 条目已缺失"注记保留（属如实标注，非指针）。

**跨事务影响（须在关闭时一并处理）**：
- `CHANGE-20260917-009`：Impact Map 中 `changelog-2026-08.md` 的 `PRESERVE / DONE` 已被 `d896110` 推翻，本事务已在其文档内加订正指针。
- `CHANGE-20260917-010`：其 §十二「文档引用 5 处断链」是迁移当时的实测值；`d896110` 之后该数值变为 11。010 的"本次新增红 0"对其**自身改动**仍成立，但"两道红全部属 g11 线"的归属陈述已被本次事实推翻。
- `CHANGE-20260916-005`：其关闭证据依赖当前检查器行为；本事务**改了检查器**（拦阻名单 ＋ 白名单），须在 005 关闭前复核其证据是否仍与验证对象对得上。（与 010 §三 第 2 条同一约束。）

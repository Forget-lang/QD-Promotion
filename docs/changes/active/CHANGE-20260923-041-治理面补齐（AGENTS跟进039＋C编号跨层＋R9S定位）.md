# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260923-041`
- 标题：治理面补齐——AGENTS/启动指令跟进 039 架构、C 编号跨层修正、R9-S 定位、口径实测结论
- 负责人：用户（方向）／AI-B（侦察、契约、迁移、验证）
- 范围：`global`
- 状态：`CLOSED`
- 触发：用户 2026-09-23 对全局文档审计作战地图的批复原话「按照你的建议都执行吧」（批 A ＋ 批 B1/B2/B3；批 C 只登记不执行）

## 二、Goal

一句话：039/040 之后**架构面已对齐、治理面仍滞后**——`AGENTS.md`（唯一入口/治理 Owner）零"风格"字样、`启动指令` 的 R9 描述与 `SKILL` 的 C 编号说明都停在旧层，`R9-视觉映射表` 未声明"通用 vs 风格"定位。本事务把治理面与跨层声明补齐，并把两项口径实测结论落档（不新增规则本体）。

- 边界：**只补治理面与声明定位，不改任何判据本体**；不并入 S3/CI/archive 台账等已登记结构性问题（见 §五）。

## 三、新口径 New Policy

1. **治理面跟进**：`AGENTS.md` 的 Owner 地图与主干必须反映六层模型——新增"风格层"行（声明源＝`scripts/ref-registry.json` 的 `styles`），R9 行改写为"导演决策 Owner"；§三 主干与 §九 隔离各补一句指针（选风格／换包不换导演稿），不复述细节。
2. **跨层编号**：C 编号是**跨层编号体系**（本步＋所选风格包共享同一套 C-xx 命名），说明与引用必须指到编号的真实所在层。
3. **R9-S 定位**：`R9-视觉映射表` ＝ **导演层通用默认起手式（工具无关）＋风格包可覆盖**；表内具体构件名是"当前已建成风格的实现举例（remotion-components）"，风格化片以所选风格包的表达能力替换举例，**叙事判断不变**（换包不换导演稿）。
4. **交付规格多落点**：`1080×1920 @30fps` 的**真源＝`R3` §7.4**；风格包 ③ 与通段验收声明同一规格属**契约明文授权的复述**（039 §二十三：平台交付规格属共享层硬约束，每包 ③ 必须声明画幅）——据此**不登记** `caliberOwners`（登记会把授权复述与 `R6` 的产品海报底图同尺寸误红），改为在真源处写明授权关系。

## 四、Replace

| # | 位置 | 旧 | 新 |
|---|---|---|---|
| 1 | `AGENTS.md` 页首 | "最后校验：2026-09-16" | "最后校验：2026-09-23" |
| 2 | `AGENTS.md` §二 Owner 地图 R9 行 | "视觉导演 ｜ R9/R9-视觉映射表 ｜ 视觉方向与映射" | "导演层（导演决策 Owner）｜ `R9-视觉导演与审美决策.md` / `R9-视觉映射表.md` ｜ 为什么这样讲＋视觉方向与映射；不写具体风格实现" |
| 3 | `AGENTS.md` §二 Owner 地图（新增行） | （无） | "风格层（表达能力包）｜ `video/styles/<id>/`（清单与声明源＝`scripts/ref-registry.json` 的 `styles`）｜ 用什么视觉语言表达；不拥有内容流程／叙事决策" |
| 4 | `AGENTS.md` §三 主干 | （无风格句） | 补一句：行业线每片**先选风格**；风格包不构成入口，经 `SKILL.md`/`R9` 内部路由 |
| 5 | `AGENTS.md` §九 视觉隔离 | （无风格轮换句） | 补一句：**同风格不得连续复用**；跨风格验收＝**导演稿替换测试**（判据在 `R9` §十五，长期有效） |
| 6 | `AI工作启动指令.md`:94 | "- 视觉导演：`R9`" | "- 导演层（导演决策）：`R9`" |
| 7 | `SKILL.md` 第 2 步 判据编号说明 | "第 2/4/5 步的人判画面判据带 `C-xx｜` 前缀" | "人判画面判据带 `C-xx｜` 前缀——**编号跨层**：第 4 步的在本步；第 2／5 步的多数已随迁入所选风格包（`remotion-components` §③A 的 C-02/03/15/16、§③C 的 C-12/13）" |
| 8 | `SKILL.md` 第 4 步 分镜稿模板 | "本屏判据自查：对照第 2/4/5 步 C 编号逐条 ✓/✗" | "…对照本文与所选风格包内的 C 编号逐条 ✓/✗" |
| 9 | `SKILL.md` §四 一屏标杆 | "（见第 2 步 C-16）" | "（见 `remotion-components` 包 §③A 的 C-16）" |
| 10 | `R9-视觉映射表.md` 表头 | 属主句（无定位） | 补定位句（见 §三.3）；表头列注明"举例＝remotion-components 构件，风格化片按所选包替换实现" |
| 11 | `R3` §7.3 画幅行 | 只列值 | 补一句"本值＝交付规格真源；各风格包 ③／通段验收声明同一规格属 039 明文授权的复述（不另立属主）" |
| 12 | `SKILL.md`:201 ＋ 三个风格包 ③（`_template`／`whiteboard`／`remotion-components`） | 引"`R3` **§7.4**"（画幅／安全区共性） | 引"`R3` **§7.3**"——**执行期新发现**（同族）：画幅/安全区真源在 §7.3「布局规则」，§7.4 是「代码结构规则」，属"锚点存在但指错节"（文档闸门只验节存在，抓不到） |

## 五、Remove

**Remove**：上表各处的旧表述；改后 §九 关键词复扫须归零或只剩变更史注记。

**不并入本事务（保持挂账，只登记结论）**：

| 项 | 结论／去向 |
|---|---|
| `caliberOwners` 登记交付规格（B1 实测结论） | **实测＝不登记**：`1080×1920` 落点共 **13 处**——8 处＝契约授权复述（风格包 ③／输出规格／通段验收／模板）；2 处＝`R6` 的产品海报底图同尺寸（**同名异义**，与交付规格无关）；2 处在真源 `R3` 自身（§7.3 画幅行＋屏结构描述）；1 处＝`SKILL` C-07 的屏几何容量说明。登记会把前 11 处全部误红 ⇒ 改为在真源写明授权关系（§四#11） |
| caliber 检查的**代码块豁免**（B3） | 登记为**检查器已知缺口**：本轮实测代码块内的口径复述数为 **N**（见 §九 实测），修检查器须另立事务并带正负向测试 |
| `MAP` 单线世界观、`AI使用手册`:69／爆款 README 的"唯一作业流程"、`check-release-feedback` 读 archive、CI 冻结 g11、`gate-all` 豁免正则只匹配 g\d{2}、声明与强制不对称、APPLET 快照 | 均已登记 P0/批次，本次不动 |
| 教程线视觉语言、风格化机检覆盖、白板 motion 重定标、第 4 步模板参数化 | 已登记，各自随所属事务 |

## 六、Preserve

- `outputs/archive/*`、`docs/changes/*`、`outputs/g06–g11`、`video/styles/*/probe`：零改动。
- 变更史注记（"原二分废止"等）与 `deprecatedTerms` 登记：保留。
- 039/040 已定的冻结条（六层模型、边界表、换包判据）：本事务只做"治理面补齐"，不改其内容。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `AGENTS.md`（5 处：日期／R9 行／新增风格行／§三／§九） | 治理 Owner | REPLACE＋ADD | 关键词与结构核对＋闸门 | 批 A |
| `AI工作启动指令.md`:94 | 用户侧控制台 | REPLACE | 同上 | 批 A |
| `SKILL.md`（3 处 C 编号） | 方法 Owner | REPLACE | 同上 | 批 A |
| `docs/internal/R9-视觉映射表.md`（定位句＋表头注） | 导演层辅助表 | ADD＋REPLACE | 边界句核对＋闸门 | 批 B2 |
| `docs/internal/R3-Remotion技术参考.md` §7.4（授权复述句） | 技术参考（规格真源） | ADD | 与 039 §二十三 对账 | 批 B1 |
| `caliberOwners`（交付规格） | 注册表 | **不动**（实测决定） | 结论入 §五 与 §九 | 批 B1 |
| caliber 代码块豁免 | 检查器 | **不动**（登记缺口） | 实测计数入 §九 | 批 B3 |

## 八、Migration Plan

1. 批 A：AGENTS 5 处 → 启动指令 1 处 → SKILL 3 处。
2. 批 B2：R9-视觉映射表定位句＋表头注。
3. 批 B1：R3 §7.4 补授权复述句（实测结论同时落档）。
4. 批 B3：跑"含代码块"的 caliber 实测计数并登记。
5. 复跑 `check-doc-references`／`check-change-contract`／`gate-all`；关键词复扫；行为零漂移核对。

## 九、Mechanical Checks

- 文档引用检查：`check-doc-references` 零硬失败（本事务新增/改动的引用全部指到真实落点）。
- 闸门：`check-change-contract` PASS；`gate-all` 与 041 前逐行一致（除已声明的文案变化）。
- 关键词复扫：`视觉导演：`（旧 Owner 描述）→ 0；`第 2/4/5 步 C 编号` → 0；`见第 2 步 C-16` → 0。
- **B3 实测**：以"含代码块"的模式重跑 caliber 检查（`/tmp/caliber-probe.mjs`，不改仓库）：**命中数 N = 0**——即当前**没有**口径复述藏在代码块里（040 修掉的那处是最后一例）；缺口仍在（豁免机制存在），登记为"机制缺口·当前残留 0"。
- **B1 实测**：交付规格落点 **13 处**逐条定性（见 §五 表）；结论＝授权复述＋同名异义，**不登记** caliber。
- **执行期新发现（同族，已随修）**：4 处引"`R3` §7.4"实为 §7.3（§四#12）；写本契约时两次踩"标题必须逐字"（`Remove`／`新口径 New Policy` 后不得带括注）——已按机检要求修正。

## 十、Negative / Semantic Counterexample

- **负向（治理面写成第二 Owner）**：在 `AGENTS.md` 里把风格包的十栏/判据细则抄进去——制造第二定义源。拦截：本事务只写"行＋指针"，逐条与 §四 表对账；发现复述即回退。
- **负向（口径白名单化）**：为让 caliber 通过而把交付规格改写成"1080×1920（见 R3）"这种去值形态——属于"为过检查器改正确语义"（整改总纲 §4 明禁）。拦截：本事务**不登记**该 caliber，从根上避免。
- **语义反例**：把 `R9-视觉映射表` 的定位句写成"本表只服务 remotion 风格"——则白板片的"叙事任务→表现"映射失去默认起手式，且与"换包不换导演稿"冲突。拦截：逐条核对定位句与 039 §三.2 边界表。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝治理面补齐与定位声明（文档层），不产出关键帧或成片；判据与闸门本体零改动。
```

## 十二、Closure Report

- 已修改：`AGENTS.md`（5 处：校验日期／R9 行改导演层／新增"风格层"行／§三 主干补风格句／§九 补轮换与换包判据）、`AI工作启动指令.md`:94、`SKILL.md`（判据编号说明＋第 4 步自查行＋§四 C-16 指针＋:201 的 §7.4→§7.3）、`docs/internal/R9-视觉映射表.md`（定位句＋表头注）、`docs/internal/R3-Remotion技术参考.md`（§7.3 画幅行补"交付规格真源＋授权复述"）、`video/styles/_template/SKILL.md`／`whiteboard/SKILL.md`／`remotion-components/SKILL.md`（③ 的 §7.4→§7.3）
- 已废止："视觉导演"作为 R9 的定位描述；"第 2/4/5 步 C 编号"落点；"`R3` §7.4"作为画幅／安全区引用；`R9-视觉映射表` 的"无定位"状态
- 已保留：六层模型／边界表／换包判据等冻结条；`caliberOwners` **不登记**交付规格（实测结论入 §五）；`outputs/archive`、`docs/changes`、历史注记、`deprecatedTerms` 零改动
- 影响面：`7 / 7 已处理`（§七 逐行）
- Owner/入口冲突：**PASS**（风格层行只声明 Owner 与声明源，未复述判据细则；未新增第二入口）
- 旧口径扫描：**PASS**（`视觉导演：`／`第 2/4/5 步 C 编号`／`见第 2 步 C-16` 复扫各 0 命中；§7.4 误引归零，仅剩真源与授权复述）
- 机械检查：**PASS**（`check-doc-references` 0 硬失败；`check-change-contract` PASS(43)；`gate-all` 13/15、2 项跳过、无红灯，与 041 前行级 diff 仅"事务计数 42→43"）
- 负向测试：**PASS**（注入回归：把 AGENTS 的"导演层（导演决策 Owner）"行改回"视觉导演"→ 复扫命中 1；还原后 0。另：本事务执行期文档闸门**当场抓到**我自己引入的 2 处坏引用〔R3 自引 `§二十三`、AGENTS 的 `video/styles/<id>/` 占位路径〕并已修——检查器有效性的当场实证）
- 语义反例：**PASS**（`R9-视觉映射表` 定位句写成"通用默认＋风格可覆盖"，未落成"只服务 remotion"；AGENTS 风格层行只写"行＋指针"，逐条与 §四 表对账无复述）
- 真图/成片：N/A（Real Output 声明为 `not-applicable`）
- 本次新增红：**0**

**结论：CLOSED**

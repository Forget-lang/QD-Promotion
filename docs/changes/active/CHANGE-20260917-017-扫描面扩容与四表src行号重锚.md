# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260917-017`
- 标题：检查器扫描面扩容（积分/会员两表）+ 四表 src 行号重锚
- 负责人：用户 + 本地 Agent
- 范围：`global`
- 状态：`VERIFYING`
- 基线：`HEAD = origin/main = 9f49fa9`
- **定位说明**：落点 2 类——`scripts/check-ui-truth.mjs`（检查器，1 个）＋ `spec/*.json`（真源 4 份的 `src` 证据定位字段）。属 015/016 已挂账「未做」清单的直接执行，跨对象 → 走 R8。

## 二、Goal

1. **扫描面扩容**：`check-ui-truth.mjs` 的 ⑤ 层自证目前只扫优惠券＋次卡双表；把 `spec/point-fields.json`（积分）与 `spec/member-fields.json`（会员权益）纳入，使新表同样受"表里的名字必须逐字回源码"约束。
2. **src 行号重锚**：清零 ⑤b（当前 51 处漂移，含扩容后新表新增的漂移），并把 `member-fields.json` 的裸文件名 `member-benefit.vue`（applet 有两份同名文件，消歧有解析到错文件的风险）改为全路径。**每处新行号都必须经过"原话确在该位置"的验证后才写入**——行号是证据定位，重锚是重新取证，不是机械改数字。

## 三、新口径 New Policy

1. `TABLES` 增加两表（tag：`积分` / `会员权益`），⑤ 层自证、④ 层表外提醒、`retiredRows` 豁免对四表一视同仁。
2. ③ 层原生组判定（`nativeGroups`）**维持只由优惠券表供给**（加 `nativeGroupEligible` 标记）：积分/会员页无分组行结构，不参与视频分镜的组归属判定，避免组名 `includes` 误匹配。
3. ⑤ 成功行文案由"双表（优惠券+次卡）"改为按 `TABLES` 动态拼接，避免下次扩容再忘。
4. **重锚纪律**：新 `src` 行号区段必须满足——该条目 `label` 的全部可取证片段落在闸门同款窗口（±10 行）内；区段选取取"覆盖全部片段的最小跨度"（≤60 行），并重新验证该字段 `limits/defaults` 里引用的「」原话在目标文件中仍然全局存在（防证据腐烂）。
5. **描述名处理**：新表中确属我方描述名（源码查无逐字原话）的行，按闸门建议标 `onScreen: false` 并写明理由；真实界面名被加了括注后缀的（如「客户备注（对外可见）」），label 改回源码逐字名，括注移入 `note`。

## 四、Replace

- `spec/member-fields.json`：src 裸文件名 → 全路径（`pages_merchant/customer/member-benefit.vue` 等）。
- 各表 `fields[].src` / `createGroups[].range` 的过期行号 → 验证后的新行号。
- ⑤ 成功行"双表"硬编码 → 动态。

## 五、Remove

- **无删除**：不改任何字段的 `label` 正文（仅"括注后缀回源码名"这一类微调，正文语义不变）、不动 `limits/defaults/playLeverage` 语义、不动 `constraints` 数值、不删任何既有行。

## 六、Preserve

- 检查器其余六层判定逻辑（①②③④⑥）逐字不动；`PAD=10`、窗口算法、`nameParts` 规则不动。
- 真值表的产品事实内容（015/016 刚重核的字段语义）不动；只动证据定位（src）与个别 label 的括注后缀。
- `ref-registry.json`、其余 `scripts/*.mjs`、CI 工作流零改动。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `scripts/check-ui-truth.mjs` `TABLES` | 检查器 | REPLACE | 四表入扫；③ 层行为不变；⑤ 成功行动态 | DONE |
| `spec/point-fields.json` `fields[].src` | Product Truth 证据定位 | REPLACE | 9 处重锚 + 3 处描述名/错指向修复，逐处验证 | DONE |
| `spec/member-fields.json` `fields[].src` | Product Truth 证据定位 | REPLACE | 裸文件名→全路径；4 处重锚 + 9 处修复 | DONE |
| `spec/coupon-fields.json` `src/range` | Product Truth 证据定位 | REPLACE | 67 处重锚 | DONE |
| `spec/card-fields.json` `src/range` | Product Truth 证据定位 | REPLACE | 29 处重锚 | DONE |
| 描述名 label（如有） | Product Truth | REPLACE | 4 个改回界面真名、5 个 `onScreen:false`＋理由 | DONE |
| `_meta.lastVerified` 四表 | 真源元信息 | PRESERVE | 四表均为 2026-09-17，零改动 | DONE |
| 六层判定逻辑（①②③④⑥） | 检查器 | PRESERVE | 逐字不动（diff 仅 TABLES/③ 标记/文案动态化） | DONE |
| `docs/changes/active/CHANGE-20260917-017-*.md` | 本事务 | ADD | 本文件 | DONE |

## 八、Migration Plan

1. 改检查器（`TABLES` ＋ ③ 层标记 ＋ 动态文案）→ 立即跑负向测试确认既有断言不破。
2. 跑 `check-ui-truth`，收集扩容后的全量 ⑤b/⑤c 清单。
3. 编写一次性重锚脚本（带逐处验证与断言），对四表统一执行；无法机器验证的留原样并单独列出人工处理。
4. 人工处理遗留项（描述名/多义名）。
5. 复跑 `check-ui-truth`（目标：`tableBad=0`、`tableMoved=0`）、`test-visual-shot-contract-negative`、`check-change-contract`、`gate-all` 比对基线。

## 九、Mechanical Checks

- JSON 合法：四份表 `json.load` 通过。
- 上屏真实性闸门：⑤ 层 `tableBad=0`、`tableMoved=0`；通过行显示四表。
- 负向测试：`test-visual-shot-contract-negative` exit 0（检查器改动不破坏既有断言）。
- 判定零变化：`gate-all` 仍 `2/15` 且红项构成与基线逐项一致（全部属 g11 线）。
- 契约闸门：`check-change-contract` PASS（含本契约）。
- 证据防腐：每字段 `limits/defaults` 中引用的「」原话在目标源码文件中全局仍存在（重锚脚本一并校验）。

## 十、Negative / Semantic Counterexample

**反例 1（机械改行号、不看内容）**：脚本拿正则猜一个新行号写回去。
**拦截层**：§三 第 4 条——只写"原话确在该位置"验证过的行号；验证不过的留原样并转人工。

**反例 2（为过闸门把描述名硬改成别的话）**：把「顾客侧展示」改成源码里另一处不相干的文案凑命中。
**拦截层**：§三 第 5 条——描述名只能标 `onScreen:false`＋理由，不许张冠李戴。

**反例 3（扩扫描面顺手扩 ③ 层）**：把积分/会员表的组名也喂给原生组判定。
**拦截层**：§三 第 2 条——`nativeGroupEligible` 仅优惠券表，③ 层行为不变（负向测试守护）。

**反例 4（重锚时顺手改字段语义）**：借改 src 之机"优化" limits/playLeverage 正文。
**拦截层**：§五 无删除 ＋ §六 Preserve；diff 只允许出现在 src 行号、label 括注后缀、检查器三处。

## 十一、Real Output Verification

**声明（二选一，机器可读，必填）**：本事务到底需要什么真实产物，由本事务自己声明，机器不猜。

```text
- Real Output 声明：`not-applicable`
- 理由：只改检查器扫描面与真值表证据定位，不产出、不修改任何视频画面 / 分镜 / Remotion 组件 / 渲染产物。
```

- 真实关键帧/短片：不适用
- 人工验收判据：⑤ 层四表自证通过且零漂移；`gate-all` 红项构成零变化
- 结果：PASS（not-applicable）

## 十二、Closure Report

- 已修改：`scripts/check-ui-truth.mjs`（TABLES 四表入扫 ＋ `nativeGroupEligible` 仅优惠券表供给 ③ ＋ ⑤ 成功行/跳过行动态化）、`spec/coupon-fields.json`（67 处 src/range 重锚）、`spec/card-fields.json`（29 处）、`spec/point-fields.json`（9 处重锚 ＋ 3 处描述名/错指向修复）、`spec/member-fields.json`（4 处重锚 ＋ 9 处修复：裸名→全路径、4 个描述名标 `onScreen:false`、2 个改回界面真名）、本契约
- 已废止：无
- 已保留：四表全部产品事实正文（label 语义/limits/defaults/playLeverage）、六层判定算法（PAD/窗口/nameParts 逐字未动）、`_meta.lastVerified`（四表均 2026-09-17）、`ref-registry.json` 与其余脚本零改动
- 影响面：`9 / 9`
- 旧口径扫描：PASS —— "双表（优惠券+次卡）"措辞在脚本内已无残留（④ 注释、⑤ 跳过行一并改齐）；spec 侧无任何"绝对行号当真源"的新表述
- 机械检查：PASS —— 四表 JSON 合法（coupon 保持 indent=1 逐字节可往返）；上屏真实性闸门 `tableBad=0 / tableMoved=0 / tableUnresolved=0`，`真值表自证 ✅ 四表 90 字段名 + 68 分组行名（158 个有效名字）逐字命中`；`test-visual-shot-contract-negative` exit 0；`check-change-contract` PASS（18 transactions）；`gate-all` `2/15` 且红项构成与基线逐项一致（全部属 g11 线）；`fields[].src` 每处新行号都经"原话确在该位置"窗口复核（±10 行，同检查器算法）
- 负向测试：PASS —— **扩容当场抓到 9 个新表问题（正是本次扩容的目的）**：① 「发放统计」src 指到 stat 页但真名在积分主页宫格（错指向）→ 改指 `point/index.vue:26-33`；② 「等级名称」第一引用指 constants（该文件查无）→ 换 member-benefit.vue:70-78；③④ 「等级图标」「等级颜色」源码查无连写 → `onScreen:false`+理由；⑤⑥⑦ 「顾客端」「等级数量」「顾客侧展示」我方描述名 → `onScreen:false`+理由；⑧⑨ 「类型方向」「顾客指派」「按等级圈人」→ 改回界面逐字真名（「发放积分 / 扣除积分」「会员等级」「群发对象」）。另：member 表裸文件名 `member-benefit.vue` 在 applet 有两份同名文件、消歧可能解析到 user 侧 → 全部补全路径
- 语义反例：PASS —— §十 四个反例逐条对照：① 机械改行号 —— 未发生（109 处全部验证式回写，0 条人工遗留）；② 描述名硬凑 —— 未发生（5 个标 false，4 个取真名，均有源码定位）；③ ③ 层误扩 —— 未发生（`nativeGroupEligible` 仅优惠券表，负向测试守护）；④ 借机改正文 —— 未发生（diff 仅 src/range 行号、label 括注、检查器三处；四表 diff 116/116 行且每处单行）
- 真图/成片：N/A（not-applicable，见 §十一）
- 本次新增红：**0** —— 迁移中一度 3/15（第 3 道为本契约 Impact Map 的 PENDING 行，回填后消除），最终 `2/15` 且红项构成与基线逐项一致

**结论：NOT CLOSED**

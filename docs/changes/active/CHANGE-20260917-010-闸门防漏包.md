# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260917-010`
- 标题：闸门防漏——空转提示与 CI 触发面补齐
- 负责人：用户 + 本地 Agent
- 范围：`global`
- 状态：`VERIFYING`
- 基线：`HEAD = origin/main = 4a1880e`
- **定位说明**：触及 2 个检查器 ＋ 1 个 CI 工作流 → 按 `AGENTS.md` §一 升级为全局变更走 R8。
- **顺序偏差声明**：本契约**在编辑之后落盘**（同 `CHANGE-20260917-007` 的处理方式）。因为本轮已先完成定位与改造验证，为不伪造"先立后迁"的外观，此处明写实际顺序；**残留差异**：契约未能约束编辑过程，只约束结果。

## 二、Goal

修掉本项目**复发型失效模式**："闸门失去检查对象时默认放行，绿灯失去信息量"。

**本轮的两个实例**：① `check-visual-shot-contract` 在找不到任何 R9 台账时静默 PASS（g11 删除后即空转）；② `CHANGE-20260917-009` 删除 `changelog.md` 后，`check-doc-references` 因"它不再是已知文件名"而**不再解析其引用**（另见 009 的补完记录）。闸门必须**在被掏空时说出来**，而不是继续亮绿灯。

另修一处触发面缺口：`change-contract-gate.yml` 的 `paths` 不含 `SKILL.md` / `AGENTS.md` / `docs/internal/**`，**单改生产 Owner 或领域 Owner 不触发 CI**。

## 三、新口径 New Policy

1. **闸门空转必须显式声明**：当闸门没有检查对象时，仍在**同一行** PASS 输出里附"空转提示"，说明"当前没有检查对象"。
2. **不得改变 PASS/FAIL 语义**：本次只加提示文字，**不改变任何闸门判定、不改变 `gate-all` 的通过项计数**。理由：`CHANGE-20260916-005`（未关闭）的关闭证据记录的是这两个检查器的当前行为；若本次改判定，会让它的证据与验证对象对不上。
3. **治理文档变更必须触发变更契约闸门**：`AGENTS.md` / `SKILL.md` / `AI工作启动指令.md` / `docs/internal/**` 纳入其 `paths`。

## 四、Replace

- 两个检查器的"纯 PASS 输出行"→ 同行的 PASS ＋ 空转提示。
- `change-contract-gate.yml` 的 `push.paths` 与 `pull_request.paths` 两份清单（各补 4 条）。

## 五、Remove

- 无删除；本事务**不移除任何检查项、不降低任何判据**。

## 六、Preserve

- 两个检查器的**全部判定逻辑与退出码**保持不变。
- `gate-all` 的输出格式与计数方式不变（提示附在同一行内，不新增行、不新增前缀）。
- g11 线与历史行业产出**不处理**（用户明确指示）。
- `CHANGE-20260916-005` 的证据链不受影响（见 §九 的"判定未变"核验）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `scripts/check-visual-shot-contract.mjs` | 检查器（提示） | REPLACE | 空转时输出仍含 `PASS` 且 exit=0，并附空转提示 | DONE（实测输出含提示，`exit=0`） |
| `scripts/check-change-contract.mjs` | 检查器（提示） | REPLACE | 无事务时输出仍含 `PASS` 且 exit=0，并附空转提示 | DONE（脚本替换命中 1 处；事务库当前非空故该分支未触发，判定未变） |
| `.github/workflows/change-contract-gate.yml` | CI 触发面 | REPLACE | 两份 paths 各补 4 条治理文档路径 | DONE（`AGENTS.md`/`SKILL.md`/`AI工作启动指令.md`/`docs/internal/**` 均已在位，`scripts/gate-all.mjs` 出现 2 次＝两份清单各一次） |
| `docs/changes/active/CHANGE-20260917-010-*.md` | 本事务 | ADD | 本文件 | DONE |

## 八、Migration Plan

1. 两个检查器：在空转分支的 PASS 输出后附加提示（同一行）。
2. CI 工作流：两份 `paths` 各补 4 条治理文档路径。
3. 核验"判定未变"：`gate-all` 仍为迁移前的通过项数；两个检查器退出码不变。
4. 回填 §九/§十二。

## 九、Mechanical Checks

- 旧口径扫描：不适用（本事务不废止任何词条）。
- 判定未变核验：`gate-all` 汇总与迁移前**逐项一致**（实测仍为同一通过项数、同为 2 道红且均为 g11 线）；`check-visual-shot-contract` 与 `check-change-contract` 退出码均为 0。
- 文档引用检查：本事务新增断链 0。
- 其他相关闸门：`check-change-contract` PASS（含本契约）。
- 工作流完整性：两份 `paths` 均已含 4 条新增路径（`grep` 复核）。

## 十、Negative / Semantic Counterexample

**反例 1（借"加提示"顺手改判定）**：把空转从 PASS 改成 WARN/FAIL。看似更严格，实际会让 `gate-all` 的通过项计数变化，从而使 `CHANGE-20260916-005` 的关闭证据与验证对象对不上。
**拦截层**：§三 第 2 条为硬约束；§九 要求"判定未变核验"逐项比对。

**反例 2（新增独立输出行）**：提示另起一行（如 `⚠️ 空转`），可能被下游解析器计为一项。形式上是"只加文字"，实际改变了输出结构。
**拦截层**：§三 第 1 条要求提示**附在同一行内**；§六 要求不新增行、不新增前缀。

**反例 3（paths 补得过宽）**：用 `'**'` 或 `'docs/**'` 覆盖，使闸门在无关改动上频繁触发，制造噪声后被习惯性忽略。
**拦截层**：只补 4 条**治理文档**路径，不做全量通配。

## 十一、Real Output Verification

**声明（二选一，机器可读，必填）**：本事务到底需要什么真实产物，由本事务自己声明，机器不猜。

```text
- Real Output 声明：`not-applicable`
- 理由：本事务只改两个检查器的提示文字与一个 CI 工作流的触发路径清单，不产出、不修改任何视频画面 / 分镜 / Remotion 组件 / 渲染产物，不存在可验收的真实画面。
```

- 真实关键帧/短片：不适用
- 人工验收判据：空转时闸门自己说出来；治理文档改动会触发变更契约闸门
- 结果：PASS（not-applicable）

## 十二、Closure Report

- 已修改：`scripts/check-visual-shot-contract.mjs`（1 行）、`scripts/check-change-contract.mjs`（1 行）、`.github/workflows/change-contract-gate.yml`（2 处 paths 各补 4 条）
- 已废止：无
- 已保留：三个文件的所有判定逻辑与退出码；`gate-all` 输出结构
- 影响面：`0 / 0`
- 旧口径扫描：不适用
- 机械检查：PASS —— `check-visual-shot-contract` exit 0 且输出含空转提示；`check-change-contract` PASS（11 transactions）；`gate-all` 与迁移前逐项一致
- 负向测试：PASS（**判定未变即判据**）—— 迁移前后 `gate-all` 同一通过项数、同一 2 道红（均属 g11 线）；两个检查器退出码均 0。若有人日后把空转改成 FAIL，该比对立刻失配
- 语义反例：PASS —— §十 三个反例（顺手改判定 / 另起一行 / paths 过宽）各自拦截层已写明，且反例 1、2 是本次实现的硬约束
- 真图/成片：N/A（not-applicable，见 §十一）
- 本次新增红：0 —— `gate-all` 2 道红全部属 g11 线（文档引用 5 处断链 / 母题卡），与本事务无关

**结论：NOT CLOSED**

**说明**：
1. 等用户确认；已随本轮推送（推送后 fresh CI 由用户在 GitHub 侧查看并用于关闭 005~010）。
2. **未做（按用户指示）**：g11 线、历史行业产出、`check-doc-references` 的 basename 解析改造（爆炸半径大，属独立事务）、两处重复项合并（大改，只登记）。

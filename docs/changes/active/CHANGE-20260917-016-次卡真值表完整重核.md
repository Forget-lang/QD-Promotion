# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260917-016`
- 标题：次卡真值表完整重核（015 的下一批）
- 负责人：用户 + 本地 Agent
- 范围：`global`
- 状态：`VERIFYING`
- 基线：`HEAD = origin/main = 212e942`
- **定位说明**：Owner 落点 1 个（`spec/card-fields.json`）→ 单 Owner 单表，但它是**所有次卡类内容抄字段名的源头**，按 `AGENTS.md` §一 最小定位判为需留痕的变更 → 走 R8 留档。
- **顺序偏差声明**：本契约在迁移完成后落盘（同 `CHANGE-20260917-010` 的处理方式）；因本笔是对 015「未做」清单的直接执行，取证在 015 期间已完成。**残留差异**：契约未能约束编辑过程，只约束结果。

## 二、Goal

执行 `CHANGE-20260917-015` §十二「未做」第 1 项：把 `spec/card-fields.json` 补到与 `applet@42a473b` 一致（015 期间实证的 4 处新增 + 2 处数值变化 + 2 条需实测）。

## 三、新口径 New Policy

1. **版本门禁首次登记**（顶层 `versionGate`）：次卡（TIMES）**无门禁、免费版可用**；通卡 / 权益卡**需专业版及以上**。对外一律不提（`forbidden_claims #11`）。
2. **「每张包含次数」上限随版本档位**：免费 7 / 标准 30 / 专业 100 / 企业 365（`create.vue:286-289` 动态「最少1次，最多N次」）。**对外口径维持 1000**（用户 2026-09-17 13:12 裁定，013 已登记）。
3. **「有效期」上限随版本档位**：免费/标准/专业 365、企业 1095。
4. **新增两行**：「仅新客户可领」（需标准版）、「券内容图片」（**onScreen=false**：该行为 `m-cover-uploader` 上传组件，页面无自带标题文案，无可逐字引用的 label；内容可讲"最多 9 张图"，不得引用该字段名）。
5. **UI 变更登记**（`uiNotes202609`）：使用须知组新增「清空」按钮及确认弹窗。
6. **新增 `unverified` 键**（与 `coupon-fields.json` 结构对齐）：转赠默认值、到期提醒默认值两条需实测。

## 四、Replace

- 「每张包含次数」：`最少 1 次，最多 1000 次` → 随版本档位 + 对外口径维持 1000。
- 「有效期」：`最少 1 天，最多 365 天` → 随版本档位（企业 1095）。

## 五、Remove

- **无删除**。不改任何既有字段的 `label` / `src` / `playLeverage` 正文；不动 `constraints` 数值；不动检查器。

## 六、Preserve

- `_meta.source` / `_meta.usage` / `_meta.machineCheck` 不动；`lastVerified` 前推至 2026-09-17（本表已逐条重核并补齐，同 `coupon-fields` 的 015 处理）。
- 其余 27 条既有字段的 `label` / `limits` / `defaults` / `visibleWhen` / `playLeverage` 零改动。
- `card-fields` 既有 `offScript` 三条（013 所加的版本档位条 + 活码条 + 转赠条）不动。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `spec/card-fields.json` 顶层 `versionGate` | Product Truth | ADD | 次卡无门禁 / 通卡·权益卡需专业版，并注明"对外不提" | DONE |
| `spec/card-fields.json` `fields` | Product Truth | ADD | ＋「仅新客户可领」「券内容图片（onScreen=false）」两行 | DONE |
| 「每张包含次数」`limits` | Product Truth | REPLACE | 上限随版本档位 + 对外口径 1000 | DONE |
| 「有效期」`limits` | Product Truth | REPLACE | 上限随版本档位（企业 1095） | DONE |
| `uiNotes202609` / `unverified` | Product Truth | ADD | 清空按钮登记；2 条需实测 | DONE |
| `_meta.lastVerified` | 真源元信息 | REPLACE | 2026-09-07 → 2026-09-17（已逐条重核） | DONE |
| `scripts/*.mjs` | 检查器 | PRESERVE | 零改动 | DONE |
| `docs/changes/active/CHANGE-20260917-016-*.md` | 本事务 | ADD | 本文件 | DONE |

## 八、Migration Plan

1. 定点替换 4 处 + `fields` 追加 2 行 + 顶层/尾部键扩展。
2. `check-ui-truth` 复跑（上屏真实性闸门对次卡表逐字自证）。
3. `gate-all` 与基线比对。

## 九、Mechanical Checks

- JSON 合法：`json.load` 通过；`fields` 27 → 29 条；新增 `versionGate` / `uiNotes202609` / `unverified` 三键。
- **上屏真实性闸门**：过程中两次抓到问题并当场修掉——① `fields` 追加时插错位置（JSON 解析失败，精确修复）；② 新增行 label「券内容图片」**源码查无**（该行为上传组件、页面无自带标题）→ 按闸门建议标 `onScreen:false` 并写明理由。最终 `真值表自证 ✅ 77 个字段名 + 68 个分组行名逐字命中`。
- 判定零变化：`gate-all` 最终 `2/15`，红项构成与基线逐项一致（全部属 g11 线）。
- 检查器零改动：`scripts/` 0 行。
- 契约闸门：`check-change-contract` PASS（含本契约）。

## 十、Negative / Semantic Counterexample

**反例 1（把次卡次数上限改成 7/30/100/365 的版本口径）**：真值随版本，就让对外也说"你这个版本只能 7 次"。
**拦截层**：§三 第 2 条——对外口径维持 1000（用户 13:12 裁定），版本差异只在真值层。

**反例 2（为凑字段名而编造 label）**：给上传组件安一个源码里没有的标题。
**拦截层**：本笔实际发生并按闸门建议改用 `onScreen:false` + 理由（"源码查无：券内容图片"即机检证据）。

**反例 3（把"需专业版"写进内容）**：讲通卡/权益卡时说"这是付费功能"。
**拦截层**：§三 第 1 条 ＋ `forbidden_claims #11`。

**反例 4（顺手把 015 已关闭的范围扩回来）**：改 coupon-fields 或 features。
**拦截层**：§五 明写不动；015 已关闭，新范围必须另立事务（本笔即如此）。

## 十一、Real Output Verification

**声明（二选一，机器可读，必填）**：本事务到底需要什么真实产物，由本事务自己声明，机器不猜。

```text
- Real Output 声明：`not-applicable`
- 理由：只重核次卡真值表（字段/上限/门禁登记），不产出、不修改任何视频画面 / 分镜 / Remotion 组件 / 渲染产物。
```

- 真实关键帧/短片：不适用
- 人工验收判据：上屏真实性闸门逐字自证通过；`gate-all` 红项构成零变化
- 结果：PASS（not-applicable）

## 十二、Closure Report

- 已修改：`spec/card-fields.json`（顶层 `versionGate` ＋ `fields` ＋2 行 ＋ 「每张包含次数」/「有效期」`limits` 订正 ＋ `uiNotes202609` ＋ `unverified` 新键 ＋ `lastVerified` 2026-09-07→2026-09-17）、`docs/changes/active/CHANGE-20260917-016-*.md`
- 已废止：无
- 已保留：其余 27 条字段的 `label`/`src`/`playLeverage` 正文；`facts.constraints` 数值；三份既有 `offScript` 条目；`scripts/` 与快照零改动
- 影响面：`8 / 8`
- 旧口径扫描：PASS —— "最多 1000 次""最多 365 天"两处旧表述已不再以绝对形式存在，替换为"随版本档位 + 对外口径"双层写法
- 机械检查：PASS —— JSON 合法（`fields` 27→29）；上屏真实性闸门 ✅（77 字段名 + 68 分组行名逐字命中）；`check-change-contract` PASS；`gate-all` `2/15` 且红项构成与基线逐项一致；`scripts/` 零改动
- 负向测试：PASS —— **上屏真实性闸门两次当场拦截**：① fields 追加位置错误 → JSON 解析失败（机检不可绕过）；② 新增 label「券内容图片」源码查无 → 按闸门建议改 `onScreen:false` 并留理由。两条都是"表里的假名字会被后续每一片照抄"这一风险的实证
- 语义反例：PASS —— §十 四个反例逐条对照：① 版本口径进内容 —— 未发生（对外维持 1000）；② 编造 label —— 发生过一次、被闸门拦下并按建议改 `onScreen:false` 留痕；③ "需专业版"进内容 —— 未发生；④ 扩 015 范围 —— 未发生（另立本事务）
- 真图/成片：N/A（not-applicable，见 §十一）
- 本次新增红：**0** —— 迁移中一度 3/15，两次拦截均已当场修正，最终 `2/15` 且红项构成与基线逐项一致

**结论：NOT CLOSED**

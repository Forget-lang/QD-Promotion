# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-063`
- 标题：建立贴图宣传方法与独立风格层
- 负责人：项目 AI
- 范围：`global`
- 状态：`VERIFYING`

## 二、Goal

建立独立的「贴图宣传策划与制作」方法，并为它建立独立的静态贴图风格层。方法层负责研究、原创攻略、平台安全与交付；风格层负责字体、色彩、材质、版式和静态视觉表达，避免把具体画法再次写死在方法文档中，也避免把静态贴图误接入现有视频 `styles`。

## 三、新口径 New Policy

1. 贴图宣传是独立的静态视觉资产生产职责，不进入视频生产流程。
2. 官网只提供产品场景、玩法与讲解语境；产品能力/数字/限制必须回 Product Truth 验真。
3. 每张贴图先回答一个明确的经营问题或认知问题，再设计文案与画面；禁止先找官网素材再拼成海报。
4. 官网内容不得直接抄写；允许学习场景和机制，但必须由 AI 重新组织、原创表达并结合券到卡包真实产品能力设计。
5. 默认一张图一个主题、一张图一个独立成品；禁止九宫格、拼图、长图合集。
6. 平台公开发布版默认按抖音/小红书安全口径设计：不放第三方导流元素、联系方式、二维码、搜索导流或诱导互动话术；尺寸默认 1080×1440（3:4），需要 9:16 时单独重设计。
7. AI 可自由生成场景、人物、隐喻和氛围；不得凭空生成会被理解为真实产品界面的 UI、字段、数字、结果或功能限制。
8. 独立静态贴图采用独立 `poster/styles/` 风格命名空间；方法层必须声明 `styleId`，具体视觉表达只由所选风格包承载；不得复用视频 `styles` 作为静态贴图 Owner。

## 四、Replace

- 替代“靠会话记忆约定独立静态贴图怎么做”的临时流程。
- 替代“直接从官网选标题/文案再美化”的做法。
- 替代“一次生成多张合成图再拆分”的做法。

## 五、Remove

- 新贴图不再采用九宫格/五宫格/合集式单图。
- 不再把官网营销话术直接复制进贴图。
- 不再把第三方平台名称、二维码、联系方式等作为公开贴图的默认设计元素。

## 六、Preserve

- 已完成的「券到卡包功能介绍」9 张作品作为既有资产保留。
- 已确认的蓝白、轻 3D、SaaS 产品视觉经验保留为审美参考，并正式收敛为首个贴图表达能力包 `light-saas-3d`；保留的是视觉语言，不保留任何单张构图模板。
- Product Truth、redlines、玩法版图、官网资源调用边界继续沿用原 Owner。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 状态 |
|---|---|---|---|---|
| `docs/internal/贴图宣传作业规范.md` | 方法 Owner | ADD | 已创建并冷读 | PASS |
| `AGENTS.md` | AI 入口/Owner 地图 | REPLACE | 已注册独立贴图方法 Owner，并明确与既有抖音图文线按是否伴生 gXX 判线 | PASS |
| `scripts/ref-registry.json` | 权威文档注册 | REPLACE | alias + extraDocs 已登记；新增 `posterStyles` 声明源 | VERIFYING |
| `poster/styles/_template/SKILL.md` | 贴图风格骨架 | ADD | 风格包边界、字段与禁止越界项 | VERIFYING |
| `poster/styles/light-saas-3d/SKILL.md` | 首个贴图风格包 | ADD | 蓝白轻 3D SaaS 视觉语言正式收敛 | VERIFYING |
| 现有 9 张贴图 | 历史交付 | PRESERVE | 未改动旧资产 | PASS |

## 八、Migration Plan

1. 新建贴图宣传作业规范，定义职责边界、官网深挖法、原创攻略方法、图片制作规则、平台安全规则和验收清单。
2. 在 AGENTS 注册独立贴图方法 Owner，但不把它并入视频 contentLines，也不替代 SKILL 已有抖音图文线；以“是否伴生 gXX”作为判线。
3. 建立 `poster/styles/_template/` 风格骨架，并建立首个可用包 `poster/styles/light-saas-3d/`。
4. 在 ref-registry 增加独立 `posterStyles` 声明源，并将模板/首包纳入文档索引；不新增第三个视频 content line，不让贴图资产进入视频 `styles`/轮换/闸门认领。
5. 冷读方法层与风格层，重点检查“方法 vs 风格”的 Owner 边界、“独立贴图 vs 既有抖音图文伴生线”的边界，以及“官网不得直抄”“一图一成品”“尺寸”“平台安全”“真实产品能力”等约束。
6. 验证新规范与现有 Product Truth / redlines / 内容资源调用协议 / R9 没有发生第二 Owner 冲突。

## 九、Mechanical Checks

- 旧术语/旧口径扫描：PENDING
- 旧组件/旧布局扫描：N/A
- 文档引用检查：PENDING
- JSON/Owner 注册检查：PENDING
- 贴图风格包越界扫描：PENDING

## 十、Negative / Semantic Counterexample

反例：把官网“结账送券”页面的标题、段落顺序和标语原样搬进一张贴图，只更换配色和插画。虽然没有出现“复制官网”字样，但实质仍是官网直抄，应被新技能的人判拒绝。

反例：生成一张“优惠券 + 门店 + 二维码 + 加好友领取”的漂亮海报。即使把二维码换成图形符号，仍属于导流表达，应由平台安全层拒绝。

## 十一、Real Output Verification

- Real Output 声明：`not-applicable`
- 理由：本事务只建立工作技能与 Owner 路由，本次迁移不要求把新贴图作为关闭证据。

## 十二、Closure Report

- 已修改：`docs/internal/贴图宣传作业规范.md`, `AGENTS.md`, `scripts/ref-registry.json`, `poster/styles/_template/SKILL.md`, `poster/styles/light-saas-3d/SKILL.md`
- 已废止：会话临时流程、官网直抄、合成贴图做法（新产出路径不再采用）；方法文档内具体视觉套路也不再作为长期规则 Owner
- 已保留：既有 9 张贴图资产；原 Product Truth / redlines / 玩法版图 Owner
- 影响面：`4 / 7`
- Owner/入口冲突：PENDING
- 旧口径扫描：PENDING
- 机械检查：PENDING
- 负向测试：PENDING
- 语义反例：PENDING（在原有官网直抄、二维码导流、虚构 UI 之外，新增“把静态贴图接入视频 `styles`”“风格包反向要求改写内容”两类反例）
- 真图/成片：N/A
- 本次新增红：PENDING

**结论：VERIFYING**
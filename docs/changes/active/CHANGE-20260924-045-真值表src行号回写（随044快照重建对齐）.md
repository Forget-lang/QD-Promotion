# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260924-045`
- 标题：真值表 `src` 行号回写——随 APPLET 快照 09-24 重建对齐（验收线：`check-ui-truth` ⑤b 归零）
- 负责人：用户（方向）／AI-B（侦察、契约、迁移、验证）
- 范围：`project`
- 状态：`CLOSED`
- 触发：用户 2026-09-24「你来提交并执行吧」；上一批（044）实测留档的挂账项①＝`check-ui-truth` ⑤b「src 行号漂移 82 处」

## 二、Goal

一句话：快照重建（044）后产品源码行号整体位移，`spec/coupon-fields.json` 里 49 个字段 `src` 与 15 个分组 `range` 指向的行段不再包含对应原话（闸门以"原话仍全局存在"降级为 advisory）——本事务把这些定位一次性回写到**当前源码的真实行段**，让 ⑤b 归零、证据链重新可点。

- 边界：**只改行号/范围数字**，字段名、引文、limits/defaults/playLeverage 等一律不动；其余三表（次卡／积分／会员）本轮无漂移，零改动。

## 三、新口径 New Policy

1. `src` / `range` ＝ **对当前 APPLET 源可点开的定位**（回写依据：闸门同款窗口判定 ＋ "收紧到实际出现行"）。
2. 回写后证据链要求：`check-ui-truth` ⑤ 通过 且 **⑤b 归零**。

## 四、Replace

| # | 位置 | 旧 | 新 |
|---|---|---|---|
| 1 | `spec/coupon-fields.json` 49 个字段 `src` | 09-15 版行号（如 `create.vue:39`／`create.vue:50`…） | 09-24 源实际行号（如 `create.vue:69`／`create.vue:80`…） |
| 2 | 同上 15 个 `createGroups[].range` | 旧行段（如 `create.vue:39-116`／`create.vue:148-185`…） | 新行段（如 `create.vue:69-146`／`create.vue:178-215`…） |
| 3 | 同上 `_meta.lastVerified` ＋新增 `lastSrcRewrite` | `2026-08-30` | `2026-09-24` ＋一行回写溯源说明 |

## 五、Remove

**Remove**：上述旧行号/旧行段。

**不并入本事务（登记备查）**：`spec/coupon-fields.json` 内**非闸门覆盖**的 32 处证据型 `src`（`linkages[]` 11／`issueChannels.private`／`verifyPageKeyInfo{,.items[]}`／`statPageKeyInfo{,.labels[]}`／`messageReminders`／`customerSidePhrases{,.items[]}`／`offScript.items[]`）——它们不在 ⑤b 判定面内，本轮未逐条复核；如需回写，另立小事务（同类方法可直接复用）。

## 六、Preserve

- 字段名／引文／`limits`／`defaults`／`visibleWhen`／`linkages`／`playLeverage`／`createGroups[].rows` 文本：零改动。
- `spec/card-fields.json`／`point-fields.json`／`member-fields.json`：零改动。
- `../applet`（live 源）与快照：零改动（本事务只改"表里的定位"）。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `spec/coupon-fields.json`（64 处数字＋1 处 `_meta`） | 真值表定位 | REPLACE | ⑤/⑤b 复跑＋JSON 校验＋抽样对源 | 批 1 |
| 其余三表 | 真值表 | PRESERVE | ⑤ 仍通过 | 批 1 |

## 八、Migration Plan

1. 以闸门同款规则（PAD=10 窗口 ＋ 裸文件名按 `pages_coupon` 消歧）计算新行段 → "收紧"到实际出现行。
2. 文本级精确改（`fields[]` 按 `label` 反查；`range` 按文档顺序）——不重排 JSON、不改缩进/转义。
3. 复跑 `check-ui-truth`（期望 ⑤ 通过、⑤b 消失）＋`gate-all`／`check-doc-references`／`check-change-contract`。

## 九、Mechanical Checks

- `check-ui-truth`：**⑤b 归零**（原 82 处）、⑤ 通过、③ 通过（分组归属仍对）。
- 变更面：`git diff --stat` ＝ 单文件 64 行数字替换 ＋ `_meta` 1 处；无其他文件被触。
- JSON 可解析；字段名/引文零变化（diff 逐行核对仅数字）。
- 抽样对源：`发放方式→32`／`适用门店→233-244`／`使用须知→465`／`一键套用模版→483`／`领取周期→331`／`创建优惠券→603` 与 live 源实测行一致。
- 全闸门：`gate-all` 13/15、无红灯；`check-doc-references` 0 硬失败；`check-change-contract` PASS。

## 十、Negative / Semantic Counterexample

- **负向（过窗不指真处）**：把 `src` 写成"窗口能过、但指向别处"的行——拦截：回写前先"收紧到实际出现行"（取窗口内该文本的实际行段），并抽样 6 处与源码逐条对行。
- **负向测试（本机实测）**：故意把 `封面图片` 的 `src` 改为 `create.vue:300` → ⑤b 立即报「1 处」并点名该字段；还原后 ⑤b 归零（闸门对该类错误有效）。
- **语义反例**：为让 ⑤b 变绿而删掉/改短引文（如把 `线下收款口令 / 开启领取口令` 拆成单词）——属"改语义过闸"。拦截：§六 明文禁止改字段名/引文；diff 逐行核对仅数字变化。

## 十一、Real Output Verification

```text
- Real Output 声明：`not-applicable`
- 理由：本事务范围＝真值表内的定位回写（数字层），不产出关键帧或成片；产品事实本身零改动。
```

## 十二、Closure Report

- 已修改：`spec/coupon-fields.json`——49 个字段 `src` ＋ 15 个 `createGroups[].range` 行段回写；`_meta.lastVerified` 08-30→09-24 并新增 `lastSrcRewrite` 溯源行
- 已废止：09-15 版行号/行段（如 `create.vue:39-116`）；"src 行号漂移 82 处"的 advisory 状态
- 已保留：字段名／引文／`limits`／`defaults`／`visibleWhen`／`linkages`／`playLeverage`／`rows` 文本零改动；`card-fields.json`／`point-fields.json`／`member-fields.json` 零改动；`../applet` 与快照零改动
- 影响面：`2 / 2 已处理`（§七 逐行）
- 旧口径扫描：**PASS**（本事务只改数字定位，无话头类口径；039 四条旧口径复扫仍 0 命中，见 `gate-all` 全绿）
- 机械检查：**PASS**（`check-ui-truth`：**⑤b 由 82 处归零**、⑤ 与 ③ 通过；`gate-all` 13/15、2 项跳过、无红灯；`check-doc-references` 0 硬失败；`check-change-contract` PASS(47)；`git diff --stat` ＝单文件 64 行替换＋`_meta` 1 处，无其他文件被触；JSON 可解析）
- 负向测试：**PASS**（本机实测：把 `封面图片` 的 `src` 改成 `create.vue:300` → ⑤b 立即报「1 处」并点名；还原后 ⑤b 归零）
- 语义反例：**PASS**（diff 逐行为"仅数字变化"，字段名与引文未动；抽样 6 处对源一致：`发放方式→32`／`适用门店→233-244`／`使用须知→465`／`一键套用模版→483`／`领取周期→331`／`创建优惠券→603`）
- 真图/成片：N/A（Real Output 声明为 `not-applicable`）
- 本次新增红：**0**

**结论：CLOSED**

# Change Contract

## 一、基本信息

- changeId：`CHANGE-20260921-034`
- 标题：状态源身份收口——历史快照冻结与架构索引去实时化
- 负责人：用户（定性与拍板）＋ 项目 AI（契约、迁移、验证）
- 范围：`global`
- 状态：`PROPOSED`
- **前置**：`CHANGE-20260920-031`／`032`／`033` 均 `CLOSED`；`docs/changes/active/` 在本事务建立前为空。
- **定位四问**：① 层＝治理层的**状态归属与历史边界**（不是生产方法层）；② Owner＝实时状态唯一取值处＝`AGENTS.md` §十一 所指的 `docs/changes/{active,closed}/` ＋脚本输出＋当前产物（本事务不新设状态 Owner）；被修的两份文件只是**引用者／索引者**，不拥有任何规则；③ 牵动＝两份文档正文 ＋ `check-doc-references` 扫描面复跑；**零脚本、零注册表、零 spec、零闸门判据**；④ 旧口径＝两份文件中"自称当前状态"的**身份句**（不是内容重写）。

### 侦察结论（本契约的事实基础，均本机实测）

1. **S2（用户定性 P0，停线触发点）**：`outputs/archive/挂账总览.md:1` 标题写「当前活挂账清单」、`:14` 二级标题写「当前活挂账」、`:34` 原文「不以历史挂账推断当前状态；**以本表、`PROJECT-CONSISTENCY-MAP.md`**、active Change Contract 与最新 CI 为准」。同目录 `README.md` 自定纪律为"本目录不得被注册为新的生产 Owner、AI 自动入口或用户当前指挥接口"，`AGENTS.md` §十一 规定 `outputs/archive/` 只作历史证据 → **历史文件主动对当前会话发指挥句**，与治理规则直接冲突。
2. **S1（用户定性 P0，恢复链污染）**：`docs/internal/PROJECT-CONSISTENCY-MAP.md:3` 「状态：生效」、`:4` 「索引权威关系与**当前闭环状态**」、`:6` 「本账是**当前状态总账**」。而 `AGENTS.md:158` 把它指定为「只用于**恢复状态**，不是入口」——限定的是入口资格，**未免除其状态职能**。于是合法接班链（新会话 → `AGENTS.md` → §十一 恢复状态 → 读本账）会取到**已证伪的当前态**：§2 :47/:48 仍写"新 g11 尚未进入机检收口／尚未进入最终 Gate／上一版产物已全部删除"，§3 :62-73 整节写"当前生产事项：g11…第 1 步治理回归未完成"。反证：`outputs/archive/发布后验台账.json` g11 视频行与图文行均为 `发布时间 2026-09-18 / 状态 已发布-待回填`；`video/src/VTemplate.tsx:189` 实为 `const sceneScale = 1;`（`CHANGE-20260918-023` REPLACE）。
3. **为何 §2「当前状态」列是结构缺陷而非笔误**：该列要求一份**稳定架构索引**逐格填报波动状态，每次生产推进都必然使其过期——`CHANGE-20260917-008`（硬编码 active 事务号）与 `CHANGE-20260917-011`/`012`（变更史、fresh CI 行）已三次修补同一列，每次修完再次漂移。故本批从**列语义**下手，不再逐格填数。
4. **S3 不在本批（用户裁定）**：`scripts/ref-registry.json:88-98` `extraDocs` 登记 4 份 archive 文档、以及"扫描面全绿与被扫文档是否过期无因果关系"（`caliberOwners` 实测 10 条 pattern **不含 `sceneScale`**），属检查器语义边界改造，**另立事务**并须带负向测试。本批因此**不改 `ref-registry.json`、不改 `scripts/`**。
5. **S2→S1 的顺序理由**（用户裁定）：S2 修改面最小且是最直接的停线证据；S1 紧随其后拆掉"实时状态账本"身份。
6. **证据保全已核**：`PROJECT-CONSISTENCY-MAP.md:52-60`（P0-A Product Truth 闭环块）所载 `原始 archive SHA-256`／`298 文件`／`Snapshot SHA-256` 三值在 `spec/product-truth/README.md:8-10` **逐字重复存在**，且 :35 反向引用本账 → 本账改指针不丢证据。

## 二、Goal

只有一个目标：**让新会话从 `AGENTS.md` 出发、沿 §十一 的合法恢复路径接班时，不再得到伪当前态。** 具体＝① 撤掉 archive 内文件对当前会话的指挥句；② 撤掉架构索引的"实时状态账本"身份，把它改造成不承担波动的**稳定架构恢复索引**（保留 §1 主链、§2 层级职责、§6 接班规则的价值，不修成空壳）。

**本批不做**：不重写 archive 其余内容；不改检查器；不动 `ref-registry.json`；不整理与本目标无关的表述。

## 三、新口径 New Policy

> 以下为逐字文本，批准即按此执行。所有引用只指向 Owner，不复述规则正文。

### 3.1 `outputs/archive/挂账总览.md` — 头部 3 行（:1、:3、:5）替换

原：

```text
# 挂账总览（当前活挂账清单；已完成事项不再重复挂账）

> **规则**：已定方向、未开工/未闭环的事项才进入本表；开工一项、闭环一项就销项。变更史记 `docs/changes/`，本表不复制当前待办。历史片（g01–g10）按发布后验协议执行历史豁免；新发布片进入台账并等待用户回填后台数据。
>
> **重要**：Product Truth snapshot、UI Truth、safe-area probe、R9 Visual Shot Contract、g11 fresh frames 与 gate-all 已闭环，不得重新作为 P0/P1 挂账。
```

改为：

```text
# 挂账总览（历史挂账快照 · 内容截至 2026-09-16）

> **性质**：本文件位于 `outputs/archive/`，只作历史追溯证据，不承担当前状态，不得被任何会话当作取值处（目录纪律见本目录 README；状态归属见 `AGENTS.md` §十一）。
> 下文各节措辞是**快照当时的口径**，部分已被后续事务取代；按本目录纪律不逐条回填正文，需要现行状态请回 `docs/changes/` 与脚本输出。
```

### 3.2 `outputs/archive/挂账总览.md` — 身份标题 2 处

- :14 `## 二、当前活挂账` → `## 二、挂账（快照当时）`
- :31 `## 四、下一片前置纪律` → `## 四、下一片前置纪律（快照当时口径）`

### 3.3 `outputs/archive/挂账总览.md` — 删除 :34 越权指挥句（本契约核心动作）

原：

```text
2. 不以历史挂账推断当前状态；以本表、`PROJECT-CONSISTENCY-MAP.md`、active Change Contract 与最新 CI 为准。
```

改为：

```text
2. 本表属历史证据，**不得用作当前状态判据**；现行状态一律回 `docs/changes/` 与脚本输出（见 `AGENTS.md` §十一）。
```

§四其余 1/3/4 条与 §一、§二、§三正文**逐字不动**（含 :25 `sceneScale=0.76` 等已过期事实——历史允许过期，不允许指挥）。

### 3.4 `docs/internal/PROJECT-CONSISTENCY-MAP.md` — 头部身份 3 行（:3、:4、:6）替换

原：

```text
> 状态：生效
> 用途：索引权威关系与当前闭环状态，不新增业务规则。
> 文档引用纪律：本账只引用当前有效路径；不保留已删除文件名、历史错误路径或“旧名→新名”的废弃话头。
> 接班纪律：本账是当前状态总账，不是新会话第一入口。新会话统一从 `AGENTS.md` 开始，再按当前任务读取 `SKILL.md` 与对应 Owner。
```

改为：

```text
> 状态：生效
> 用途：**稳定架构恢复索引**——只索引权威层级与主链关系，不新增业务规则。
> 文档引用纪律：本账只引用当前有效路径；不保留已删除文件名、历史错误路径或“旧名→新名”的废弃话头。
> 实时状态边界：本账**不提供实时状态**，也不承担"当前状态总账"角色；取值处见 `AGENTS.md` §十一。本文件任何一节都不得写入片号级或事务级的波动状态。
> 接班纪律：新会话统一从 `AGENTS.md` 开始，再按当前任务读取 `SKILL.md` 与对应 Owner。
```

### 3.5 `docs/internal/PROJECT-CONSISTENCY-MAP.md` — §2 第四列去实时化（:36 列名 + 4 个波动格）

列名：`| 层 | 权威 | 负责什么 | 当前状态 |` → `| 层 | 权威 | 负责什么 | 边界（波动状态不写入本列） |`

仅替换以下 4 个单元格，其余 8 行（「生效」等结构性描述）**不动**：

| 行 | 原单元格 | 改为 |
|---|---|---|
| 产品事实 | `**snapshot 已入主分支；CI 已验证**` | `真源在 APPLET；仓库内快照的取值与校验见 `spec/product-truth/README.md`` |
| 红线 | `硬禁层无命中；人工确认按当前生产流程执行` | `词/元素级硬禁与人工确认层以脚本输出为准，本行不填读数` |
| 镜头契约 | `机制生效；新 g11 尚未进入机检收口` | `机制生效；逐片收口与否以脚本与当前产物为准` |
| 成片验证 | `新 g11 尚未进入最终 Gate；上一版产物已全部删除，不再作为当前证据` | `统一总闸门；逐片红绿以 `gate-all` 输出为准，本行不填读数` |

### 3.6 `docs/internal/PROJECT-CONSISTENCY-MAP.md` — §3 整节改造（本批最大删除项，可单独否决）

- 节名 :50 `## 3. 当前状态与已关闭事项` → `## 3. 实时状态与历史证据的取值处`
- **删除 :62-73 整个「当前生产事项：g11 · 烧烤夜宵 · 片1」子节**（伪当前态本体）。
- :52-60 P0-A 明细块 → 压成指针（三值 SHA 已在 `spec/product-truth/README.md:8-10`，闭环证据在 `CHANGE-20260915-003`/`004`）：

```text
### P0-A：APPLET Product Truth

- 快照构成、SHA-256 与 materialize 校验：以 `spec/product-truth/README.md` 为准。
- 闭环过程与判据：见 `docs/changes/` 内 `CHANGE-20260915-003` / `004`。本账不复制其状态结论。
```

- §4 :62 之前那句「任何环节未闭环，都不得把后续环节的局部通过描述成"项目已完成"」**保留**（§1 尾，属纪律非状态）。

### 3.7 唯一性声明（防本事务自己造出第二状态源）

本批**不新增**任何"状态源"文件、不新增索引层、不改 `AGENTS.md`。改造后 `PROJECT-CONSISTENCY-MAP.md` 的角色由"账"降为"索引"，实时状态取值处仍是 §十一 指向的那一组，不增加、不替换。

## 四、Replace

| 被替代的旧口径 | 替代者 | 位置 |
|---|---|---|
| archive 文件可声明"当前活挂账"并以本表为状态判据 | archive 一律为历史快照，不得发指挥句 | 3.1／3.2／3.3 |
| `PROJECT-CONSISTENCY-MAP.md` = 当前状态总账 | = 稳定架构恢复索引 | 3.4 |
| §2 「当前状态」列逐格填波动读数 | 「边界」列只填取值处指针 | 3.5 |
| §3 「当前生产事项」按片号写进度 | 取消该形态，状态回 `docs/changes/` ＋脚本 | 3.6 |

## 五、Remove

- `挂账总览.md:34` 的"以本表…为准"指挥句（语义删除，非文件删除）。
- `PROJECT-CONSISTENCY-MAP.md` §3 的 g11 进度子节（:62-73）。
- 停止未来产出：本仓库内**任何非 `AGENTS.md` §十一 指定面**的文档不得再写片号级/事务级波动状态（由 3.5/3.6 的列与节形态消除，机器强制留 S3 批）。
- **不删除任何文件**；不改 `ref-registry.json`；不改 `scripts/`；不改 `docs/changes/template.md`。

## 六、Preserve

- `挂账总览.md` §一/§二/§三 正文与其中已过期事实（含 :25 `sceneScale=0.76`）原样保留为历史证据，只加"快照当时"身份框。
- `PROJECT-CONSISTENCY-MAP.md` §1 唯一主链、§2 层级与前 8 行结构性描述、§4「已完成且不得重新发明」（含 :86-94 用户侧操作层两份文档的角色声明与 :94 fresh CI 历史括注）、§5 R9/契约机制、§6 接班规则。
- `outputs/archive/` 其余 7 项；`scripts/`、`spec/`、`AGENTS.md`、`SKILL.md`、两线 Owner、`docs/changes/closed/` 全部、`outputs/g06–g11` 与 `video/` 全部产物。
- `AGENTS.md:158` 对 `PROJECT-CONSISTENCY-MAP.md`「只用于恢复状态，不是入口」的定位**原样不动**——本批是把它已有的降级语义落到被降级文件自己身上，不是修改治理规则。

## 七、Impact Map

| 资产 | 类型 | 动作 | 验收方式 | 归属阶段 |
|---|---|---|---|---|
| `outputs/archive/挂账总览.md` | 历史证据（越权方） | REPLACE（:1/:3/:5 头部 ＋ :14/:31 标题 ＋ :34 指挥句） | 与 §三 逐字一致；grep「当前活挂账」「以本表」＝0 命中；`check-doc-references` 0 硬失败 | 阶段 B |
| `docs/internal/PROJECT-CONSISTENCY-MAP.md` | 架构恢复索引 | REPLACE（:3/:4/:6 身份句 ＋ §2 列名与 4 格） | 与 §三 逐字一致；grep「当前状态总账」「当前状态 \|」列名＝0 命中 | 阶段 B |
| 同上 §3 | 伪当前态载体 | REMOVE（:62-73 整节；:52-60 改指针） | 节名与指针文本一致；三值 SHA 在 `spec/product-truth/README.md:8-10` 仍在位（删除前复验） | 阶段 B |
| 冷读接班验证 | 人判证据 | ADD（验证动作，非文件） | 见 §十一「人工验收判据」：从 `AGENTS.md` 出发的合法恢复路径全程取不到伪当前态 | 阶段 C |
| `scripts/`、`ref-registry.json`、`spec/`、`AGENTS.md`、`SKILL.md`、两线 Owner、`outputs/g*`、`video/`、`docs/changes/template.md` | — | PRESERVE | `git status` 与 `git diff --stat` 证明未触碰 | 全阶段 |

## 八、Migration Plan

1. **阶段 A · 基线留存**：记录改前 `check-doc-references`／`check-change-contract`／`gate-all` 输出，与两文件当前行快照（含 §3 SHA 三值在 `spec/product-truth/README.md` 的在位复验）。
2. **阶段 B-1 · S2**：只改 `挂账总览.md`（3.1／3.2／3.3）→ 复跑 `check-doc-references`，与基线逐字比对。
3. **阶段 B-2 · S1**：只改 `PROJECT-CONSISTENCY-MAP.md`（3.4／3.5／3.6）→ 复跑同一检查，比对基线。
4. **阶段 C · 验证**：§九 定向 grep ＋ §十 反例 ＋ §十一 冷读接班验证（三件事分开取证）。
5. **阶段 D · 收口**：Closure Report → 满足全部关闭条件后 `CLOSED` → 移入 `closed/` → 在最终状态上复跑机械验证。

## 九、Mechanical Checks

- **旧身份话头扫描（定向 grep，非新增闸门）**：在 `outputs/archive/挂账总览.md` 与 `docs/internal/PROJECT-CONSISTENCY-MAP.md` 上验 6 个模式归零——`当前活挂账`、`以本表、`、`当前状态总账`、`当前生产事项`、`\| 当前状态 \|`、`新 g11 尚未`。
- **全扫描面越权残留**：对 `docsDir` ＋ `extraDocs` 全量 grep `以本表、.*为准` 与 `当前状态总账`，命中面须等于"仅 `docs/changes/closed/` 内历史事务叙述"（历史不回填）。
- **文档引用**：`node scripts/check-doc-references.mjs` 0 硬失败；§号引用与路径引用与阶段 A 基线逐字比对（本批摘掉的 :66-70 引用只会减少断链风险，不新增）。
- **契约闸门**：`node scripts/check-change-contract.mjs` 通过（含本事务自身）。
- **总闸门**：`node scripts/gate-all.mjs` 0 红，且与阶段 A **逐字一致**（两文件均不被任何判据闸门消费；`PROJECT-CONSISTENCY-MAP.md` 仅出现在 `spec/product-truth/README.md:35` 的反向引用中）。
- **口径唯一性**：新增文本不得命中 `deprecatedTerms`（30 条）或 `caliberOwners`（10 条 pattern）；特别注意不复述 120px、时长、字速等属主外数值，§三 文本已按"只给指针"设计。

## 十、Negative / Semantic Counterexample

1. **负向测试（关键词改了但本质仍违规）**：把 3.3 的替换句写成「可参考本表了解项目当前状态」——去掉了"以…为准"字样，但仍把 archive 文件接进当前状态认知链。本批拦截层＝**阶段 C 的定向 grep 只覆盖字面，抓不到该变体** → 由 §十一 冷读验证（人判）承担，并**如实登记为本批的已知能力边界**：字面级机器拦截对"以我为准"的语义变体无效，这正是 S3 批要建的"当前文档不得以指挥句指向 archive／状态账"正向检查的必要性证据。
2. **语义反例（§2 列改造的绕过形态）**：把 :48 单元格改成「截至本事务，g11 已完成全部 Gate」——语义上仍是片号级实时状态，只是从"未"变"已"，下一片开工又立即过期。拦截层＝3.5 的**列语义改造**（该列定义为不填波动读数）＋ §十一 冷读验证逐行看是否残留片号。
3. **反例（把"冻结"做过头）**：为让 grep 归零而把 `挂账总览.md` 正文的过期事实一并改写成现行口径——违反 §六 PRESERVE 与 `README.md` 的历史定位，属"用修改历史来制造绿的当前态"。拦截层＝§九 的 grep 只针对**身份句**清单，不含事实句；本契约 §六 明文禁止。

## 十一、Real Output Verification

- Real Output 声明：`not-applicable`
- 理由：本事务交付物为两份文档的身份句与节形态编辑，不含视觉或内容产物；核心验收（冷读接班链）是人判认知验证，不是真实产物，按 R8 §十 不假装机器可验。

- 真实关键帧/短片：本事务不存在内容产物（见上）
- 人工验收判据（冷读接班验证，逐条走并记录）：
  1. 只读 `AGENTS.md` → 按其 §八/§十/§十一 指示去恢复状态；
  2. 沿 §十一 指向读 `PROJECT-CONSISTENCY-MAP.md` 全文 → 记录是否取得任何片号级/事务级波动状态；
  3. 全仓（`docsDir` ＋ `extraDocs` 面）grep `archive` 内是否存在指向当前会话的"以…为准"句；
  4. 判定标准：第 2/3 步均**取不到伪当前态**即通过。该判据由用户或新任会话独立执行一次效力更高（避免自证）。
- 结果：PENDING

## 十二、Closure Report

- 已修改：PENDING
- 已废止：PENDING
- 已保留：PENDING
- 影响面：`0 / 4`
- 旧口径扫描：PENDING
- 机械检查：PENDING
- 负向测试：PENDING
- 语义反例：PENDING
- 真图/成片：PENDING
- 本次新增红：PENDING

**结论：NOT CLOSED**

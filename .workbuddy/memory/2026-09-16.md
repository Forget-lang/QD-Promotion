# 2026-09-16 · 工作日志

## 接手侦察：摸清「券到卡包」文档架构
- 工作空间 `/Users/quanxiaoyan/Desktop/baijiang/quandao` 实为**两个独立 git 仓库**：`applet/`（微信小程序源码 + .agents 知识库）与 `promotion/`（内容运营/宣传视频生产仓库）。用户说「先了解一下整体的文档架构」，我通读了两侧的入口文档、Owner 地图、真源、门禁与产物结构，产出八层架构视图（入口 / 流程 / Owner / 真源 / 知识 / 门禁 / 产物 / 历史），要点已沉淀到本项目 `MEMORY.md`。
- 跑了项目「开工三动作」中的前两项：
  - `node scripts/gate-all.mjs` → **14/15 通过、1 项跳过**（效果尺子，g10 已裁放行），无红灯。
  - `node scripts/list-assets.mjs` → 素材库登记正常（A 级背景各族、C 级 6 款已 bundle 字体、D 级 88 张截图含 22 张三平台禁用）。
- 发现并查清一处异常：**首次 gate-all 报「❌ 1/15 未通过」，复跑 6 次全部稳定通过**。根因不是脚本缺陷，而是该仓库工作区当时正被**外部会话并发写入**（`SKILL.md` 与 `CHANGE-20260916-004` 的 mtime 同为 17:02:46，git 工作区已干净、HEAD=a18a1e8「将 CHANGE-004 移入 active」）。首次运行恰好踩在写入中间态上。
- 待用户决定的观察项：`docs/internal/PROJECT-CONSISTENCY-MAP.md` §2 只登记 CHANGE-003 已 CLOSED，未反映 CHANGE-004 处于 `active/`，状态总账落后于实物。按纪律未擅自修改。
- 当前生产事项：g11 烧烤夜宵 · 片1，停在 SKILL 第 1 步治理回归。

## 补记：读透 promotion「宣传工作」的实质内容
用户澄清关注点：**promotion 是宣传工作，applet 只是前端小程序源码**（仅作产品事实取证）。据此补读了 `SKILL.md` 全文、`docs/战略简报.md`、`docs/internal/内容资源调用协议.md`、`CHANGE-20260916-004`，并抽取了 g06–g11 六片的实际选题。
- 工作目标：宣传免费的电子券工具「券到卡包」小程序，吸引实体店老板了解使用；抖音=短视频、小红书=图文笔记、搜狐=长文攻略。
- 内容方法论核心：一个行业=一个系列；用一条闭环五拍（抛悬念→讲道理→证明→落地→金句）讲透一套行业攻略；观众是 0 接触的实体店老板，场景与教程不分两段；功能点少而透（2~4 个）；画面重心给制券环节。
- 资源调用唯一顺序：Product Truth 定真假 → 官网提供场景/玩法 → 玩法版图给组合视角 → 爆款语料做传播研究 → SKILL 组织成内容；禁止先找爆款再反推玩法。
- 已产出：g06 教培托管、g07 咖啡茶饮、g08 火锅（节日备券节奏）、g09 宠物店（次卡）、g10 美容沙龙（老带新转赠奖励）、g11 烧烤夜宵（重建中）。
- **真实效果数据（2026-09-10 实读）**：抖音播放 19–28 / 完播 0% / 2s 跳出 44–63%；小红书 183–1144；搜狐 8–42。诊断瓶颈在「入口（封面点击 + 前 2 秒）」而非选错行业；改进按漏斗顺序、相邻两条只改一个变量。
- **发现一处记忆与现实的口径冲突**：用户级长期记忆里的 `docs/00–12` 体系、`S0–S10` 环节、「AI 技术制作角标常驻」、封面走创客贴版、素材五库结构、小红书发视频等，与现行 promotion 口径（SKILL 八步法 + docs/internal/R* + Remotion 自渲封面 + spec/assets.json 分级 + 不加角标 + 小红书图文）不一致，疑似已被 2026-08-29 清零重启及后续收口替换。已写入项目记忆的「待用户确认」区，**未擅自删除旧记忆**，等用户拍板后清理。

## 评估：近三天（09-14 ~ 09-16）治理改动是否算正向优化
用户要求评估这三天改动「算不算正向优化文档架构、会不会更好维护、更让 AI 遵守铁律」。用 git 证据核查后结论如下。

**改动事实（git 实测，基线 = ed4da9d 09-11 10:24）**
- 三天 **293 次提交**（09-15 161 次、09-16 132 次），134 个文件，+6170 / −3554。
- docs/ 从 **5 篇增至 21 篇**：新增 R7 组件库、R8 变更收敛协议、R9 视觉导演+映射表、R10 事实源解析、内容资源调用协议、整改作战总纲、PROJECT-CONSISTENCY-MAP，以及 `docs/changes/`（README + template + active/closed + 6 份 Change Contract）。
- `AI工作启动指令.md` 三天内**新建并重写**：993 行 → 272 行（−502/+188），定位从"复制业务流"改为"只讲怎么执行 Owner"，明文禁止复制业务流。
- 门禁：`gate-all` GATES 数组 **8 → 13**（新增 change-contract、visual-shot-contract、motif-card、voice-brand、release-feedback）；`scripts/*.mjs` **15 → 22**；新增 2 个**负向测试**（change-contract、visual-shot-contract）与 2 条 CI（change-contract-gate、visual-shot-contract）。
- CI 真实运行过：`github-actions[bot]` 产生 4 次提交（含"加入一次性工作流→执行→自删"模式）。远端残留分支 `tmp-check-workflow`、`fix/change-004-skill-ref`。
- **内容侧零产出**：三天新增媒体文件仅 1 个（`video/public/backgrounds/g11/bg.png`），**0 条新成片**；g11 停在「第 1 步未完成」，且被 Change Contract 明写为**负向回归样本**（用真片状态证明完成门拦得住后续生产）。

**评估结论（三维）**
1. **文档架构：正向。** 消除了双 Owner/双入口——把"怎么执行"（稳定协议层）与"执行什么"（易变业务 Owner）分离；铁律从"文档里的话"变成入口强制（AGENTS.md 唯一入口 + Owner 路由表）、协议强制（12 条执行协议 + "完成"统一定义 + 11 条"不算完成"）、机械强制（13 道闸门 + 负向测试 + CI）三层回路。特别击中 AI 通病：读过/写计划/草稿/自己说差不多/历史产物看起来做过/只通过关键词检查，均被判为不算完成。
2. **可维护性：改善但有代价。** 治理元层自身成为最大 churn 热点（`visual-shot-contract.yml` 三天被改 **33 次**、`PROJECT-CONSISTENCY-MAP.md` **22 次**、AI工作启动指令 12 次、AGENTS.md 11 次、R10 10 次），并发生过总账被覆盖又恢复（09-15 15:11 两次 restore）。手工对账的"状态总账"本身就是失效点。
3. **铁律约束力：显著增强。** 关键是负向测试——证明检查器真的会拦，而不是永远返回绿。这是三天里最实的收益。

**实测发现的残留缺口（可作后续整改输入，本次未改）**
- 负向测试覆盖不全：change-contract 只验 PENDING 行，未验"缺章节""无证据写 CLOSED"；visual-shot-contract 只验 Peak Frame 缺失。闸门最关键的"防刷绿"分支无测试保护。
- 闸门触发面窄：`change-contract-gate.yml` 只在 `docs/changes/**` 或少数脚本变动时触发，单改 `SKILL.md` 不触发；兜底仍靠人记得跑 gate-all。
- 长期人工债：24 处"需人工确认"红线提示、51 处"真值表 src 行号漂移"被明确标注"不计红绿"，容易被无视。
- CI 具备仓库写能力（one-time workflow 自加自删），存在绕过人的路径；R8 §十一 已禁止"自动把状态改绿"的关闭脚本，方向正确但执行面仍需盯。
- **未新增任何文件**：本评估只输出结论，遵守项目自身的「ADD 不是默认动作 / 看到缺一个说明就新增一个文档 = 反复出现的错误动作」。

## 第二轮：ChatGPT 完成一轮想法落地后的复查（17:31）
上一轮评估后，仓库又推进了 16 条提交（`a18a1e8` → `909802d`，17:04~17:23），只动了 4 个文件。用户让我重新了解文档现状。
- **做了什么**：① 把 `CHANGE-20260916-004` 关闭并移入 `closed/`，且**显式收窄关闭范围**——只关闭「全局文档/治理/Owner/入口/资源职责/当前历史边界整改」，g11 的生产/渲染/发布/发布后验**不作为关闭条件**（另建事务）。② 把 Impact Map 中 5 个 PENDING 行补成 PASS，附真实 CI 运行号（change-contract-gate #104、visual-shot-contract #144）。③ 重写 `check-change-contract.mjs`。④ 同步一致性总账。
- **检查器改动（净收益 + 一处回退）**：
  - 新增（有效）：扫描范围 active/ → active/+closed/；**禁止事务散落在 `docs/changes/` 根目录**；**状态↔目录一致性**（active 不得为 CLOSED、closed 必须为 CLOSED）；负向测试从 1 个 fixture 扩到 **3 个**（PENDING 行 / 根目录散落 / closed 缺结论）。
  - **删除（回退）**：原「声明 CLOSED 必须含 旧口径扫描/机械检查/负向测试/语义反例/本次新增红 五项 PASS 标记」的校验被整体移除。现在 closed/ 事务只要 `状态：CLOSED` + `结论：…CLOSED…` 就通过，**不需要任何证据**。
  - 回退原因（实测复核）：扫描扩到 closed/ 后，6 份历史事务中 **4 份缺证据标记**（20260915-002 缺机械检查/新增红0；20260915-003 缺三项；20260916-003 缺新增红0），会全红；于是选了全局放宽而非「登记豁免历史事务」。另外旧校验用精确字符串（如 `机械检查：PASS`）而文档实际写 `机械检查：**PASS**`（带加粗），本身也会误报——**正确修法是正则容忍加粗 + 历史豁免名单 + 新事务恢复五项要求**，不是删掉。
  - 刚关闭的 004 本身证据齐全（机械检查/负向测试/新增红0/结论 均有），回退不影响它，但护栏对**未来事务**失效。
- **手工总账滞后被当场证实**：总账 17:17 同步为「004 是当前唯一 active 事务」，17:23 004 即被关闭归档 → **6 分钟内过期**。且 `docs/changes/active/` 目录已随事务移走而消失（git 不跟踪空目录），闸门对「目录缺失」判 PASS，当前**零事务、闸门空转**。
- **实时状态**：gate-all 15 项 → 14 通过 / 1 跳过（效果尺子·g10 已裁放行）、无红灯；change-contract 与 visual-shot-contract 两个负向测试均 PASS。
- 本轮仍未新增任何文件，仅输出复查结论。上轮提的剩余缺口仍有效：负向测试还差「缺章节」分支；`change-contract-gate.yml` 单改 `SKILL.md` 不触发。
- **补充精度（重要，避免以后误判）**：被删掉的五项证据校验，**旧版也只作用于 `active/` 目录**（旧代码只 `readdirSync(activeDir)`），触发条件是文件里出现精确字面 `**结论：CLOSED**`。也就是说旧护栏本身**不完整**——直接写 CLOSED 并把文件移进 `closed/` 就能绕过。所以「扫描范围扩到 closed/」这个方向是对的，错的是扩了范围之后**没把证据要求一起带过去**，反而整体删掉。正确修法 = 证据要求作用到 closed/ + 历史事务显式豁免名单 + 匹配容忍加粗。

## 第三轮：对「放宽原因」的 git 复现取证（订正我自己的错误）
用户转来 ChatGPT 的复核，其中一条要求：不得在未重新取证的情况下断言「4 个历史事务当时因证据标记而红」。**该要求成立，我去复现了，并订正自己的错误。**
- 方法：`git archive a041754 docs/changes scripts/check-change-contract.mjs` 还原那一刻的树与 checker，直接跑。
- **真实结果（a041754，17:04，扩范围后、放宽前）**：`CHANGE-CONTRACT FAIL: 19 structural issue(s)`，覆盖 **3 份**（不是 4 份）：`20260915-002` 缺 `Migration Plan`；`20260915-003` 缺 3 个章节 + 缺全部 5 项证据标记；`20260916-003` 缺 10 个章节。`20260915-001` 与 `20260915-004` **通过**。
- **主因是「12 章节完整 schema 被套用到已归档事务」，不是五项证据标记**——五项标记只命中 1 份（20260915-003）。我先前说的「4 份全红」**是错的，已订正**。
- **放宽那一版（49f8c37，17:10）一次性做了两件事**：① 把 12 章节 schema + PENDING 检查移进 `if (state === 'active')`（**合法修复**，针对真正主因）；② 把 mustPass 五项证据校验换成 `结论：CLOSED` 正则（**过度**）。commit 注释理由是「不追溯套用当前 active schema」——该理由对「章节 schema」成立，对「关闭证据」不成立，因为关闭证据本就是 R8 为「关闭时刻」定义的，不属于 active 阶段 schema。**理由用错了对象。**
- **伪造单实测（关键证据）**：构造 `状态：CLOSED` + `结论：CLOSED`，但 机械检查/负向测试/语义反例 全为 `PENDING`、`本次新增红：3`：
  - 当前 checker（HEAD）→ **PASS，EXIT=0（放行）**
  - 放宽前 checker（a041754）→ **FAIL，15 项**，含 `CLOSED but missing 机械检查：PASS` / `负向测试：PASS` / `语义反例：PASS` / `本次新增红：0`
  → 证明「关闭状态不可伪造」这条护栏**从有到无**，且当前负向测试未覆盖该攻击面（ChatGPT 的判断成立）。

## 第四轮：评估 ChatGPT 对补稿的复核（两处驳我，一处成立一处部分成立）
- **驳我的第 1 条：完全成立，且理由比它说的更硬。** 我提议把「历史 Change Contract 豁免清单」放进 `scripts/ref-registry.json`。查证后**撤回**：`R10` 第 67 行明文规定该文件「继续保持唯一文档/事实登记角色，**不复制事务状态**」，已关闭的 `CHANGE-20260915-001` 也写明它「不重复承载事务状态」、禁止成为「第二个状态系统」。→ 往里面塞事务豁免会**直接违反现行 Owner 明文**。正确落点 = `docs/changes/` 目录内的数据文件 + `docs/changes/README.md` 声明约定。
- **驳我的第 2 条：我接受，并给出第三条可落地的路。** 它反对我「缺 scope 时默认按 visual/production 从严」，理由是会误伤文档类事务。实测支持它：`- 范围：` 字段 6 份里 2 份不合规（1 份散文、1 份缺失），**不能作为分支依据**。修正方案：不猜默认值，改为把「必须显式声明」变成结构要求——`Real Output Verification` 章节须给出枚举声明（`applicable: <证据列表>` 或 `not-applicable: <理由>`），checker 只验声明存在且合法、不替它推断需要什么。
- **它补我的实操坑成立**：`git cat-file -e <sha>^{commit}` 在 GitHub Actions **浅克隆**下会因对象不在本地而误报 → 须 `fetch-depth: 0` 或降级为格式校验，必须显式设计。
- **双方一致**：CHANGE-005 自指问题（不能用自己证明自己），验收须用独立临时 fixture 跑「合法单 PASS / 缺证据 FAIL / 伪造 CLOSED FAIL」。
- 我另提一个例外：它列「暂不做 9. 不修改 AGENTS / SKILL / **R8**」，但我认为 R8 §十 的 Closure Report 字段名需与 checker 校验字段**最小同步**（否则又是协议层与执行层断裂）——只增补声明格式一行，不动任何原则条款。此点待用户拍板。

## 第五轮：三方口径收敛，产出 CHANGE-005 草稿（17:55）
用户确认三方口径统一，并把整改边界锁死（不动 AGENTS/SKILL/Product Truth/生产流程/Owner 体系/CHANGE-004/R8 §九；允许改 checker、负向测试、`docs/changes/README.md`、新建 `legacy-compatibility.json`、R8 §十 最小同步），要求先出契约草稿再动代码。
- **新增文件**：`docs/changes/active/CHANGE-20260916-005-变更契约关闭证据门修复.md`（状态 `PROPOSED`），并重建了 `docs/changes/active/` 目录。
- **写草稿前先做了测量**（不猜）：逐项实测 6 份 closed 事务对「恢复后标准」的达标情况——`20260915-001` / `20260915-004` / `20260916-004` 全达标；`20260915-002` 缺 4 项、`20260915-003` 缺 5 项、`20260916-003` 缺 1 项。→ 豁免清单只需登记这 **3 份**，其余 3 份不登记。矩阵已写入契约 §9.1。
- **顺带得到「宽容格式、不宽容语义」的实证**：`本次新增红` 实际有三种合法写法——`0`、`0（未引入新的内容/视觉红线…）`、`**0 个阻塞性红点**`。我自己第一次用 `0(\s|$|个)` 的正则测量时把 `0（…）` 判成了缺项，**证明精确匹配会误伤已达标事务**；契约里据此写明必须做数值比较 + 容忍尾随说明。
- **验收自检**：新建契约后跑三项——`check-change-contract.mjs` PASS（checked 7 transactions）、负向测试 PASS、`check-doc-references` PASS；`gate-all` 仍为 **14/15 通过 + 1 跳过、无红灯**，未引入任何新红。
- **唯一提给用户的范围修正**：`docs/changes/template.md` 是 §十一/§十二 的**结构定义 Owner**（checker 的 12 章节名即来自它），但不在用户锁定的允许改动清单里。建议纳入，否则「协议与机器同步、仓库照抄的模板仍是旧的」——同一类断裂换个位置复现。**已标在 Impact Map 里等拍板，未擅自改。**

## 第六轮：产出整改计划 + 评估 ChatGPT 的 6 处修正（18:00）
用户要求「先只规划、不整改」，我出了《CHANGE-005 整改计划（执行前设计稿）》：含 5 个待决问题、对契约草稿的 5 处修订建议、checker 判定顺序、解析层设计、15+1 fixture 矩阵、执行顺序与提交策略、验收判据、风险回滚、明确不动的清单。
- ChatGPT 回评提出 6 处修正，我的评估结论：
  - **接受**：① commit SHA 不做重（本次只要求「PASS 必须有明确证据定位」，SHA 存在性降为可选/非阻塞）；② 「3 条豁免」不得写成设计目标（条数由实测决定，是输出不是目标）；③ fixture 不凑数（改为 7 个逻辑分组 + 组内多断言）；④ `template.md` 纳入范围（双方一致）；⑤ CHANGE-005 自身必须**完全不靠 legacy** 通过（它应是第一份完全符合新版契约的事务）。
  - **不完全接受**：它把我「6 份历史判定与改前完全一致」改成「不得产生**无法解释**的新红」。我认为「无法解释」主观不可判定，是弱化。融合方案：**主判据保留「新旧对拍零差异」（客观可自动验证），兜底才用「若差异不可避免须逐条登记理由，不允许未登记差异」**。
  - **给出比双方更好的第三方案（closed 的 Real Output）**：我原方案「closed 不强制存在 + 非阻塞警告」它担心 warning 变废纸；它的方案「由兼容范围决定」会导致 6/6 都豁免该项（接近 blanket）。第三方案：**closed 校验『`Real Output Verification` 章节必须存在』（结构性）；章节内含 `applicable:`/`not-applicable:` 标记时格式必须合法；不要求必须有标记（标记由 active 分支强制）**。→ 001/0915-004/0916-004 免登记即 PASS；20260916-003 因本就缺章节、已在册，追加一项；新事务 active 出身必有标记。清单仍 3 条，无 blanket、无永久 warning。关键论证：`Real Output Verification` **不是「今天才出现的章节」**（template.md 早有、5/6 历史事务都有），故对它做章节存在性校验**不违反**新口径第 1 条。
- **仓库状态异常（非我造成）**：`CHANGE-005` 文件已被写入 `docs/changes/active/`（17:55，13KB）且**已进入 git 暂存区（`A`）**，但**未提交**；同时 `.idea/workspace.xml` 也被改动。我没有执行 `git add`/`commit`，怀疑是 IDE 或另一会话所为。已如实报给用户，未擅自 unstage。

## 第七轮：执行 CHANGE-005（18:05~18:25，用户指令「你来执行整改」）
按批准的设计完成迁移与本地验证。改动 6 文件 +760/−158，新增 1 文件。
- **改动清单**：`scripts/check-change-contract.mjs`（重写）、`scripts/test-change-contract-negative.mjs`（重写为 7 组 81 断言）、`docs/changes/README.md`（声明豁免清单 Owner）、`docs/changes/template.md` §十一/§十二、`docs/internal/R8-变更收敛协议.md` §十、新增 `docs/changes/legacy-compatibility.json`。
- **执行中发现并修正的 4 个实现级事实**（都不是猜的，是跑出来的）：
  1. **证据字段必须限定在 `Closure Report` 章节内读取**——否则会读到 §十「语义反例」的叙述散文，把 `001` 的散文误判为不合规。
  2. **字段书写有两种合法形态**：`template.md` §十二 的列表式（`- 机械检查：PASS`）与 `R8` §十 的**文本块式**（无前导 `-`）。`20260916-003` 用的是后者，只认列表式会把它整份误判为缺证据 → 前导 `-` 改为可选。
  3. **豁免范围比先前测量更宽**：限定 Closure Report 内读取后，`20260915-002` 的「负向测试」实为 ✗（先前全库 substring 测量为 ✓），故其豁免项由 4 项改为 **5 项**；`20260916-003` 追加 `Real Output Verification 章节`。**条目数仍为 3，未变。**
  4. **两处对我建议的修订**（已在契约中记录）：① 证据定位不做机器强制（实测三份「达标」事务中 2 份完全没有定位标注，强制就需再点名 6 份 → 改为写进 `template.md`/`R8` 作人工约定）；② commit SHA 不做校验（避免把门禁修复扩成证据溯源系统）。
- **新检查器判定结构**：active = 12 章节 + Impact Map 待办态（扩展为「单元格含待办词即算未完成」，封住 `PENDING CLOSURE NOTE` 式绕行）+ Real Output 声明合法；closed = 状态/结论 + 豁免清单减法（只豁免所列项）+ 关闭证据五项 + Real Output 章节存在性与格式；另加豁免清单 4 条自检。保留 root 拒收与状态↔目录一致性。
- **验收全部通过**：
  - 负向测试 **81/81 全绿**；同一测试在改动前的 checker 上 **54/81 失败**（红→绿两端已留证）
  - `check-change-contract` → PASS，checked 7 transactions
  - `check-doc-references` → 0 硬失败
  - `gate-all` → 14/15 通过 + 1 跳过，**零新增红**
  - **主判据「基线对拍零差异」达成**：6 份 closed 在新旧两版 checker 下**逐份全部 PASS、零差异**
  - CHANGE-005 自身不在豁免清单内，**零豁免通过**
- **状态**：契约推进为 `VERIFYING`，结论仍为 `NOT CLOSED`（按 R8 §十一，还差 final candidate 推送后的 fresh CI 与用户关闭决定）。**未提交任何 git commit。**

## 第八轮：用户提出新工作模式（「异地总参 + 本地执行 Agent」架构）
用户与 ChatGPT 商定一个新协作模式并要我了解：**判断与执行分离**——用户给方向；ChatGPT 做「总参/审计」（看 GitHub、评估另一个模型的理解与计划）；我这个本地 Agent 做「作战执行」（进本地仓库侦察、改文件、跑闸门、出证据）。`AI工作启动指令.md` 成为二者之间的**接口协议**（从「Prompt Launcher」升级为「执行 Agent 的作战协议」）。
- 核心诉求：新增一个启动场景，让新会话的 AI 先具备**「先全局理解、再排兵布阵、最后局部执行」**的能力（六个能力：战场侦察 / 阵型识别 / 冲突侦察 / 影响面判断 / 行军路线 / 形成**仅存在于当前会话**的临时作战地图）。另加一个机制：**上级批准的方案交到本地 AI 后，必须重新侦察核验前提，不一致就 STOP 报告**，不得盲从。
- 用户明确：**先只出设计，不改仓库**。我已产出设计稿（见对话），并指出该改动可能需走 R8（涉及 `AI工作启动指令.md` + `AGENTS.md` §八 + 场景导航 ≥2 个对象）。

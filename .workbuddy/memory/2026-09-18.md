# 2026-09-18 工作日志

## 09:48 · promotion 仓库冲突诊断（只读，未处置）

**现象**：`promotion` 工作树停在合并中间态（存在 `.git/MERGE_HEAD`），4 个文件冲突：
`outputs/g11-烧烤/03-母题一页.md`、`video/src/data/g11.ts`、`video/src/videos/g11/index.tsx`、`video/src/videos/g11/types.ts`。

**根因（reflog 实证）**：**已推送的提交被 `git commit --amend` 改写**——
- `HEAD@{2} 8885303`（09:14:47，g11 分镜稿+决策卡，已推送）
- `HEAD@{1} 891dc3b`（09:21:56，g11 UI/data 重写+bench 图）→ **已推送到 origin/main**（`origin/main == 891dc3b`）
- `HEAD@{0} 2894779`（**amend**）→ 本地把 891dc3b 改写成新 SHA，合并基点仍是 8885303

随后 `git pull` 因双方各有 1 笔独有提交 → 变成真合并（`ORIG_HEAD=2894779`，MERGE_MSG 记录 4 处冲突）→ 同内容双侧都改 → 冲突。

**内容差异极小且方向单向**（`git diff 2894779 891dc3b` = 5 文件、+15/−33）：remote(891dc3b) 就是 amend 之前的旧版，HEAD(2894779) 是新版。差异明细：
| 文件 | HEAD（新，正确） | origin/main（旧） |
|---|---|---|
| 03-母题一页.md | 五轴第 5 条＝**节奏密度**＋明度为附注 | 五轴第 5 条误写**明度结构** |
| data/g11.ts | `typeLabel`、面额行无行名、字幕「满六十八」 | `couponType`＋面额行名（触红线）、「满 68」 |
| index.tsx | `fontFamily` 已去重（tsc 干净）、`p.typeLabel` | 重复 `fontFamily`（tsc 报错）、`p.couponType` |
| types.ts | 接口/注释同步 typeLabel | 旧接口 |
| 07-片1-分镜稿.md | 仅本地有「闸门交汇冲突」备注 19 行 | 无 |

**待用户拍板收口路线**：① 完成合并全取 HEAD 版 → merge commit → push；② `merge --abort` + `git push --force-with-lease`（远端对齐 amend 后版本，历史线性）。**建议 ②**。

**机制陷阱（新登记）**：本项目多会话并发，**对已推送的提交做 amend 必然制造同源双胞胎分叉**，下次 `pull` 即变真合并 + 同内容双侧冲突。规范：推送后勿 amend；如需修，改发一笔新提交。
另：`.idea/workspace.xml` 近期有改动 → 有 IDE 打开本仓库，冲突中间态疑似 IDE 的 pull 动作留下；处置前先确认无并发会话在写。

## 09:52 · 冲突已收口（用户拍板：全取最新版）

- 处置路线选 **①完成合并保留 HEAD 版**（未用 force push，避免改写远端历史）：`git checkout --ours -- <4 文件>` → `git add` → 校验。
- 校验链（可复用配方）：① 残留冲突标记 grep 为空；② `git diff HEAD --stat` 为空＝工作树与最新版逐字节一致；③ tsc 无错；④ gate-all 14/15 无红、check-ui-truth PASS、红线硬禁 0、change-contract PASS(23)；⑤ 提交后 `git diff --stat 2894779 HEAD` 为空＝合并结果内容**完全等于** amend 后的最新版；⑥ 推送后 `HEAD == origin/main`。
- 结果：`92ae16e merge(g11): 收口 origin/main 分叉，冲突四文件全取最新版` 已推送（`891dc3b..92ae16e main -> main`）。合并态已清除，工作树干净；`applet` 侧不受影响（`develop == origin/develop`，干净）。
- 结论留档：**"同源双胞胎"冲突是无损的**——只要先逐行核对远端无独有内容，`--ours` 取新版即零丢失；代价仅为历史里多一笔 merge commit。

## 09:58 · 复核 ChatGPT 评审时的三条机制事实（已实测，供后续事务引用）

**① `gate-all` 的 15 项构成**（实测 `scripts/gate-all.mjs`）：**13 个 check-* 闸门 ＋ 文字安全区探针 ＋ 效果尺子**。
- **tsc 不计入**：`WITH_TSC = argv.includes('--tsc')`（第 37/91 行）→ 只有加参数才成行；本次"14/15"里**不含 tsc**，tsc 是另跑的。
- 唯一跳过项＝**效果尺子（最新成片·已裁）**，其对象是**现存的 g10 成片**，跳过理由＝该成片比 `video/src` 最新改动旧 9568 分钟（**过期证据**）＋ 有**用户 2026-09-11 登记的裁放行**。→ 准确的"跳过原因"**不是**"仓库里还没有成片"。

**② CI 的真实情况**：仓库**有**两个 workflow（`.github/workflows/change-contract-gate.yml`、`visual-shot-contract.yml`），但均按 **paths 过滤**触发，paths 为 `docs/changes/**`、`scripts/check-change-contract.mjs`、`docs/internal/**`、`AGENTS.md`、`SKILL.md`、`AI工作启动指令.md`、`spec/product-truth/applet-snapshot.json`、`scripts/gate-all.mjs` 等——**不含 `outputs/**`、不含 `video/src/**`**。故本次提交 `92ae16e`（只改 outputs + video/src）在 GitHub 上**不会有 workflow run / commit status**；"14/15" 属**本地脚本层**，不得写成 CI 通过。

**③ 闸门交汇冲突的精确形态（比此前描述更硬）**：`check-ui-truth` ⑥ 第 219 行 `miss = [...allowed].filter(k => !faceUsed.includes(k))` ＝**声明券种后，该券种 `faceFields` 必须全集出现**（缺一即 `typeMissing` 硬失败）→ 对"手气券"而言**声明即必写那两个危险行名**，硬碰硬、无"声明但少写"的余地。
- 关键发现：检查器**①层已有 `onScreen:false` 豁免先例**（第 25 行注释："豁免：`onScreen:false` 的条目（按定义不上屏，是我方描述名）"）→ 未来修法应表述为**把①层已承认的原则贯彻到⑥层**（语义 key 通道），而非新发明规则，治理上更易通过。
- 当前 S3 处置的**精确代价**：屏上 `6 / 88` **在机器视野里没有字段身份**（比"失去券种↔面额配对保护"更准）——写错数值也不会有机器发现。

**④ 撤回"把板做大"的建议（证据支持）**：`R3:417` 明确左右信息安全边界 **≥120px**（2026-09-11 因抖音 20:9 长屏放大 1.18×、左右各裁约 82px 而再调），`video/src/VTemplate.tsx:188` g11 `sceneScale = 0.76` 为**纳入安全区的生产值**（第 187 行注释：安全区取证走独立 Composition `safeAreaProbe`，不再缩小场景）→ 标杆不因"主体偏小"回炉。

## 10:10 · g11 铺其余镜头（7 屏全出 → 真图 + 成片）

**产出**：`outputs/g11-烧烤/frames/`（7 屏真图 + `00-七屏汇总.png`）· `outputs/g11-烧烤/g11-烧烤-无声成片.mp4`（1080×1920 / 30fps / 96.66s / 22.2MB / 2898 帧；**无声**——SKILL 阶段一按字数占比定节拍，阶段二逐句 TTS 需前置资源，尚未做）。

**系统性修复（工程侧）**：
- `Board` 新增 **fit 模式**（不给 `h` → 板高随内容 + 垂直居中于 y150~1710；给了 `h` → 旧 space-between，**S3 标杆逐字节不变**已验）。病因：旧实现 `space-between` + 写死板高（1120~1250）把 3~5 个内容块撑出大片空洞（S1/S4/S5/S6/S7 全中）。
- S6 由"三块接力链"重建为 **手机卡包屏**（我的卡包／夜宵手气券／88 元／消费门槛／有效期／券码）+ 右侧三拍——原实现板内 4/5 空白且「倒计时归零」「满 68 直接减」折行截断。
- **大数字加 `whiteSpace:nowrap` 全局防护**（S1 字号加到 94 时「翻台三轮」折行，回调 82）。
- S5 去掉**元信息上屏**（原批注"时段最多 2 段，本片用 1 段"＝制作备注，观众看不懂）→ 改经营理由「周末用不了，留着高峰，先填周一至周四的空桌」。
- S5 取证结论：值「自领取日起N天内有效」是**产品真实选项文案**（`applet/pages_coupon/coupon/create.vue:657` `{label:'自领取日起N天内有效'}`），**原话上屏合规**，不是缺陷。
- 上屏词取证：「我的卡包」（pages.json:6）、「扫码核销」（pages/merchant/index.vue:103）、「券码」（同文件:225）均在源码 ✓。

**闸门结果（`gate-all --tsc`，16 项）**：14 绿 + **2 红**（均新出，见下）。

**红灯 ① · 文字安全区探针（字幕带）**：S6/S7 报"局部字形越左右线"。
- **实证＝指向一个更严重的问题**：裁 y1740–1940 只见炭火背景、**无任何字形** → 探针的判定来自**背景纹理**（高对比炭火肌理被当墨级像素，`full-bleed 过滤`未拦住）＝**假红**。
- 但它暴露**真问题**：g11 字幕实际渲染在 **y≈1575–1640**，而 `R3:418` 规格是 **y1760–1920**（g10 对照裁 y1600–1720 为纯色背景、无字幕 ✓ 规格）。根因＝`sceneScale = 0.76` **把整个场景（含字幕层）一起缩放**，字幕被从设计带拉进内容区约 190px。
- 修法两选（**动共享层 `VTemplate.tsx`，须用户拍板**）：(a) 字幕层移出缩放容器（板仍 0.76，字幕回 1760–1920）；(b) `sceneScale=1` + 板宽收至 840/left 120（120..960，恰好贴安全线）→ 板变大、字幕本来就在规格位。**未擅动。**

**红灯 ② · 效果尺子（成片）**：`静止 57% / 中位帧间差 0.21 / 占用率 51%`，合格线 `≤45% / ≥0.35 / ≥70%` → **三项全不达标**。g10 先例：静止 28%✓ 帧差 0.37✓ 占用率 54%✗（根因为氛围型底图贡献≈0%，已由用户 2026-09-11 登记裁放行）。g11 的静止/帧差可在**不碰共享层**的前提下改善（氛围层持续运动 + 揭示动作铺满整屏时长）；占用率可能要沿用同类裁放行或走"板放大"修法。**待拍板。**

**机制陷阱（新登记 · 可复用）**：
- **Remotion 分屏帧位**：`computeTotalFrames = Σ floor(dur×30) − (n−1)×12`（`TRANSITION_FRAMES = 12`）→ 场景 i 起点＝累计帧 − i×12。**第一轮我按 dur 直接相加算帧位，把 S3 渲成了 S4 内容**（S3 实际 755–1354，我却用了 1360）。取关键帧必须留出转场 12 帧，取屏末前 20 帧左右。
- **板式陷阱**：固定板高 + `space-between` 必出大空洞；板高随内容 + 定 gap 才稳。
- **大数字必 nowrap**：中文 4 字在 880 板上，字号 >84 就折行。

## 10:30 · 用户指出：我自造了非规范产物（已纠正）

**用户问"七屏汇总图是规范定的吗"→ 查证结论：不是，且规范明文禁止。** `SKILL:216`（Owner 正文）原文：

> 无声版阶段渲**逐屏单张**交你看（**不拼九宫格**、**不渲无声版**）……你说"画面 OK"才进阶段二。

- **违规 ①**：`outputs/g11-烧烤/frames/00-七屏汇总.png` = 我自造的九宫格拼图 → **已移入 macOS 废纸篓**。旁证：g06~g10 的 `frames/` 里**只有逐屏单张**，从无汇总图。
- **违规 ②（同一句还禁的）**：`outputs/g11-烧烤/g11-烧烤-无声成片.mp4` = **阶段一不该渲无声成片** → 已报告，待用户拍板是否同删。
- **连带影响**：正因为渲了 mp4，`gate-all` 的**效果尺子**从"跳过"变成实测 g11 → 出现三项未达标红（阶段一按规范本不该有这条红）。诊断数据已留档（见上一节），删 mp4 后该红回到"跳过"。
- **纪律回收（写入 MEMORY.md）**：产出口径＝阶段一只交**逐屏单张真图**（交付帧取**屏尾全内容帧**）；不拼九宫格、不渲无声成片；成片在**阶段二（TTS→loudnorm→atempo 1.2）**之后渲。**我今后不得自造非规范产物**（ADD 不是默认动作，这条对我自己同样适用）。

## 10:35 · 用户追问「不渲无声版」何时定的 → git 行级溯源（结论：用户口径优先，规范待收敛）

**用户拍板：无声成片仍要渲**（"我要看整体效果"）。

**溯源（`git log -L 216,216:SKILL.md`）**：
| 提交 | 日期 | 对该句做了什么 |
|---|---|---|
| `2607161` **质量整改 / SKILL v6 收敛 + 效果尺子 + G05 专属屏** | **2026-08-29** | **该句诞生**：`无声版阶段渲逐屏峰值帧单张交你看（不拼九宫格、不渲无声版）；你说"画面 OK"才进阶段二`——**与效果尺子同一提交引入** |
| `552cc57` | 2026-09-03 | 峰值帧 → **屏尾全内容帧**（C-06），「不渲无声版」**原样保留** |
| `9759284` | 2026-09-07 | 补 C-14（交付帧即发布稿），**原样保留** |
| `3442296` | 2026-08-29 | SKILL 从 `.workbuddy/` 迁入仓库根（同日） |

→ **全仓仅 SKILL:216 一处**出现「不渲无声版」；**无任何后续拍板撤销它**；但它**与现行验收机制存在内部张力**：
- 零条 §3「一条视频的成败只由两件事决定——`check-motion` 的数字 + 你看真图的判断」；
- 第 6 步叙事 8 问第 **⑤**「全片 100 秒内有无持续变化？」——**只有整片能答**；
- 第 6 步量化表定标样本用的是"**G04 成片**/参考片"，效果尺子机检对象也是"最新成片"。
→ 即：要跑量化与 8 问，**必须有成片**；而 216 说阶段一不渲成片。**建议另立小事务收敛 SKILL:216**（把"配一版无声成片供整体节奏验收"写实），当前按**用户口径执行**。

## 10:45 · CHANGE-20260918-022 已闭环（SKILL:216 口径收敛）

**用户拍板 → 立极小事务 → 执行 → 验证 → CLOSED**，全程一小时内的标准流程。

- **改动**：① `SKILL.md` 第 6 步交付行 1 处 REPLACE（「…（不拼九宫格、不渲无声版）…」→「…**逐屏单张 ＋ 一版无声成片**…（**不拼九宫格**——九宫格不能替代单张）…」）＋ 追加「阶段一那版无声成片」条款 1 条（归档路径／量化＋8 问⑤跑在它上面／非发布终态）；② `scripts/ref-registry.json` `deprecatedTerms` 新增 1 条（「不渲无声版」）＝**防回流机检**；③ **未动任何检查器代码**，`scripts/**` 逻辑零改动。
- **验证（V1~V4 实测）**：V1「无声成片」命中 2 处且与「逐屏单张」同句；V2「不拼九宫格」1 处在位（只删该删的）；V3 `check-motion`/`atempo 1.2`/C-06/C-14 引用 10 处在位；**V4 扫描面写回旧话头 → `check-doc-references` 硬失败（exit 1，命中 `docs/internal/_tmp-022-neg.md:3`），删临时文件复跑回 PASS**（＝020/021 建立的防回流链真实生效）。
- **闸门**：`check-change-contract` PASS（24 笔）；`gate-all --tsc` 14 通过/16、红灯 2 道**均为既存已定性项**（探针假红＋设计阶段尺子红），**本次新增红 0**。
- **档案**：`docs/changes/closed/CHANGE-20260918-022-阶段一交付含无声成片.md`；`active/`=0、`closed/`=**24**。
- **待办仍在**：① 红①字幕被 `sceneScale=0.76` 拉出设计带（修法 a/b，动共享层，待拍板）；② 红②是否继续调动态（静止 57%/帧差 0.21）；③ 本轮全部改动（含 g11 工程 + frames + 无声成片 + 022）**尚未 git 提交**。

## 10:50 · 「指标 vs 人眼」对账：逐屏实测（推翻"某几屏死住"假设）

用户转来 ChatGPT 建议（保留尺子为诊断、占用率降级、不急着改脚本、先做人眼对账）。我做了三处事实核准 + 逐屏实测：

**事实核准**：
1. 尺子自述边界 ✓ 属实（`check-motion.mjs:16-20` 原话："别拿它当质量分…**低于它不等于不合格、高于它更不等于好看**…禁止为过这条线加装饰"）。
2. **裁放行机制已存在且用过三例**：`scripts/ref-registry.json` 的 `motionWaivers`（gate-all:15-16 读）——g06（静止 67%/帧差 0.08/占用 77%）、g07（静止 68%/占用 13%，理由"深色底尺子假象"）、g10（占用 54%，氛围底贡献≈0），均含 `approvedBy`（用户看片裁决）+ 详细 reason。→ **ChatGPT 说的"降级为诊断"实质＝把既有"个案裁放行"升格为常态规则**；短期**不需要改脚本**。
3. **"逐 Shot 时间结构"已有 Owner**：`docs/internal/R9-视觉映射表.md:46/48` 的 Shot 表列含 `Peak Frame / State Change`，且**强制字段**（"缺任意一项，视为仍处于文案层，不得直接进入代码"）。→ 不能新建第二张"逐 Shot 表"（违反一概念一 Owner）。

**逐屏实测（把成片按真实分屏边界切段后各跑一次 `check-motion`）**：

| 屏 | 静止 | 中位帧差 | 均值帧差 | 明显变化(>6) | 占用率 |
|---|---|---|---|---|---|
| S1 | 47% | 0.33 | 0.73 | **0 次** | 52% |
| S2 | 61% | 0.15 | 0.46 | **0 次** | 49% |
| S3 | 59% | 0.20 | 0.47 | **0 次** | 49% |
| S4 | 57% | 0.23 | 0.49 | **0 次** | 49% |
| S5 | 56% | 0.27 | 0.49 | **0 次** | 53% |
| S6 | 48% | 0.33 | 0.49 | **0 次** | 54% |
| S7 | 57% | 0.18 | 0.51 | **0 次** | 56% |
| 全片 | 57% | 0.21 | 0.50 | **0 次** | 51% |

**结论（两条新事实）**：
- **不是"S2/S3/S5 死住"**——七屏全部偏高（47~61%）、帧差全部偏低（0.15~0.33）、占用率全部 49~56% ⇒ **问题全局均匀，不是某个镜头的失误**；ChatGPT 假设的"定位到哪几屏死"在本片对不上账（对账价值转成"证明这是风格性/测量尺度问题"）。
- **`changes`（帧间差 >6）全片与每屏都是 0 次**——CHANGE_T=6 这条分支对本风格**从未触发**；深底 + 细粉笔笔画 + 小字在 64×64 下低于测量地板。⇒ 三个数字对"深底细笔画的克制风格"**分辨率不足**，这正是需要人眼对账的证据（不是改脚本的理由）。
- **不动脚本**（对齐 ChatGPT）；短期用既有 `motionWaivers` 机制；**红①字幕越位与尺子无关**（确定性规格缺陷）必须独立修——ChatGPT 未提。

## 11:00 · CHANGE-20260918-023 已闭环（红① 修好）+ 一个假设被自己的实测推翻

**红① 修法（用户授权「按你的建议来」）**：根因查清——字幕由**共享分发器 `scenes/index.tsx` 的 `Subtitle`** 渲染、处在 `VTemplate` 的 `transform: scale(0.76)` 容器**内部**，故整场景缩放把字幕从设计带 y1760-1920 拉到 y≈1580。而 0.76 的来由是把越界的板（`100+880=980 > 960`）拉回安全区。
- 改法：**不再缩放场景**（`VTemplate.sceneScale` 一律 1）＋ **板自身落进安全线**（`BOARD_LEFT 130 / BOARD_W 820`，旋转 ±0.6° 角点外扩 ~6px 已留余量）。
- 验证：`tsc` 零错；7 屏重渲 + 人眼复核（S3/S6 无溢出）；裁 y1745-1925 **实证字幕已在规格带内**；**N1 负向测试＝旧代码与改后代码在同帧渲 g10 → `cmp` 逐字节一致**（历史片零影响）；`check-change-contract` PASS(25)；`active/`=0、`closed/`=**25**。文档同步：`03-母题一页.md` 的 0.76 约束段改写。

**⚠️ 一个假设被自己实测推翻（重要）**：改后 `静止 57%→53%`、`帧差 0.21→0.26`、`明显变化 0→4 次`（都改善），但 **占用率 51%→48%（反降）**。原因：占用率＝与**画面边缘中位色**差 >24 的像素占比 → **深色板越大，可见明亮炭火越少**，占比反降。⇒ 我先前"放大板可抬占用率、一举解决占用率"的判断**是错的**；要为过 70% 只能把板缩小/露更多火光，与质量目标相反。**结论：占用率对本风格宜作诊断或裁放行，不宜硬线**（与 ChatGPT 主张一致，但现在有了实测依据）。

**探针假红机制已定位（供后续小事务修）**：`probe-safe-area.mjs` 字幕带判据＝「与画面边缘中位色差 >`BG_T(24)` 粗筛 → 局部墨级差 >`INK_T(100)` 定罪」＋ `full-bleed 过滤`（要求同行左右各 ≥75% 覆盖）。**明亮炭火肌理（BG-TEX-002，g11 专属）会同时满足粗筛与墨级 → 被当成字形**；而稀疏火光不满足 75% 覆盖 → 过滤不生效。实证：字幕已在规格带内、band 内可见内容只有炭火，报出 8700~9400 墨级像素。**待办：是否立 024 修探针判据（改检查器，须用户点头）。**

## 11:20 · CHANGE-20260918-024 已闭环（外部审计的三项收口）

外部审计（ChatGPT）核 CHANGE-023 后提三项，我逐条核实**全部成立**，立 024 收口：

1. **旧口径残留（第二 Owner）** ✓ 证实：`video/src/videos/g11/index.tsx:4` 文件头注释仍写「板宽 840、left 120」，实现已 `820/130` → **已对齐**（并在注释里指向 R3 §7.3 与事务号）。
2. **长期规则藏在 CLOSED 事务里** ✓ 证实：023 的 New Policy（`sceneScale` 一律 1／安全区不得靠整场景缩放）是**技术层长期规则**，却只存在于已关闭事务 → **已回写 `R3 §7.3`**（新增 1 行「安全区达标方式」），且**片内参数 820/130 不写进 Owner**（V2 实证 R3 内 `820` = 0 处）。
3. **证据等级写得比实测高** ✓ 证实：023 闭包称 N1"证明 g06~g10 渲染行为未变"，实际只对 g10 做了同帧逐字节回归 → **已收紧**为「g10 逐字节直接实证；g06~g09 由"分支从未命中"的代码事实支持，未逐一回归」＋勘误标注（023 是 CLOSED 文件，仅做措辞勘误，不改结论与状态）。

- 验证：V1 注释≡常量 ✓；V2 规则在 R3 且片内参数未入 Owner ✓；V3 023 两处收紧 ✓；V4「板宽 840」全仓残留 1 处＝**024 契约自身对旧口径的引用**（契约格式要求，且 `docs/changes/**` 不在扫描面）✓；`tsc` 0 错；`check-doc-references` PASS；`check-change-contract` PASS(26)；`active/`=0、`closed/`=**26**。
- 为使效果尺子证据不过期，重渲了一次无声成片（画面内容不变）。
- **现状（不得写成"全绿"）**：`gate-all --tsc` **14 通过/16**，两红均已定性＝① 探针背景纹理假红（**维持不修**，同意审计意见）② 效果尺子设计阶段红（53%/0.26/48%）。
- **git**：023/022 已被提交并推送（`HEAD==origin/main==e064557`）；**024 的本轮改动尚未提交**。

## 11:35 · 状态更正 + 逐镜对账数据（取消缩放后「七屏均匀」结论已过时）

**状态更正（我上一条表述过时）**：024 已提交——`HEAD == origin/main == c55059c`（`chore(documentation): 统一安全区规则注释与证据等级表述`，含 024 契约 + R3 一行 + g11 注释 + 023 勘误），工作树干净。

**逐屏实测（当前成片，`sceneScale=1` 之后）——出现分化，推翻此前"七屏全部类似区间"的判断**：

| 屏 | 静止 | 中位帧差 | 明显变化 | 占用率 | 性质 |
|---|---|---|---|---|---|
| S1 | 47% | **0.41 ✅** | 1 | 51% | 两栏卡（有实体对象） |
| S2 | **58%** | **0.14** | 1 | 46% | 纯文字账目屏（最弱） |
| S3 | 54% | 0.23 | 1 | 43% | 表单字段屏 |
| S4 | 46% | 0.32 | 1 | 44% | 字段＋倒计时条 |
| S5 | 48% | 0.33 | 0 | 51% | 字段＋时段带 |
| S6 | **33% ✅** | **0.42 ✅** | 0 | 53% | **手机 Mockup＋88 揭晓（双达标）** |
| S7 | **57%** | **0.18** | 0 | 56% | 纯文字金句屏（弱） |

- **规律**：**弱屏＝纯文字屏（S2 账目 / S7 金句）；强屏＝有实体对象与揭晓动作的屏（S6 手机＋大数字 / S1 两栏卡）**。这既是"设计意图 vs 机器读数"的实质线索，也可能是 64×64 采样对**细粉笔笔画**的测量地板所致。
- **口径提醒**：分段实测切掉了 12 帧转场，数字**用于排序/定位，不作达标判定**（达标判定仍看全片：53%/0.26/48%）。
- **设计侧对账**：R9 Shot 表 14 镜**每镜都写了 Peak Frame ＋ State Change**（强制列），共 14 处状态变化；机器全片只识别出 **4 次「明显变化」**（帧间差 >6）→ 差距来自"我们靠局部揭示、而非整屏切换"的表达强度与机器阈值的错配。
- **拟交付**：会话内出《逐镜对账表》（14 镜设计值 × 7 屏实测 × 人眼待判列），**不新建文档**；结论落地时才写进该去的地方（裁放行→`motionWaivers`；改画面→相应事务）。

## 12:00 · 人眼对账结论落地：只改 S2（用户看片拍板）

**用户逐屏看片结论**：只有 **S2 真闷**（前半段空板＋"等字出现"）；**S7 不闷**（有意收口定格，"稳不是死"）；S3-02/03 略弱但不值得单独回炉；S1/S4/S5/S6 保持。**不登记 `motionWaivers`、不动全局 VTemplate、不动 motion checker、不开 025。**

**S2 改法（克制：不加内容、不加装饰）**——
1. **把「上块 → 下块」的切换本身做成看得见的板面状态变化**：一道粉笔**横扫划掉**上块（6.3–7.8s，对齐口播「喊了等于没喊」）＋ 上块整块**压暗到 0.42**；中缝分隔线改为**自左划开**（8.2–9s）。
2. **重排写入时点**（消空档）：标题 0.2s / 首行 2s / 次行 5s → 划掉 → 分隔线 → 下块标题 9.5s → 三行 10.3/11.7/13s → 结论句 15s。**最长空档从 ~4s 压到 2s。**

**实测（这轮数据回答的问题＝"修掉一个人眼确认的弱屏，机器指标会同步改善吗"）**：

| | 改前 | 改后 |
|---|---|---|
| **S2 本屏** 静止 | 58% | **48%** |
| **S2 本屏** 中位帧差 | 0.14 | **0.34**（≈合格线 0.35） |
| 全片 静止 | 53% | 52% |
| 全片 中位帧差 | 0.26 | 0.28 |

→ **结论：单屏修复对"该屏"指标改善显著（帧差 ×2.4），对"全片平均"几乎无感**（S2 只占 17/96 秒）⇒ **全片平均不适合定位问题，逐屏切段跑尺子才是定位工具**（切段时注意：切掉 12 帧转场，数字只用于排序/定位，不作达标判定）。
→ 另一个副产品：`明显变化(>6)` 仍为 1 次/屏量级，说明 CHANGE_T=6 对本风格的"局部揭示"式表达仍不敏感（与探针假红同源：深底细笔画在 64×64 下被低估）。

**文档同步（C-14：帧上元素须是分镜稿写过的）**：`07-片1-分镜稿.md` S2 的「画面／入场」两行改写＋新增「2026-09-18 局部强化」出处段；`08-R9视觉决策卡.md` S2-01/S2-02 的 `Motion / Peak Frame / State Change` 三列同步；S2 交付帧（`frames/S2-道理屏.png`）重渲。
**闸门**：`check-visual-shot-contract` PASS；`tsc` 0 错；`gate-all --tsc` 14/16（两红＝探针假红＋尺子 52%/0.28/48%，均既存）。
**未提交**：`video/src/videos/g11/index.tsx`、`07-片1-分镜稿.md`、`08-R9视觉决策卡.md`、`frames/S2-道理屏.png`、`g11-烧烤-无声成片.mp4` 共 5 个文件。











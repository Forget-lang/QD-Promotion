# 2026-08-14 工作日志 · 券到卡包

## 晚间 · G02 视频 v2 二次校正（用户 4 点指令）
1. **堵 #8 品牌绿洞**：根因 `VTemplate.tsx` 硬编码 `BRAND='#07C160'` 作 S7 整屏背景，「禁用品牌绿」从未进真源。用户口径：绿色看着可以、按设计稿来、AI 不要预设颜色。整改——代码 `BRAND`→`GREEN`；设计稿新增「配色方案」段锁色值；`redlines.json` 加 `BRAND` 命名守卫（不放行具体绿色）。
2. **S7 结束语**：改为「券到卡包 / 再小的门店也可以制作电子券」，移除关注/收藏/下个视频。§2.1#4 放宽（产品名作结尾署名可轻显示），「券到卡包」移出 codeBanned 硬禁。
3. **视频时长重定义**：≤120s 一次讲透，不硬切「下个视频」。SCENES 拉长到约 67s；skill §二#10 / §三 时长基线放宽（废 15–40s / 25–40s 上限）。设计稿去除分 part 结构，规划完整单视频（7 屏对应 0–67s）。
4. **全量对齐**：跑 `check-redlines.mjs` → code 层 0 命中，outputs/docs 命中均为规则说明/搜狐合规位。无声版重渲中（后台 task 4HT8y5）。

## 关键决策（待用户确认画面）
- 配色权交还设计稿：AI 不预设颜色，设计稿须显式列每个画面色值（已落到 分镜规划.md「配色方案」）。
- 视频不再用「下个视频」钩子，单条把设计点一次讲透，时长上限 2 分钟。

## 深夜 · 设计稿先行规范讨论（用户主导细化）
- **封面 ≠ 视频第一帧（已核实抖音/小红书机制）**：小红书 feed 显示封面缩略图、点击才播；抖音封面作 poster+主页九宫格、可上传自定义图，主feed自动播放。结论：封面是「未播放态」缩略图/海报，第一帧是播放起点，二者独立。封面优先从 背景素材/ 选模板叠加标题（古朴风格17+小红书风格20+根20，文件名不清可重命名），无合适再在创客贴做带标题 PNG（9:16 抖音/小红书、16:9 搜狐头图），发布时手动设为封面；视频第一帧=S1 动画钩子，不与封面强等，但须视觉/主题连贯。原"封面=第一帧且文案须同句"设想已撤销（用户纠正）。发布坑：抖音须手动「设默认封面」并关「自动同步最新视频封面」，否则被重置为首帧；小红书封面按 3:4 裁切中间区，标题须居中留白。
- 设计稿=本单元视频+文章视觉/内容唯一真源，含 6 段：元信息/封面规范(创客贴流程+不AI生成)/视频分镜详规(每屏加"动画细节"段)/配色字体/文章/分平台+验收清单。
- 流程：AI 出设计稿 → 人审查 → 人回传创客贴封面 png → AI 无声视频 → 人确认画面 → AI 合成语音。口播逐字稿必须在设计稿定稿，无声视频按口播节奏锁帧，语音后只调音频。
- 本地素材库**有现成封面模板**：`背景素材/`（古朴风格17张`古风竖版背景图需求(N).png` + 小红书风格20张`N.jpg` + 根目录20张`1–20.jpg`，根目录与小红书风格疑似同名待核对去重），文件名不清可重命名（如`封面模板-风格-NN`）。封面优先从此选模板叠加标题；无合适风格再走创客贴。AI 不生成封面（质量不可控）。
- 设计稿规范两条纪律（我提的分析点，非用户待拍板项，直接落 SKILL/话术）：① **流水线铁律**：口播逐字稿在设计稿阶段定稿+每屏时间码锁定→无声视频严格按时间码渲染确认画面→语音后合成仅对轨/语速微调、不大幅改画面、音频时长须对得上已锁帧（防声画错位）。② **文档边界**：设计稿规定 What（显什么/何时/配色/动画意图），不规定 How（代码）；实现受阻在设计稿批注可行性、不停滞；"设计稿先行"流水线须写进 SKILL §五 真源防不同步（曾踩坑）。
- 用户侧真正待拍板仅规范级 2 项（不含具体单元内容，钩子句等是写某单元设计稿时再定）：① 文件结构：单一 `设计稿.md` 统稿 vs 拆多文件 ② 封面来源优先级是否写死“背景素材模板优先、无合适再创客贴”。封面来源已确认有模板。拍板后把“设计稿先行”流水线写进 SKILL §五 真源（作为通用 SOP，不与某单元成品混谈）。

## 深夜末 · 设计稿规范已定稿并写进 SKILL §5.0（用户批准“按建议执行”）
- 用户批准：单一 `设计稿.md` 统稿；六段结构（元信息/封面规范/视频分镜详规/配色字体/文章/分平台验收清单）；两条纪律（流水线铁律：口播定稿→锁帧→语音后合成仅对轨；文档边界：规定 What 不规定 How）。已写入 SKILL §五 顶部「5.0 设计稿先行」+ 步骤3/4 指向设计稿。下一次新窗口执行 G02 设计稿即用此规范 + 话术。

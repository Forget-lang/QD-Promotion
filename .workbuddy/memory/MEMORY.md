# 项目长期记忆 · 券到卡包（quandao 工作区）

> 「券到卡包」电子券工具内容运营项目。`docs/`=事实知识库+SOP；`applet/`=业务源码（不在本仓库）。效力：docs/00事实表 > 其余 docs > outputs。
> **记忆更新规范（2026-08-17 确立）**：所有决策、进展、口径变更直接更新本文件对应章节，不再依赖当日日志中转；当日日志（`.workbuddy/memory/YYYY-MM-DD.md`）仅作可选的过程流水，不是权威记忆。

## 1. 项目定位
- 面向实体店**免费**电子券工具（做券→发券→领券→扫码核销→统计，企微自动发券）；主体郑州百桨数字科技。
- 三大产品：优惠券(6型)/券包/次卡（次卡含 通卡·次卡·权益卡 三类型，2026-08-16 代码新增）。
- 仅宣传线冷启动，无真实案例，正文用行业泛称（咖啡店/火锅店等），不编具体店名/数据。

## 2. 铁律红线（完整真源=quandao-content skill §二；冲突以 skill 为准）
- 不虚构功能（以 applet/ 代码为准）、不编案例数据、数字以 00§6 为准、禁极限词。
- 分平台 CTA：三平台文章/视频绝口不提微信/小程序/搜索，导流仅限账号资料位；禁小程序码。
- 企微泛称红线：企微发放功能作为核心宣传卖点，但对外（含账号资料位）一律不直说「企业微信/企微」，只用功能泛称（加好友/进群自动发券）；视频号平台不做宣传发布（仅品牌承接位）。
- 当前视频/文章**不使用截图、不显真实产品 UI、不用 AI 生图**（2026-08-14 决策）；主视觉=Remotion 代码 UI 组件+项目商用字体+SVG自绘图标。
- 配色以分镜规划(设计稿)为准，AI 不得自定颜色；代码用设计命名常量(GREEN等)，禁 BRAND 类品牌暗示命名。
- 抗同质化(风格轮换)、保完率(≤180s，前3s强钩子，一次讲透不硬切"下个视频")。
- (2026-08-17 决策)「免费使用」可说但不着重强调，不堆促销/不承诺永久免费；收尾 CTA=自然收尾，不硬推关注/收藏，可轻署「券到卡包」；宣传文档在 quandao/promotion、业务代码在 quandao/applet（分开）。
- (2026-08-17 决策) **核销活码（5分钟刷新/防截屏）功能不在宣传内容中使用**——产品真实功能保留在 00 事实表备查，但对外文案/视频/选题/卖点一律不提；核销卖点只讲「扫码核销、手动输码兜底」。
- (2026-08-17 决策) **视频设计稿工作流确立**：AI 按 13 §5 模板产出帧级设计稿（含图片素材 8 字段规格表），人审确认后，人按规格表到设计平台找图（不用 AI 生图），AI 再写 data/gXX.ts 执行。13 为 Remotion 技术唯一源（动画硬规则/设计稿模板/风格五维状态/组件规格）。

## 3. 关键事实与缺口
- 次卡发放需平台审核通过(auditStatus)方可发放(硬阻断)；券包仅展示审核警示标签、发放不受阻断；优惠券无审核。审核属平台合规机制，宣传一律不提。(2026-08-16 校正)
- 券包/次卡仅私密发放，无公开领取；统计无趋势图表；无支付/收款/会员积分/在线预约/第三方对接；企微群发非全自动。
- 公众号/客服等真实能力曾从 docs 移除(用户决策)；如需对外提及须先回查 applet/ 代码确认。

## 4. 文档与资源索引
- 顶层：`docs/战略简报.md`(用户读)、`docs/工作流指引.md`(AI接手手册)。内部 `docs/internal/`：00事实表(唯一事实源)/02业务流程/04宣传策略/05行业场景/06选题库/07渠道/10视频/11素材规范/12 SOP/13 Remotion技术规范与设计稿工作流。
- 红线+核心模型+分平台CTA+生产SOP 单一权威=quandao-content skill；跨 agent 入口=仓库根 AGENTS.md。
- 资源：商用字体(33，金矿，已接入 video)、图标素材(771，纹理库非业务图标)、背景素材(35，古朴15+小红书20)、插图库(空)、截图素材(已删)。

### 目录规范（2026-08-17 确立）
```
promotion/
├── docs/              权威文档层（长期有效，改一处全量对齐）
│   ├── 战略简报.md
│   ├── 工作流指引.md
│   └── internal/      00-13 编号权威文档
├── outputs/           产出物层
│   ├── gXX-行业/       每个行业单元一个文件夹，只放最终交付物
│   │   ├── 四件套.md
│   │   ├── 攻略文章.md
│   │   ├── 设计稿.md
│   │   └── *.mp4
│   └── archive/       归档：过程文件/审计报告/调研资料，不属最终交付物
├── scripts/           工具脚本（红线闸门等）
├── video/             Remotion 视频工程
└── .workbuddy/        AI 工作区（memory/skills/tmp）
```
规则：权威文档只在 `docs/internal/` 保留一份，`outputs/` 不放文档副本（避免不同步）；需要复制时从 `docs/internal/` 取。`outputs/` 根目录不放散文件，过程文件一律进 `archive/` 并加日期前缀。

## 5. 生产物与进度
- 交付范式：按行业单元(如 G02 茶饮咖啡)端到端产一套完整内容包(7层)，关键节点留确认闸门。
- 行业：G01火锅/G02茶饮咖啡/G03美业/G04汽车/G05零售/G06连锁。
- G02 现状（2026-08-17 视觉升级版）：
  - 视频代码完成视觉升级：typography 三档字体性格落地（impact/clean/friendly）、HookScene 支持 number/contrast/question 三种钩子型、SolutionScene 新增 horizontal 横向三卡版式、全场景部署 DotGrid 点阵+GlowOrb 柔光背景装饰。
  - 文案打磨：S1/S2/S3/S5 文案精简优化；设计稿按 13 §5 模板重写到帧级精度（含动画时序/背景装饰规范/口播稿/合规自查）。
  - tsc 零错误、7 帧 still 单帧验证全部通过、红线闸门 code 层零命中。
  - 待办：人审无声版画面（升级后） → TTS 合成（最后一步） → 加 BGM → 成片 → 发布。
  - 四件套/攻略文章/设计稿已完成，合规检查通过。
- Remotion 技术状态（2026-08-17 更新）：7 种场景积木、8 个动画组件（FadeInUp/SlideInLeft/SlideInRight/ScaleIn/WipeIn/Pulse+DotGrid/GlowOrb 背景装饰）、7 套 palette、4 种 motion、3 种 typography（已落地）、转场系统（wipe/slide/fade）；钩子型已实现 number/contrast/question（3/5）；版式变体已实现 solution horizontal/vertical；story/challenge 钩子型待按需实现。
- Icon MCP Server（2026-08-17 接入）：全局安装 `icon-mcp-server@1.4.0`，项目根目录 `.mcp.json` 已配置。`search_icons`（无需 Key，搜 Element Plus + Ant Design 图标库）、`generate_icon`（需 `DASHSCOPE_API_KEY`，AI 生成）。返回 SVG 统一转内联 React 组件加入 `video/src/components/icons.tsx`，禁止直接引用外部 SVG/CDN（13 §8.3）。
- 素材引用规范（2026-08-17 补全）：13 号文档新增 §4.1「本地素材优先检索」——设计稿阶段先检索本地素材库（背景素材/图鱼纹理/插图库），本地有合适的标注源路径直接引用，不足才输出外部找图规格；根目录素材使用时复制到 video/public/ 下三个子目录（backgrounds/textures/illustrations）；§5.5 素材清单三选一（无/本地已有/外部找图）；§6.1 工作流步骤②④已同步更新。11 号文档同步修正背景素材数量（55→35）、对齐 AI 生图口径。方圆体 friendly 字重从 700/500 修正为 400/400（仅单一字重）。Icon MCP Server 已接入（详见上一行），11/12/13/07 文档已同步引用。
- 文档体系重构完成（2026-08-17）：04/05/06/07/10/11/12 七份文档全量修整对齐。核心变更：①10 号删除与 13 重复的技术实现细节，聚焦内容方案，vmin→px、P1-P7→语义命名、删 16:9 引用、加 NODE_OPTIONS/核销活码红线；②12 号更新目录路径为 outputs/gXX-行业/、加核销活码红线、明确脚本与设计稿关系、加 NODE_OPTIONS；③07 号删除重复封面口径、"回微信"→"账号资料位导流"、删版本号、加核销活码红线；④11 号从 128 行精简到 100 行，合并重复的§3/§6画面规范、修正字体字重、删过时 G02 状态、路径统一 outputs/gXX-行业/；⑤04 号删截图留存、统一 CTA 口径、加核销活码红线；⑥05 号删虚构店名示例、引用源从 02 改为 00；⑦06 号 V1-V8 "插画"→"代码 UI"、修 11§6→11§3、路径统一。交叉引用校验通过（仅 1 处 00~07 范围引用已修正）。
- 文档体系收口验证（2026-08-17）：系统性完整性检查发现并修复 5 类遗漏：①SKILL §二 补核销活码红线（原红线唯一真源缺失此条，违反决策回写硬约束）；②redlines.json 补"活码"违禁词进入 codeBanned 机器守卫；③README 补 13 号文档索引、修正旧版 13 归档说明；④工作流指引补 13 引用/核销活码/NODE_OPTIONS、文件名从"分镜规划.md/画面使用说明.md"对齐为"设计稿.md"；⑤实际目录 outputs/茶饮咖啡/ 重命名为 outputs/g02-茶饮咖啡/，同步修正 video/package.json 渲染路径和 SKILL 示例命令。红线闸门 code 层零命中。

## 6. 协作硬规则（用户最高准则，详见 AGENTS.md / skill §八）
- **全量对齐**：改任何文档先列全部引用该事实的文件，改完逐一核对，绝不留"对不上"的尾巴；改红线/CTA/事实口径后必跑 `node scripts/check-redlines.mjs`（video/src 零命中）。
- **用户建议≠钉死规则**：用户规划权交 AI；收到建议须分析→必要时反对给更好方案→对好口径再执行；推测不许包装成结论。
- **视频无预设跨视频模板/无统一模板**：每条视频每镜独立设计，基础动画仅作可复用组件。
- **文档治理**：`outputs/gXX-行业/`只放最终交付物；过程讨论/审计报告进 `outputs/archive/`（加日期前缀）；权威文档只在 `docs/internal/` 保留一份，outputs 不放副本；权威收敛(docs/00=事实、12=SOP、13=视频技术、skill=红线)。
- **记忆更新**：所有决策/进展/口径变更直接更新 MEMORY.md 对应章节，不依赖当日日志中转。
- **决策回写权威层**：用户拍板须同步写 docs/ 或 skill，不只留记忆(.workbuddy/ 其他 agent 读不到)。
- **产物保留 vs 规范清理**：已生成的视频/图片等产物必须留档(命名清楚供对照)，规范文档错误则删改。
- **意图落盘三原则**：原话优先；落盘前回读确认；推断标(待确认)。
- **参考图吸收精髓**(非读一下完)、**本地资源优先+向外找优秀模板**(站酷/Pixso/Dribbble等)。
- **环境/删除操作先列清单向用户确认**再执行。

## 7. 参考标杆
- 鱼皮(程序员鱼皮)视频风格：代码驱动 UI 组件、文字是主角视觉是骨架、颜色有语义、信息密度高；G02 v2 按此范式。

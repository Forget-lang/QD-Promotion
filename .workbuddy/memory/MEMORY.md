# 项目长期记忆 · 券到卡包（baijiang/quandao）

> **路径（2026-09-18 用户调整）**：本记忆目录已从 `quandao/.workbuddy/` 迁至 **`quandao/promotion/.workbuddy/memory/`**，并**纳入 promotion 仓库版本控制**；旧路径已不存在。**写完记忆会产生仓库改动**（`.workbuddy/**` 不在任何闸门扫描面，不影响门禁）。
> **开工第一步：先读 `promotion/AGENTS.md`**（唯一 AI 入口 / Owner 地图 / 全局纪律；其 §一 要求任何改动或产出之前先做最小定位）。
> 本文件只记**指针、现状与陷阱**，不复述 Owner 正文（规则正文看对应 Owner）。

## 工作空间
- 两个独立 git 仓库：`applet/`（小程序源码，唯一规范 `applet/AGENTS.md`，仅作产品事实源）、`promotion/`（宣传内容生产仓库，用户主战场）。
- 仓库被**多会话并发写入**：gate-all 撞中间态会出「❌ N/15」假红，复跑即恢复；报红先复跑再判断。

## promotion 是什么
- 宣传免费电子券工具「券到卡包」（制券→发券→领券→扫码核销→统计），吸引实体店老板；不卖货、不导流。
- 平台：抖音=短视频(9:16)＋**图文线**（首图即封面 3:4、备忘录/清单风、与行业片默认**伴生**；CHANGE-019）；小红书=**图文笔记**；搜狐=长文攻略。
- 流程唯一 Owner `SKILL.md`（八步法）；资源调用顺序唯一 Owner `docs/internal/内容资源调用协议.md`；质感锚稿 `outputs/bench/anchor-*.png`。
- 效果差（09-10 实读：抖音播放 19–28/完播 0%），**瓶颈在入口**（封面+前 2 秒）；改进按漏斗、相邻两条只改一个变量，24h 回填 `outputs/archive/发布后验台账.json`。
- 底线：不虚构功能/数字/案例；三平台禁小程序码及微信系二维码；画面零品牌名、口播必提「券到卡包」一次；不加 AI 角标；标题字数硬限（抖音≤30/小红书≤20/搜狐 20~30）。

## ★ 内容方法论现行口径（CHANGE-020／021，2026-09-17 落定）
- **中军军令**：五拍规定**认知职责**不规定表达方式；洞察规定**理解机制**不直接规定台词；爆款规律提供**可选传播策略**不提供每片必执行模板。
- **五拍＝认知功能骨架**（吸引/保持/理解与相信/落地/收口），写法不固定；「错误示范/生活类比/不是X是Y/定律句」已从五拍解耦。
- **「理解」段双入口＝设计入口**（机制入口／场景入口），不得固化成两套模板。
- **洞察库＝机制索引不是台词库**：定律句**不得直接进稿**；链条＝定律→机制理解→当前行业/人物/情境→**选表达形态**→原创表达；每条机制带「讲法形态空间」（≥2 个已区分实现＝**知识资产完整度要求，非生产配额**）。
- **18 通则＝候选传播规律／研究索引**（按需回查，**无"每片逐条对照"**）；**写法条目＝可选表达工具**（非必过）；**复用纪律：上一条片用过的表达方式不构成下一条的默认选择**，须重新证明"这次为何仍适合"。
- **内容输入包必含《表达形态选择及理由》**（选择证据，含"本片排除的形态+理由"，**非数量配额**）；证据纪律 5 条已回收进解剖 §〇（高频≠有效／需独立案例／需高表现 vs 普通对照／候选与已验证分层／自有数据可否证外部假设）。
- 旧话头已登记 `ref-registry.json` `deprecatedTerms`（28 条）且**已接机检消费端**（021）；term 必须精确（中文无字符边界，短 term 会误伤合法名）。

## 文档八层与硬纪律
- `AGENTS.md`→`SKILL.md`→`docs/internal/`（R2/R3/R6/R7/R8/R9+映射/R10/内容资源调用协议/整改作战总纲/PROJECT-CONSISTENCY-MAP）→`spec/` 真源→`knowledge/`→`scripts/`（gate-all 15 项）→`outputs/`+`video/`→`docs/changes/{active,closed}/`。`AI工作启动指令.md`（场景 A~M）与 `docs/AI使用手册.md` 都不是 Owner。
- 开工三步：gate-all → list-assets → 读 docs/changes 最新事务。红灯不产出。
- 一个概念一个 Owner；第二 Owner/入口/新旧并存 → STOP。跨对象改动走 R8；**ADD 不是默认动作**。改检查器必须同步 `docs/changes/template.md`。
- 历史/归档只作证据；旧片构图不得当新片模板。「完成」=完成条件+真实证据；CI 不可得时按 R8 §十一「干净检出等价重现」替代（012 口径）。

## 当前状态（2026-09-18 15:0x 复核）
- `active/`=**0**；`closed/`=**31**（001~029）；g11 四线已发布（09-18），产物两线全齐。
- **gate-all 0 红**：14 ✅ ＋ **文字安全区探针 / 效果尺子 两行 ⏭️ 已裁放行**（用户 09-18「g11 都放行」拍板，理由含完整诊断：探针＝BG-TEX-002 明亮炭火纹理假红，机制见 023 §十二；尺子＝54%/0.23/48% 三项结构性低估，见 029 motionWaivers 条）。**探针判据缺陷仍登记待修**（不因豁免销账），他片复发再立事务。
- **g11 进度**：第 1~5 步全成（内容输入包／母题一页／口播／分镜稿／R9 决策卡／工程／7 屏真图／无声成片）；**一屏标杆 S3 已过用户质感验收**。事件化局部优化已落地：**S2**（旧块划掉＋压暗）、**S4**（补 `00:00` 档＋解锁板面状态变化）、**S5**（`WeekChip` 逐格点亮）、**S3**（路线 A 时点重排：金额揭示提前、尾部静置 4.3s→2.7s）；**S7 判为设计意图**（稳而非死）、S1/S6 成立。**阶段二＋第 7 步产物已齐**：有声成片 `outputs/g11-烧烤/g11-烧烤-有声成片.mp4`（3188 帧 / 106.27s，TTS→loudnorm→atempo 1.2 实测回填；7 屏响度 −18.7~−20.0 dB 均匀）；封面 4 张 `covers/`（`G11Cover.tsx`：Hook「周一空桌」/ Payoff「8 点开抢」｜coverKit ＋ CV-001 底）；三份发布稿 `03-抖音`/`04-小红书(图文)`/`05-搜狐`（含入口三件套自查）。
- **g11 定位**：Legacy Baseline／旧方法基线样本，**不得描述成"新方法验证"**；g11 vs g12 对照属后续「方法效果观测」。
- **待办**：① **g11 后验回填**（09-18 四线发布，台账视频行＋图文行已立、状态=已发布-待回填；**24h 读数＝09-19 同时段**：视频读播放/封面CTR/2秒跳出/完播、图文读进入率/翻完率/赞藏评，回填后置「已回填」；**7d 复盘＝09-25**，一句话结论写台账）；② **g12**＝新方法首个验证样本（g11 基线已发布，对照就差它）；③ 尾债（只记）：`07`/`08` 把 S3-02 批注区称「粉笔引线」而实现是 `NoteBand`；启动指令不在引用闸门扫描面；`真图/成片`↔Real Output 映射未机检；AGENTS §一 与总纲 §0 戒律逐字重合；`outputs/*/00-交付说明.md` 不在扫描面（g10/g11 起未再建，状态记账由台账承担）；探针判据缺陷（023 §十二，已豁免未修）。**019/027 移交项已全部销账**（台账三指标＝028；图文机检＝030）。
- **红线功能字段剥离＋图文机检（2026-09-18 CHANGE-030 现行）**：① `check-redlines` 扫 `video/src/data/**` 前先剥离 spec 四表登记的功能级字段名（`FUNC_LABELS` 自动派生；用户声明「手气券最大/最小金额是功能级、使用没问题」）——**字段名不构成极限词违规，`outputs` 对外文案不豁免**；`spec/redlines.json` 词表逐字不动；⑥ 券种↔面额配对零改动 → 未来片正常「声明 couponType＋写真名」，**语义代号方案废止**。② `check-ui-truth` 新增 **⑦ 图文机检**：`video/src/graphic/*.ts` 图内字段名必须逐字命中 spec 四表/券种名/applet 源码原话，`onScreen:false` 名上图文＝硬失败。
- **闸门豁免机制（2026-09-18 CHANGE-029 现行）**：效果尺子＝`ref-registry.json` 的 `motionWaivers`（g06~g11 共 6 条）；文字安全区探针＝**`safeAreaWaivers`**（029 新增豁免口，g11 一条）——均按片号 `gXX` 从成片/静帧路径提取匹配，**只在 gate-all 汇总层放行**（行转 ⏭️ 已裁放行＋批准人＋理由，**原判照旧显示**；探针本体仍独立可跑、仍 exit 1）；新片不命中、闸门对新片全效力；豁免理由必须含批准人＋诊断依据，禁止通配片号/全局跳过。
- **发布后验台账口径（2026-09-18 CHANGE-028 扩展，现行）**：行字段＝片号／**类型（视频[新行必写]｜图文；缺省=视频仅为兼容历史行）**／发布时间／变量假设／指标组／状态／备注；**视频指标＝播放量/封面CTR/2秒跳出/完播，图文指标＝进入率/翻完率/赞藏评**（`check-release-feedback.mjs` 按 `类型` 分支硬拦、非法类型 ❌）；片进「已发布」AI 连机加行（状态=已发布-待回填），数据回流填满指标＋一句话复盘结论同表（台账即复盘唯一落点）；**g11 已于 2026-09-18 四线发布**（抖音短视频/抖音图文/小红书/搜狐），台账两行待回填。
- **抖音图文线·交付结构（2026-09-18 CHANGE-027 定稿，现行）**：图 → `outputs/gXX-行业/graphic/`（N 张 PNG，**统一 3:4 / 1080×1440，首图即封面**）；发布稿 → `06-发布稿-抖音图文.md`；数据文件 → `video/src/graphic/<gXX>.ts`（图内字段名逐字登记，供机检接入）。**版式**：**各图共用同一张底（含首图即封面，不单独换底）**——g11 首篇用点阵网格纸 `PaperBg`（`xhs/BG-XHS-02-点阵网格.jpg`，来自 `assets.json` A.xhs）；首图＝**深墨大字 ＋ 珊瑚橙副标**（原 16:2x 前用珊瑚橙底＋`coverKit` 白/金字，用户 16:22 指令改为与内页同底后，白/金字在白底不可读故弃用 coverKit）；内页＝点阵网格备忘录清单（`Page` 内容 flex 垂直居中、页眉 absolute 置顶）；条目＝序号块＋标题＋说明，重点用珊瑚橙色块。⚠️ **该「背景统一」目前只是产物事实，尚未写入 SKILL 图文线版式段（待立事务）**。**钩子结构四平台必须互不相同**（019 明令）：短视频＝身份+问句｜小红书＝身份+痛点+方案+数字｜搜狐＝问题词+结果词｜图文＝**数字清单型**。**结构必须按图文形态重写、禁止搬运口播稿**。**两项移交**：① 数据文件扫入 `check-ui-truth`（019 §三.5）；② 台账字段扩展（图文三指标＝进入率/翻完率/赞藏评）＋ 发布时加行（019 §三.6）——台账 `rows` 是固定视频四数、`check-release-feedback.mjs` 读它判定，**加独立指标键即改检查器** → 均待用户点头。
- **封面系统口径（2026-09-18 g11 实操，现行）**：`video/src/covers/coverKit.tsx` 提供 `CoverBg`（固定背景 `cover/CV-001-珊瑚橙渐变底.jpg`，来自 `spec/assets.json` A.coverBackground，**不复用视频 bg**）＋ `Hook`（数黑体超大白字＋描边增重）＋ `Payoff`（暖金字）＋ `SubLine`（方圆体副标）；每片建 `GXXCover.tsx` 导出 4 组件并在 `index.tsx` 注册 Composition（`gXX-cover-dy` 1080×1920／`-dy43` 1440×1080／`-xhs` 1080×1440／`-sohu` 1920×1080，`durationInFrames={1}`），渲图落 `outputs/gXX-行业/covers/封面-*.png`。**文案＝Hook（痛点/玩法名 3~4 字，单字占画面宽 ~22%）＋ Payoff（方法/承诺，含数字锚点）＋ SubLine（说明）**；零 UI 组件、零品牌名、不刻意提"免费"、不用极限词；缩到 360px 小图须可读（g11 实测）＝判据。⚠️ **改 `video/src/**`（哪怕只新增封面组件）会让"最新成片"变过期证据** → 尺子报"过期证据，不算通过"，须重渲成片。
- **阶段二（有声成片）口径与工具（2026-09-18 g11 实操，现行）**：链路＝逐屏 TTS（`node scripts/tts.mjs <文本> <输出相对 video/public/audio 的路径> [语速]`，凭据取 `.env` 的 `VOLC_TTS_API_KEY`/`SPEAKER_ID`）→ `bash scripts/process-audio.sh <raw目录> <tmp目录>`（loudnorm −16 LUFS；输入须命名 `sN_raw.wav`）→ **`ffmpeg -af atempo=1.2`** → ffprobe 实测 → 回填 `data/gXX.ts`（`dur`/`voiceOffset`/`voiceDur`/字幕帧界）＋ `hasAudio: true`。**`dur = voiceOffset 0.27 + voiceDur + 尾读缓冲（制券/数据/CTA 0.9s、其余 0.5s）**；字幕帧界＝`offsetF(floor(0.27×30)=8) + round(voiceDur×30) × 字数占比`，句间留 3 帧、末句帧尾＝offsetF+voiceFrames。**`ffmpeg-static` 只有 `ffmpeg` 没有 `ffprobe`** → 用系统 `/opt/homebrew/bin/ffmpeg|ffprobe`（实测可用；R3 里"本机无系统 ffmpeg"是 2026-08-22 旧记载）。⚠️ **屏时长拉长会直接拉低 `check-motion` 读数**：字数估算（≤6 字/秒）通常偏短，实测回填后屏变长 → 静止占比升、中位帧差降（g11：无声 96.66s→有声 106.27s，尺子 49%/0.32 → 54%/0.23）——**尺子对"时长"敏感，又一条"宜作诊断、不宜作硬线"的实证**。
- **g11 若要做 g11 vs g12 方法效果对照 → g11 必须真实发布取数**（发布稿＋封面＋有声成片是前提，不是可选项）。

## spec 真值现状（applet 换代重基线已完成，013~017）
- applet `develop==origin/master==42a473b`（09-14）；快照 `spec/product-truth/applet/source.tar.xz` 与 live 298/298 逐字节一致。
- 口径：审核机制与版本档位**一律不提**（对外次数上限口径=1000，该说 7 次卡就说 7 次）；会员等级/积分**按真实实现讲**（5 级/手动发扣 1~1000/不可兑换抵现）；主推「有免费版，可免费使用」、不承诺永远免费；「顾客端不做收款」；受门禁功能照常讲不提版本约束；不讲发票、广告位、推荐位。
- 真值表：`point-fields`(14)·`member-fields`(10)·`coupon-fields`+wecard·`card-fields`(29+versionGate)；四表 109 处 `src` 行号验证式重锚（tableBad=0）。

## quandao-mcp-server（外部校对尺，018 登记 R10 §6）
- 用户级 MCP（`~/.workbuddy/mcp.json`，已激活）；7 工具全只读：4 能力说明书 + `industry_tree` + `position_list`/`recommend_list`。
- 三硬边界：①不作机检判据；②`industry_tree` 只作行业身份校准、**不得当候选行业清单**；③上屏仍回源码取证。落进 spec/新开真源＝全局变更走 R8。引用须标「外部能力文档：quandao-mcp · 工具名（非小程序源码）」＋取用日期。

## 机制陷阱（实测留档）
- 闸门失去检查对象即默认放行（visual-shot-contract 无 R9 卡时空转 PASS；active/ 消失时契约闸门空转）。
- `check-motif-card` 只扫 outputs 最新一个 gXX；`check-doc-references` 只扫 `docs/internal`+`ref-registry.extraDocs`（**不含** `outputs/**`），`outputs/**` 不在任何 CI `paths`。**⚠️ 修正（2026-09-18 实测）：`outputs/**` 确实进闸门——`check-redlines` ② 文案层会把 `outputs/**/*.md` 当"将发布文案"逐字扫极限词/违禁词**（实例：临时文件 `outputs/g11-烧烤/G11-优化执行说明.md` 的「最新」二字 → 红线闸门硬失败）。**结论：临时/说明类文件不要放 `outputs/`**（放仓库外），否则会硬失败。（2026-09-18 补：CI 有两个 workflow＝`change-contract-gate`/`visual-shot-contract`，**均路径过滤**、不含 `outputs/**` 与 `video/src/**` → 只改这两处的提交在 GitHub 上**没有 run/status**，"gate-all 14/15" 属本地脚本层，≠ CI 通过。`gate-all` 15 项＝13 闸门＋文字安全区探针＋效果尺子，**tsc 仅在 `--tsc` 参数下计入**。）另：**仓库私有、本地无 `gh`**（GitHub 操作走网页/用户端）。
- **契约坑**：① `applicable` 声明的"适用证据"会把其后**所有 `- ` 行继续收集**（含"结果：PENDING"）→ 报 unsatisfied；在证据条后加一行非列表行终止。② **证据文本不得含 PENDING 类词**（PENDING/TODO/TBD/待办/待验证/**未完成**/NOT VERIFIED），否则移档后 FAIL——写"随…产出后消除"。③ Impact Map 末列 active 阶段禁 PENDING 类词。④ §十一「结果」须显式写 PASS。
- 改 `spec/*.json`：原文 1 空格缩进，必须定点插入，禁整体 json.dumps 重排。
- 关闭事务机器要求：`closed/` 须「状态：CLOSED」+「结论：CLOSED」+Closure 证据全 PASS+自指路径改 `closed/`；移档前全仓 grep changeId 确认引用方是裸 id。
- 已知未修尾债（只记）：启动指令不在引用闸门扫描面；`真图/成片`↔Real Output 映射未被机器强制；AGENTS §一 与总纲 §0 戒律逐字重合；`outputs/*/00-交付说明.md` 不在扫描面；**闸门交汇冲突**——`check-ui-truth` ⑥（声明 couponType 必带该券种面额行名）与 `check-redlines` ②（`data/*.ts` 禁极限词字面）对真值表登记为 `onScreen:false` 的面额行名互相矛盾（g11 S3 处置：不声明券种、行名不登记、券种仅作展示值上屏；是否开字段真值豁免口子待拍板）。
- **换语言五轴＝栅格/容器形状/标题承载/入场语法/节奏密度**（SKILL:83；「明度结构」**不是**轴，属颜色层，按"只换色＝没换语言"不得充当轴）——写母题一页时勿写错。
- **安全区达标方式＝R3 §7.3 现行规则（2026-09-18 CHANGE-024 回写）**：安全区**不得靠"整场景缩放"实现**（会连带缩放字幕等独立安全区元素）；做法＝**主体容器自身落进 x120..960**、共享层 `sceneScale` 保持 1。**片内尺寸（如 `BOARD_W/BOARD_LEFT`）属该片工程参数，不写进 R3**。另：**长期规则不得只留在已 CLOSED 的变更事务里**（历史事务是证据，不是 Owner）。
- **git 多会话陷阱（2026-09-18 实测）**：对**已推送**的提交做 `git commit --amend` → 本地与远端成"同源双胞胎"分叉（同内容两 SHA），下次 `pull` 变真合并 → **同内容双侧冲突**（g11 四文件实例）。规范：**推送后勿 amend，改动一律发新提交**。冲突中间态下任何闸门结果无意义，先收口再跑 gate-all。
- **产出口径（2026-09-18 CHANGE-022 收敛后，现行）**：阶段一交付＝**逐屏单张真图（屏尾「全内容帧」）＋ 一版无声成片**（`outputs/gXX-行业/gXX-行业-无声成片.mp4`）；`check-motion` 量化三项与叙事 8 问第⑤问（全片持续变化）**跑在无声成片上**；**无声成片不是发布终态**（终态＝阶段二有声成片）；**禁止九宫格拼图**（单张不可替代）。旧口径「不渲无声版」（2026-08-29 `2607161` 质量整改批写入、无拍板撤销）已于 022 废止并登记 `deprecatedTerms`（V4 实测：写回即硬失败）。**我 2026-09-18 自造过 `00-七屏汇总.png`＝越界（已移废纸篓）——不得自造非规范产物。**
- **效果尺子的正确用法（2026-09-18 实证）**：`全片平均`对"单屏修复"几乎无感（S2 帧差 0.14→0.34，全片仅 0.26→0.28）⇒ **定位问题必须逐屏切段跑**（用真实分屏边界 `Σfloor(dur×30)−i×12` 切段；切掉 12 帧转场，数字只作排序/定位，不作达标判定）。**纯文字屏**（金句/账目）天然低分，**有实体对象＋揭晓动作的屏**（手机 Mockup＋大数字）天然高分。**"让切换本身成为视觉事件"**（如旧块粉笔划掉＋压暗，语义＝被否掉）是"纯文字屏偏闷"的非装饰解——优于加粒子/呼吸光。
- **片内视觉优化纪律（2026-09-18 CHANGE-025，现行）**：目标判据＝「**可感知事件密度**」（观众能感知"事情正在发生"），不是"信息展示完整"。提高手段**只允许两种**——① **时点重排**（压掉"等字出现"的长空档）② **显式状态变化**（划掉／压暗／转色／放大／划出下划线／逐格点亮）；**禁为迎合机器指标加装饰元素**（粒子／呼吸光／无意义抖动）。**新增可见元素须先补 `07-片1-分镜稿`**（C-14）。**外部临时清单不得落在 `outputs/**`**（红线闸门当文案逐字扫）→ 转变更事务或在仓库外。纪律由**产物层人工判据**兜（文本层抓不住"软化/配额/换词不换形"）。
- **指标对「出场顺序」不敏感（2026-09-18 CHANGE-026 实证）**：S3 时点重排后本屏 `54% / 0.21`（改前 `54% / 0.23`）、全片 `49% / 0.32 / 48%`（**与改前完全一致**）——因为 `静止占比` 量的是「**动的时间总量**」，重排只改「动的时刻」（弹簧揭示仍 16~26 帧、9 个事件总动时长未变；缩短的尾部空档被中段新增空档抵消）。⇒ **改善指标只能增加"动的时间"**（拉长动画／加装饰均违纪律）；**观感类诉求（顺序／生成感／信息节奏）只能用"看真图"验收，不得拿指标当验收判据**。
- **Remotion 取帧/板式/尺子陷阱（2026-09-18 实测）**：① 分屏帧位＝`Σ floor(dur×30) − (n−1)×12`（`TRANSITION_FRAMES=12`，`video/src/VTemplate.tsx`）→ 场景 i 起点＝累计帧−i×12；按 dur 直接相加必错（曾把 S3 渲成 S4 内容），关键帧取屏末前 ~20 帧。② 板式：固定板高 + `space-between` 必出大片空洞 → 改「板高随内容 + 定 gap」；大数字必须 `whiteSpace:nowrap`（中文 4 字 >84px 折行）。③ 效果尺子合格线（`check-motion.mjs`，**采样 64×64 @2fps**）：静止（连续样本帧间差 ≤0.3 的占比）≤45%｜中位帧间差 ≥0.35｜占用率（与**画面边缘中位色**任一通道差 >24 的像素占比）≥70%。**静止/帧差靠"持续可见的动"就能改善**（氛围光呼吸＋揭示动作铺满整屏时长）；**占用率结构性偏低**——深色黑板压深色炭火底，非背景像素天然少（g10 同病 54%，已由用户 2026-09-11 裁放行）。④ ~~`sceneScale=0.76`（VTemplate:188，仅 g11）会把字幕层一起缩放~~ **已修（2026-09-18 CHANGE-023）**：`sceneScale` 一律 1；安全区改由**板自身落进 x120..960**（`BOARD_LEFT 130 / BOARD_W 820`，旋转 ±0.6° 角点外扩 ~6px 已留余量）→ 字幕回 R3 规格带 y1760-1920，内容观感放大 1.22×。**教训：安全区不得靠"整场景缩放"（会把字幕层一起缩放）**；历史片零影响（N1：同帧新旧代码渲 g10 逐字节一致）。⑤ **占用率与深色主体反相关**：占用率＝「与**画面边缘中位色**任一通道差 >24 的像素占比」→ 深色板越大、可见亮底越少、占比反降（实测 51%→48%）；**"放大主体抬占用率"已被实测否定**，为过线只能缩小板/露更多火光（与质量目标相反）⇒ 该项对本风格宜作诊断/裁放行，不宜作硬线。

# 券到卡包 · AI 启动提示词

> **定位：用户操作接口，不是规则 Owner，也不是 AI 自动入口。**
> 用途：开新会话、换 AI 工具或切换工作场景时，复制对应场景的提示词，告诉 AI“这次要做什么、先从哪里接手”。
> 原则：**提示词只指路，不复述规则。** AI 自动进入仓库仍以 `AGENTS.md` 为唯一入口；规则、流程与事实分别回到各自 Owner。
> 当前路由：`你 → AI启动提示词.md → AGENTS.md → SKILL.md / 当前任务 Owner → 输出 → Gate`。

---

## 使用纪律

1. 本文件是给**你指挥 AI**用的，适合复制、粘贴、改任务参数；不承担项目规则定义。
2. AI 接到本文件后，仍必须从 `AGENTS.md` 开始接管仓库，并按当前任务读取对应 Owner。
3. 本文件不得复制 `SKILL.md`、`spec/`、R2–R10 的规则正文；出现冲突时，以对应 Owner 为准。
4. 场景模板只描述“怎么叫 AI 开工”，不保存某个行业、某片视频或某次治理事务的临时状态。
5. 本文件属于**当前用户操作界面**；历史版本进入 `outputs/archive/` 后只能作为历史证据，不再承担当前指挥功能。

## 场景 A：文档审计 / 治理核验

```text
你是「券到卡包」项目的 AI 助手。

【任务】做一次文档/治理审计。

先读：AGENTS.md → docs/internal/整改作战总纲.md → 当前任务涉及的 Owner。
不要先改文件。

先完成战场侦察：
1. 找规则、Owner、入口、依赖、重复、冲突、断档。
2. 区分当前指挥链与历史证据。
3. 判断每个被触及概念的唯一 Owner。
4. 给出 ADD / REPLACE / REMOVE / PRESERVE 影响面。

再执行现有 Gate/Checker；所有结论带真实命令或文件证据。
发现第二 Owner、第二入口、新旧并存或历史越权，立即 STOP，先回到侦察。

未得到用户确认，不修改全局权威文档。
```

## 场景 B：正式产出一条视频

```text
你是「券到卡包」项目的 AI 助手。

【任务】（填写你的任务）
- 按 SKILL 完整产出一条视频；或
- 做 XX 行业/XX 片，按当前任务进度接手。

先从 AGENTS.md 开始接管：
1. 执行 AGENTS.md §三「开工三动作」。
2. 读取 SKILL.md。
3. 按任务读取唯一对应 Owner，不自行发明平行规则。
4. 读取当前输出与最新 Gate 结果。

执行纪律：
- 红灯不交付。
- 产品事实回 Product Truth / spec 真源。
- 视觉方向回 R9；生产步骤回 SKILL；组件复用回 R7；治理问题回 R8/整改总纲。
- 人工确认节点明确停下，不替用户拍板。
- 完成后给真实命令、退出码、产物与 Gate 证据，不用“已完成”代替证据。
```

## 场景 C：设计稿 → 编码

```text
你是「券到卡包」Remotion 编码 AI。

先读：AGENTS.md → SKILL.md（对应制作步骤）→ docs/internal/R3-Remotion技术参考.md → 当前设计稿与实时组件清单。

【任务】把当前设计稿实现到 video/。

要求：
- 设计稿是输入，不自行改变视觉决策；视觉问题回到用户/当前视觉 Owner。
- 技术实现遵守 SKILL 与 R3；组件复用查 R7，不自行建立第二组件目录。
- 代码完成后运行现有 typecheck / still / motion / similarity / Gate 检查。
- 不以文档绿代替真图验收。
```

## 场景 D：背景素材入库验证

```text
你是「券到卡包」素材管理 AI。

先读：AGENTS.md → 当前素材/产品真源 Owner → 当前任务相关规则。

【任务】逐张验证用户提供的背景图，合格才入库。

要求：
- 先按当前真源判断事实、红线、素材登记要求。
- 任一硬性条件不满足，拒收并说明具体原因。
- 合格素材进入当前规定目录并登记到唯一素材真源。
- 入库后跑对应 facts / Gate 检查并给真实结果。
- 不把历史归档中的旧素材规则重新当成当前规则。
```

## 启动后 AI 回执模板

```text
已接手。

自动入口：AGENTS.md
当前任务 Owner：{填写}
当前任务：{填写}
已运行：{命令 / 退出码}
当前 Gate：{结果}
下一步：{动作}
人工确认点：{如有}

我只把本提示词当作任务路由；具体规则以当前 Owner 为准。
```

---

> **记住：** 这份文件解决的是“你怎么叫 AI 干活”；`AGENTS.md` 解决的是“AI 进入仓库后遵守什么”；`SKILL.md` 和各领域 Owner 解决的是“具体怎么做”。三者职责不同，不互相替代。

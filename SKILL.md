---
name: quandao-content
description: 券到卡包宣传视频怎么做——唯一作业文档。行业选型→视觉母题→口播去冗→分镜真图→声画节拍→量化验收→发布。红线与事实见 spec/。
version: 7.0.0-重置
---

# 券到卡包 · 宣传视频怎么做

> 版本：7.0.0-reset（2026-08-29 清零重启，变更史见 changelog）。本文是唯一作业文档、**做一条视频的完整方法**——文档是工具不是圣经，随做随改。
> 硬事实与红线不在本文复述：产品能力与数字 → `spec/facts.json`；平台红线 → `spec/redlines.json`；素材登记 → `spec/assets.json`。
> **本文只写"能被机器判负"或"你亲眼看图判定"的标准。只能靠自觉打勾的条款一律不写——文档一多，AI 就会为了符合文档而产出，而不是为了好看而产出。**

---

## 零、三条不可破

1. **不虚假宣传**：功能、数字、术语必须能在 `spec/facts.json` 找到依据；不编店名、案例、数据；对外数字只允许 facts 产品参数与标注「示例值」的假设配置。
2. **不触平台红线**：三平台绝口不提微信/小程序/搜索，导流仅限账号资料位；封面不刻意提"免费"；极限词零命中。以 `check-redlines` 输出为准。
3. **效果优先于合规表**：文档全绿不等于片子能看。一条视频的成败只由两件事决定——**`check-motion` 的数字 + 你看真图的判断**。出现"符合规范但没人看"，责任在方法，改方法，不改说辞。

**协作方式**：AI 是策划者与设计者，用户给方向、拍板观感、提供数据。实质性改动（改 2 份以上文档、动共享组件、改变流程）先复述理解再提方案；发现缺陷当场定位并提修法，解决后推进下一步。**用户拍板的口径必须回写本文或 changelog，不能只留在聊天里。**

---

## 一、开工三步

1. `node scripts/gate-all.mjs` — 一条命令跑完全部闸门（清单与条数以脚本输出为准，文档内不复述条数）。**红灯不产出、不交付。**
2. `node scripts/list-assets.mjs` — 组件、素材、已产出片的实时清单。状态一律以脚本输出为准，不凭记忆。
3. 读 `outputs/archive/changelog.md` 顶部 10 条 — 近期决策与踩坑，避免推翻刚拍板的口径。

---

## 二、一条视频的七步

> **判据编号说明（2026-09-01）**：第 2/4/5 步的人判画面判据带 `C-xx｜` 前缀——审片点名用编号（如「C-11 破了」）；分镜稿每屏须带「本屏判据」自查行（见第 4 步模板）。**可机检的判据（字段名回源码、禁词、时长公式等）不编号**——它们的执行保障是闸门，以脚本输出为准。

### 第 1 步 · 选行业与定内容（现场调研，不预置清单）

- **一个行业 = 一个系列，不是一条片子（2026-08-30 用户拍板，同日两次修正后定稿）**。判定单位 = **一个痛点 + 解决它的一套完整攻略**。
  - **有头有尾**：一条叙述线固定三段——**商家那个痛点（场景）→ 这套攻略怎么做（功能点逐个讲透）→ 做完各方看到什么结果**（家长那边、商家后台、核销那一下）。缺头或缺尾都不算"攻略讲完整"。
  - **功能点少而透（这一条是系列存在的理由）**：一条攻略用到哪几个功能就讲哪几个（一般 2~4 个，例：兑换券 + 满减券走「指定手机号可领取」+ 转赠奖励），**每个都要讲到"怎么设 → 为什么这么设 → 设完之后哪一方会看到什么"**；本篇用不上的功能一个字不提。想讲的范围大过这个数，就说明这套攻略该拆成两条痛点两篇——**铺得广只会变成每个功能提一句，既不吸引人也不细致**（用户原话）。
  - **禁止两种走形**：① 把制券页→券详情→领取页→核销页逐个界面走一遍当讲透（那是页面顺序，观众只会跟着点，学不会怎么选）；② 把两条不相干的攻略缝在一条片尾（真实产出已否决过，案例见 changelog）。
  - **系列内各片 = 不同痛点、不同功能组合**：下一篇换一个痛点、另一套玩法，已讲透的功能不回头重讲。**用哪几种功能组合由 AI 依该痛点的实际情况自由设计，不设「片 N 必讲某券种」的分工表**——本文任何功能点举例只用于说明"讲透"，不固定片序配功能。
  - **每片独立成立**：平台不给"上一集"入口，禁止"看上一条"类话术。跨片复用的是开场签名、视觉母题、已做好的界面组件，不是内容。屏数与时长由这套攻略要讲透的功能点深浅决定，**不为时长砍功能点**；母片可以长到让人把界面认全。
- **画面权重（2026-08-30 用户拍板：难理解的讲透，简单的不细说）**：屏数与时长的大头给**制券环节**（逐项陈列这张券要设什么，着重讲必须这么设的那几项和理由）；**发放环节**只讲需要设数据的（私密发放的发放数量、超时时间、发放备注），其余一次就会的动作一句话带过；**领取环节不单独成屏**，只讲由设置决定的那个结果（例：开了领取开始时间，到了点才能领）；**券详情不单独成屏**；**核销环节只讲页面上的重要信息**（这张券是不是转赠来的、奖励发没发成功）。真值表与逐项玩法杠杆见 **`spec/coupon-fields.json`**（含显隐条件、联动关系、上限值、消息提醒边界、各设置项的 playLeverage）——**功能点上禁止猜测与推理，凡表里没有的说法不上屏、不进口播**。
- **选行业**：先要用户手上的抖音后台数据（播放/完播/收藏/涨粉）——完播低修内容、播放低换选题，两者修法相反，没数据就明说"靠猜"。再按时令 / 节日 / 经营周期 / 玩法可落地性 / 与已产出行业的差异化，给候选 Top3 + 选定理由 + 宣传窗口期。依据必须可验证（"9 月开学季"），不许拍脑袋。
- **调研三问**（缺一不可，都要落到"某个时刻"）：老板最头疼的是哪个时刻 → 他现在怎么做（找反常识切入点）→ 我们哪个功能真能解决那一刻。
- **定攻略**：从产品真实能力出发（做券→发券→领券→核销→统计哪一段被用上），把"这个行业的老板该怎么做那一件事"写成一套攻略：**痛点一句话 + 选 2~4 个功能点 + 每个功能点讲到"怎么设 / 为什么 / 设完哪一方看到什么"**。**动笔前先查两件**：`facts.forbidden_claims`（能力边界写错，整条片子作废，例：券包与次卡只支持私密发放）+ `spec/coupon-fields.json`（这项功能要开哪几行、哪几行因联动自动出现或消失、消息提醒的边界）。一个行业的痛点不止一个时，就在这里分行：**一行一套攻略，一套攻略一片**。
- **强监管业态**（教培 / 美业养生 / 健身母婴 / 储值预付）：逐条过 `redlines.rules.industry_specific`；表没覆盖但涉及健康、金钱、升学、资质承诺的 → 标「待确认」交人。
- 产出**系列级三件**：① 系列规划（几条痛点攻略 / 每片讲哪条链的哪三个功能点 / 发布节奏贴经营周期）② 全系列共用的**功能真值表**（券类已在 `spec/coupon-fields.json`；换产品线时先建表再动笔，后续片只补自己新增的行）③ 母题与开场签名（一次定，系列内沿用）。
- 产出**每片内容输入包**：本片故事线三段（痛点 → 攻略 → 结果）+ 选中的功能点清单，逐个写**配置明细**（券种 + 每项取什么值 + 为什么这么取 + 联动自动带出什么 + 顾客侧与核销侧的结果）+ 攻略长文 + 口播稿 + 封面文案。

### 第 2 步 · 行业视觉母题（**决定成败，先于任何画面**）

> **一套行业母题 = 一个系列共享；跨行业必须换整套语言。** 骨架（栅格、标题承载、容器形状、入场语法、节奏）按**行业**新做：同一行业的各片用同一套母题与开场签名（这是系列识别，不是偷懒），换行业时整套重做。**换配色、换图标、换纹理不算换设计语言**——"来来回回就那几个样式"正是这么来的。

先回答一个问题：**这个行业的信息，最自然的物理载体是什么？** 由它长出母题，再由母题决定每屏怎么排、元素怎么动。

| 行业 | 信息载体 | 可长出的动效语法（示例，不是清单） |
|---|---|---|
| 咖啡茶饮 | 菜单板 / 小票 / 杯垫 | 小票逐条打印出字段；液面填充做进度 |
| 火锅 | 桌号牌 / 时段客流 / 锅底 | 时段曲线随口播爬升；气泡做数据点 |
| 美容养生 | 护理本 / 次卡存折 / 镜面 | 翻页式次卡；镜面擦除转场 |
| 少儿教培 | 报名表 / 课程表 / 印章 / 作业本 | 表格逐格填写；盖章做结论强调 |
| 零售门店 | 价签 / 收银小票 / 货架 | 价签翻转；小票逐行打印做强调 |

- **C-01｜禁止 AI 自绘具象插画**（简笔人物、场景涂鸦必然廉价，直接拉低整片质感）。图形只做抽象装饰：色块、线条、几何、图标。
- **C-02｜质感锚（2026-08-29 用户逐张认可，做屏前先看这四张图）**：`outputs/bench/anchor-bundle-open.png`（产品特写）· `anchor-pain.png`（痛点·实物传单）· `anchor-mech.png`（机制对比）· `anchor-table.png`（多列对照表，ref-15 拆底稿实证）；实现代码在 `video/src/bench/`——新屏从锚稿抄结构，禁止脱离自由发挥；对不上锚稿 = 回炉。锚未覆盖的新屏型走「ref 拆底稿 → 一屏标杆」路径，17 屏型底稿样张在同目录 `outputs/bench/bench-rNN.png`（2026-08-29 用户已认可："效果都可以，这就是我说的信息密度"）。一条片内每屏按叙事功能挑**不同**的锚/底稿——片内布局多样、跨片骨架稳定。
- **C-03｜复用边界（三条实物体感标杆后明确；锚现四张，第四条对照表非实物）**：可跨行业复用的只有【做法】——真物或重排版、密度=字段、四层画面结构（背景/主张/实物/字幕道）；【载体】必须每行业重找（美术班=传单、火锅=排队号票、宠物店=疫苗提醒卡……载体不对味即返工）；【外观】每行业新配（9 套真实主题色轮换、券种组合、字段内容）。一屏标杆审的是"像不像上一条"，似曾相识=不过。
- **C-04｜要"真实感"只有两条正路**：**产品真实页面的高保真复刻**（按 `docs/internal/R6-applet前端UI储备.md` 像素真值用 Remotion 手工做，构图取"实物特写"不复刻整页；⚠️ H5 构建/浏览器录屏路线已永久关闭——applet 只兼容微信小程序端，拿不到他端形态）；**行业实拍素材**（需商用授权，库里没有就让人提供，不许拿代码画冒充）。
- 产出**母题一页**：栅格与内容区坐标 / 标题承载方式 / 容器语言 / 装饰母题 / 色彩与质感配方 / 动效语法，逐行写明"上一条片是什么、本片为何不同"。

### 第 3 步 · 口播稿（**去废话是这一步的核心**）

原则：**画面写全，嘴上只讲画面没有的东西。** 参数、字段、口径交给画面，口播只给钩子、判断、原因、动作。

- **限定（2026-08-30 用户拍板，g06 首轮失利源头）**：「这一项为什么这么设、不这么设会怎样」永远归"嘴上要讲"——画面能显示"有这一行、填了什么值"，显示不了"为什么偏偏这么填、这么填之后顾客那边和核销时会发生什么"。把这条当"画面已有"省掉，观众就是 0 接触、学不会怎么选。
- 每句必须承担四类之一：**给钩子 / 给判断 / 给原因 / 给动作指令**。做不到就删。**给判断不许只给总结词**（"锁单""开门""补齐规则"这类是提炼后的行话，只服务已懂的人，对 0 接触观众等于让他脑补）；判断必须连着理由或后果一起说出口。
- 必删：复述画面已有文字；元话语（"这些只是文字说明"）；同义重复；跨屏重复的转场词（"换个思路""再往前一步"全片各最多一次）。
- 缓冲词（很多/往往/不少/通常）：红线要求（顾客反应不得绝对化），保留但压密度，同屏 ≤2 处。
- 数字一律中文（"三天"非"3 天"）；多音字歧义改词避开。
- **字数与时长不设固定区间（2026-08-25 用户拍板；曾误设字数配额已作废，见 changelog）**：先讲满是硬要求——**讲满指深度不指广度**：本篇选中的那 2~4 个功能点，连同它们的关联面（联动会带出什么、顾客侧和核销侧会看到什么）一并讲到透；本篇用不上的功能不为了"显得全"去提（干货足 = ≥6 信息单元 + 每屏可读 ≥3 条，这些单元从选中的功能点里出）。时长按复杂度自然落位（简单 60-90s / 标准 90-120s / 复杂 120-150s，硬上限 200s，超了拆下一片）。语速 ≤6 字/秒，冗余句 ≤8%。不为凑长塞话，也不为显短砍干货——检验法：删掉一半会损失重要信息，才算讲满。
- **去冗的边界是"第一遍听懂"，不是字数少（2026-08-30 用户拍板）**：省略到需要观众猜宾语、猜指代 = 把内容甩给了观众。首版「兑的就是一个下午的托管，接校、点心、作业看护」被否——"点心是什么？"。每句写完自查：听一遍能不能复述出"谁、做什么、得到什么"；行业黑话与内部简写（"兑换物""留手机号"）一律换成商家嘴里的说法。压缩过头比留一句缓冲词危害大得多。
- **创作尺度不许写成数量配额（2026-08-30 用户拍板，同型纠正第三次）**：AI 提的「每屏至少一句操作句、位置+动作+结果三件里两件、纯总结句全片 ≤2 处」被明确回否——「我不想把视频做成一个通过套用规定的模版来设计出来的」。可量化的创作尺子会反向绑架设计：为过闸，每屏会被写成同一个形状。**尺子分两层**：① 硬闸 = 事实与合规（红线词、上屏字段名与数字回 `applet` 源码取证、禁假 UI、字幕遮挡、静止占比·帧间差·占用率这类观众反应数字），全部机检、二值；② 软尺 = 懂不懂、直不直白、像不像教程、风格是否雷同，**用自检问句 + 用户看真图判定，不折算配额**。自检问句范例：「一个从没打开过这个 App 的人看完这一屏，他的手知道下一步点哪里吗？看完这一片，他知道自己这套攻略要开哪几张券、每一项怎么设、设完会发生什么吗？」——不规定怎么拍，只规定答不出就改。

### 第 4 步 · 分镜稿（每屏一条，**必须先给真图再要批准**）

```
屏号 / 叙事职责（钩子·痛点·思路·步骤·字段·卡面·进阶·机制·收尾）
本屏讲哪一项设置：字段名（逐字取自 `spec/coupon-fields.json`）→ 本片填什么值 → 为什么这么填 → 不这么填会怎样（发放、核销类屏改填：这一屏唯一要观众看到的重要信息）
这一环商家在想什么：场景动机一句话（怕什么 / 要什么），它决定这一屏为什么填这个值
布局来源：四锚之一（特写/痛点/机制/对照表）· 或 ref-XX 拆底稿 · 或全新做（须先过一屏标杆才许进编码）
口播句 → 指向区域：这一句点亮哪一块（一块可含多个元素，不是一句一条字）
信息全景清单：本屏一次摆出的全部可读信息（≥3 条，钩子屏也要有）
画面占用率下限 ≥70%（只防"空和死"，高于它不代表更好，不许为它加装饰；也不许拿大片空白当"呼吸感"）
入场语法：本屏元素怎么出现（必须属本片母题，不许沿用上一条片的入场方式）
本屏判据自查：对照第 2/4/5 步 C 编号逐条 ✓/✗（✗ 必须写原因与修法，交真图时此行随屏附上）
```

- **C-05｜场景与教程不许分成两段（2026-08-30 用户拍板）**：观众是**对这个小程序 0 接触**的实体商家，"听不明白、看得迷迷糊糊"就没有体验意愿。一条片子的脊椎是**一套攻略的完整故事线**：头（痛点，直接压在真实界面上说）→ 主体（本篇选中的那 2~4 个功能点，逐个讲透：这一项怎么选、填什么值、联动当场带出什么）→ 尾（做完各方看到什么：家长那边、后台数据、核销那一下）。屏序跟着**功能点的选择逻辑**走，不是跟着点击顺序走，更不是把制券页→券详情→领取页→核销页各摆一屏走流程。场景不做独立铺垫段——场景当每一项的动机（要在门口发 → 公开领取 + 海报；怕被反复领 → 每人限领总量 1 张；家长答应来不会自己来 → 填写手机号 + 到期提醒；试完才谈钱 → 满减券走私密发放），逐环推着操作往前走。做到**同格**：画面里「兑换内容」填的就是「下午托管一次」、「使用须知」写的就是接孩子/写作业/晚饭，场景以"商家真要填进去的值"长在教程界面上。前几秒仍要有钩子，但钩子直接长在第一屏那张正在被填的界面上，不另开铺垫屏。
- **审批必须给真图**：设计稿阶段就把每屏渲成静态图，文字规格只做注脚。给你文字让你想象画面 = 没交付。
- **C-06｜每屏要经得起暂停截图**：单独看是一张完整干货卡。
- **C-07｜内容返工时样式冻结，但内容必须落在骨架的几何容量里（2026-08-30 用户验收后定）**：用户点头过的屏型与视觉不再动，返工只改屏序 / 屏上信息 / 口播。代价是**每副骨架的行数、卡数、句数都是硬上限**（g06：make-basic 2 组 5 行、make-rules ≤6 行、chain 3 节点、ledger 2×≤4、idea 卡标题 ≤5 字），超了只能减行或拆屏，不许缩字号硬塞、也不许靠删字段凑数。两条配套硬规矩：① **每一行在轮到自己之前不得占位**——空白回执的真根因不是卡片入场早晚，而是"行还没擦入就先占了整行高度"，卡片于是挂成一个只有打孔齿的高空框。g06 的修法：`FormLine` 在 `f < delay` 时 `return null`（卡片随内容长高），卡片与组标题入场取本组首行那一拍前 10 帧（`HandIn delay={bf(首行节拍) - 10}`）。**只给容器套 `HandIn` 不管用**（半修照样留空白回执卡，案例见 changelog）。各片骨架全部**内联**在 `videos/gXX/index.tsx`，仓库没有 `src/skeletons/` 目录，入场与占位就在各片文件里改。② **验收必须抽过程帧，不能只抽峰值帧**——峰值静帧干净不代表这一屏能看，g06 的 S5 就是这么漏过去的（10 屏静帧全过，逐帧看才发现中段三分之二是空框，且七行超出 1080×1920 可用高度）。每屏至少抽「入场 / 中段 / 峰值」三帧。
- **C-08｜新屏型先找排版底稿**：四张质感锚覆盖不了的屏型（四象限、步骤条、价格表等），从 `outputs/样本库/ref-01~19`（编号 01~19 缺 07，**库里原件 18 张**——15 已转第四锚 `anchor-table.png` 但原件留库不删；对应的 bench 拆底稿样张是 17 张、缺 r07/r15，见 `outputs/bench/bench-rNN.png`）挑最接近的一张，拆它的栅格/字号对比/用色做新组件（只借骨架不抄内容，配色换产品主题色），照旧先过一屏标杆。新参考图按 `ref-NN-屏型名-来源.扩展名` 命名入库即可走同一流程。
- **C-09｜底部结论条限制（2026-08-29 用户指出样张 17 屏同一条的雷同问题）**：样张里统一的 ConcBar/Foot 是样张约定不是成片条款——正式片每片结论条最多用 2 次（一般给机制屏/收尾屏），且每次换配色与形状贴合本片主题；样张脚注（"屏型排版样张…"）一律不进成片。锚与底稿锁的是视觉语言，列数/行数/高亮位/主题色/字段语义都是分镜阶段定的参数。
- **上屏字段名必须回源码取原话（2026-08-30 用户拍板）**：我们是拿行业场景教商家怎么用，所以画面里每个字段名、提示语、上限值都要能在产品后台逐字对上——去 `../applet/` 读 `label` / `title` / `placeholder` / `description` 原文（g06 的制券页是 `pages_coupon/coupon/create.vue`），不许凭印象造"看起来像"的名字。值按行业场景填，且必须过产品自己的上限：兑换内容只有 10 字，装不下服务明细，明细写「使用须知」（≤500 字）。一屏装不下真实字段就拆屏，别靠缩字号硬塞。产品原话里含红线词的（如到期提醒的说明含站外生态词）不引用原句，措辞只说"提醒""通知"不带平台名；但**送达不用打折**——订阅消息的送达是产品支持的能力，可以直接说"到期前会提醒家长""奖励到账会收到通知"，不要写成"顾客订阅过才收得到"这类保留说法。**券类字段已逐字收敛进 `spec/coupon-fields.json`**——先查表，表里没有的再回源码取证并把新行回写进表（含 `src` 行号），不许凭印象复述。**组标题也在真值范围内**：一行设置项挂在哪个组下，照表里的 `createGroups`——组名要么照抄页面原生那 8 个真词（`createGroups` 里 title 非 null 的正好 8 个），要么明确写成我方自述的分步标题（如「⑤ 到期提醒」），**绝不能把 A 组的行挂到 B 组标题下**（字段名对、组归属错，`check-ui-truth` ③ 分组归属会硬失败）。
- **C-10｜仿产品屏不画页面 chrome（2026-09-01 用户拍板）**：返回键、状态栏、胶囊按钮这类真产品界面里**与叙事无关的页面装饰**一律不画——观众不会去点，占了顶栏又不传递任何信息。顶栏只留页面真实标题（标题本身仍按上条取原话），面包屑这类"告诉观众怎么走到这一页"的信息可按需保留。「这种都不重要」就是这条的判断尺：上屏的每个元素要么在讲功能、要么在讲场景，二者皆不是就不上屏。
- **C-11｜合规标注不占屏底部，统一挂顶部（2026-09-01 用户二批拍板）**：铁律 1 的示例值标注这类合规义务，挂到**顶部已有元素**上——制券屏走面包屑行尾（如 `… 类型：兑换券 · 数值为示例`）、无面包屑的屏走左上角标（如 `第二招 · 发出去（示例）`）；**不得在屏底部另起一行小字**，屏底部只留字幕道。标注义务本身不减：既不许"为清底部"把标注整个删掉，也不许缩进正文里蒙混。**旧法废止**：把合规标注放屏底页脚的写法已废止（义务判断对、位置判断错），详见 changelog 2026-09-01，不得回用作参照。

### 第 5 步 · 三层运动（**流畅感的真正来源：声画同步**）

| 层 | 做什么 | 目的 |
|---|---|---|
| 底层持续微动 | 背景缓推、光晕漂移、纸面呼吸、高光扫过 | 帧间差永不归零，消灭死寂，不抢注意力 |
| 中层口播指向 | 每句口播到点，对应区域入场/放大/描边/划线强调；讲过的区域降到 `opacity: 0.72`（即 72% 不透明、不是"72% 透明"——0.55 实测太暗被否，浅底上要能读清，见 g06 `region()`） | 讲到哪、跳到哪、亮到哪——灵动感 |
| 顶层信息全景 | 未被念到的参数保持可见安静 | 信息密度与可截图性，不占口播时长 |

- **C-12｜节拍 = 口播句，不是画面元素。** 一屏 8 句最多 8 次指向；给每个元素各排一次入场是轰炸，不是同步。
- **C-13｜停留期 ≠ 静止期**：观众读信息时，画面仍要有底层微动与指向变化。
- 节拍时间先用「字数占比」估算（与字幕同源、零额外成本）；实测偏拍明显再升级为逐句 TTS。

### 第 6 步 · 渲染与量化验收

```bash
node scripts/check-motion.mjs <媒体文件>     # 静止占比 / 中位帧间差 / 画面占用率
node scripts/check-similarity.mjs            # 整屏结构与任何已产出片重复数必须 = 0
node scripts/check-redlines.mjs              # 硬禁层 0 命中
```

| 指标 | 合格线 | 实测参照（2026-08-29，RGB 口径） |
|---|---|---|
| 画面静止占比（帧间差 ≤0.3） | ≤45% | G04 = 67%（不合格）；参考片 = 20% |
| 中位帧间差（持续微动强度） | ≥0.35 | G04 = 0.12；参考片 = 0.81 |
| 画面占用率 | ≥70%（下限，非质量分） | G04 成片 63%；参考片 95%；G05 v2 九屏 63~68%；2026-08-31 实测：已逐张认可的 bench 样张 11~69% 全部低于此线，g06 片1 十二屏 74~82% |
| 每屏可读信息条数 | ≥3（含钩子屏，人判·无尺） | v1 钩子屏 = 0 |
| 口播冗余句占比 | ≤8%（人判·无尺） | 现稿 ≈25%（G05 已删，此数为历史记录） |
| 跨片结构重复数 | 0（例外须批准并登记） | G05 v1 = 6/9 |
> 阈值按"一条已知差的 + 一条已知好的"实测定标（绝对阈值随采样分辨率漂移，同一参考片曾算出 30% 与 77% 两个结果）；换机器或改采样后重新定标，不许凭感觉调。两个定标样本（G04 差片+同事好片）已随清零删除，45%/0.35/70% 为 08-29 实测**封版基线**；今后重新定标须先各存一份好/坏样本到 `outputs/archive/calibration/` 再跑。本表静止占比 / 中位帧间差 / 画面占用率由 `check-motion` 机检、跨片结构重复数由 `check-similarity` 机检；「每屏可读信息条数」「口播冗余句占比」无自动尺子，属第 3 步软尺，交稿前人逐屏逐句判，不当闸门。

- 无声版阶段渲**逐屏峰值帧单张**交你看（不拼九宫格、不渲无声版）；你说"画面 OK"才进阶段二。
- 阶段二：逐屏 TTS → loudnorm → ffprobe 实测 → **atempo 1.2**（seed-tts 的 `speed_ratio` 实测无效，此值已定稿）→ dur 与字幕按实测回填 → 渲染 → **盲听全片**（首字完整、尾音不爆、节奏自然）。以渲染结果为准，不信预览器。
- 渲染必带：`NODE_OPTIONS=""` + `--browser-executable` 指向 Chrome Headless Shell——**架构随芯片**（Intel=`mac-x64`，M 系=`mac-arm64`，Remotion 按 `os.arch()` 自动下载对应目录），跨机通用取法：在 `video/` 下先 `npx remotion browser ensure`，再 `B=$(find node_modules/.remotion/chrome-headless-shell -name chrome-headless-shell -type f)`，渲染命令里用 `"$B"`，**两台机器同一条命令、勿照抄对方路径**。⚠️ 不许指向系统 Chrome——Chrome 151 + macOS 14 下 headless 因 CVDisplayLink 挂死（2026-08-29 实测）。渲染走管道时 `| tail` 会吞失败退出码，以产物文件存在为准。

### 第 7 步 · 发布（三平台）

- **抖音**：9:16 短视频，唯一短视频阵地。手动设封面并关闭"自动同步首帧"；话题 3-5；周二/四 12-13 或 18-20 点。
- **小红书**：图文笔记（不发视频），封面 3:4 由 9:16 裁中间区，关键信息放中间安全区；标签 6-10；正文 ≥5 信息单元。
- **搜狐**：长文攻略，16:9 头图，**正文必须 3~5 个 `###` 次标题**——搜狐是滚动阅读 + SEO 平台，无次标题是短板不是简洁（2026-09-01 g06 首稿实证）；次标题落在叙事转折处、顺带多一次关键词露出，交稿前自查 `grep -c '^###' <稿子>` ≥ 3。SEO 埋词；≥8 信息单元；**字数以讲清楚为准，不硬凑**。
- 发布稿一律 `.md`（不用 txt，粘贴会丢格式）。配图先查 `spec/assets.json` D 级，发布前裁掉微信元素。
- CTA 口径统一：只出现在账号资料位（主页简介 / 置顶评论 / 私信回复），正文与画面不写导流话术。
- 发布后 1 小时回评论、24 小时看数据、7 天复盘写进 changelog——**后台数据与选型命中是下一片的输入，没有数据就只能靠猜**。

---

## 三、工程约定

- **一行业一套专属屏**：`video/src/videos/gXX/`（屏组件 + 本片零件 + payload 类型）。屏组件**不得跨行业 import**；共享层已冻结——只含原子件与 `VTemplate`。旧共享层已废止（详见 changelog）：12 个共享场景 + "不写 ui 按 type 回退"旁路 → **ui 必填，缺了直接抛错**；9 个"统一外观"业务组件（CouponCard/PhoneMockup/StepFlow/CompareCard/StatCounter / StatCard/IconBadge/SectionTitle/HighLightText）→ 禁止预建/复用外观件，每片照锚稿手写。**共享件现行消费者状态**以 `node scripts/list-assets.mjs` + grep 实时为准（文档宪法铁律三，本文不复述快照；提醒一句：**零引用 ≠ 已删除**，能调用没人用的原子件仍在）。规则恒定：共享层只有原子件与 `VTemplate`，**没有任何现成的券面/手机壳/步骤条外观**——要上屏照锚稿手写，别以为 import 一下就有。
- 数据文件每屏写 `ui: 'gXX-名字'`，业务数据进 `payload`（形状由本片 `videos/gXX/types.ts` 定义，通用 `Scene` 只留分发/时长/字幕/语音偏移字段）；在 `scenes/index.tsx` 的 `VIDEO_RENDERERS[视频id]` 注册；`ui` 名拼错运行即抛错，不会静默回退旧组件；数据文件的**导出名必须与视频 id 一致**（check-similarity 防伪按该名找 `videos/<key>/index.tsx`）。
- 结构指纹 = `type + ui`，由 `check-similarity` 比对。**⚠️ 这道机检只防「把上一片的 ui 名照抄」**：ui 按约定必须带 `gXX-` 前缀，所以换掉前缀重做一副一模一样的骨架，指纹永远不碰撞（2026-08-30 负向测试证实）。**结构雷同的真判据是「一屏标杆」——你逐张看真图**，别拿这道绿当"结构没重复"。确需复用旧结构 → 在 `ref-registry.json` 的 `similarityExemptions` 登记理由与批准人。
- `dur = ceil01(口播字数÷6) + extraHold`（÷6 = 语速上限的保守估算，实测落 5.5-6 字/秒；信息屏 0 / 数据屏 0.8 / CTA 1.5）；**绝对起始帧 = Σ前各屏帧数 − 12×前屏数**（`TransitionSeries` 每屏重叠 12 帧），峰值帧 = 起始帧 + floor(本屏帧数×0.3)。
- 字数口径：剔除空白与标点后逐屏机器计数（别手估，同一稿会数出两个结果）。
- 卡片宽度按 `border-box` 理解（Remotion 注入全局 CSS）；安全区达标与否**量渲染像素**，不靠读 CSS 推断。
- 交付落 `outputs/gXX-行业/`：内容输入包、分镜稿、逐屏关键帧、成片、封面、三份发布稿；过程文件进 `outputs/archive/`。

---

## 四、清零重启（2026-08-29 用户拍板）

G02~G05 全部是**梳理流程用的测试产物**，已物理删除（git 可查，但不得回流为参照；否决理由与案例回放见 changelog 2026-08-29）。从现在起：文档是工具不是圣经，不行就改；每条视频从空白起步，用效果说话。

**本轮被否决的具体做法（防重蹈）：**
- ❌ 拿代码画的"表示物"当设计：线条拱门、圆圈火柴人、灰粉横条当名单、蓝框当漏斗——占位符质感，一眼假。**元素要么是真的（真实产品界面/真实形态的数据），要么是纯抽象装饰；两头不沾就别放。**
- ❌ 把内部约束写进画面：「券包不支持公开扫码领取」这类负面句式——观众不需要知道平台不支持什么，只正向说"通过私密方式一对一发放"。
- ❌ 把填表规则当信息：「券名称不超过 18 个字」——直接摆成品示例（真实券名、面额、有效期），让观众看到结果长什么样。
- ❌ 对着指标凑画面：为填占用率加装饰、为压静止数加晃动。**占用率/静止数只能防"空和死"，防不了"糙"；质感唯一判据 = 用户看真图。**

**新增硬闸门——一屏标杆：** 任何一条视频，先只做 1 屏、渲真图、**交用户认可"就是这个质感"，才许铺其余屏**。一屏不过，整片不开工（G05 九屏一起交导致整体否决的直接教训）。

**保留的工具（清零不清能力）：** `check-motion` 效果尺子、`band-occupancy` 分带诊断（教训：占用真空常在顶/底带，别对着已 70~90% 的中段硬填）、节拍层实现（`useBeat` / `useBeatFrame` 两个 hook，内联在各片 `videos/gXX/index.tsx` 顶部——按字幕句序号取本屏节拍与帧位，新片照这个写法在片内重写；指路只指符号名不写行号，见铁律三）、Chrome Headless Shell 渲染路径。相似度基线随历史片删除归零，`check-similarity` 脚本保留，但**它能拦的只有"照抄上一片的 ui 名"**（能力边界见 §三 结构指纹条）——跨片结构雷同目前真正的防线只有一屏标杆。

---
name: quandao-content
description: 券到卡包宣传视频怎么做——行业线唯一作业文档。行业选型→视觉语言→口播去冗→分镜真图→声画节拍→量化验收→发布。红线与事实见 spec/。
version: 8.0.1
---

# 券到卡包 · 宣传视频怎么做

> 版本：8.1.0（2026-09-24 风格包体系与闸门接线收口）。本文是**行业线唯一**作业文档、**做一条视频的完整方法**——文档是工具不是圣经，随做随改。
> 当前官网、爆款语料、玩法版图的职责边界只认 `docs/internal/内容资源调用协议.md`；历史 archive 仅作证据，不得作为当前生产入口。
> 硬事实与红线不在本文复述：产品能力与数字 → `spec/facts.json`；平台红线 → `spec/redlines.json`；素材登记 → `spec/assets.json`。
> **本文只写"能被机器判负"或"你亲眼看图判定"的标准。只能靠自觉打勾的条款一律不写——文档一多，AI 就会为了符合文档而产出，而不是为了好看而产出。**

---

## 零、三条不可破

1. **不虚假宣传**：功能、数字、术语必须能在 `spec/facts.json` 找到依据；不编店名、案例、数据；对外数字只允许 facts 产品参数与标注「示例值」的假设配置。**外部模型（如 qwen）产出的口播/文案草稿同样受本铁律与第 1 步「功能点禁止猜测」约束**——术语、机制、数字一律回 `facts.json` + 对应产品线真值表（券类 `coupon-fields.json` / 次卡 `card-fields.json`）验真后才准入稿。
2. **不触平台红线**：三平台绝口不提微信/小程序/搜索，正文、画面、账号资料位一律不写导流（含变体暗号）；封面不刻意提"免费"；极限词零命中。以 `check-redlines` 输出为准。
3. **效果优先于合规表**：文档全绿不等于片子能看。一条视频的成败只由两件事决定——**所选风格包「验收位图」声明的机检读数（remotion-components 类＝`check-motion` 三项数字）+ 你看真图的判断**。出现"符合规范但没人看"，责任在方法，改方法，不改说辞。

**协作方式**：AI 是策划者与设计者，用户给方向、拍板观感、提供数据。实质性改动（改 2 份以上文档、动共享组件、改变流程）先复述理解再提方案；发现缺陷当场定位并提修法，解决后推进下一步。**用户拍板的口径必须回写本文或对应变更事务，不能只留在聊天里。** **替代型决策（新口径否定旧口径）落地三件套，缺一即未落地**——① 新口径写进正文唯一属主（同一内容只准一处说，其余只留指针）；② 旧口径当场登记进 `scripts/ref-registry.json` 的 `deprecatedTerms`（文本旧话头）或 `caliberOwners`（数值旧口径），这一步把「清旧」从自觉变脚本强制；③ 跑 `check-doc-references` 必须 0 硬失败。**只加新文件/新引用、不 grep 旧词并登记，等于只做了「新增」没做「替代」**（防新旧并存误导新会话；机检即脚本口径唯一性层 + 废弃话头层）。

---

## 一、开工三步

1. `node scripts/gate-all.mjs` — 一条命令跑完全部闸门（清单与条数以脚本输出为准，文档内不复述条数）。**红灯不产出、不交付。**
2. `node scripts/list-assets.mjs` — 组件、素材、已产出片的实时清单。状态一律以脚本输出为准，不凭记忆。
3. 读 `docs/changes/active/` 与 `docs/changes/closed/` 的最新变更事务 — 近期决策与踩坑，避免推翻刚拍板的口径。

---

## 二、一条视频的八步（第 4.5 步视觉转换是内容→画面的翻译关）

> **判据编号说明（2026-09-01 立；2026-09-23 CHANGE-041 修正落点）**：人判画面判据带 `C-xx｜` 前缀——审片点名用编号（如「C-11 破了」）；分镜稿每屏须带「本屏判据」自查行（见第 4 步模板）。**编号是跨层的**：留在本步的（C-05～C-11／C-14／C-17～C-19）在本文；随迁的（C-02／C-03／C-15／C-16 在所选风格包 §③A，C-12／C-13 在该包 §③C）以包为属主，本文只留指针。**可机检的判据（字段名回源码、禁词、口播数字中文、背景底必选等）不编号**——它们的执行保障是闸门，以脚本输出为准。

### 第 1 步 · 选行业与定内容（现场调研，不预置清单）

- **一个行业 = 一个系列，不是一条片子（2026-08-30 用户拍板，同日两次修正后定稿）**。判定单位 = **用一条闭环故事（五拍，见下条）讲透的一套行业攻略（由这个行业某个最痛的时刻引出）**。
  - **一条闭环故事讲透一套攻略（2026-09-08 用户拍板，替代"清单式切功能点"；2026-09-17 CHANGE-020 改认知职责口径）**：五拍是**认知功能骨架**——**吸引（抛悬念·痛点）→ 保持（讲道理）→ 理解与相信（证明·示例值演示）→ 落地（功能点可照抄）→ 收口（金句·认知收口）**。它规定的是「这一段要完成什么认知任务」，**不规定用什么写法完成**——「错误示范」「生活类比」「不是 X——是 Y」「定律句」这类具体手法**一律不在五拍内固定**（2026-09-17 CHANGE-020）。行业是主轴；`knowledge/洞察库.md` 的机制是「理解」段的**引擎之一**，主角始终是"这个行业怎么用电子券把活动做好"。进入「理解」段有两条**设计入口（不是两套模板）**：**机制入口**（先定机制，再落到本行业情境）与**场景入口**（先有具体行业情境，再回指机制）。`knowledge/爆款整片解剖.md` 是**候选传播规律与研究索引**——按需回查（写开头 / 搭结构卡壳时查），**不是每片必须逐条对照的作业清单**。
  - **有头有尾（五拍的粗分档）**：全片五拍天然分三档——**头 = 抛悬念（商家那个痛点场景）→ 身 = 讲道理 + 证明 + 落地（功能点逐个讲透）→ 尾 = 金句（做完各方看到什么结果·家长那边、商家后台、核销那一下）**。缺头或缺尾都不算"攻略讲完整"。
  - **功能点少而透（这一条是系列存在的理由）**：一条攻略用到哪几个功能就讲哪几个（一般 2~4 个，例：兑换券 + 满减券走「指定手机号可领取」+ 转赠奖励），**每个都要讲到"怎么设 → 为什么这么设 → 设完之后哪一方会看到什么"**；本篇用不上的功能一个字不提。想讲的范围大过这个数，就说明这套攻略该拆成两条痛点两篇——**铺得广只会变成每个功能提一句，既不吸引人也不细致**（用户原话）。
  - **禁止两种走形**：① 把制券页→券详情→领取页→核销页逐个界面走一遍当讲透（那是页面顺序，观众只会跟着点，学不会怎么选）；② 把两条不相干的攻略缝在一条片尾（真实产出已否决过）。
  - **系列内各片 = 不同痛点、不同功能组合**：下一篇换一个痛点、另一套玩法，已讲透的功能不回头重讲。**用哪几种功能组合由 AI 依该痛点的实际情况自由设计，不设「片 N 必讲某券种」的分工表**——本文任何功能点举例只用于说明"讲透"，不固定片序配功能。
  - **每片独立成立**：平台不给"上一集"入口，禁止"看上一条"类话术。跨片复用的是开场签名、所选风格包的视觉语言（续用同风格时）、已做好的界面组件，不是内容。屏数与时长由这套攻略要讲透的功能点深浅决定，**不为时长砍功能点**；母片可以长到让人把界面认全。

> **内容层声明（2026-09-23 CHANGE-039）**：五拍＝**内容传播骨架**（内容层），归属第 1 步与本文，**不删除、不被替代**；**叙事结构与观看路径＝导演层专属**（R9 通用判据 ＋ 逐片《导演稿》，见 R9 与第 4.5 步）；**风格包不得拥有独立导演方法／故事结构／观看路径**（判据：**换包不换导演稿**——同一份《导演稿》换任一风格包仍应能落地；要求改写叙事顺序／信息释放即越界）。导演叙事不得漏讲五拍骨架里必讲的信息。
- **画面权重（2026-08-30 用户拍板：难理解的讲透，简单的不细说）**：屏数与时长的大头给**制券环节**（逐项陈列这张券要设什么，着重讲必须这么设的那几项和理由）；**发放环节**只讲需要设数据的（私密发放的发放数量、超时时间、发放备注），其余一次就会的动作一句话带过；**领取环节不单独成屏**，只讲由设置决定的那个结果（例：开了领取开始时间，到了点才能领）；**券详情不单独成屏**；**核销环节只讲页面上的重要信息**（这张券是不是转赠来的、奖励发没发成功）。真值表与逐项玩法杠杆见 **`spec/coupon-fields.json`**（含显隐条件、联动关系、上限值、消息提醒边界、各设置项的 playLeverage）——**功能点上禁止猜测与推理，凡表里没有的说法不上屏、不进口播**。
- **选行业**：先要用户手上的抖音后台数据（播放/完播/收藏/涨粉 + **封面CTR / 2 秒跳出**，判据按第 7 步「发布后验闭环」漏斗顺序读）——封面CTR≈0 修入口三件套 → 2 秒跳出高修前 2 秒开口 → 完播最低修闭环结构（**旧「完播低修内容、播放低换选题」二选一废止，以漏斗顺序为唯一判据，避免与第 7 步打架**）；修法相反，没数据就明说"靠猜"。再按时令 / 节日 / 经营周期 / 玩法可落地性 / 与已产出行业的差异化，给候选 Top3 + 选定理由 + 宣传窗口期。依据必须可验证（"9 月开学季"），不许拍脑袋。
- **调研三问**（缺一不可，都要落到"某个时刻"）：老板最头疼的是哪个时刻 → 他现在怎么做（找反常识切入点）→ 我们哪个功能真能解决那一刻。
- **回语料检索同类（2026-09-09 用户拍板，把「行业设计时参考相关文章」落成必做动作）**：定攻略前，按本片行业回 `knowledge/爆款文案技能包/knowledge/cases.jsonl` 检索同类语料——**用标注字段**（`domain`／`tags`／`hook_type`／`structure`／`proof_devices`／`retention_devices`）检索，**分层抽样**：≥3 篇同赛道 Tier A（点赞 Top 10%）＋ ≥1 篇低表现做**对照**；逐篇看「这行老板最在意什么、别人怎么开头／讲道理」，喂给五拍骨架（借结构不搬话术，口径见 `docs/internal/内容资源调用协议.md` §4）。检索不到同类就标注「语料稀疏，退回 18 通则」，不硬凑。**挖到的新故事结构/新钩子模式，回流 `爆款整片解剖.md`（其 §〇 生命周期修订承接），不搬语料；挖到的新定律（适用痛点 + 承载功能能说清、语料出处能回 JP 编号）回流 `knowledge/洞察库.md` 并按库首格式成条，只有模糊念头说不出承载功能的，不硬塞**。
- **功能深挖（2026-09-03 用户拍板，必须牢记）**：功能不做表面攻略——选定功能点后至少挖三层：**谁在什么时刻用它、能和哪些功能组合成玩法、还能延伸到什么生意方向**。素材源 = `spec/coupon-fields.json` 各行 playLeverage + `knowledge/玩法版图.md`（**原子×组合视角：从行业痛点反向拼原子、不先选券种**，见其 §三（组合视角））+ 官网功能介绍页的场景示例区（18 个获客场景、每个标注功能组合，玩法层回表溯源，见 `docs/internal/内容资源调用协议.md`「官网读取」）。**挖到的新组合玩法若版图没有且逻辑成立，回流 `玩法版图.md` §二（组合玩法层）**。判据：只讲"这个功能是什么、怎么设"而不讲它的延伸面 = 不合格。
- **定攻略**：从产品真实能力出发（做券→发券→领券→核销→统计哪一段被用上），把"这个行业的老板该怎么做那一件事"写成一套攻略——**先答五拍的认知职责：抛哪个痛点悬念（吸引）→ 「理解」段走机制入口还是场景入口、要讲清哪条机制或哪个情境（保持）→ 用什么示例值证明（理解与相信）→ 落地到哪 2~4 个功能点（每点讲到"怎么设 / 为什么 / 设完哪一方看到什么"）→ 金句收什么认知（收口）**（写法不固定，见 §三 写法工具包）。**动笔前先查两件**：`facts.forbidden_claims`（能力边界写错，整条片子作废，例：券包与次卡只支持私密发放）+ `spec/coupon-fields.json`（这项功能要开哪几行、哪几行因联动自动出现或消失、消息提醒的边界）。**官网只读蓝本、组合挖法回玩法版图（2026-09-09 拍板，替代「≤5 页素材清单」）**：官网是玩法蓝本 + 对账镜，能力/数字永不采信、只认 facts/真值表；组合玩法挖法回 `knowledge/玩法版图.md` §三（组合视角），蓝本读取与避雷见 `docs/internal/内容资源调用协议.md` §3/§4（变更史见 `docs/changes/`）。一个行业的痛点不止一个时，就在这里分行：**一行一套攻略，一套攻略一片**。
- **强监管业态**（教培 / 美业养生 / 健身母婴 / 储值预付）：逐条过 `redlines.rules.industry_specific`；表没覆盖但涉及健康、金钱、升学、资质承诺的 → 标「待确认」交人。
- 产出**系列级三件**：① 系列规划（几条痛点攻略 / 每片讲哪条链的哪三个功能点 / 发布节奏贴经营周期）② 全系列共用的**功能真值表**（券类 `spec/coupon-fields.json`、次卡 `spec/card-fields.json`；换产品线时先建表再动笔，后续片只补自己新增的行）③ 视觉语言与开场签名（视觉语言由所选风格包定；同风格续用时沿用）。
- 产出**每片内容输入包**：本片故事线五拍（认知职责：抛悬念 → 讲道理 → 证明 → 落地 → 金句，写法不固定）+ 各拍对应的功能点清单，逐个写**配置明细**（券种 + 每项取什么值 + 为什么这么取 + 联动自动带出什么 + 顾客侧与核销侧的结果）+ 攻略长文 + 口播稿 + 封面文案 + **调研留痕**（缺一节＝第 1 步没做完）：**① 语料检索结果**——回 `knowledge/爆款文案技能包/knowledge/cases.jsonl` 挑的 3~5 篇哪几篇 + 各借了什么结构（一句）；**② 避雷核对**——读了 `docs/internal/内容资源调用协议.md` 官网信任边界后，本片避了哪几条雷（一句）；**③ 表达形态选择及理由**（2026-09-17 CHANGE-020 新增）——本片选了哪些表达形态、为什么；上一条片用过什么形态、本片为什么继续用或主动避开。**它是选择证据，不是数量配额**——不得写成"每片必须选择 N 种形态"。

### 第 2 步 · 视觉语言（**由所选风格包定义**）

- **系列级视觉语言原则（风格专属，已迁出）**：一套行业母题＝一个系列共享、跨行业整套重做 —— 正文见包 §③A。

> **适用范围（2026-09-23 CHANGE-039）**：本步是**路由步**——具体视觉语言设计法由**所选风格包**定义（当前 `remotion-components` 的＝其 §③A「母题设计法」）；风格化产出的「母题一页」是否适用，由所选风格包在其「验收位图」栏声明（`check-motif-card` 按声明取值）。**本步不定义叙事**——叙事归导演层（R9）。

- **母题设计法（风格专属，已迁出）**：开工隔离声明／视觉定位卡／背景底必选／素材最小化＋程序化质感／行业载体示例表 —— 正文迁入 `video/styles/remotion-components/SKILL.md` §③A，**本节不复述**。

- **AI 创作边界协议（三层 · 跨风格单源 · 2026-09-23 用户裁定，替代旧 C-01／C-04）**：从"禁止 AI 生成"升级为**区分事实与表达**——产品事实必须真实，视觉表达可以自由（用户原话方向："确实没有外部图片素材，要靠 AI 的能力发挥"）。本协议不编 C 号（它是跨界协议，不是单屏判据）。
  - **L1 不可生成（产品真实性层）**：产品界面、功能字段、操作流程；**事实数字**（字段数值／金额与规则数值／上限值）；**经营结果数字**（后台读数、核销与复购等结果口径）——来源只能是真实产品资产：真实产品截图（D-1 登记且 `current-verified`）／按 `docs/internal/R6-applet前端UI储备.md` 像素真值在实现层复刻／真实录屏。**AI 不得生成或推测**。
    - **叙事数字不受此限（2026-09-23 用户补正）**："晚上 10 点""7 天""一天只有 2 桌"这类属故事表达，可由 AI 生成；判定＝该数字是否指向**产品能力／规则／结果承诺**——指向则按事实数字处理。**叙事数字不得构成效果承诺**（"7 天回本""客流翻倍"一律禁止）。
  - **L2 可以生成（创意表达层）**：商业场景、人物、环境、情绪、隐喻、空间、视觉故事、叙事数字。抽象手机、光影中的手机轮廓、顾客收到福利的情绪场景等**均为合法**；但不得出现可被误认为真实产品的界面。
  - **L3 信息层统一控制（文字归属）**：① **产品事实文字**（界面文案／字段名／金额数字／规则说明／数据结果）→ 一律真实来源或由实现层排版生成，接受与上屏文字相同的红线与真值检查；**AI 不得生成**；② **品牌文字** → 画面零品牌、口播必提一次（沿用 `brand_display`）；③ **装饰性视觉文字**（招牌／海报／包装／艺术字／年份／抽象符号）→ **允许 AI 生成**，但不得承担产品事实信息、不得出现导流词／极限词／站外生态词／品牌名，形态与密度上限由所选风格包在「视觉语言」栏声明，**默认须为氛围性**（虚焦／远景／低对比／局部）；需可辨读者由该风格包显式声明并列入人判清单。
  - **能力边界（如实标注，不假装能验）**：L1 靠风格包「产品证据位」栏 ＋ 人工逐屏对账把关（`check-ui-truth` 只覆盖上屏字段名，不识生成界面）；L3 的产品事实文字受红线闸门约束，装饰性文字主要靠人判。
- **屏级画面纪律（风格专属，已迁出）**：质感锚 C-02／复用边界 C-03／开工隔离 C-15／换语言五轴证据门／布局指纹 C-16／母题一页产出 —— 正文迁入同上 §③A；**本风格适用性**由该包「验收位图」声明（`check-motif-card`／`check-layout-diversity` 按包声明取值）。

### 第 3 步 · 口播稿（**去废话是这一步的核心**）

原则：**画面写全，嘴上只讲画面没有的东西。** 参数、字段、口径交给画面，口播只给钩子、判断、原因、动作。

- **限定（2026-08-30 用户拍板，g06 首轮失利源头）**：「这一项为什么这么设、不这么设会怎样」永远归"嘴上要讲"——画面能显示"有这一行、填了什么值"，显示不了"为什么偏偏这么填、这么填之后顾客那边和核销时会发生什么"。把这条当"画面已有"省掉，观众就是 0 接触、学不会怎么选。
- 每句必须承担四类之一：**给钩子 / 给判断 / 给原因 / 给动作指令**。做不到就删。**给判断不许只给总结词**（"锁单""开门""补齐规则"这类是提炼后的行话，只服务已懂的人，对 0 接触观众等于让他脑补）；判断必须连着理由或后果一起说出口。
- 必删：复述画面已有文字；元话语（"这些只是文字说明"）；同义重复；跨屏重复的转场词（"换个思路""再往前一步"这类全片不宜反复——软尺，判据同去冗边界，不折算次数配额）。
- 缓冲词（很多/往往/不少/通常）：红线要求（顾客反应不得绝对化），保留但压密度——同屏不宜堆砌，软尺，不折算次数配额。
- 数字一律中文（"三天"非"3 天"）——机检：`node scripts/check-voice-discipline.mjs` 扫 `video/src/data/*.ts` 口播字幕，阿拉伯数字即硬失败；多音字歧义改词避开。
- **不设预设时长（2026-09-21 用户裁定：时长是内容讲透的结果，不是前置约束；与 2026-08-25「不设固定区间」拍板同源，08-28 AI 私设配额已作废，变更史见 docs/changes/）**：先讲满是硬要求——**讲满指深度不指广度**：本篇选中的那 2~4 个功能点，连同它们的关联面（联动会带出什么、顾客侧和核销侧会看到什么）一并讲到透；本篇用不上的功能不为了"显得全"去提（干货足 = ≥6 信息单元 + 每屏可读 ≥3 条，这些单元从选中的功能点里出）。时长由本篇深浅自然产生：**片长不设任何档位、区间或上限（含"参考档"）**；要收短只能靠第 1 步「功能点少而透」与「定攻略」两条内容判据拆片或换功能组合，不靠砍干货。语速 ≤6 字/秒，冗余句 ≤8%（这两条是表达尺，不是片长档）。不为凑长塞话，也不为显短砍干货——检验法：删掉一半会损失重要信息，才算讲满。
- **去冗的边界是"第一遍听懂"，不是字数少（2026-08-30 用户拍板）**：判据共用下一条「清晰度前置关」这一把尺，不在此处复述；去冗方向只补充一条——压缩过头比留一句缓冲词危害大得多。
- **创作尺度不许写成数量配额（2026-08-30 用户拍板，同型纠正第三次）**：AI 提的「每屏至少一句操作句、位置+动作+结果三件里两件、纯总结句全片 ≤2 处」被明确回否——「我不想把视频做成一个通过套用规定的模版来设计出来的」。可量化的创作尺子会反向绑架设计：为过闸，每屏会被写成同一个形状。**尺子分两层**：① 硬闸 = 事实与合规（红线词、上屏字段名与数字回 `applet` 源码取证、禁假 UI、字幕遮挡、静止占比·帧间差·占用率这类观众反应数字），全部机检、二值；② 软尺 = 懂不懂、直不直白、像不像教程、风格是否雷同，**用自检问句 + 用户看真图判定，不折算配额**。自检问句范例：「一个从没打开过这个 App 的人看完这一屏，他的手知道下一步点哪里吗？看完这一片，他知道自己这套攻略要开哪几张券、每一项怎么设、设完会发生什么吗？」——不规定怎么拍，只规定答不出就改。

- **清晰度 = 第一道强制前置关（2026-09-10 用户拍板）**：任何上屏进稿的文字（口播句与画面大字/标题/按钮/旁注）产出前先自答——第一遍能否复述出「谁 / 做什么 / 得到什么」；答不出（砍宾语、指代悬空、生造词、行业黑话与内部简写）＝退回重写，不靠观众脑补；答得过才许进写法五条 / 分镜。反例「……就是不带你？」（不带谁、去哪）、「点心是什么？」（托管文案砍掉宾语）。画面文字同尺见第 4 步 C-17。
- **口播写法工具包（2026-09-10 立，17 篇爆款整片解剖提炼；2026-09-17 CHANGE-020 改口径）**：填句前先用五拍做**认知职责自查**（每段要完成哪个认知任务：吸引 / 保持 / 理解与相信 / 落地 / 收口，见第 1 步）；「理解」段的进入方式与素材来源见第 1 步**双入口**（机制入口 / 场景入口），不被固定到某个素材库。**大前提：表达力 = 结构，不是形容词——全靠结构动作（给钩子 / 判断 / 原因 / 动作指令），不靠形容词堆砌。**
  - **复用纪律（2026-09-17 CHANGE-020 新增）**：下列五条是**可选表达工具**（不是必过清单，不必逐条沾边）；且 **上一条片用过的表达方式不构成本片的默认选择**——不是禁止复用，而是复用前必须重新证明"这一次为什么仍然适合"，结论写进内容输入包的《表达形态选择及理由》。
  - **① 承诺-兑现链（吊着不散）**：任何时刻都有一件「已承诺、还没兑现」的东西吊着观众——一个没拆开的数字、一个下一步才给的做法、一个待验证的悬念；第一句先亮结果再倒回去讲做法，关键结论说完原话钉一遍。全片没有一行纯过渡空转——每句要么往前推新承诺，要么兑现上一个（通则 1·6·12）。
  - **② 功能不裸讲（嵌进故事）**：功能点永不干巴列条，嵌在「算账式带算」「商家最痛的那个时刻」「设完之后顾客侧 / 核销侧会看到什么」里讲；功能只在「讲道理 / 证明 / 落地」三拍的具体推进里出现，不是一张独立清单（通则 2·8）。
  - **③ 具体颗粒度 + 人性总纲（可信 + 可懂双锚）**：数字具体到不可疑（只准 facts 参数与标注「示例值」，不编案例），每个机制最后收一条人性定律（`knowledge/洞察库.md`）——观众带走的是「懂了为什么」的认知，不是清单；每个商业机制配一条生活类比兜底，零专业门槛（通则 3·4·9·13）。
  - **④ 错误示范 + 预判疑问（先立错再给对）**：先让观众看见「自己正做错」（错误示范先行，演示即痛点），刚冒头的反驳下一句就接住（预判疑问）——正解因有了对照才有分量（通则 5·7）。
  - **⑤ 借势 + 过程即内容（可信度双来源）**：借公认标杆给方法定位（胖东来 / 雷军式嫁接），或用「普通人一步步怎么做出来」的过程实录本身当内容——真实感比形容词更有说服力（通则 14·17·18）。
  - **这套五条怎么长出你要的情绪**：吸引力←承诺-兑现链（通则 12 结果前置）、生动←功能不裸讲 + 生活类比兜底（通则 4）、逻辑←承诺-兑现链 + 五拍骨架（通则 1）、说服力←具体颗粒度 + 错误示范（通则 9/5）、情绪价值←`洞察库.md` 定律的心理机制（通则 13）。落到句子上是四类结构动作——具体场景 + 人物反应（「旁边桌大姐笑出声，拍张照发朋友圈」）；身体/动作隐喻替代形容词（不说「顾客很喜欢」，说「嘴上不说，脚会自己往这走」）；换位设问 + 反常识（「为什么每个人扫完码脸上都是那种表情」——留认知缺口吊着）；结果前置 + 具体到不可疑。收口用一句可转发公式（「让客人愿意来、来了不想走、走了还想带人来」）。
  - **可迁移的只有机制与抽象结构**（Hook 机制／信息增量顺序／痛点展开／教学步骤组织／证明方式／情绪与认知节奏／反模式——口径见 `docs/internal/内容资源调用协议.md` §4），**语料原话 / 案例 / 数字一律不搬**，产出照过红线闸门；`knowledge/爆款整片解剖.md`（§二、跨篇通则 + §三、逐篇解剖）是**候选传播规律与研究索引**——写卡壳按「使用索引」回查，**不把 18 条当清单背、不当每片作业**。
  - **反面样本（qwen 产出，已毙，写时避坑）**：「客人做完直夸好，就是不带你？」（宾语砍没、逼观众猜）、「白群发」「领一堆不疼不痒」（生造词 + 成语误用）、「回店券」（自造术语，官方词 = 转赠奖励）、「老板自动回老客一张券」（机制反了，实为受赠人核销后系统奖励原转赠人）。**语料铁证**：985 篇 likes 对 script 长度的 spearman = -0.014，长短本身不是爆款规律（`knowledge/爆款文案技能包/knowledge/corpus_stats.json`）。

### 第 4 步 · 分镜稿（每屏一条，**必须先给真图再要批准**）

```
屏号 / 叙事职责（钩子·痛点·思路·步骤·字段·卡面·进阶·机制·收尾）
本屏讲哪一项设置：字段名（逐字取自 `spec/coupon-fields.json`）→ 本片填什么值 → 为什么这么填 → 不这么填会怎样（发放、核销类屏改填：这一屏唯一要观众看到的重要信息）
这一环商家在想什么：场景动机一句话（怕什么 / 要什么），它决定这一屏为什么填这个值
布局来源：四锚之一（特写/痛点/机制/对照表）· 或 ref-XX 拆底稿 · 或全新做（须先过一屏标杆才许进编码）（当前 Remotion 类字段；风格化片按所选风格包声明）
口播句 → 指向区域：这一句点亮哪一块（一块可含多个元素，不是一句一条字）
信息全景清单：本屏一次摆出的全部可读信息（≥3 条，钩子屏也要有）
画面占用率下限：按所选风格包的「验收位图」（remotion-components＝其 §⑦B 阈值表；属主在包，本模板不复述数值）
入场语法：本屏元素怎么出现（必须属所选风格包的视觉语言，不许沿用上一条片的入场方式）→ 视觉层最终落法见第 4.5 步 Shot 表
本屏判据自查：对照本文与所选风格包内的 C 编号逐条 ✓/✗（✗ 必须写原因与修法，交真图时此行随屏附上）
（「布局来源 / 入场语法」两视觉字段的最终落法 = 第 4.5 步 Shot 表的 Visual Subject / Motion / Asset / Remotion / Exit，不各写一套）
```

- **C-05｜场景与教程不许分成两段（2026-08-30 用户拍板）**：观众是**对这个小程序 0 接触**的实体商家，"听不明白、看得迷迷糊糊"就没有体验意愿。一条片子的脊椎是**一套攻略的完整故事线**：头（痛点，直接压在真实界面上说）→ 主体（本篇选中的那 2~4 个功能点，逐个讲透：这一项怎么选、填什么值、联动当场带出什么）→ 尾（做完各方看到什么：家长那边、后台数据、核销那一下）。屏序跟着**功能点的选择逻辑**走，不是跟着点击顺序走，更不是把制券页→券详情→领取页→核销页各摆一屏走流程。场景不做独立铺垫段——场景当每一项的动机（要在门口发 → 公开领取 + 海报；怕被反复领 → 每人限领总量 1 张；家长答应来不会自己来 → 填写手机号 + 到期提醒；试完才谈钱 → 满减券走私密发放），逐环推着操作往前走。做到**同格**：画面里「兑换内容」填的就是「下午托管一次」、「使用须知」写的就是接孩子/写作业/晚饭，场景以"商家真要填进去的值"长在教程界面上。前几秒仍要有钩子，但钩子直接长在第一屏那张正在被填的界面上，不另开铺垫屏。
- **审批必须给真图**：设计稿阶段就把每屏渲成静态图，文字规格只做注脚。给你文字让你想象画面 = 没交付。
- **C-06｜每屏要经得起暂停截图**：单独看是一张完整干货卡。
- **C-07｜内容返工时样式冻结，但内容必须落在骨架的几何容量里（2026-08-30 用户验收后定）**：用户点头过的屏型与视觉不再动，返工只改屏序 / 屏上信息 / 口播。代价是**每副骨架的行数、卡数、句数都是硬上限**（g06：make-basic 2 组 5 行、make-rules ≤6 行、chain 3 节点、ledger 2×≤4、idea 卡标题 ≤5 字），超了只能减行或拆屏，不许缩字号硬塞、也不许靠删字段凑数。两条配套硬规矩：① **每一行在轮到自己之前不得占位**——空白回执的真根因不是卡片入场早晚，而是"行还没擦入就先占了整行高度"，卡片于是挂成一个只有打孔齿的高空框。g06 的修法：`FormLine` 在 `f < delay` 时 `return null`（卡片随内容长高），卡片与组标题入场取本组首行那一拍前 10 帧（`HandIn delay={bf(首行节拍) - 10}`）。**只给容器套 `HandIn` 不管用**（半修照样留空白回执卡）。各片骨架全部**内联**在 `videos/gXX/index.tsx`，仓库没有 `src/skeletons/` 目录，入场与占位就在各片文件里改。② **验收必须抽过程帧，不能只抽峰值帧**——峰值静帧干净不代表这一屏能看，g06 的 S5 就是这么漏过去的（10 屏静帧全过，逐帧看才发现中段三分之二是空框，且七行超出 1080×1920 可用高度）。每屏至少抽「入场 / 中段 / 峰值」三帧。
- **C-08｜新屏型先找排版底稿**：四张质感锚覆盖不了的屏型（四象限、步骤条、价格表等），从 `outputs/样本库/ref-01~19`（编号 01~19 缺 07，**库里原件 18 张**——15 已转第四锚 `anchor-table.png` 但原件留库不删；对应的 bench 拆底稿样张是 17 张、缺 r07/r15，见 `outputs/bench/bench-rNN.png`）挑最接近的一张，拆它的栅格/字号对比/用色做新组件（只借骨架不抄内容，配色换产品主题色），照旧先过一屏标杆。新参考图按 `ref-NN-屏型名-来源.扩展名` 命名入库即可走同一流程。
- **C-09｜底部结论条限制（2026-08-29 用户指出样张 17 屏同一条的雷同问题）**：样张里统一的 ConcBar/Foot 是样张约定不是成片条款——正式片每片结论条最多用 2 次（一般给机制屏/收尾屏），且每次换配色与形状贴合本片主题；样张脚注（"屏型排版样张…"）一律不进成片。锚与底稿锁的是视觉语言，列数/行数/高亮位/主题色/字段语义都是分镜阶段定的参数。
- **上屏字段名必须回源码取原话（2026-08-30 用户拍板）**：我们是拿行业场景教商家怎么用，所以画面里每个字段名、提示语、上限值都要能在产品后台逐字对上——去 `../applet/` 读 `label` / `title` / `placeholder` / `description` 原文（g06 的制券页是 `pages_coupon/coupon/create.vue`），不许凭印象造"看起来像"的名字。值按行业场景填，且必须过产品自己的上限：兑换内容只有 10 字，装不下服务明细，明细写「使用须知」（≤500 字）。一屏装不下真实字段就拆屏，别靠缩字号硬塞。产品原话里含红线词的（如到期提醒的说明含站外生态词）不引用原句，措辞只说"提醒""通知"不带平台名；但**送达不用打折**——订阅消息的送达是产品支持的能力，可以直接说"到期前会提醒家长""奖励到账会收到通知"，不要写成"顾客订阅过才收得到"这类保留说法。**券类字段已逐字收敛进 `spec/coupon-fields.json`**——先查表，表里没有的再回源码取证并把新行回写进表（含 `src` 行号），不许凭印象复述。**组标题也在真值范围内**：一行设置项挂在哪个组下，照表里的 `createGroups`——组名要么照抄页面原生那 8 个真词（`createGroups` 里 title 非 null 的正好 8 个），要么明确写成我方自述的分步标题（如「⑤ 到期提醒」），**绝不能把 A 组的行挂到 B 组标题下**（字段名对、组归属错，`check-ui-truth` ③ 分组归属会硬失败）。**券种决定面额字段组合**：写制券屏前先按 `coupon-fields.json` 的 `couponTypes[key].faceFields` 核对字段集，**禁跨券种套字段**（例：满减券进 `faceFields` 的是「优惠金额」——「满 X 减 Y」里的 Y；门槛 X 走「消费门槛」基本信息行、不属面额字段集，`coupon-fields.json` 里满减券 `faceFields` 只列优惠金额。代金券=`原价`+`券面额`「X 代 Y」——别把「减 X」挂到代金券上）。`check-ui-truth` ⑥ 已校验券种↔面额字段配对（制券屏须声明 `couponType`，配错/缺字段/用了面额字段却没声明即硬失败，不再留"不拦、靠人工"的口子）。
- **C-10｜仿产品屏不画页面 chrome（2026-09-01 用户拍板）**：返回键、状态栏、胶囊按钮这类真产品界面里**与叙事无关的页面装饰**一律不画——观众不会去点，占了顶栏又不传递任何信息。顶栏只留页面真实标题（标题本身仍按上条取原话）；面包屑不再保留——「画面零面包屑、零示例标」统一口径见 C-11 / C-18（2026-09-11 二次拍板收紧，废止本句旧「面包屑可按需保留」）。「这种都不重要」就是这条的判断尺：上屏的每个元素要么在讲功能、要么在讲场景，二者皆不是就不上屏。
- **C-11｜画面只保留内容信息，屏底不占（2026-09-01 二批拍板 → 2026-09-11 二次拍板）**：屏底部只留字幕道，**不得在屏底另起一行小字**（屏底页脚废除法）。顶部**不挂面包屑（行业泛称）**、**不挂「数值为示例」类示例标**——画面只保留标题/字段/卡片等内容信息，示例值性质由口播教学语气承载（如「有效期给十五天」）。**专属屏 payload 不得自建 `foot`/页脚类底部承载字段**（复发案例，2026-09-03）。
- **C-14｜验收帧即发布稿，实现期不得私加可见内容（2026-09-07 用户拍板）**：任何验收门（静帧、过程帧、成片）呈报的画面必须就是正式发布内容——帧上所有可见文案、数值、角标、标签只能来自分镜稿或用户拍板过的文档，占位、测试、草稿文案一律不得出现；代码期也不得上屏分镜稿没写的内容性元素（样式层实现细节除外）。分镜稿须穷尽本屏全部可见内容元素（含顶部角标、系列标签这类 chrome）——要加先补稿过验收，不许先斩后奏。首发案例：g08 左上「券到火锅 · 中秋档期」系列眉标，分镜稿/母题一页/口播文案零记载、代码期私加，已删。
- **C-17｜画面文字先过清晰度尺（2026-09-10 用户拍板）**：画面大字/标题/按钮/字段旁注 0 接触观众第一眼必须读得懂——判据共用第 3 步「清晰度前置关」这把尺；画面独有补一条「无破词断行」。反例「夸上 / 天，」（86px 破行）。
- **C-18｜画面零品牌名 + 口播必提一次（2026-09-11 用户拍板）**：品牌名「券到卡包」不上画面（顶部眉标/面包屑/字段行/结尾署名）、不上封面、不上话题标签；品牌名在口播客观、不经意提及一次——**且这一次是硬义务：口播必提、禁止删到零提及（零画面 + 零口播 = 品牌隐身、引流归零）**。画面只保留内容信息（不挂行业泛称面包屑、不挂示例标）。规范正文与生效范围见 `spec/redlines.json` `rules.brand_display`（2026-09-11 起 g10 及以后生效，历史 g06-g09 豁免）；机检兜底见 `scripts/check-voice-brand.mjs`。
- **C-19｜屏内容自足，不预告下一屏（2026-09-11 用户拍板）**：当前屏的口播与画面文字只讲本屏的事——**不写「下一屏讲 X」「接下来看」这类跨屏预告句**；衔接靠下一屏自然展开。预告句属元话语：白占画面与时长，还把观众注意力提前拽走（口播侧去冗已禁元话语，本条把同尺补到画面文字侧）。首发案例：g10 S3 注意卡尾「下一屏讲转赠怎么开」，已删。

### 第 4.5 步 · 视觉转换（**导演层定"怎么讲"，风格包定"怎么画"**）

> **主体口径（2026-09-23 CHANGE-039 修订，替代 2026-09-15 铁律的后半句）**：默认假设没有任何外部实拍素材，除非明确提供——**该前提不变**；**画面来源由所选风格包定义**（风格包清单＝`scripts/ref-registry.json` 的 `styles`；无风格包授权时按 §五 背景素材线处理，不得拿代码画冒充真实场景）。本步职责 = 把第 3 步口播 + 第 4 步分镜（内容层）经**导演层（R9）**翻成《导演稿》，再由**所选风格包**给出画面方案。**AI 生成边界一律以第 2 步《AI 创作边界协议》为准**（L1 产品事实不可生成 / L2 场景人物环境隐喻可生成 / L3 信息文字归排版层）。
> **旧口径退出（2026-09-23）**：原"只用 Remotion 最擅长的几种表达方式"的限定**已废止**（旧话头已登记进 `scripts/ref-registry.json` 的 `deprecatedTerms`，此处不复述原句）——它把表达方式锁死在单一实现上；"需要照片才能画漂亮"的禁令随之退出（AI 生成场景／人物／环境为合法手段）。

- **先选风格（每一片先答这一问，2026-09-23 CHANGE-039 替代原"片类判定"）**：按本片内容与已产出风格序列，从 `scripts/ref-registry.json` 的 `styles` 选定一种风格包——风格包定义"**用什么视觉语言表达**"；叙事与信息释放由**导演层**定（R9 ＋ 逐片《导演稿》，见下条与 R9）。**同风格不得连续复用**（机检＝`scripts/check-style-rotation.mjs`；确需连用的例外登记在 `scripts/ref-registry.json` 的 `styleRotationWaivers`）；每片须在内容输入包里声明所用 `styleId`。原"纯 Remotion 片／实拍素材片"二分自 2026-09-23 起废止，其约束并入所选风格包的实现说明。
- **画面骨架与本风格硬约束（风格专属，已迁出）**：三层视觉结构（Atmosphere／Typography／Product）与本风格硬约束清单 —— 正文见 `remotion-components` 包 §③B。

- **分工不打架**：第 3 步口播管"讲什么"、第 4 步分镜管"每屏讲哪一项设置"，本步管"每镜头画什么、怎么动、怎么退场"——三份各管一层，不互抢职责。第 4 步分镜里「布局来源 / 入场语法」两视觉字段的最终落法 = 本步 Shot 表（Visual Subject / Motion / Asset / Remotion / Exit），不再各写一套。

### 第 4.5 步附 · 风格化产出通段（**跨风格公共路径**，2026-09-23 立 · 已实测）

> 适用范围：**任何风格包**从"口播稿"到"带语音成片"的公共路径。**流程 Owner＝本节**；风格包只提供自己的执行接口（见各包 ⑧），不得另写流程。

1. **口播稿**（第 3 步产物）→ **逐句配音**：`scripts/tts.mjs`（豆包 seed-tts 2.0；密钥在 `.env`，不打印）。
2. **时间轴**：由**真实语音时长**驱动元素的 `startMs／durationMs`（不靠估算）；总长 ＝ lead ＋ 语音 ＋ 尾定格。**导演节奏仍由《导演稿》定**（哪句配哪个元素、留白多少、是否停顿）。
3. **画面**：按所选风格包的 ④ 素材生成法出画面（白板＝线稿＋annotation；`remotion-components`＝`data/gXX.ts`＋组件）。
4. **渲染**：风格包的实现层出**无声片**。
5. **合成**：合音轨 → **带语音成片**（白板的一键链＝`build_voiced.sh`）。
6. **验收（硬）**：画幅／帧率对齐交付规格（**1080×1920 @30fps**）＋ **音画对齐**（语音落在元素窗口内）＋ 各包「验收位图」声明的闸门与人判项。

白板风格的可跑实现与其接口说明见 `video/styles/whiteboard/SKILL.md` ⑧。

### 第 5 步 · 三层运动（**流畅感的真正来源：声画同步**）

> **本步的风格专属正文已迁出**（三层运动表／C-12／C-13／动效升级两项／文字保锐与主体静止红线／节拍估算）→ 见 `remotion-components` 包 **§③C**。**跨风格要点**：节拍由**语音时间轴**驱动（见第 4.5 步附·通段），"停留期≠静止期"由各风格包在其视觉语言里自证。

### 第 6 步 · 渲染与量化验收

```bash
node scripts/check-motion.mjs <媒体文件>     # 静止占比 / 中位帧间差 / 画面占用率
node scripts/check-similarity.mjs            # 整屏结构与任何已产出片重复数必须 = 0
node scripts/check-redlines.mjs              # 硬禁层 0 命中
```

> **效果尺阈值与正负样本定标已迁出**（阈值表＋"一条已知差/一条已知好"定标法）→ 见 `remotion-components` 包 **§⑦B**（阈值随该风格定标，属主同迁）。

- **质感 8 维人判清单（2026-09-14 用户拍板，补 `check-motion` 测不到的"糙"）**：量化指标只防"空和死"，防不了"糙/廉价"。一屏标杆与成片逐帧验收时，量化指标之外每维过一遍自问句：**①空间**——有无前/中/后景层次，还是单层平铺？**②光影**——有无明确主光源、亮处亮/暗处沉？**③材质**——有无真实质感，还是平涂色块？**④景深**——背景真参与层次，还是贴死当壁纸？**⑤摄影机**——有无"人在拍"的推拉摇移感？**⑥UI**——像真实产品有环境光，还是贴上去的截图？**⑦动画**——有无惯性/重量，还是机械飞入？**⑧排版**——有无明确视觉层级，还是均质填空？任一维回归"卡片堆字段 / 平涂 / 贴图"就回炉，不等量化指标报红。
- **统一验收清单（工艺 8 维 + 叙事 8 问，2026-09-14 定 · 属主本文，替代并只存此处）**：一屏标杆与成片验收**一次过两张卡，不许只测数据就算验过**。工艺 8 维见上条；**叙事 8 问**承接 g11 历史"8 问"（旧清单无文档属主、只在会话记忆，故并入这里成为正文属主，一经并即时废弃），逐帧再答：①前 2 秒停不停？②有没有 PPT 感？③有无可记忆的视觉事件？④产品在工作原理出现的瞬间真看懂了吗？⑤全片 100 秒内有无持续变化？⑥背景是"活的"还是死壁纸？⑦UI/信息是否反客为主、盖住别的？⑧结尾金句留不留得住记忆。**任一问答"否"≥3 就回炉**，不等机器报红——它俩正好补量化闸门测不到的"糙/廉价/死板"。**与 `R9` §十五 的分工（2026-09-23 CHANGE-039 §三十 专项对账）**：本清单是**成片验收的观测判据**（逐帧答、答否 ≥3 回炉）；`R9` 的「叙事导演」六项是**设计期导演决策判据**（《导演稿》逐项作答）——决策属 `R9`、验收属本步，二者互为补充、不互相替代、不构成第二 Owner。

- 无声版阶段渲**逐屏单张 ＋ 一版无声成片**交你看（**不拼九宫格**——九宫格不能替代单张）；**交付帧取屏尾「全内容帧」**——行随节拍入场的屏，30% 峰值帧会停在半打印状态、经不起暂停截图（C-06；g07 实测）；入场/中段过程帧仍按 C-07 ②逐屏抽查；交付帧即发布稿——帧上可见内容只能是分镜稿/拍板文档写过的，占位、测试、私加元素不得出现（C-14）。你说"画面 OK"才进阶段二。
- **阶段一那版无声成片（2026-09-18 用户拍板）**：归档 `outputs/gXX-行业/gXX-行业-无声成片.mp4`，供**整体节奏验收**——`check-motion` 量化三项（静止占比/中位帧间差/画面占用率）与叙事 8 问第 ⑤ 问（全片 100 秒内有无持续变化）**都跑在这一版上**，不得以"只有静帧"推迟；**它不是发布终态**，终态仍是有声成片（阶段二）。
- 阶段二：逐屏 TTS → loudnorm → ffprobe 实测 → **atempo 1.2**（seed-tts 的 `speed_ratio` 实测无效，此值已定稿）→ dur 与字幕按实测回填 → 渲染 → **盲听全片**（首字完整、尾音不爆、节奏自然）。以渲染结果为准，不信预览器。
- **渲染参数（风格专属，已迁出）**：`NODE_OPTIONS`／`--browser-executable`（Chrome Headless Shell）等 → 见 `remotion-components` 包 **§⑧B**。

### 第 7 步 · 发布（三平台）

- **抖音**：短视频 + 图文**两条线**（2026-09-17 用户拍板立项图文线；同赛道工具号同形态已验证）。
  - **短视频线**（原口径逐字保留）：9:16 短视频，唯一短视频阵地。手动设封面并关闭"自动同步首帧"，封面竖 9:16 + 横 4:3 **两版都要**（网页端发布需分别设置，2026-09-03 用户实测）；话题 3-5（泛流量大话题为主，**不带品牌词，用泛流量/精准话题**，各条换话题组合防同质化）；周二/四 12-13 或 18–20 点。
  - **图文线**（2026-09-17 立项）：1~5 张图，**首图即封面，3:4（1080×1440）**（信息流展示最完整；也可 9:16）；站内 BGM 发布端人工挂（AI 不产音频）；标题 ≤30 字照旧、钩子五型照旧；话题 3-5 照旧（不带品牌词）；发布时段照短视频线。**版式**：备忘录/清单风——大标题＋步骤条目＋高亮标记（✅❌/色块下划线）**；各图共用同一张底——首图即封面也不单独换底（确需换底：先说明理由，经用户拍板——2026-09-18 g11 首篇拍板）**，零拍摄、零 TTS、零视频渲染，HTML/Remotion 渲静态图，产物归 `outputs/` 走「确认即入库」。**品牌名口径（不放宽红线）**：`rules.brand_display` 原样适用——画面/首图/话题标签不出现品牌名，产品名只在**标题或正文文字区**客观提一次；竞品是「图内点名工具名」，我方降一档放正文文字区，首篇实测后若转化不足，另走 R8 议「图内文字提及」例外。**上屏真实性照旧**：图内产品字段名/界面名必须逐字取自 `spec/*-fields.json` 并回 `APPLET` 源码取证，机检覆盖随首篇图文数据文件成型在试点事务中接入。**后验指标（图文版）**：**封面点击率／文案完读率／赞藏评**（＝平台「流量分析」页字段名，2026-09-24 对齐），替代视频三指标；复用 `outputs/发布后验台账.json`（类型标注「图文」），台账字段与机检接入随首篇试点落地；「一次只看一个变量」归因原则照旧。**与片的关系＝伴生**（2026-09-17 用户确认）：抖音图文默认**随当前行业片同步生成**——当前在做/刚做完 gXX-行业，图文就讲同一行业同一玩法，选题、真值表、字段取证全部复用该片产出，与小红书图文、搜狐长文同为「一片多形态」；但图文结构必须按图文形态重写（大标题＋步骤清单），**禁止搬运口播稿/视频文案**，钩子结构不与视频及其他平台相同，读数分账（同题跨形态对比归因）。无进行中片时图文可先行独立发布（如首篇试点），该行业后续出片时同题复用；图文产物登记进对应 `outputs/gXX-行业/` 包，交付结构随首篇试点定。
- **小红书**：图文笔记（不发视频），封面 3:4 用本片专用渲染合成（同 g07 `g07-cover-xhs` 路径；无专用件时才从 9:16 裁中间区），关键信息放中间安全区；标签 6-10（精准+流量平衡，不带品牌词，用泛流量/精准话题）；正文 ≥5 信息单元。
- **搜狐**：长文攻略，16:9 头图，**正文必须 3~5 个 `###` 次标题**——搜狐是滚动阅读 + SEO 平台，无次标题是短板不是简洁（2026-09-01 g06 首稿实证）；次标题落在叙事转折处、顺带多一次关键词露出，交稿前自查 `grep -c '^###' <稿子>` ≥ 3。SEO 埋词；≥8 信息单元；**字数以讲清楚为准，不硬凑**。
- **标题字数硬限（平台上限，2026-09-03 用户发布时实测）**：抖音 ≤30 字、小红书 ≤20 字、搜狐 20~30 字。我方写作基线再收紧：抖音钩子式 ≤14 字（问句在前）、小红书关键词式 ≤20 字、搜狐核心搜索词进前 12 字。抖音话题**不计入**标题字数上限（2026-09-03 用户实测），3-5 个照常带。交稿前逐份数标题行字数（含标点）——**超限 = 缺陷，不是风格**。
- **标题钩子写法（三平台，2026-09-09 源自 985 篇同赛道爆款标题解剖 + 三平台调研）**：标题必命中至少一型钩子——身份/场景点名、疑问、数字清单、反常识、结果动作，纯陈述句 = 无钩子、退回重写。分平台：**抖音**「身份 + 问句」或「身份 + 反常识」前 3 秒抢注意力、痛点开门不铺陈；**小红书**搜索入口 = 身份 + 痛点 + 方案 + 数字（让目标老板搜得到、点得开）；**搜狐**标题含问题词 + 结果词、核心搜索词前置（与字数硬限一致）。三平台不许只改长度不换钩子——「同一件事换说法」不是「换个标题」，各平台钩子结构必须不同，且与封面主文案、口播前 2 秒三者一致（见入口三件套①）。出标题候选前先回 `knowledge/爆款文案技能包/knowledge/cases.jsonl` 查同赛道高表现标题（同行业 tags + 高 likes）当钩子参考池——借结构不搬话术，查不到同类就退回本五型。
- **入口三件套自查（2026-09-03 用户拍板）**：发布前四条逐查——① 标题、封面、口播前 2 秒说的是同一件事；② 封面主文案 ≤10 字、含至少一个数字锚点、缩到手机小图仍可读（成片帧直接顶替封面 = 不合格）；③ 口播第一句 2 秒内完成「身份 + 痛点」，禁场景铺垫开头（判据：前两秒能否答出"说谁、什么痛"）；④ 搜狐发布后手动填 60~120 字摘要 + 百度搜索资源平台提交本文 URL。案例与数据依据见 `outputs/发布后验台账.json`。
- **封面设计规范（2026-09-09 用户拍板 · 2026-09-11 收紧）**：
  - **只放两行文案**：封面只有「主标题 + 副标题」——**零绘制 UI 组件**（券形/卡面/邀请卡/按钮/图标/参数表/装饰图形全禁）。画面信息就两行字，其余交给背景。布局统一（抖音 9:16+4:3、小红书 3:4、搜狐 16:9 一套版式）。
  - **文字**：主文案 ≤10 字含一个数字锚点、缩到手机小图仍可读；副标题 ≤16 字，与主标题/口播前 2 秒同一件事；主标题/副标题均不出现品牌名（见 rules.brand_display）。
  - **字体（2026-09-14 定稿 · 大字压屏）**：主标题数黑体（AlimamaShuHei · 端正超重海报字）hook 超大白字 + payoff 暖金大字 + 副标题方圆体（圆润亲和）——一硬一柔成对比；白/金字 + 深红描边增重（`paintOrder: stroke fill`）在珊瑚橙底上撑立，主标单字占画面宽 ~24%（264/1080）保证缩到信息流小图仍清晰；零 UI 组件、零品牌名。字体资源全集在 `商用字体/`（30+ 字体：普惠体 10 字重 / 华文黑体 / 思源黑体 / 宋刻体 / 毛笔等），新增字体 = 复制到 `video/public/fonts/` + 在 `video/src/index.tsx` 的 FONT_CSS 加 `@font-face`；「得意黑 + 156px」为**历史豁免口径**（g06-g09 已发布不回改，达人可回选）。封面是静态缩略图，**不套**视频安全区 x[120,960] 的 120px 限宽（那是 20:9 全屏播放缩放口径，见 R3 §7.3）。
  - **背景（2026-09-11 定 · 关键收口）**：封面背景**一律从封面背景资源库选**（`spec/assets.json` A.coverBackground），**不复用视频背景 `bg.png`**；封面背景不 AI 生成，成图走资源库 + 用户提供。「背景两档分工、A 档复用视频背景」旧口径作废，禁止再走 A 档。
- 发布稿一律 `.md`（不用 txt，粘贴会丢格式）。配图用 Remotion 手工重绘的**去微信化示意图**（不用带微信 UI 元素的原生截图，小红书明文禁「其他平台的截图/小程序」）。
- CTA 口径统一：正文、画面、账号资料位（主页简介 / 置顶评论 / 私信回复）一律不写导流话术与变体暗号；产品名在口播内客观、必提一次（一字不提 = 引流归零；画面/封面/话题标签均不出现品牌名，见 rules.brand_display），转化靠用户自发搜索「券到卡包」。
- **账号配置（个人号 · 不认证企业，2026-09-08 调研定）**：昵称/头像/简介/置顶/私信只放人设身份 + 价值，不放产品词、联系方式、变体暗号；判据一句话——陌生人看了判断「这是分享的人」而非「卖东西的人」。规范全文见 `spec/redlines.json` `rules.account_profile`。
- **内容配比（个人号 · 防商业判定，2026-09-08 调研定）**：干货 : 产品提及 = 8:2（养号期纯干货 10:0 洗标签）；单条内容产品界面画面占比 ≤ 1/3。搜狐长文不写「伪装成攻略的隐性推广」（搜狐明文恶意营销），产品只客观提及、文末不放公众号。规范全文见 `spec/redlines.json` `rules.content_ratio`。
- **发布后验闭环（2026-09-14 用户拍板 · 三指标漏斗判据 + 必回填）**：发布后 1 小时回评论、24 小时去抖音后台读三个数（封面点击率 CTR / 2 秒跳出率 / 完播率）、7 天复盘写结论。**按漏斗顺序判下一步**：封面CTR≈0 → 修「封面+标题+首帧」入口三件套（首帧改用全片最强痛点瞬间，别用品牌名/大标题占首帧）；2 秒跳出最高 → 修口播前 2 秒开口（判据见入口三件套③）；完播最低 → 修闭环五拍结构；**中段工艺（安全区/字体/布局/动效）在封面CTR 与前 2 秒没过之前不先动**。**实际读数必须当场记进 `outputs/发布后验台账.json`（机检闸门 scripts/check-release-feedback.mjs 读它——片进「已发布」加行待回填、数据回流填满四数并置「已回填」；已回填缺数的片会红灯），一句话复盘结论记进 `outputs/发布后验台账.json`**——只写别处、不落台账，机检就拦不到，等于没记。后台数据与选型命中永远是下一片的输入。判据一句：**播放量 ≈ 流量池 × 封面CTR × 2秒留存 × 完播**——封面CTR 与前 2 秒是最先过闸的两道，这两道没过，中段做再完整也无观众可见。**过闸基准不写死数值**：以自身历史播放数据定基线（无自身数据则用爆款语料只作初判、随实测校准），不得把外部经验值当硬口径。
- **一次只看一个变量（2026-09-14 用户拍板 · 让后验数字可归因）**：相邻两条片只改一个变量（封面组合 / 前 2 秒开口 / 选题行业 / 时长……），发布读数变化才归因得到它；一次改多个 = 读数出来无从判断是谁在起作用。7 天复盘只答一句「这个变量改到位没有？」，没到位下一条同变量再改，到位再动下一个。判据一句：改一个 → 读一个 → 归一个，改多了等于没归、白发布。
- **各片状态记账（2026-09-09 用户拍板 · 用户零接触）**：片子的生命周期状态（制作中 / 已验收待发布 / 已发布）由 AI 从你的验收话（"画面 OK""可以了""发布吧"）当场捕获记账，**不需要你专门告知或维护任何状态字段**——状态是 AI 的流水线记账。`outputs/gXX-行业/00-交付说明.md` 的「状态」行只在「发布」这一节点冻结为快照一次（发布即转快照冻结），不逐片日常核对；**片进「已发布」时 AI 连机在 `outputs/发布后验台账.json` 加一行（状态=已发布-待回填 + 单变量假设 + 发布时间，四数待回填）**；后台四数归台账、一句话结论归台账，不归状态行。

---

## 三、工程约定（风格专属，已迁出）

> 屏目录／UI 注册／结构指纹／dur 与转场口径＝`remotion-components` 风格包的实现约定，正文见包 §⑧A；**跨风格工程共性**（画幅 1080×1920@30fps、安全区、渲染即网页）以本包 ③ 与 `R3` §7.3 为准。

## 四、清零重启（2026-08-29 用户拍板）

G02~G05 全部是**梳理流程用的测试产物**，已物理删除（git 可查，但不得回流为参照；否决理由与案例回放原载历史账本，已随其退役）。从现在起：文档是工具不是圣经，不行就改；每条视频从空白起步，用效果说话。

**本轮被否决的具体做法（防重蹈）：**
- ❌ 拿代码画的"表示物"当设计：线条拱门、圆圈火柴人、灰粉横条当名单、蓝框当漏斗——占位符质感，一眼假。**元素要么是真的（真实产品界面/真实形态的数据），要么是纯抽象装饰；两头不沾就别放。**
- ❌ 把内部约束写进画面：「券包不支持公开扫码领取」这类负面句式——观众不需要知道平台不支持什么，只正向说"通过私密方式一对一发放"。
- ❌ 把填表规则当信息：「券名称不超过 18 个字」——直接摆成品示例（真实券名、面额、有效期），让观众看到结果长什么样。
- ❌ 对着指标凑画面：为填占用率加装饰、为压静止数加晃动。**占用率/静止数只能防"空和死"，防不了"糙"；质感唯一判据 = 用户看真图。**

**新增硬闸门——一屏标杆：** 任何一条视频，先只做 1 屏、渲真图、**交用户认可"就是这个质感"，才许铺其余屏**。一屏不过，整片不开工（G05 九屏一起交导致整体否决的直接教训）。**交标杆前必须先并排自证（C-15/换语言五轴证据门）**：把新标杆与上一条行业的同型屏并排渲一张，逐轴写出 ≥3 处**非颜色**结构差异（栅格/值承载形态/栏数/动效语法）——**写不出就回炉，不许把"打算这么换"当证据交给你**；只换色/换图标 = 不过。标杆屏数据须先带 `layoutKind` 并过 `check-layout-diversity`（见 `remotion-components` 包 §③A 的 C-16）。单屏质感过关 ≠ 结构拉开；`check-similarity` 的绿不算证据（它只防照抄 ui 名，见 §三）。

**保留的工具（清零不清能力）：** `check-motion` 效果尺子、`band-occupancy` 分带诊断（教训：占用真空常在顶/底带，别对着已 70~90% 的中段硬填）、节拍层实现（`useBeat` / `useBeatFrame` 两个 hook，按需选用、不再每片必写——g08 起声画节拍改用每屏固定帧 delay 手写编排；选用的片在 `videos/gXX/index.tsx` 顶部按字幕句序号取节拍与帧位）、Chrome Headless Shell 渲染路径。相似度基线随历史片删除归零，`check-similarity` 脚本保留，但**它能拦的只有"照抄上一片的 ui 名"**（能力边界见 `remotion-components` 包 §⑧A 结构指纹条）——跨片结构雷同目前真正的防线只有一屏标杆。

---

## 五、背景素材入库（三关验收）

> 定位：独立供给线——用户随时提供候选图，AI 逐张验收，合格即收录，供**采用背景底片的风格包**选底（当前仅 `remotion-components` 使用；风格是否使用背景素材库由其「验收位图」声明，`check-bg` 按声明取值）。（每片是否真把选好的底写进 `bgImage` 由 `check-bg` 机检；「背景底必选」硬判据已随风格专属条文迁入 `video/styles/remotion-components/SKILL.md` §③A，本文不复述。）
> 总纲：**风格放开，质感必须精致，可读性是底线**。背景风格家族是该风格（当前＝`remotion-components`）的创作变量，不是全局固定项——库里家族多，跨行业才不会视觉疲劳。
> 技术底账（为什么这么定）：Remotion 对背景图零风格限制（渲染即网页，任何图都能渲）；真实技术约束只有两条——① 大面积裸纯色/极平滑渐变经 H.264 压缩易出色带，细噪点/纹理反而是防色带的标准手段；② 前景可读性（内容区对比度、文字背后无高频碎细节）。精致关就是这两条的技术兜底。

### 风格家族（现行五族，可增）

| 家族 | 气质 | 目录 |
|---|---|---|
| 抽象光效 | 柔光斑/渐变/光晕，柔和高级 | `背景素材/抽象模板/{暖\|冷}色系/`（现存，编号 BG-ABS 延续） |
| 扁平纹理 | 莫兰迪/马卡龙纯色+细纹理，干净现代 | `背景素材/扁平纹理/` |
| 几何线稿 | 细线网格/点阵/抽象线条，科技结构感 | `背景素材/几何线稿/` |
| 实景场景虚化 | 真实行业场景照片重度虚化，沉浸+行业辨识度最强 | `背景素材/实景场景/{行业}/` |
| 材质肌理 | 纸纹/布纹/水彩纸，温度手作感 | `背景素材/材质肌理/` |

新家族编号前缀：扁平 BG-FLAT / 几何 BG-GEO / 实景 BG-SCN / 材质 BG-TEX，序号各族独立递增。

### 三关验收（任一关不过 = 拒收，说清违反哪条）

**第一关 · 硬红线（家族分写）**
- 共同：无文字/logo；可商用授权（AI 生成的须注明）；≤2-3 色系无撞色。
- 抽象/扁平/几何/材质族：无具体物体/人脸；内容区留白干净——内容区 = 屏幕核心内容集中带，垂直范围以 R3 §7.3 布局规则「核心内容集中」条为准。
- 实景场景族：允许场景物体（场景天然有物），但守四条——无人脸（肖像权+抢注意力）、无可读文字与品牌 logo、场景泛化（generic 教室/门店，不是某家具体店）、重度虚化只当氛围底。

**第二关 · 精致关（风格不限，五条质量线）**
1. 拒裸纯色零处理——压缩出色带且显廉价；带细腻纹理/噪点的纯色底是安全的（噪点防色带）。
2. 拒高频碎花、图案抢戏——背景永远退在内容后面。
3. 拒生硬过渡——渐变出硬边、明显色带、过饱和撞色。
4. 拒低质感——粗噪点、压缩糊感、水印感、剪贴画感。
5. 收：过渡柔和、纹理细腻、配色克制、构图有呼吸留白，任一风格家族皆可。

**第三关 · 缺口关（家族 × 色系）**
- 按「风格家族 × 色系」格子算缺口，每格 1-2 张即可；已饱和且不明显更优 → 不收。
- 目的：机制上逼着库往多样化长，不许再长成"一个家族的世界"。

### 入库流程

1. 非 9:16 居中裁剪成 9:16（**封面背景除外**：不强制 9:16，竖版 ≥3:4 即可，给三平台封面统一铺底）。
2. 复制到家族目录，命名 `BG-{族前缀}-{序号}-{名}.png`；**封面背景命名 `CV-{序号}-{名}`**。
3. `spec/assets.json` 按家族分区登记（A.abstract / A.flat / A.geometric / A.scene / A.texture；**封面背景登记 A.coverBackground**）：`styleFamily`、`colorScheme`、浅底必须标 `darkText:true`、注明 AI 生成或授权状态。
4. 前景兼容验证：临时合成渲染 1 帧看文字/卡片清晰，验后删临时代码（封面背景验「主标题+副标题」在底上可读）。
5. `node scripts/check-facts.mjs` 对账 + 跑 gate-all + 记入当前变更事务。

### 纪律

- 不删用户原图。
- 不登记不算入库：只在 assets.json 登记后 AI 才"知道"它、才能选用。

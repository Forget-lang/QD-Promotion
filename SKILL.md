---
name: quandao-content
description: 券到卡包宣传视频当前生产契约。准确内容→行业痛点攻略→视觉母题→口播/分镜→真实画面→声画节拍→验收→完整成片。知识生命周期与反馈分流见 docs/。
version: 8.0.0-knowledge-lifecycle
---

# 券到卡包 · 宣传视频当前生产契约

> 本文件只回答一个问题：**在当前阶段，AI 默认应该怎样生产一条宣传视频。**
> 它不是历史日志、实验档案、用户聊天记录，也不是产品百科。
> 产品事实与红线见 `spec/`；知识成熟度见 `docs/KNOWLEDGE-STATUS.md`；反馈分流见 `docs/KNOWLEDGE-LIFECYCLE.md`。

## 零、最高目标

最终目标不是“做出一个符合文档的片子”，而是让 AI 最终能够从宣传目标直接产出**准确、有吸引力、有质感、可发布的完整视频**，并逐步减少人工审核。

当前仍处于能力研发期，因此 AI 必须把真实产物质量放在“文档表面完整”之前：发现方法不够好时，优先修方法和产物，不为了让旧文档继续成立而牺牲质量。

## 一、不可破的底线

1. **不虚假宣传**：功能、数字、术语必须有 `spec/` 或产品源码依据；不编店名、案例、数据；不把示例值包装成真实结果。
2. **不触平台红线**：按 `spec/redlines.json` 和闸门执行；机器检查是底线，不是质量上限。
3. **效果优先于文档绿灯**：真实成片 + 用户判断是最终质量反馈；`check-*` 负责事实、合规、工程和明显退化的自动兜底。
4. **不把内部工作语言带进成片**：策划标签、工程取证、调试说明、用于自证的脚注，不得默认成为观众内容。

## 二、知识读取原则

新会话按以下顺序恢复当前状态：

1. `docs/PROJECT-UNDERSTANDING.md`：理解项目目标、人机分工、研发阶段。
2. `docs/KNOWLEDGE-STATUS.md`：判断哪些方法稳定、哪些仍在实验、哪些已废弃。
3. `docs/KNOWLEDGE-LIFECYCLE.md` + `docs/EXPERIMENT-PROTOCOL.md`：处理新反馈、实验和方法升级。
4. 本文件：执行当前生产方法。
5. `spec/`：需要事实时读取。
6. `docs/internal/`、样本库、代码：按任务需要深入。
7. `changelog` / Git 历史：仅在需要恢复历史原因时读取，不能覆盖当前稳定方法。

## 三、开工

```bash
node scripts/gate-all.mjs
node scripts/list-assets.mjs
```

必须看真实退出码。状态类问题以脚本/源码为准，不凭文档记忆。

## 四、一条视频的生产方法

### 1. 选行业与定攻略

目标不是“介绍产品”，而是让实体店商家看到一个与自己经营有关、值得继续看的解决方案。

基本单位：

> **一个明确商家痛点 + 解决它的一套完整攻略。**

攻略必须有头有尾：

`真实经营场景 → 如何解决 → 关键功能怎么设/为什么 → 设置后各方看到什么结果`

功能少而透。默认只选真正参与这套攻略的功能；每个关键功能必须讲清：

- 怎么设；
- 为什么这样设；
- 设置后顾客/员工/商家哪一方会看到什么结果。

不要把“制券页 → 详情页 → 领取页 → 核销页”流水走一遍当成讲透，也不要把互不相关的两套攻略硬拼成一条片。

行业选择应结合现有发布数据、时令/经营周期、真实场景、产品能力和与已有内容的差异；没有数据就明确说明依据不足，不虚构确定性。

涉及产品功能时，先查：

- `spec/facts.json`；
- 对应产品线真值表，例如 `spec/coupon-fields.json`；
- 有疑问回 `../applet/` 源码取证。

### 2. 先定行业视觉母题，再做画面

同一行业系列可以共享母题；换行业必须重新设计完整视觉语言。

不要只换颜色、图标、纹理就宣称“换了一套视觉”。优先问：

> **这个行业的信息，最自然的物理载体是什么？**

由行业载体决定：

- 栅格与信息层级；
- 标题承载；
- 容器语言；
- 信息密度；
- 入场/转场语法；
- 节奏。

优先使用真实产品 UI、真实行业物体或高质量视觉参考；不要用廉价的 AI 自绘具象插画填充空白。

### 3. 写口播与内容表现

口播必须服务于攻略，而不是给画面做说明书配音。

原则：

- 说商家能听懂的经营语言；
- 删内部行话；
- 不夸大顾客行为或经营结果；
- 产品能力表达必须有事实依据；
- 数字按当前语音规则处理；
- 复杂功能讲透，简单动作不要占大量时长。

AI 应先判断“这句话对商家有什么价值”，再决定是否保留。

### 4. 做分镜：先保证内容与真实画面，再写代码

每屏需要能回答：

```text
这一屏在讲什么？
为什么现在讲？
观众应该看到什么？
哪些内容必须来自真实产品/真实载体？
这一屏的视觉重点是什么？
```

涉及 UI 的内容优先来自真实产品源码/真实截图/已验证 UI 资产，不凭空造字段。

视觉参考采用“借做法，不复制载体与外观”：从样本中提取版式、层级、信息组织和质感方法，再为当前行业重新组织。

### 5. 一屏标杆

任何新的视频视觉方向，先完成至少一个真实可渲染的标杆屏并进行实际观看。

标杆重点验证：

- 质感；
- 信息密度；
- 行业匹配度；
- 产品真实性；
- 视觉语言是否成立；
- 是否出现廉价几何/占位符感；
- 与既有视频是否只是换色换图标。

一屏标杆不过，不应直接铺完整片。

当前认可的视觉锚和样本入口以 `outputs/bench/`、样本库及当前资产盘点为准，不在这里复制动态清单。

### 6. 声画与动效

动画必须由 Remotion 时间轴驱动；具体技术规则、组件 API 和工程约定见 `docs/internal/R3-Remotion技术参考.md`。

原则：

- 动效服务内容重点，不为了“有动”而动；
- 节拍跟着口播和叙事，不给每个元素各排一遍动画；
- 保留必要的信息密度和可截图性；
- 不用指标强行塞装饰；
- 需要真实感时优先真实 UI / 真实行业物体，避免代码画假东西。

### 7. 验收与完整成片

必须依次核查：

1. 产品事实与红线；
2. 口播/字幕与当前内容源的一致性；
3. UI 与真值表/源码一致；
4. 画面真实可看，而不是只看代码；
5. 动效与节拍；
6. 自动闸门、TypeScript、渲染结果和必要的负向测试。

当前阶段仍可以使用中间无声成片作为审核介质，因为它能降低返工成本；但**目标态不是永远停留在这个流程**。随着能力成熟，应逐步让 AI 直接生成最终完整视频（内容、画面、配音、字幕、声画同步一次完成）。

## 五、反馈进入生产后的处理规则

用户只需要描述问题或目标：

> “这屏不好看。”
>
> “这里太流水账。”
>
> “这个功能说错了。”
>
> “能不能像这个参考图？”

AI 不要求用户指定改哪个文档，而按 `docs/KNOWLEDGE-LIFECYCLE.md` 判断：

`INSTANCE / PROJECT / SERIES / GLOBAL / PRODUCT / EXPERIMENT`

默认最小变更：

- 当前项目问题 → 修当前项目；
- 行业/系列问题 → 在对应范围处理并验证；
- 新方法 → 先实验；
- 经真实产物验证、具有跨项目复用价值的方法 → 再升级稳定能力；
- 产品事实 → 回 `spec/` / 产品源码。

**不要因为用户说了一句“我不喜欢”，就修改整个生产系统。**

## 六、稳定方法的演进

本文件只保留当前默认生产方式。

实验过程、失败原因、候选方案不继续堆在这里；稳定方法成熟后更新本文件，并把详细实验过程留在实验/历史层。

如果未来新方法证明优于现行方法：

```text
实验
→ 真实产物验证
→ 用户认可
→ 证明可复用
→ 更新当前生产方法
→ 旧方法降级/废弃
```

如果真实产物证明当前方法不好：

> 改方法，不为了维护旧文档而维护坏方法。

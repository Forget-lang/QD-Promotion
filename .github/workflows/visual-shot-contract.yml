# CI contract: source-of-truth map and fresh g11 keyframes are part of verification evidence.
# Evidence refresh: rerun this workflow on the current main revision after the latest g11 safe-area fix.
# PR evidence path: use pull_request CI when API-created main commits do not emit push workflows.
# Evidence refresh trigger: 2026-09-15 Product Truth snapshot is now committed on main.
name: visual-shot-contract

# 风格包 · remotion-components（信息卡·组件驱动）

> **本文件是风格包（styleId=remotion-components），不是行业线作业流程；作业流程唯一 Owner 是仓库根 `SKILL.md`。**
> 本包只定义"**用什么视觉语言表达**"。**不含**：故事结构、信息释放顺序、悬念/转折设计、观看路径、情绪曲线、镜头决策意图——这些属导演层（`docs/internal/R9-视觉导演与审美决策.md` ＋ 逐片《导演稿》）。
> 验收判据：**换包不换导演稿**——同一份《导演稿》换用本包或其它风格包都应能落地；若本包要求改写叙事顺序／信息释放／观看路径，即本包越界。

## ① 身份

- **styleId**：`remotion-components`｜**名称**：信息卡·组件驱动（Remotion 实现）
- **来源**：**旧链归位**（2026-09-23 用户裁定）——即 g06–g11 使用的既有行业线风格，**唯一有正式片战绩**的风格。登记后与 `whiteboard` 并存，**并存是合法的**（这正是风格架构的目的：消除"两个默认入口"）。
- **适合**：功能密集的字段精讲、机制对比、多列对照、流程关系、需要高信息密度与产品界面证据的段落。
- **不适合**：需要实景、人物、情绪叙事与"过程感"的段落（那是其它风格的场域）。
- **状态**：`active`

## ② 表达能力（**不是叙事法**）

- **用什么表达**：信息卡／大数字／大字／手机 Mockup／券面／图标／连线／密度／光影／粒子／空间层次。
- **能表达**：字段与数值、阈值与条件、机制对比、多列对照、前后差异、状态变化。
- **明确不承担**（属导演层，写进本包即越界）：故事结构、信息释放顺序、观看路径、情绪曲线；也不决定"这里必须出现什么内容"。

## ③ 视觉语言（**指针式：正文仍在既有属主，本栏只做归属与入口**）

- **行业视觉母题**（栅格／容器形状／标题承载／入场语法／节奏密度／装饰母题／色彩质感配方）：见本包 §③A（2026-09-23 自 `SKILL.md` 第 2 步迁入）。
- **视觉定位卡四栏**（核心痛点／情绪关键词／视觉关键词／禁止出现）：见本包 §③A。
- **背景风格族**（按行业气质选族）：族表在本包 §③A；素材库入库流程见 `SKILL.md` §五（共享资产线）。
- **三层视觉结构**（Atmosphere／Typography／Product）：见 `SKILL.md` 第 4.5 步。
- **画幅（硬）**：**1080×1920 @30fps（9:16 竖版）**，与 `R3` §7.3 一致（抖音／小红书交付规格；该值为交付规格真源）。
- **禁止**：SaaS PPT 感、全屏毛玻璃、堆字段凑密度、密集 icon 拼贴（判据见 `SKILL.md` 第 2 步与第 4.5 步）。

## ④ 素材生成法（画面从哪来）

- **画面来源**：**程序化绘制**（Remotion 组件树，工程在 `video/src`）＋ **背景素材库一张底**（`背景素材/` 的 BG-* ，以 crop／pan／depth／light 多重构图撑满全片）。
- **产品界面**：按 `docs/internal/R6-applet前端UI储备.md` 像素真值手工复刻，或使用 D-1 登记的真实截图。
- **AI 生成边界**：一律遵守 `SKILL.md` 第 2 步《AI 创作边界协议》L1／L2／L3（本栏不复述）。

## ⑤ 产品证据位（L1 落地）

- 产品界面／字段／事实数字只认三来源：真实产品截图（D-1 登记 `current-verified`）／按 R6 像素真值复刻／真实录屏。
- 上屏字段名必须逐字回源码（`check-ui-truth`）；组归属照真值表 `createGroups`，禁跨券种套字段。

## ⑥ 红线落位图

| 载体 | 规则 |
|---|---|
| 画面 | **品牌名零画面**（眉标／面包屑／字段行／署名／封面／话题一律不出现） |
| 口播 | 品牌名客观提一次（硬义务） |
| 字幕 | 落 `R3` §7.3 安全区；数字用中文；不预告下一屏 |
| 字段行 | 原话含站外生态词的，措辞只说"提醒／通知"不带平台名（`spec/redlines.json`） |

## ⑦ 验收位图（逐闸门声明）

| 闸门 | 声明 |
|---|---|
| check-redlines／check-ui-truth／check-facts／check-doc-references | APPLY |
| check-voice-discipline／check-voice-brand／check-release-feedback | APPLY |
| check-visual-shot-contract | APPLY |
| check-similarity／check-layout-diversity／check-motif-card／check-bg | APPLY（本包即这些闸门的设计对象） |
| gate-all-safearea-observation／gate-all-motion-observation | APPLY |

**导演稿替换测试（跨风格人判验收，2026-09-23 冻结）**：同一份《导演稿》切换不同风格包时，本包**不得要求**改变叙事结构、信息释放顺序、观看路径或情绪曲线。

## ⑧ 实现说明

- 工程：`video/src`（Remotion）；每片＝`video/src/data/gXX.ts` ＋ `video/src/videos/gXX/index.tsx` ＋ `video/src/covers/`。
- 组件目录：`docs/internal/R7-Remotion组件库.md`（**目录层**：有什么、叫什么、归哪层、谁在用）；技术原理与参数：`docs/internal/R3-Remotion技术参考.md`。
- 渲染：Chrome Headless Shell（取法与跨机口径见 `SKILL.md` 第 6 步）；转场／时长口径见 `R3` §7.x 与 `SKILL.md` 工程约定。
- 本包**不属**风格化引擎目录（无 Python 环境）；它的"引擎"就是 Remotion 工程本身。

## ⑨ 交付物清单

沿用现有编号产物（`00-交付说明`／`01-内容输入包`（须含本片 `styleId` 声明）／`02-口播文案`／`03-母题一页`（本风格 APPLY）／`07-片1-分镜稿`／`08-R9视觉决策卡`（含「导演稿」节）／发布稿）＋ 成片（无声／有声）＋ `frames/` ＋ `covers/`。以 `SKILL.md` 的编号口径为准，本栏不复述。

## ⑩ 隔离与轮换

- **同风格不得连续复用（自本包登记起向前生效）**：不得连续两条都用本风格；历史片 g06–g11 为既成事实、**不回溯**。
- **视觉隔离基准＝同风格内**：比较对象是上一条同为本包的片；跨风格不比较。
- **扩展判据（本包自证）**：本包不引用其它包的实现；不要求 `R9`／`SKILL.md`／`AGENTS.md`／核心 Gate 为本包增改规则——新增本包＝**注册表登记一行**（正文以指针形式复用既有属主，见 ③）。
- **过渡期声明（如实标注）**：本包当前为**"登记＋指针"式**——风格专属正文仍住在 `SKILL.md` 第 2／4／4.5／5／6 步与 `R3`／`R7`。**把正文下沉进本包、`SKILL.md` 只留通段**是下一批（归位 5b）的工作，未完成前不得声称"SKILL 已中立化"。


## ③A 母题设计法（自 `SKILL.md` 第 2 步迁入 · 2026-09-23 批 5b 第一波）

> 迁入说明：原文逐字搬移，未改写。`SKILL.md` 原位置已改为指针。**跨风格协议（AI 创作边界协议）不在本栏**——它留在 `SKILL.md` 第 2 步（跨风格单源）。

- **开工视觉语言隔离声明（2026-09-08 用户拍板，把防换皮从「事后拦截」前移到「事前隔离」）**：做母题前先跑 `node scripts/list-assets.mjs` 看已产出片的风格序列（palette / motion / transition / hookStyle / 屏幕序列），再声明本片三件事——本片行业母题 + 背景风格族 + 转场字幕风格——并逐条写出与上一条片（最新一条 g0X）≥3 处**非颜色**的结构差异。先锁这份声明再动笔找载体；它和下文「换语言五轴证据门」一前一后：声明是动手前锁方向，五轴门是标杆渲出后拿真图对证据。

- **视觉定位卡（情绪先行，2026-09-14 用户拍板，先于物理载体）**：定载体前先答一张卡——**核心痛点一句话 / 情绪关键词 ≤5（如 热·真实·烟火气·克制）/ 视觉关键词（如 夜宵电影感·深色·暖橙·浅景深·少UI）/ 禁止出现（如 SaaS PPT·蓝紫科技风·全屏毛玻璃·密集icon）**。判据 = 母题一页须含这四栏；「禁止出现」任一项命中画面 = 拒收。作用：把"高级感/舒服"这类模糊要求变成动手前就定死的情绪与禁令——AI 不再边画边猜气质，也不再默认滑回信息卡。

先回答一个问题：**这个行业的信息，最自然的物理载体是什么？** 由它长出母题，再由母题决定每屏怎么排、元素怎么动。

- **背景底必选（硬判据，2026-09-04 用户拍板）**：每条视频必须从 `背景素材/` 资源库选一张背景图打底，写进 `data/gXX.ts` 的 `style.bgImage`（指向 `video/public/` 下真实存在的图片）；**不得用手写程序化纯色/渐变 Ambient 顶替整张底**（母题装饰层可叠在背景图之上，但不能当底）。缺 `bgImage` = `check-bg` 闸门硬失败。库里没有合适色系时走 `SKILL.md` §五：AI 出规格与检索词、用户找图回传、三关验收入库——不是 AI 直接生成。

- **素材最小化 + 程序化质感（2026-09-14 用户拍板，实景素材匮乏时的默认纪律）**：实景素材（夜市/门店/场景照）只承担**环境底色 + 情绪氛围**——重度虚化、比重压到最低、每片 1 张底起步；真正的"高级信息密度"靠**程序化五刀**撑——①空间层次（前/中/后景）②光影（明确主光源）③摄影机运动（push/pull / 视差 / 手持感）④氛围层（粒子·烟雾·光斑·明暗呼吸）⑤真实UI重量（环境光·阴影·轻微透视）。**同一张底用 crop/scale/pan/depth/light 多重构图撑满全片各屏（g11 S1 近炭火→S6 回投，先例在案），不许每屏讨新素材**。判据 = 缺实景素材时**禁止退回"一张卡片堆字段"凑密度**（那正是"像 PPT"的真根因）；背景上残余实景成分需重度虚化退居氛围（不直给清晰实景当内容——属产品证据位仍守《AI 创作边界协议》L1：真实页面复刻 / 真实截图 / 真实录屏）。**本行只定素材策略；本风格"能画什么 / 不能画什么"的资产白名单与设计禁令，落在本包 §③B——不在两处重复写。**

| 行业 | 信息载体 | 可长出的动效语法（示例，不是清单） |
|---|---|---|
| 咖啡茶饮 | 菜单板 / 小票 / 杯垫 | 小票逐条打印出字段；液面填充做进度 |
| 火锅 | 桌号牌 / 时段客流 / 锅底 | 时段曲线随口播爬升；气泡做数据点 |
| 美容养生 | 护理本 / 次卡存折 / 镜面 | 翻页式次卡；镜面擦除转场 |
| 少儿教培 | 报名表 / 课程表 / 印章 / 作业本 | 表格逐格填写；盖章做结论强调 |
| 零售门店 | 价签 / 收银小票 / 货架 | 价签翻转；小票逐行打印做强调 |

- **C-02｜质感锚（2026-08-29 用户逐张认可，做屏前先看这四张图）**：`outputs/bench/anchor-bundle-open.png`（产品特写）· `anchor-pain.png`（痛点·实物传单）· `anchor-mech.png`（机制对比）· `anchor-table.png`（多列对照表，ref-15 拆底稿实证）；实现代码在 `video/src/bench/`——新屏从锚稿抄结构，禁止脱离自由发挥；对不上锚稿 = 回炉。锚未覆盖的新屏型走「ref 拆底稿 → 一屏标杆」路径，17 屏型底稿样张在同目录 `outputs/bench/bench-rNN.png`（2026-08-29 用户已认可："效果都可以，这就是我说的信息密度"）。一条片内每屏按叙事功能挑**不同**的锚/底稿——片内布局多样、跨片骨架稳定。
- **C-03｜复用边界（三条实物体感标杆后明确；锚现四张，第四条对照表非实物）**：可跨行业复用的只有【做法】——真物或重排版、密度=字段、四层画面结构（背景/主张/实物/字幕道）；【载体】必须每行业重找（美术班=传单、火锅=排队号票、宠物店=疫苗提醒卡……载体不对味即返工）；【外观】每行业新配（9 套真实主题色轮换、券种组合、字段内容）。一屏标杆审的是"像不像上一条"，似曾相识=不过。
- **C-15｜开工隔离（2026-09-07 用户拍板，堵"新会话悄悄复用上一条画面"）**：做新片第 2 步起，**禁止打开任何已产出片的 `videos/g0X/index.tsx` 或 `data/g0X.ts` 当模板**；首屏只从"本行业物理载体 + 真值表"推。锚稿（C-02）给的是做法与质感线，不是让你照搬上一片的组件树。
- **换语言五轴证据门（2026-09-03 立、2026-09-07 改时序，堵"动手前意图自证打保票、被真图证伪"）**：五轴【栅格】【容器形状】【标题承载】【入场语法】【节奏密度】的自证，**必须在标杆渲出之后、交用户之前做**——把新标杆与**上一条同型屏并排**，逐轴写出 ≥3 处**非颜色**的结构差异（栅格怎么分、值用什么形态承载、几栏、动效语法各是什么）。**写不出 = 只是换皮，回炉重做，不许拿"打算这么换"糊弄**；≥3 轴只是换色/换图标/换纹理 = 没换语言。
- **C-16｜布局指纹机检（2026-09-07 用户拍板）**：每屏数据带 `layoutKind`（取值如 `card-list`/`form`/`two-column`/`flow`/`hero-object`/`compare-list`/`cta-statement`）；`check-layout-diversity` 闸门拦"新片与上一条**同 type 屏用了同 layoutKind**"，命中即红灯——要过闸要么真换呈现架构，要么在 `scripts/ref-registry.json` 的 `layoutWaivers` 登记非空 `structuralDiff` 证据（把五轴证据落成闸门工件，逼证据存在）。
- **真实感来源（原 C-04，2026-09-23 起并入《AI 创作边界协议》，此处只留指针）**：产品界面的真实性只认三来源——真实产品截图／按 R6 像素真值在实现层复刻／真实录屏（⚠️ H5 构建与浏览器录屏路线仍永久关闭——applet 只兼容微信小程序端）；行业实拍素材需商用授权。**场景化视觉与隐喻画面不再受限**，边界见上条协议 L2。
- 产出**母题一页**：栅格与内容区坐标 / 标题承载方式 / 容器语言 / 装饰母题 / 色彩与质感配方 / 动效语法，逐行写明"上一条片是什么、本片为何不同"。并**把「视觉定位卡」四栏 + 一行「背景素材张数」原样写回**（定义属主在本栏上两条：视觉定位卡 / 素材最小化纪律，这里只是承接、不许另发明口径）——让情绪契约与素材纪律随产出物进机检 `check-motif-card`，缺栏 / 缺张数 = 拒收。



## ③B 画面骨架与硬约束（自 `SKILL.md` 第 4.5 步迁入 · 2026-09-23 批 5b 第二波）

> 迁入说明：原文逐字搬移。Shot 表字段仍属导演层（R9），不在本栏。

- **三层视觉结构（任一片通用的画面骨架，2026-09-15 拍板）**：
  - **Layer 1 · Atmosphere 氛围层**：炭火 / 深底 / 烟雾 / 火星 / 光晕 / 颗粒 / 暗角——**永远动、但很慢**，只做情绪（slow zoom / pan / parallax / embers / glow breathing），**动态不得影响文字阅读**。
  - **Layer 2 · Typography 排版层**：超大数字 / 超大关键词 / 细线 / 小标签 / 强弱层级 / 大留白——没有实拍后**最重要的画面力量**。
  - **Layer 3 · Product 产品层**：手机 / 券 / QR / 核销 / 设置 / 转赠 / 奖励——**产品永远是"证据"，不是整个画面的背景**。
  - 一句记法：**少"卡片/图标/流程框"、多"空间/排版/运动关系"**。三层实现配方（KenBurns 缓推、blur→focus、scale punch、inertia、particle attraction、line tracing 怎么搭）见 R3 §7.x——本步只讲"该画什么"，"怎么搭"回 R3。
- **本风格硬约束清单（2026-09-15 拍板，堵"没素材→拿矩形假装→PPT"；原片类口径随 2026-09-23 CHANGE-039 废止——旧话头已登记进 `deprecatedTerms`，此处不复述原句）**：
  - **禁用依赖**（不生成、不假设这些素材）：烧烤店街景、排队人群、老板/顾客人物、餐厅照片、烤串实拍、夜市场景照。**需要真实照片才能成立的画面，一律改成 文字/数字/UI/图形/动态关系 完成——禁止拿廉价占位图顶替。**
  - **可用资产（画面只能从这 12 类出）**：① 炭火背景（一张底起，程序化质感撑全片）② 手机 Mockup ③ 电子券 UI ④ 兑换券 UI ⑤ QR Code ⑥ 文本 ⑦ 数字 ⑧ 线条 ⑨ 几何图形 ⑩ 火星粒子 ⑪ 烟雾/Glow ⑫ 简单 SVG/CSS 图形。
  - **设计禁令**：不画成 PPT；不用默认毛玻璃卡当主视觉结构；不堆 icon；不做左右两栏信息对比；不做传统五节点流程图；不让整屏长期挂多张卡片；不大量用 translateY+opacity 入场；不让每个元素都动。
  - **一镜一主语**：**每个镜头只能有一个视觉主语**；优先级 = 手机/券 > 大数字 > 核心关键词 > 状态变化 > 装饰。
  - **语义动效**：所有运动必须有语义（核销→券消失；奖励→新券生成；转赠→券横移到另一台手机；朋友核销→奖励光回流；到期→数字递减）——**不做无语义的装饰动画**。
  - **排版**：优先超大数字/超大关键词/大留白；重要信息不全塞卡片；信息出现后及时退出保画面干净。
  - **色彩基准（本风格默认；行业专属以本包 §③A 母题为准）**：深底 #120B08/#24130C/#42180D，强调 #F06A24/#FF8B38/#FFC05A，字 #F7F1EA/#C6BDB5；**避免蓝/紫/荧光绿**。
- **产出 Shot 表（逐镜头一行，替代旧"主视觉/装饰元素/字幕/画面节奏"的模糊写法）**：字段属**导演层**——属主＝`docs/internal/R9-视觉映射表.md` §四，本包只承接取用口径；「导演稿」六项判据属 `R9` §十五，**不在本包产出**（`CHANGE-20260924-056` 澄清命名，此前本行误称《视觉导演稿》）：

  | 字段 | 含义 |
  |---|---|
  | Shot | 镜头编号（S1-01 式） |
  | Duration | 时长（秒） |
  | VO | 口播（对应第 3 步哪句） |
  | Visual Subject | 唯一视觉主语（一个镜头只许一个） |
  | Visual Metaphor | 抽象关系用什么视觉隐喻表达 |
  | Layout | 构图（东西摆哪） |
  | Motion | 动什么（语义动效） |
  | Peak Frame | 关注峰值帧 |
  | State Change | 什么状态变了（核销/奖励/转赠/到期） |
  | Text | 只显示哪些文字 |
  | Asset | 用哪个组件/资产（回 R7 取） |
  | Remotion | 怎么实现（组件 + 关键帧） |
  | Exit | 怎么退场（转场到下一镜头） |

  - 以上 13 字段与属主 `R9-视觉映射表` §四 逐字一致；其中 `Visual Metaphor`／`Peak Frame`／`State Change` 为**强制字段**（缺任一项＝仍处文案层，不得进代码）；机检＝`check-visual-shot-contract`（必需六列 `Shot / Visual Subject / Visual Metaphor / Peak Frame / State Change / Exit`，缺列或空值即硬失败）。

  - **素材/组件清单**：把 Shot 表里的 Asset 汇总成一张清单，回 `docs/internal/R7-Remotion组件库.md` 取现成组件；库中无此组件先登记、后复用（不许每片重新发明，见 R7）。


## ⑧A 工程约定（自 `SKILL.md` 「三、工程约定」迁入 · 2026-09-23 批 5b 第二波）

> 迁入说明：原文逐字搬移。原 `SKILL.md`「三、工程约定」整节下沉至此，**旧章节号不再保留**（保留会在本包内造出一个与 ①~⑩ 并列的假顶层节）——本节即 §⑧A 正文。

- **一行业一套专属屏**：`video/src/videos/gXX/`（屏组件 + 本片零件 + payload 类型）。屏组件**不得跨行业 import**；共享层已冻结——只含原子件与 `VTemplate`。旧共享层已废止：12 个共享场景 + "不写 ui 按 type 回退"旁路 → **ui 必填，缺了直接抛错**；9 个"统一外观"业务组件（CouponCard/PhoneMockup/StepFlow/CompareCard/StatCounter / StatCard/IconBadge/SectionTitle/HighLightText）→ 禁止预建/复用外观件，每片照锚稿手写。**共享件现行消费者状态**以 `node scripts/list-assets.mjs` + grep 实时为准（文档宪法铁律三，本文不复述快照；提醒一句：**零引用 ≠ 已删除**，能调用没人用的原子件仍在）。规则恒定：共享层只有原子件与 `VTemplate`，**没有任何现成的券面/手机壳/步骤条外观**——要上屏照锚稿手写，别以为 import 一下就有。
- 数据文件每屏写 `ui: 'gXX-名字'`，业务数据进 `payload`（形状由本片 `videos/gXX/types.ts` 定义，通用 `Scene` 只留分发/时长/字幕/语音偏移字段）；在 `scenes/index.tsx` 的 `VIDEO_RENDERERS[视频id]` 注册；`ui` 名拼错运行即抛错，不会静默回退旧组件；数据文件的**导出名必须与视频 id 一致**（check-similarity 防伪按该名找 `videos/<key>/index.tsx`）。
- 结构指纹 = `type + ui`，由 `check-similarity` 比对。**⚠️ 这道机检只防「把上一片的 ui 名照抄」**：ui 按约定必须带 `gXX-` 前缀，所以换掉前缀重做一副一模一样的骨架，指纹永远不碰撞（2026-08-30 负向测试证实）。**结构雷同的真判据是「一屏标杆」——你逐张看真图**，别拿这道绿当"结构没重复"。确需复用旧结构 → 在 `ref-registry.json` 的 `similarityExemptions` 登记理由与批准人。
- **布局指纹 = `layoutKind`（粗粒度，2026-09-07 加）**：每屏数据带 `layoutKind`（呈现架构类别，如 `card-list`/`form`/`two-column`/`flow`/`hero-object`/`compare-list`/`cta-statement`），由 `check-layout-diversity` 拦"新片与上一条**同 type 屏用了同 layoutKind**"——补 `type+ui` 拦不住的"换皮同构"。命中要么真换架构，要么在 `ref-registry.json` 的 `layoutWaivers` 登记非空 `structuralDiff` 证据（= 换语言五轴证据门的闸门工件）。它仍是人判的兜底、不替代一屏标杆，但让"悄悄复用上一条画面"从纯靠自觉升级成会撞红灯。
- `dur = ceil01(口播字数÷6) + extraHold`（÷6 取自语速上限口径，**属主见 `SKILL.md` 第 3 步，此处不复述**）；**绝对起始帧 = Σ前各屏帧数 − 12×前屏数**（`TransitionSeries` 每屏重叠 12 帧），峰值帧 = 起始帧 + floor(本屏帧数×0.3)。
- 字数口径：剔除空白与标点后逐屏机器计数（别手估，同一稿会数出两个结果）。
- 卡片宽度按 `border-box` 理解（Remotion 注入全局 CSS）；安全区达标与否**量渲染像素**，不靠读 CSS 推断。
- 交付落 `outputs/gXX-行业/`：内容输入包、分镜稿、逐屏关键帧、成片、封面、三份发布稿；过程文件进 `outputs/archive/`。

---



## ③C 三层运动与动效纪律（自 `SKILL.md` 第 5 步迁入 · 2026-09-23 批 5b 第三波）

> 迁入说明：原文逐字搬移。节拍来源在**有声片**以通段实测语音时长为准时（本栏的"字数占比估算"仅用于无声阶段预排）。

| 层 | 做什么 | 目的 |
|---|---|---|
| 底层持续微动 | 背景缓推、光晕漂移、纸面呼吸、高光扫过 | 帧间差永不归零，消灭死寂，不抢注意力 |
| 中层口播指向 | 每句口播到点，对应区域入场/放大/描边/划线强调；讲过的区域降到 `opacity: 0.72`（即 72% 不透明、不是"72% 透明"——0.55 实测太暗被否，浅底上要能读清，见 g06 `region()`） | 讲到哪、跳到哪、亮到哪——灵动感 |
| 顶层信息全景 | 未被念到的参数保持可见安静 | 信息密度与可截图性，不占口播时长 |

- **C-12｜节拍 = 口播句，不是画面元素。** 一屏 8 句最多 8 次指向；给每个元素各排一次入场是轰炸，不是同步。
- **C-13｜停留期 ≠ 静止期**：观众读信息时，画面仍要有底层微动与指向变化。
- **动效升级两项（2026-09-03 用户拍板三项，2026-09-07 g08 实测连否两项：原「音频驱动」观感"微微颤动"除名，voice.tsx 已删；原「Drift 视差漂移」即便整像素步进仍是主体元素停留期持续移动，用户终裁「主体元素停留期不要持续微动」除名，camera.tsx 零引用已删——停留期镜头感改由 KenBurnsBg 背景缓推 + 卡顶流光承担）**：
  1. **动态字幕**：Subtitle 入场改 spring（透传本片 motion），旧行降 0.72 让位新行；位置/描边/安全区口径不变。
  2. **母题转场**：转场族新增 `reveal`（有机曲边扫过揭示 + 主色边缘描边）；行业专属边缘形态按母题在此骨架上每片扩展，选型写进母题一页。
- **文字保锐 + 主体静止红线（g08 补29/30/31 三案实证）**：任何持续作用于文字层的 transform 禁止亚像素连续值——非整数位移/微旋转/连续缩放都会令 Chromium 重光栅化文字（肉眼即"持续微微抖动"，实测相邻帧 PSNR 50.7dB/36.4dB/31.8dB）；PushIn 0.001 量化步进救不了文字——步进点实测 23.7dB（比连续缩放逐帧 36.4dB 单次更大），量化只是把逐帧沸腾改成每 6 帧一次的 5Hz 抽动；Drift 即使位移 `Math.round` 取整，整像素步进仍是主体元素停留期持续移动（补31 用户终裁不要）。停留期镜头感只由 KenBurnsBg 背景缓推（只动背景图，全片 1→1.04）+ 卡顶流光承担；新增镜头类组件一律先过此红线，且不得让主体元素在停留期持续移动。
- 节拍时间先用「字数占比」估算（与字幕同源、零额外成本）；实测偏拍明显再升级为逐句 TTS。



## ⑦B 效果尺阈值与定标（自 `SKILL.md` 第 6 步迁入 · 2026-09-23 批 5b 第三波）

> 迁入说明：原文逐字搬移；**阈值属主随迁至本包**（`ref-registry.json` 的 `caliberOwners` 同步改指本包）。

| 指标 | 合格线 | 实测参照（2026-08-29，RGB 口径） |
|---|---|---|
| 画面静止占比（帧间差 ≤0.3） | ≤45% | G04 = 67%（不合格）；参考片 = 20% |
| 中位帧间差（持续微动强度） | ≥0.35 | G04 = 0.12；参考片 = 0.81 |
| 画面占用率 | ≥70%（下限，非质量分） | G04 成片 63%；参考片 95%；G05 v2 九屏 63~68%；2026-08-31 实测：已逐张认可的 bench 样张 11~69% 全部低于此线，g06 片1 十二屏 74~82% |
| 每屏可读信息条数 | ≥3（含钩子屏，人判·无尺） | v1 钩子屏 = 0 |
| 口播冗余句占比 | **属主见 `SKILL.md` 第 3 步**（内容层口径，本包不复述） | — |
| 跨片结构重复数 | 0（例外须批准并登记） | G05 v1 = 6/9 |
> 阈值按"一条已知差的 + 一条已知好的"实测定标（绝对阈值随采样分辨率漂移，同一参考片曾算出 30% 与 77% 两个结果）；换机器或改采样后重新定标，不许凭感觉调。两个定标样本（G04 差片+同事好片）已随清零删除，45%/0.35/70% 为 08-29 实测**封版基线**；今后重新定标须先各存一份好/坏样本到 `outputs/archive/calibration/` 再跑。本表静止占比 / 中位帧间差 / 画面占用率由 `check-motion` 机检、跨片结构重复数由 `check-similarity` 机检；「每屏可读信息条数」「口播冗余句占比」无自动尺子，属第 3 步软尺，交稿前人逐屏逐句判，不当闸门。



## ⑧B 渲染参数（自 `SKILL.md` 第 6 步迁入 · 2026-09-23 批 5b 第三波）

> 迁入说明：原文逐字搬移。

- 渲染必带：`NODE_OPTIONS=""` + `--browser-executable` 指向 Chrome Headless Shell——**架构随芯片**（Intel=`mac-x64`，M 系=`mac-arm64`，Remotion 按 `os.arch()` 自动下载对应目录），跨机通用取法：在 `video/` 下先 `npx remotion browser ensure`，再 `B=$(find node_modules/.remotion/chrome-headless-shell -name chrome-headless-shell -type f)`，渲染命令里用 `"$B"`，**两台机器同一条命令、勿照抄对方路径**。⚠️ 不许指向系统 Chrome——Chrome 151 + macOS 14 下 headless 因 CVDisplayLink 挂死（2026-08-29 实测）。渲染走管道时 `| tail` 会吞失败退出码，以产物文件存在为准。


# 风格包 · {styleId}（{风格名}）

> **本文件是风格包（styleId={styleId}），不是行业线作业流程；作业流程唯一 Owner 是仓库根 `SKILL.md`。**
> 本包只定义"**用什么视觉语言表达**"。**不含**：故事结构、信息释放顺序、悬念/转折设计、观看路径、情绪曲线、镜头决策意图——这些属导演层（`docs/internal/R9-视觉导演与审美决策.md` ＋ 逐片《导演稿》）。
> 验收判据：**换包不换导演稿**——同一份《导演稿》换用本包或其它风格包都应能落地；若本包要求改写叙事顺序／信息释放／观看路径，即本包越界。

> **使用说明（建包时删除本节；本目录是骨架模板——不是风格包、不进注册表、不参与闸门认领）**
> 新建一个风格 ＝ ① 把本目录复制为 `video/styles/{styleId}/` 并填满全部十栏；② 在 `scripts/ref-registry.json` 的 `styles.items` 登记一行（`id／label／packPath／status／piecePatterns／gateApplicability`）——`piecePatterns` 声明本风格片的产物身份（模板 `{id}`＝片号、`{dir}`＝该片在 outputs 下的目录名），闸门据此认领（见该表 `_说明`）。
> 建包纪律：**先做出能跑的产出、再登记**（不提前塞空壳包）；每包须过「导演稿替换测试」；风格意向以用户原话为准（`docs/changes/active/` 039 契约 §二十九／§二十九附）。

## ① 身份

- **styleId**：`{styleId}`｜**名称**：{风格名}
- **适合**：{本风格擅长表达什么类型的段落}
- **不适合**：{明确不承担的段落类型——交给其它风格}
- **状态**：`experimental`（验证期）／`active`（已有正式片战绩）
- **选择理由（入档）**：{为什么做这个风格；架构验证价值}

## ② 表达能力（**不是叙事法**）

- **能表达**：{本风格的表达能力（概念/关系/量/情绪/过程感…）——写"能表达什么"，不写"讲什么故事"}
- **用什么表达**：{视觉／声音／动效语言一句话}
- **明确不承担**（属导演层，写进本包即越界）：故事结构、悬念与转折的设计、信息释放顺序、观看路径、情绪曲线；也不决定"这里必须出现什么内容"（内容选择属导演层）。

## ③ 视觉语言

- **画幅（硬）**：交付画幅＝**1080×1920 @30fps（9:16 竖版）**，与 `R3` §7.4 一致（抖音／小红书交付规格）。平台交付规格属共享层硬约束，**不得因画法不同而绕开**。
- **核心视觉锚**：{线条／色彩／材质／空间／留白／字体…本风格统一的视觉语言锚}
- **一致性**：{同片内必须统一什么；同风格跨片沿用同一套语言}
- **禁止**：{本风格明确禁止的用法（如写实感、密集装饰等）}

## ④ 素材生成法（画面从哪来）

- **画面来源**：{程序化绘制／绘制代码／真实素材…；当前无图像／视频生成渠道时如实写}
- **统一视觉锚落地**：{动笔/开画前先落什么锚；开画自检项}
- **AI 生成边界**：一律遵守 `SKILL.md` 第 2 步《AI 创作边界协议》L1／L2／L3（本栏不复述）。
- **插槽声明**：将来接入新生成能力（如图像／视频生成）时，只改本包（本栏即插槽）＋注册表一行，不动核心。

## ⑤ 产品证据位（L1 落地）

- 产品界面／字段／事实数字只认三来源：真实产品截图（D-1 登记且 `current-verified`）／按 R6 像素真值复刻／真实录屏。
- **在本风格里的出现方式**：{证据帧如何接入本风格的画面语言——不得画进源图、不得生成}
- 证据帧须与《导演稿》对应节点对齐（出现在它被需要的位置，而不是被装饰）。

## ⑥ 红线落位图

| 载体 | 规则 |
|---|---|
| {本风格的主要文字载体（板书／包装／字幕…）} | {红线落位；**品牌名零画面**} |
| 字幕 | 落 `R3` §7.3 安全区；数字用中文（口播与字幕纪律同源）；不预告下一屏 |
| 口播 | 品牌名客观提一次（硬义务，零提及＝不通过） |
| 装饰性视觉文字 | 按 `SKILL.md` 第 2 步 L3.3：不得承担事实信息、默认不可辨读（氛围性）；需可辨读的由本包显式声明并入人判清单 |

## ⑦ 验收位图（逐闸门声明；**必须与注册表 `styles.items[].gateApplicability` 一致**）

| 闸门 | 声明 | 说明 |
|---|---|---|
| {对**全部已登记闸门键**逐一声明} | APPLY／N/A／OBSERVE | {适用＝写判据落点；N/A 必须写替代机制；未声明＝硬错误} |

- 闸门键清单以现有风格包 ⑦ 与注册表为准，**本包不复述**（不制造第二份清单）。
- 机检读不到本风格产物时，如实声明 N/A 并写明替代机制（如人判对比），**不得假装已覆盖**。

**导演稿替换测试（跨风格人判验收）**：同一份《导演稿》切换不同风格包时，本包**不得要求**改变叙事结构、信息释放顺序、观看路径或情绪曲线——若要求改写，即本包越界（回收给 `R9`）。

## ⑧ 实现说明

- **引擎／工程落点**：{引擎目录、依赖、输入输出契约；若适配第三方引擎写明适配边界}
- **调用链（冻结）**：内容层 → `R9`《导演稿》→ 本包 → 引擎 → 视频；引擎只负责执行／渲染／编码／时间同步／数据转换，**不读字幕做叙事判断**。
- **时间字段地位**：{如 `sceneDurationMs` 等＝实现同步参数——导演节奏来源永远是《导演稿》}
- **出处与许可**：{若适配第三方引擎：许可类型 ＋ 必须保留 LICENSE 与出处声明}
- **已知缺口／前置项**：{如实标注，不假装已解决}

## ⑨ 交付物清单（每片）

- **沿用现有编号产物**：内容输入包（须含本片 `styleId` 声明）、口播文案（如为有声）、分镜稿、R9 视觉决策卡（含「导演稿」节）、发布稿（如发布）。
- **本风格专属产物**：{该片目录下的产物形态（子目录／文件清单）——它同时是注册表 `piecePatterns` 的认领依据}
- **不做**：{本风格明确不产出的东西}

## ⑩ 隔离与轮换

- **同风格不得连续复用**：下一片应换风格；若必须连用，须在片首显式声明"连续复用例外＋理由"，交人判。
- **视觉隔离基准＝同风格内**：比较对象是上一条同为 `{styleId}` 的片；跨风格不比较。
- **扩展判据（本包自证）**：本包不引用其它包的实现；不要求 `R9`／`SKILL.md`／`AGENTS.md`／核心 Gate 为本包增改规则——新增本包＝**一个目录 ＋ 注册表一行**。

---

## 附 · 模板禁用字段（建包时逐条自检；出现即越界，回收给导演层）

- **叙事决策类（一律禁）**：故事结构、分幕／幕次设计、信息释放顺序、悬念设计、转折设计、观看路径、节奏曲线／情绪曲线、镜头决策意图、建议分镜、元素绘制顺序（"按语义顺序安排"式判据）。
- **表现内容选择类（一律禁）**：如"此处必须出现老板焦虑"——"表现什么"属导演层；本包只定"用何种**表现方式**呈现已定的内容"（表现方式 ≠ 表现内容选择）。
- **流程／状态／审核类（一律禁）**：开工步骤、验收流程式清单、状态记录、Owner 权限、确认关卡——流程 Owner 是仓库根 `SKILL.md`。
- **逐条判定口诀**：本包每新增一条规则，先判断它是否属于导演决策（故事结构／信息顺序／悬念设计／观看路径／情绪曲线）——属于则立即回收给 `R9`；只回答"怎么画／怎么呈现"才允许留在本包。
